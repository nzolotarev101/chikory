# Dogfood task specs

One file per dogfood campaign — the TaskSpec that ran (or will run) a
plan.md work-package slice through Chikory itself. Full operating manual:
[`docs/DOGFOODING.md`](../../docs/DOGFOODING.md).

## Convention

- **Path**: `examples/dogfood/dogfood-<NNN>.yaml` — `NNN` is the campaign
  number, allocated sequentially. The paired report lives at
  `docs/reports/dogfood-<NNN>.md` (same number; the report is mandatory —
  TASK-PROTOCOL §7).
- **`name:` field**: `dogfood-<NNN>-<short-slug>` (e.g.
  `dogfood-001-memory-pointer-store`).
- **Header comment**: which WP/slice it implements, the launch command, and
  — once it has run — the run-id, outcome, landed commit, and report link.
- Specs are kept after the run (they're the reproducible half of the
  report); never rewrite the `goal`/criteria of a spec that already ran —
  a new attempt or next slice is a new `dogfood-<NNN+1>.yaml`.
- After every terminal run: `/dogfood-review <run-id>` — the standardized
  review that verifies the delivery, writes the report, updates the plan,
  and readies the next spec (DOGFOODING.md §6.1).
- **Spec-style graduation (course correction 2026-07-02):** headline (ladder)
  specs are LOOSE — outcome + acceptance criteria, no prescribed diff;
  fully-prescribed-diff specs are the track-B format. Selection rules:
  DOGFOODING.md §1.5 (friction budget) + §3; queue: plan.md §6 course
  correction (WP-265 horizon ladder).
- **Mandatory spec headers (from dogfood-075):** `# Ladder-rung: <N>` +
  `# Thesis-KPI: <§1.4 KPI>` (+ `# Format: track-B (…)` if prescribed).
  Linted at launch by `scripts/dogfood-progression.sh --spec <file>`; the
  progression verdict over `docs/reports/dogfood-ledger.csv` binds what may
  headline next (DOGFOODING §1.5).

## Index

| Spec | WP / slice | Outcome | Report |
|---|---|---|---|
| [`dogfood-001.yaml`](dogfood-001.yaml) | WP-202 slice 1 — Memory Pointer store | SUCCESS (run 4 of 4; landed `e267e28`) | [`dogfood-001.md`](../../docs/reports/dogfood-001.md) |
| [`dogfood-002.yaml`](dogfood-002.yaml) | WP-201 slice 1 — Python contracts parity | SUCCESS (run-2899005b; landed `eb5c57e`) | [`dogfood-002.md`](../../docs/reports/dogfood-002.md) |
| [`dogfood-003.yaml`](dogfood-003.yaml) | WP-217 — completion signal → off-cadence judge pass | SUCCESS (run-b2f3504d; landed `ef4b16f`) | [`dogfood-003.md`](../../docs/reports/dogfood-003.md) |
| [`dogfood-004.yaml`](dogfood-004.yaml) | WP-218 slice 1 — pricing refresh + blind-cost-meter warning | SUCCESS (run-9edbcd28; landed `2a4dd21`) | [`dogfood-004.md`](../../docs/reports/dogfood-004.md) |
| [`dogfood-005.yaml`](dogfood-005.yaml) | WP-220 — `chikory land <run-id>` | SUCCESS (run-34926e85; diff verified on `wp-220-chikory-land`, commit pending review) | [`dogfood-005.md`](../../docs/reports/dogfood-005.md) |
| [`dogfood-006.yaml`](dogfood-006.yaml) | WP-222 slice 1 — executor subprocess env scrub | SUCCESS (run-559ea904; landed `18fae43`) | [`dogfood-006.md`](../../docs/reports/dogfood-006.md) |
| [`dogfood-007.yaml`](dogfood-007.yaml) | WP-223 — watch renders journal transitions, never sampled state | SUCCESS (run-22b337a9; diff verified, commit pending review) | [`dogfood-007.md`](../../docs/reports/dogfood-007.md) |
| [`dogfood-008.yaml`](dogfood-008.yaml) | WP-224 — `land --verify` + git-stderr capture | SUCCESS (run-86c4b628; diff verified, commit pending review) | [`dogfood-008.md`](../../docs/reports/dogfood-008.md) |
| [`dogfood-009.yaml`](dogfood-009.yaml) | WP-225 — de-flake the WP-217 milestone test (durable-state waitFor) | SUCCESS (run-841bc838; diff verified, commit pending review on `main`) | [`dogfood-009.md`](../../docs/reports/dogfood-009.md) |
| [`dogfood-010.yaml`](dogfood-010.yaml) | WP-209 slice 1 — process metrics in `chikory trace` (issues-found:changes-made ratio) | SUCCESS (run-c9df353b; diff verified, commit pending review on `main`) | [`dogfood-010.md`](../../docs/reports/dogfood-010.md) |
| [`dogfood-011.yaml`](dogfood-011.yaml) | WP-209 slice 2 — components-over-time (executor/judge timeline footer in `chikory trace`) | SUCCESS (run-59e0166c; diff verified, commit pending review on `main`) | [`dogfood-011.md`](../../docs/reports/dogfood-011.md) |
| [`dogfood-012.yaml`](dogfood-012.yaml) | WP-208 slice 1 — pure `notificationsFor` derivation (journal entries + NotificationPolicy → ordered messages; delivery deferred) | SUCCESS (run-ea31f96c; diff verified, harvested uncommitted on `main`) | [`dogfood-012.md`](../../docs/reports/dogfood-012.md) |
| [`dogfood-013.yaml`](dogfood-013.yaml) | WP-208 slice 2 — pure `slackPayloadFor` formatter (`Notification` → Slack `{ text }` with trigger emoji prefix; webhook POST + call-site deferred) | SUCCESS (run-048635b1; diff verified, harvested uncommitted on `main`) | [`dogfood-013.md`](../../docs/reports/dogfood-013.md) |
| [`dogfood-014.yaml`](dogfood-014.yaml) | WP-208 slice 3 pure half — pure `desktopPayloadFor` formatter (`Notification` → `{ title, body }` desktop-ping body; side-effectful delivery + call-site deferred) | SUCCESS (run-6b8f648c; landed `3e1336f`; surfaced+fixed F-20 harvest bug) | [`dogfood-014.md`](../../docs/reports/dogfood-014.md) |
| [`dogfood-015.yaml`](dogfood-015.yaml) | WP-219 S3 pure half — `readyNodes(plan, completed)` chain sequencing (pure dependency resolution over `Plan`/`PlanNode`; first consumer of the ADR-005 contracts) | SUCCESS (run-56d95ffc; landed `40ada45`; surfaced F-21/WP-226) | [`dogfood-015.md`](../../docs/reports/dogfood-015.md) |
| [`dogfood-016.yaml`](dogfood-016.yaml) | WP-219 S3 pure precondition — `hasDependencyCycle(plan)` (pure acyclic guard over `Plan` deps; the executor's check before looping `readyNodes`) | SUCCESS (run-2418f473; harvested byte-identically + staged, pending commit) | [`dogfood-016.md`](../../docs/reports/dogfood-016.md) |
| [`dogfood-017.yaml`](dogfood-017.yaml) | WP-227 — final journal drain before terminal return (dogfood-016 F-23; closes the remaining F-15 observer race) | **FAILED** (run-19c25609; spec redundant — WP-227 already hand-landed `26b9964`; true-positive judge ESCALATE on empty-diff-vs-claim) | [`dogfood-017.md`](../../docs/reports/dogfood-017.md) |
| [`dogfood-018.yaml`](dogfood-018.yaml) | WP-229 — surface the ESCALATE reason in `--watch` (dogfood-017 F-27) | SUCCESS (run-59115f35; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-27 closed; surfaced F-28) | [`dogfood-018.md`](../../docs/reports/dogfood-018.md) |
| [`dogfood-019.yaml`](dogfood-019.yaml) | WP-221 pure trigger half — `isCompletionMilestone(record)` ORs `claimsComplete` into the WP-217 empty-diff judge trigger (dogfood-003/018 F-11) | SUCCESS (run-d836635b; functional delivery exact, harvested IDENTICAL + staged on `main`, pending commit; surfaced **F-29** — test fixtures violate `ArtifactRef`, unseen by the `src`-only typecheck gate → WP-230) | [`dogfood-019.md`](../../docs/reports/dogfood-019.md) |
| [`dogfood-020.yaml`](dogfood-020.yaml) | WP-230 — typecheck gate covers `test/**` + fix dogfood-019 fixtures (dogfood-019 F-29) | SUCCESS (run-3575ba23; gate verified to trip on a bad fixture, F-29 closed; harvested IDENTICAL + staged on `main`, pending commit; surfaced **F-30** — duplicate launch `run-f24af22c`, ~$1 wasted, no WP) | [`dogfood-020.md`](../../docs/reports/dogfood-020.md) |
| [`dogfood-021.yaml`](dogfood-021.yaml) | WP-221 Slice B — runner detects the executor completion marker → `claimsComplete` (the F-11 cost win; protocol scaffolded by hand). _(Was WP-218 token-budget math, reprioritized 2026-06-14; that slice preserved unscheduled in the plan.md WP-218 row.)_ | SUCCESS (run-91eced6b; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-11 cost win complete in code, probe-retirement one observation away; no new friction, F-30 did not recur) | [`dogfood-021.md`](../../docs/reports/dogfood-021.md) |
| [`dogfood-022.yaml`](dogfood-022.yaml) | WP-219 S2 Slice 1 — pure goal-planner prompt half (`buildPlannerMessages` + `PLANNER_SYSTEM_PROMPT` + `PLAN_RESPONSE_SCHEMA`, mirroring `judge/prompt.ts`; the `decompose` router call + parse deferred). _(The old dogfood-022 WP-218 pure-token-math spec was **withdrawn 2026-06-14** — the whole token gate was hand-landed in the architect wall-clear `821d67f`; this slot is reused. WP-203 S2 digest wiring was the other candidate but is blocked on WP-202 Memory Pointer for `CompactionResult.digestRef`.)_ | **SUCCESS** (run-499218ef; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; **F-11 closed by observation** — first marker-emitting run, ONE step, no probe step; no new friction) | [`dogfood-022.md`](../../docs/reports/dogfood-022.md) |
| [`dogfood-023.yaml`](dogfood-023.yaml) | WP-219 S2 Slice 2 — pure plan-assembly half (`buildPlan(reply, input, opts): Plan`, mirroring the judge's `buildVerdict`; assembles a validated `Plan` from the planner's schema-valid `{ nodes }` reply; JSON parse + id + clock stay in the non-pure `decompose` wrapper) | **SUCCESS** (run-2d40ded5; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; S2's pure surface complete; F-11-closed `s0 j@0` shape held for a second straight run; no new friction; input tokens fell to 451k, ~half the 022 high) | [`dogfood-023.md`](../../docs/reports/dogfood-023.md) |
| [`dogfood-024.yaml`](dogfood-024.yaml) | WP-219 S2b — pure plan meta-judge prompt half (`buildPlanJudgeMessages` + `PLAN_JUDGE_SYSTEM_PROMPT` + `PLAN_VERDICT_RESPONSE_SCHEMA`, mirroring `judge/prompt.ts`; grades a `Plan` against the goal criteria → `PlanVerdict`; the non-pure plan-judge harness + verdict assembly deferred) | **SUCCESS** (run-28073328; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; schema verified field-for-field against frozen `PlanVerdict`; F-11-closed `s0 j@0` shape held for a third straight run; no new friction; input tokens set a new campaign high at 976k, ~2× the 023 low — confirms cost is noisy not monotonic) | [`dogfood-024.md`](../../docs/reports/dogfood-024.md) |
| [`dogfood-025.yaml`](dogfood-025.yaml) | WP-219 S2b — pure plan meta-judge verdict-assembly half (`buildPlanVerdict`, mirroring the judge's `buildVerdict`; folds the landed pure `planCoverageGaps` into `PlanVerdict.uncoveredCriteria` and assembles a validated `PlanVerdict` from a schema-valid `{ kind, rationale }` reply; the non-pure plan-judge harness + `decompose` wrapper deferred) | **SUCCESS** (run-0d39fd12; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; deterministic coverage override verified; `PlanVerdict` shape checked field-for-field against `types.ts:475`; F-11-closed `s0 j@0` shape held for a fourth straight run; no new friction; **WP-219's pure surface now complete**; input tokens fell back to 467k, the low band next to 023's 451k — sawtooth confirms cost is noise not ratchet) | [`dogfood-025.md`](../../docs/reports/dogfood-025.md) |
| [`dogfood-026.yaml`](dogfood-026.yaml) | WP-203 S4 — pure compaction-trace renderer (`formatEntryLine` gains a `case "compaction"` rendering the `CompactionResult` payload `tokensBefore→tokensAfter` + digest-pointer presence; the WP-209 trace-renderer pattern, pure over a synthetic `JournalEntry[]` fixture; not blocked on WP-202 — the S2 digest wiring stays deferred) | **SUCCESS** (run-f9d699d4; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; `CompactionResult` shape checked field-for-field against `types.ts:362`; both digest-present and digest-absent branches tested; F-11-closed `s0 j@0` shape held for a fifth straight run; no new friction; **WP-203 S4 pure trace surface complete**; input tokens 807k, mid-band — sawtooth confirms cost is noise not ratchet) | [`dogfood-026.md`](../../docs/reports/dogfood-026.md) |
| [`dogfood-027.yaml`](dogfood-027.yaml) | WP-228 S1 — pure launch-baseline-precheck decision (`evaluateBaselinePrecheck(results): BaselinePrecheckResult`, the analog of `buildVerdict`/`buildPlanVerdict`; turns collected acceptance-check exit codes into a `satisfied`/`passedIds`/`failedIds`/`summary` verdict so a redundant run can be warned/refused, dogfood-017 F-25; local result types, no `types.ts` change; the check-execution + `--force` wiring stays non-pure hand-design) | **SUCCESS** (run-f97a0e63; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-11-closed `s0 j@0` shape held for a sixth straight run; no new friction; input tokens 527k, low band) | [`dogfood-027.md`](../../docs/reports/dogfood-027.md) |
| [`dogfood-028.yaml`](dogfood-028.yaml) | WP-202 / CM-3 — pure Memory Pointer decision + reference rendering (`shouldPointerize(bytes, policy)` + `formatPointerReference(ref)` in a new `src/runner/memory-pointer.ts`, the analog of `buildVerdict`/`evaluateBaselinePrecheck`; decides when a tool output is large enough to store externally and renders the short context-facing reference, project.md Memory Pointer Pattern / CM-3; local policy type, imports only the frozen `ArtifactRef`, no `types.ts` change; the interception + `store.put` + injection wiring stays non-pure hand-design — first step toward unblocking WP-203 S2) | **SUCCESS** (run-7681a607; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; `ArtifactRef` imported type-only, no `types.ts` change; F-11-closed `s0 j@0` shape held for a seventh straight run; no new friction; **WP-202's pure decision+renderer surface complete**; input tokens 410k, a new series low) | [`dogfood-028.md`](../../docs/reports/dogfood-028.md) |
| [`dogfood-029.yaml`](dogfood-029.yaml) | WP-203 S2 — pure compaction digest-prompt half (`DIGEST_SYSTEM_PROMPT` + `buildDigestMessages(toDigest)` in a new `src/runner/compaction-prompt.ts`, the analog of `planner/prompt.ts`/`judge/prompt.ts`; builds the pure prompt for the compaction LLM digest call over the frozen `CompactionPlan.toDigest` and `Message`; the router call + `store.put` of the digest behind a Memory Pointer + `CompactionResult` journal write stays non-pure hand-design) | **SUCCESS** (run-74f88081; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; `Message` imported type-only, no schema/contract change; F-11-closed `s0 j@0` shape held for an eighth straight run; no new friction; **WP-203's entire pure surface now exhausted**; input tokens 462k, low band) | [`dogfood-029.md`](../../docs/reports/dogfood-029.md) |
| [`dogfood-030.yaml`](dogfood-030.yaml) | WP-201 Python-SDK parity — port the pure compaction digest-prompt half (`DIGEST_SYSTEM_PROMPT` + `build_digest_messages(to_digest) -> list[Message]` in a new `packages/sdk-py/src/chikory/compaction_prompt.py`, the Python parity of dogfood-029; mirrors the TS `compaction-prompt.ts` source-of-truth, `Message` already ported `chikory/types.py:87`, no contract change; the digest wiring stays non-pure hand-design) | **SUCCESS** (run-1a97e2ca; harvested IDENTICAL + staged on `main`, pending commit; F-11-closed `s0 j@0` shape held for a ninth straight run; no new friction; input tokens 434k, low band) | [`dogfood-030.md`](../../docs/reports/dogfood-030.md) |
| [`dogfood-031.yaml`](dogfood-031.yaml) | WP-207 context-window pacing decision — pure `decideContextWindowPacing(usage, policy)` runner helper (`continue`/`compact`/`park`) over projected context-window pressure; local types only, no `types.ts`/schema/runtime wiring; the non-pure runner/journal integration stays deferred | **SUCCESS** (run-c4aa79d3; run diff exact; landed `67eb167` with **F-31** commit-scope contamination → WP-231; F-11-closed `s0 j@0` shape held for a tenth straight run; input tokens 375k, new low) | [`dogfood-031.md`](../../docs/reports/dogfood-031.md) |
| [`dogfood-032.yaml`](dogfood-032.yaml) | WP-231 — dogfood landing-scope audit: compare run workspace diff with landed commit and flag extra/missing/different paths (dogfood-031 F-31) | **SUCCESS** (run-4f1fbe0a; landed `ded282b`; step-1 judge ESCALATE caught verifier abort risk, step 2 fixed it; **F-31 closed**) | [`dogfood-032.md`](../../docs/reports/dogfood-032.md) |
| [`dogfood-033.yaml`](dogfood-033.yaml) | WP-205 — pure branch-target parser for `chikory branch <run-id>@<step>` (`parseBranchTarget`, no workflow forking yet) | **SUCCESS** (run-41dd7c98; harvested IDENTICAL + staged uncommitted on `main`; F-11-closed `s0 j@0` shape held; no new friction; zero-step `$0.00` launch residue `run-1d4718db` ignored) | [`dogfood-033.md`](../../docs/reports/dogfood-033.md) |
| [`dogfood-034.yaml`](dogfood-034.yaml) | WP-205 — pure default branch-name helper for parsed branch targets (`branchNameForTarget`, no CLI command or git/worktree side effects yet) | **SUCCESS** (run-1634171d; harvested IDENTICAL + staged uncommitted on `main`; +5 tests, F-11-closed `s0 j@0` held for an eleventh straight run; no new friction; **WP-205 pure surface complete**; input tokens 594k mid-band) | [`dogfood-034.md`](../../docs/reports/dogfood-034.md) |
| [`dogfood-035.yaml`](dogfood-035.yaml) | WP-201 Python-SDK parity — port the two branch-target pure helpers (`parse_branch_target` + `branch_name_for_target` in a new `packages/sdk-py/src/chikory/branch_target.py`, mirroring the TS `src/cli/branch-target.ts` source-of-truth; the dogfood-030 parity pattern, no contract change) | **SUCCESS** (run-b0bc3865; harvested IDENTICAL + committed `88e496c` on `main`, landing-scope MATCH; 16 pytest cases, F-11-closed `s0 j@0` held for a twelfth straight run; no new friction; input tokens 318k, series low) | [`dogfood-035.md`](../../docs/reports/dogfood-035.md) |
| [`dogfood-036.yaml`](dogfood-036.yaml) | WP-201 Python-SDK parity — port the Memory Pointer pure surface (`should_pointerize` + `format_pointer_reference` + local `MemoryPointerPolicy` in a new `packages/sdk-py/src/chikory/memory_pointer.py`, mirroring the TS `runner/memory-pointer.ts` source-of-truth from dogfood-028; project.md Memory Pointer Pattern / CONTRACTS.md CM-3; `ArtifactRef` reused, no contract change) | **SUCCESS** (run-51645fbb; harvested IDENTICAL + uncommitted on the working tree; byte-for-byte parity verified incl. 12-char id truncation + em dash; 5 pytest cases, 72 py tests green; F-11-closed `s0 j@0` held for a thirteenth straight run; no new friction; input tokens 398k) | [`dogfood-036.md`](../../docs/reports/dogfood-036.md) |
| [`dogfood-037.yaml`](dogfood-037.yaml) | WP-219 S6 — the pure chain-trace renderer (`renderChainTrace(record, entries)` in a new `packages/sdk-ts/src/chain/trace.ts`, the chain-level analog of the per-run `renderTrace`; renders a `ChainRecord` plan tree + per-node run + outcome from the chain journal, the forensics surface unblocked by the hand-landed S3-wiring substrate; no contract change) — _refocused 2026-06-20 from the deprioritized WP-207 pacing parity port onto the critical-path chain executor_ | **SUCCESS** (run-295b2947; harvested IDENTICAL + uncommitted on the working tree; six-line render order + terminal-reason branch verified line-by-line against the spec & the per-run `renderTrace`; type-only contract imports, re-export at `index.ts:73`; 5 vitest cases, 365 TS tests green; F-11-closed `s0 j@0` held for a fifteenth straight run; no new friction; input tokens 793k) | [`dogfood-037.md`](../../docs/reports/dogfood-037.md) |
| [`dogfood-038.yaml`](dogfood-038.yaml) | WP-219 S3-pure — the chain-state reducer (`advanceChain` + `deriveChainStatus` in a new `packages/sdk-ts/src/chain/advance.ts`, the `computeVerdict` analog per ADR-005 §S3 transition rules; sibling of the landed pure `readyNodes`/`hasDependencyCycle`; consumes the hand-frozen `NodeOutcome` + `ChainRecord.nodeOutcomes` contract, no contract change in the slice) | **SUCCESS** (run-61e8b0a1; harvested IDENTICAL + uncommitted on the working tree; four-rule precedence + pure immutable fold verified line-by-line against ADR-005 §S3; type-only contract imports, re-export at `index.ts:72`; 6 vitest cases, 345 TS tests green; F-11-closed `s0 j@0` held for a fourteenth straight run; no new friction; input tokens 625k) | [`dogfood-038.md`](../../docs/reports/dogfood-038.md) |
| [`dogfood-039.yaml`](dogfood-039.yaml) | WP-201 Python-SDK parity — port the WP-219 S3 chain-state reducer (`advance_chain` + `derive_chain_status` in a new `packages/sdk-py/src/chikory/chain_advance.py`, mirroring the TS `src/chain/advance.ts` source-of-truth from dogfood-038; the four-rule ADR-005 §S3 precedence + pure immutable fold; `ChainRecord`/`NodeOutcome`/`ChainStatus` already in `chikory/types.py`, no contract change) | **SUCCESS** (run-6bb8ac7e; harvested IDENTICAL + uncommitted on the working tree; four-rule precedence + pure immutable fold verified line-by-line against the TS `advance.ts`; 6 pytest cases, 80 py tests green; F-11-closed `s0 j@0` held for a sixteenth straight run; no new friction; input tokens 755k, series high) | [`dogfood-039.md`](../../docs/reports/dogfood-039.md) |
| [`dogfood-040.yaml`](dogfood-040.yaml) | WP-201 Python-SDK parity — port the WP-207 context-window pacing decision (`decide_context_window_pacing` + local `ContextWindowUsage`/`ContextWindowPacingPolicy`/`ContextWindowPacingDecision` frozen dataclasses in a new `packages/sdk-py/src/chikory/pacing.py`, mirroring the TS `src/runner/pacing.ts` source-of-truth; the `continue`/`compact`/`park` decision over projected context-window pressure; all local types, no contract change) | not yet run — **demoted to track-B regression coverage** behind dogfood-041 (parity ports are not thesis evidence; land as a normal PR or run after the chain dogfood) | — |
| [`dogfood-041.yaml`](dogfood-041.yaml) | **FIRST CHAIN DOGFOOD** (WP-219 S3 end-to-end) — launched with the new `chikory chain` verb: decompose a goal (wire `chikory trace <chain-id>` to the pure `renderChainTrace`) into a multi-node Plan, gate it with the different-family plan meta-judge, run each node as a judge-gated child run through the durable `chainLoop`, halt on first FAILED node. Tests durable multi-run execution + compounding error + a real judge surface — what the first 39 pure-leaf runs never did. | 🔴 **DELIVERY SUCCESS, CHAIN NOT EXERCISED** (run-a28655c9; **launched with `chikory run`, NOT `chikory chain`** — single-run SUCCESS that delivered the `chikory trace <chain-id>` branch correctly (harvested IDENTICAL + staged uncommitted) but never invoked the planner/plan-meta-judge/`chainLoop`: no `.chikory/chains/`, no node runs, goal ran verbatim in 1 step. **F-32 → WP-232**. **Attempt 2** (Path A: delivery reverted, docs landed `a6880f3`, re-launched with `chikory chain`) engaged the chain path — planner decomposed a real goal into a multi-node Plan — but died at the plan meta-judge gate on a transient `transport error: fetch failed`, host-side before `startChain`, with zero persisted state → **F-33** (non-durable planning layer → WP-233) + **F-34** (`dogfood.sh` proxy not health-checked → WP-234). **Attempt 3** (proxy now health-checked, WP-234 done) got the furthest yet — planner AND plan meta-judge LLM calls BOTH succeeded (family-diverse `gpt-5.5` + `gemini-3.1-pro-preview`) — then the gate died deterministically on its own schema contradiction (`unrecognized_keys 'uncoveredCriteria'`) → **F-35 → WP-235, FIXED this session** (`PlanJudgeReplySchema` now accepts the `uncoveredCriteria` the response schema requires). **Attempt 4** (F-35 fixed) — planner decomposed a clean 2-node plan AND the meta-judge LLM PROCEEDed with a correct node→AC mapping — then the DETERMINISTIC coverage floor overrode PROCEED→REVISE because the planner's invented per-node criterion ids didn't equal the goal ids `AC-1/AC-2/AC-3` → **F-36 → WP-236, FIXED this session** (`PLANNER_SYSTEM_PROMPT` + `buildPlannerMessages` now instruct the planner to reuse each goal criterion id VERBATIM; floor unchanged as a safety net). The last GATING blocker is removed — re-launch `devbox run dogfood`; a PROCEED can finally flow into `startChain`/`chainLoop` and the nodes can execute. **🎉 Attempt 5 — THE CHAIN RAN END-TO-END** (`chain-d3794c24-…`): plan gate PROCEED → node 1 `resolve-id-and-render-trace` SUCCESS (changes made 1) → node 2 `verify-and-test` FAILED/HALT (changes made 0) → chain FAILED 1/2. Durable 2-node chain, per-node judge gating, the judge HALTing a stuck empty-diff node (true positive), and halt-on-FAILED all worked — **the thesis pillar is PROVEN to run**. It FAILED only on the deferred S4 context handoff → **F-37 → WP-237** (node 2 ran against fresh HEAD without node 1's diff; now the critical-path keystone). Secondary: planner emitted a verification-only node → **F-38 → WP-238, FIXED this session** (planner prompt now forbids verify-only nodes). Next: build WP-237 (S4 handoff), then re-launch for the first GREEN dependent chain) | [`dogfood-041.md`](../../docs/reports/dogfood-041.md) |
| [`dogfood-042.yaml`](dogfood-042.yaml) | WP-237 — first dependent 2-node chain: node A adds `formatUsd`; node B imports it to add `formatCostShare`, mechanically proving S4 predecessor git-state handoff. | **SUCCESS 2/2** (`chain-1cde6ae3-d05f-438e-b818-8af76419d6ae`; durable quota ESCALATE→approve→resume; harvested `b1b825d`; F-39/F-40/F-41/F-42) | [`dogfood-042.md`](../../docs/reports/dogfood-042.md) |
| [`dogfood-043.yaml`](dogfood-043.yaml) | WP-239 — artifact-backed fan-in proof: two independent predecessor modules must both reach one dependent importer; canonical runtime test gates deterministic merge/conflict behavior and non-shared-workspace transport. | 🟢 **SUCCESS 3/3** (`chain-6f1bf0ee-ce7a-42be-9416-4843b366cf0d`, runtime `919feb9`; node A=left-only, node B=right-only — independent baselines, no A→B leak — node C materialized BOTH predecessor artifacts via WP-239 handoff and imported both; node C judge ran canonical `fan-in-handoff.test.ts` in-loop exit 0; chain-aware harvest reconciled 6 files staged uncommitted, 397 TS + 82 py green; ≈$0.78/$20, ≈577k input tokens; no new friction, F-41/WP-232 reinforced) | [`dogfood-043.md`](../../docs/reports/dogfood-043.md) |
| [`dogfood-044.yaml`](dogfood-044.yaml) | WP-241 — chain-level child approval/resume proof (F-42): force a dependent node to park (budget-suspend), assert chain `--watch` surfaces it + the unblock command, and `chikory chain resume <chain-id> --add-budget` drives the chain to SUCCESS with the parent worker attached. | 🟢 **SUCCESS 2/2 — WP-241 PROVEN LIVE (re-run)** (`chain-1bfb9d13-6c3f-4f9d-bcb0-abba4d6730df`, runtime `4dfcac1` WP-243 seam on `8918219`, delivery landed `4730e98`; with the WP-243 `debug.parkBeforeStep` seam, node B **parked at step 0** (`budget_event` halt, `cause:"debug"`, `budgetUsd 0.05`), the chain surfaced it, and `chikory chain resume <chain-id> --add-budget 5` topped up +$5 → drove the chain to SUCCESS 2/2 with the parent worker attached — only the trigger is synthetic, `childParkedState`/`followChain` + chain-resume are real. Decomposition node-a `budgetUsd 11.95` / node-b `0.05`, family-diverse `gpt-5.5` exec + `gemini-3.1-pro-preview` judge, node B built on node A's artifact via WP-239 handoff, harvest 4 byte-IDENTICAL files, AC-1/AC-2 re-verified green vitest+tsc+eslint exit 0; ≈$0.611/$12 5.1%, judge 1.25%, ≈456k input tokens, ≈2m59s. **WP-241 → 🟢, WP-243 → 🟢, F-42 closed live.** First attempt `chain-bc247058-…` couldn't park (node B one-shotted → F-44 → built WP-243); false-start re-run `chain-8c303011-…` FAILED — cloned a HEAD that already held the deliverables (committed `b0ca2b7`) → empty diff → **new F-45** (WP-228 precheck must cover `chikory chain`) | [`dogfood-044.md`](../../docs/reports/dogfood-044.md) |
| [`dogfood-045.yaml`](dogfood-045.yaml) | **Agent-as-a-Judge core thesis** — the judge catches a regression PRE-LAND: a strict `truncateMiddle` with five edge traps (inclusive `<=` boundary, odd-split-favours-head, ellipsis counts toward maxLength, `maxLength===1`→`…`, `maxLength<1`→`RangeError`), gated by an AC that `grep`s the three hardest canonical assertions verbatim AND runs vitest, so a wrong edge forces a true-positive judge ROLLBACK/REVISE (cadence 1). WIN = caught + corrected in ≥2 steps; clean one-shot is also valid data. Launch with `chikory run`. | 🟡 **CLEAN ONE-SHOT — NO CATCH (valid data)** (`run-2978efa9-cb42-441c-8ab0-09711c45487a`, runtime `578ffde`; `codex`/`gpt-5.5` one-shot all five edge traps → judge PROCEED 2/2, 0 issues, SUCCESS in 1 step → no regression to catch. Delivery byte-perfect: 2 files IDENTICAL, both ACs exit 0 independently; $0.9849/$5 19.6%, judge 0.7%, **757k input tokens** for a 20-line fn (highest recent). **The Agent-as-a-Judge true-positive catch stays UNPROVEN after 45 dogfoods → F-46 → WP-244** (deterministic `debug.seedBadDiff` injection seam, analog of WP-243's park seam — hoping the executor fails is non-deterministic; over-specifying the answer leaves zero room to err) | [`dogfood-045.md`](../../docs/reports/dogfood-045.md) |
| [`dogfood-046.yaml`](dogfood-046.yaml) | **THE REPRODUCIBLE Agent-as-a-Judge true-positive catch** (WP-244) — executor implements `clamp` correctly at step 0; the WP-244 `debug.seedBadDiff` seam (armed via `CHIKORY_SEED_BAD_DIFF_*` at launch) then overwrites `clamp.ts` with a buggy-but-compiling `return value;` → judge's cadence-1 vitest check exits nonzero → deterministically overrides the LLM form → AC-1 FAILS (the catch, regardless of executor skill) → executor fixes from the feedback → SUCCESS in ≥2 steps. Forces the §1.1 KPI ("regressions the judge caught pre-land") on demand, the fix dogfood-045 F-46 demanded. Launch with `chikory run` + the seam env. | 🟢 **SUCCESS 2/2 — THE CATCH, PROVEN** (`run-b024565e-a927-49ce-8626-c70705c750e9`, runtime `ebab493`, delivery committed `5b6ca24`; executor wrote a correct `clamp` at step 0 → the seam overwrote it with `return value;` after the executor finished, before the judge ran → cadence-1 judge `vitest` AC went red (AC-1 `exited 1`) → deterministic override (`harness.ts:105`) blocked the SUCCESS seal (the catch) → executor restored a correct impl from the failing-test feedback at step 1 → SUCCESS. Proven from artifacts (executor diff `77bcb0`=correct vs judge evidence diff `84e435`=`return value;` vs step-2 base `fa94ca9`=corrupted), family-diverse `codex`/`openai` vs Google `gemini-3.1-pro-preview`, $1.1321/$5 22.6%, judge 1.4%, 525k input tokens. **DOGFOODING §1.1 KPI sealed & forceable.** Catch came from the judge-*executed* test, not the LLM diff read (LLM verdict on the bug was PROCEED). **F-47 → WP-245** (seam fires with no journaled telemetry; trace `injections 0` masks the catch). Next: chain-level judge-catch (dogfood-047, blocked on **WP-246** per-node seam wiring) | [`dogfood-046.md`](../../docs/reports/dogfood-046.md) |
| [`dogfood-047.yaml`](dogfood-047.yaml) | **THE CHAIN-LEVEL Agent-as-a-Judge true-positive catch** (escalates dogfood-046 into a durable multi-node chain) — node A adds a correct pure `roundTo`; node B (dependent) imports it and adds `roundToCents`, but the WP-246 seam (armed via `CHIKORY_SEED_BAD_DIFF_NODE_INDEX=1`) overwrites `round-to-cents.ts` with a buggy `return value;` after node B's step 0 → node B's cadence-1 judge `vitest` AC goes red → deterministic override → AC-2 FAILS (the chain-level catch) → node B fixes from the feedback → chain SUCCESS 2/2. Stresses TWO thesis pillars at once: durable multi-run chains (WP-219) + the judge true-positive catch (WP-244). Launch with `chikory chain` + the seam env. | 🔴/🟡 **RAN DISARMED — NO CATCH (wasted headline), MUST RE-RUN** (`chain-989b31b9-39cb-4b87-a72d-f8023b96bfb8`, runtime `3fc27bb`, delivery committed `37cddb1`; `docs/reports/dogfood-047.md`). WP-246 landed, but the chain was launched **without** the `CHIKORY_SEED_BAD_DIFF_*` env → the seam never armed (node B `task_json` has no `debug.seedBadDiff`, trace `injections 0`) → node B sealed clean SUCCESS in 1 step → **no chain-level catch** (the F-32 "path not exercised" mode the spec header warned about verbatim). Durable-chain machinery worked: 2-node SUCCESS 2/2, real WP-239 dependent handoff (node B imports node A's `roundTo`), family-diverse (`codex`/`openai` vs Google `gemini-3.1-pro-preview`), $1.1965/$12, harvested IDENTICAL. Two gaps block an honest re-run: **F-48 → WP-247** (no pre-flight guard refuses/warns a disarmed seam-spec) and **F-49 → WP-248** (the AC `check` gates on the executor's self-authored tests — the executor deviated from the spec's mandated formula/assertions and the judge passed it). **Re-run with the seam armed once WP-247/WP-248 land.** | [`dogfood-047.md`](../../docs/reports/dogfood-047.md) |
| [`dogfood-048.yaml`](dogfood-048.yaml) | **THE CHAIN-LEVEL judge-catch — ARMED RE-ATTEMPT of dogfood-047** — node A adds a correct pure `truncateDecimals` (truncate toward zero, NOT round); node B (dependent) imports it and adds `truncateToCents`, but the WP-246 seam (`CHIKORY_SEED_BAD_DIFF_NODE_INDEX=1`) overwrites `truncate-to-cents.ts` with a buggy `return value;` after node B's step 0 → node B's cadence-1 judge `vitest` AC goes red → deterministic override → AC-2 FAILS (the chain-level catch) → node B fixes from the feedback → chain SUCCESS 2/2. Fixes dogfood-047's two gaps at spec-authoring time: **F-48** — the launch command in the header INCLUDES the four `CHIKORY_SEED_BAD_DIFF_*` vars (don't drop them; verify the seam armed post-run); **F-49** — each AC `check` `grep`s the spec's mandated literal assertions verbatim before vitest, so the executor can't weaken/rewrite the gate (the seam corrupts only the impl, never the test, so the grep stays green through catch+fix). `truncateToCents(9.999)===9.99` is the truncate-vs-round discriminator. Fresh filenames (round-to* now on HEAD). Launch with `chikory chain` + the seam env. | 🟢 **SUCCESS 2/2 — THE CHAIN-LEVEL CATCH, PROVEN** (`chain-b7665e97-0416-4638-bba5-71e66293d5ea`, runtime `3fc27bb`, delivery committed `2c516d5`; node A wrote a correct `truncateDecimals` → SUCCESS; node B imported it (WP-239 handoff) and wrote a correct `truncateToCents` at step 0 → the WP-246 seam (`CHIKORY_SEED_BAD_DIFF_NODE_INDEX=1`) overwrote it with `return value;` → node B's cadence-1 judge re-ran AC-2 → `vitest exited 1` → deterministic override → **AC-2 FAILED (0/1), node B refused to seal SUCCESS (THE CATCH)** → executor restored a correct impl from the failing-test feedback → node B SUCCESS → chain SUCCESS 2/2. Verified armed (`debug.seedBadDiff` in node B `task_json`, node B 2 steps, step-2 diff base = the seeded `return value;`); family-diverse `codex`/`gpt-5.5` exec vs Google `gemini-3.1-pro-preview` judge; chain total $1.6381, judge share <1%, node B ~439k/273k input tokens. **F-48 closed (env baked into header + verified armed) + F-49 closed (AC `check` grep-pins the mandated literals — circular gate broken). WP-246 → 🟢 DOGFOOD-PROVEN.** Residual: seam still journals no telemetry — node B trace reads `injections 0` (F-47 → WP-245); minor F-50 (landed impl dropped the spec-mandated JSDoc; folds into WP-248)) | [`dogfood-048.md`](../../docs/reports/dogfood-048.md) |
| [`dogfood-049.yaml`](dogfood-049.yaml) | WP-247 (cheapest partial) — the pure seam-arming pre-flight decision `describeSeamArming(env): SeamArmingReport` in a new `packages/sdk-ts/src/cli/seam-precheck.ts`, the `evaluateBaselinePrecheck`/`precheck.ts` analog: reports armed/disarmed + optional 0-based `nodeIndex` + the empty-CONTENT warning from the four `CHIKORY_SEED_BAD_DIFF_*` keys (mirroring the real `cli/chain.ts`+`cli/commands.ts` host-side readers), so a launcher/operator sees the armed state BEFORE a run completes — removing the F-48 disarmed-seam footgun that cost dogfood-047 a headline. NEW feature code with real branching (not a parity port); AC `check` grep-pins the mandated assertions (dogfood-047 F-49 discipline). Single `chikory run`; thin launcher wire hand-landed as a §4 follow-up. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified** (`run-26e74ad3-901e-4671-b669-38cd60b76736`, runtime `4599a3c`, delivery landed `dde765b`; `docs/reports/dogfood-049.md`). `codex`/`gpt-5.5` one-shot the pure `describeSeamArming` + 4-test file, all four grep-pinned assertions verbatim; judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1, scope `✓` (run touched exactly its 3 files). $0.5362/$5 (10.8%), judge 1.3%, 387k/4.6k tokens, 2m24s. Full SDK suite 449 passed re-verified. **WP-247 cheapest partial DONE** (structural launcher guard + banner wire still owed). **New F-51 → WP-249** (harvest commit `dde765b` bundled an unrelated operator `land.test.ts` edit + cites no run-id → `git log --grep <run-id>` / `dogfood-verify §6` can't resolve the landed commit). | [`dogfood-049.md`](../../docs/reports/dogfood-049.md) |
| [`dogfood-050.yaml`](dogfood-050.yaml) | **WP-245 — seam telemetry (top observability debt)**: make the `debug.seedBadDiff` judge-catch seam journal a durable `seam` entry so `chikory trace` stops reading `injections 0` on the most important catch. Mirrors the WP-243 park seam's journaled `cause:"debug"` budget event: new additive `"seam"` `JournalEntryKind`, a `recordSeamEvent` activity (idempotent via `appendOnce` keyed on `seamEventIndex`, the `recordBudgetEvent` precedent), the agent-loop wire next to the seam fire (`agent-loop.ts:317`), and the `chikory trace` totals surfacing (`seams fired N`). Cross-file (types+activity+workflow+renderer+2 tests) — genuinely failable on replay-safety/payload/render. AC grep-pins the mandated assertions (F-49). Single `chikory run`, no seam env needed. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified** (`run-55eb5422-57f4-41b6-bec1-d91e24408b96`, runtime `a4e9665`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-050.md`). `codex`/`gpt-5.5` one-shot all 7 files (6 named + the entailed `schemas.ts` Zod-enum mirror): `"seam"` union member (`types.ts:328`), `recordSeamEvent` activity (idempotent `appendOnce` keyed on `seamEventIndex`, payload carries byteCount not content), agent-loop wire inside the seam guard (`agent-loop.ts:325`), `seams fired N` trace surfacing (additive, no-seam path byte-identical), +2 tests. Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1, scope `✓` (exactly 7 files: 6 named + the entailed `schemas.ts` mirror). $1.0340/$5 (20.6%), judge 0.7%, **770k/6.5k tokens** (series high), 30 tool calls, 3m59s. Full SDK suite 451 passed re-verified incl. the `verdict-gating` "seedBadDiff ARMED" Temporal path (proves the new activity is registered). **F-47 → WP-245 CLOSED in code.** Minor residual **F-52** (telemetry unit-proven but not yet observed live — folds into WP-245). | [`dogfood-050.md`](../../docs/reports/dogfood-050.md) |
| [`dogfood-051.yaml`](dogfood-051.yaml) | **WP-207 — pacing telemetry (context-rot pillar)**: WIRE the pure `decideContextWindowPacing` decision (`runner/pacing.ts`, dogfood-031) INTO the live agent loop, journal a durable replay-safe `pacing` entry each step from REAL per-step token usage, and surface `pacing events N` in `chikory trace` — the first live observability on the 387k–793k-input-token-per-step problem every report has flagged for 50 runs. Identical gap-pattern to dogfood-050's seam telemetry; the documented FA-3/SE-2 remaining work. `"pacing"` `JournalEntryKind` already exists (no contract change). New `recordPacingEvent` activity (mirrors `recordSeamEvent`/`recordBudgetEvent`) + agent-loop wire + trace surfacing + 2 tests. Cross-file, genuinely failable on replay-safety/usage-construction. AC grep-pins the mandated assertions + the real-decision wire (F-49). Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified** (`run-663a1baa-1a78-406f-91c5-bd0afa2fdcaa`, runtime `54a2c41`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-051.md`). `codex`/`gpt-5.5` one-shot all 5 files: `recordPacingEvent` activity (idempotent `appendOnce` keyed on `pacingEventIndex`), agent-loop wire feeding REAL `spentTokens`/`recordTokens` into the **real** `decideContextWindowPacing` + journaling every step, `pacing events N` trace surfacing (additive, no-pacing path byte-identical) + a `case "pacing"` renderer, +2 tests. One benign deviation (judge `scope ✓`): relocated `stepIndex += 1` upward so `atStep` is 0-based — behavior-safe. Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1. $2.8487/$5 (**57.0%**), judge 0.3%, **2178k/12k tokens** (NEW series-high input), 48 tool calls, 6m46s. Full SDK suite 454 passed re-verified incl. the `verdict-gating` ARMED + `crash-recovery` Temporal paths (proves `recordPacingEvent` is registered live). **WP-207 journaling+wiring half DONE.** Minor residual **F-53** (telemetry unit-proven but not yet observed live — own trace predates the wiring; folds into WP-207). | [`dogfood-051.md`](../../docs/reports/dogfood-051.md) |
| [`dogfood-052.yaml`](dogfood-052.yaml) | **WP-207 — pacing SUMMARY (context-rot pillar)**: dogfood-051 journals a `pacing` entry per step but `chikory trace` only prints a bare ` · pacing events N` COUNT (N == step count, near-useless). This makes the journaled payload ACTIONABLE: a NEW pure `summarizePacing(entries)` reducer (`runner/pacing-summary.ts`, the `evaluateBaselinePrecheck` analog) → peak window utilization + compact/park recommendation counts, surfaced additively as `peak window X% (compact C · park P)` in the same totals sub-line (byte-identical no-pacing path). New pure module + barrel re-export + renderer wire + 2 tests (the dogfood-028/029 + dogfood-026/051 patterns). Cross-file, failable on the byte-identical path / max-vs-sum / action-count / non-pacing filter. AC grep-pins the reducer in both its def + the wire (F-49). Single `chikory run`, no seam env. **The ACT half (tune compaction/checkpoint cadence) is §4-blocked on the WP-203 S2 runtime compaction trigger — operator-landed, not a dogfood.** Builds on dogfood-051: **commit dogfood-051 to HEAD first.** | 🟢 **SUCCESS 1 step — clean one-shot + F-53 CLOSED LIVE** (`run-7e13ae2a-a233-4e2d-9fdc-d564a9eee5bc`, runtime `0880806`, delivery uncommitted byte-IDENTICAL to the run workspace; `docs/reports/dogfood-052.md`). `codex`/`gpt-5.5` one-shot all 5 files: pure `summarizePacing` reducer (`Math.max` peak, `compact`/`park` counts excluding `continue`, non-`pacing` ignored, empty→0, NO contract change) + barrel re-export + trace totals wire `peak window X% (compact C · park P)` (additive/byte-identical no-pacing path) + 2 tests. Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1 scope ✓ (exactly 5 named files); $0.8032/$5 (16.0%), judge 0.9%, 597k/5.0k tokens, 19 tool calls, 3m4s; AC-1 re-run green (grep-pins + vitest 25 passed + tsc + eslint exit 0). **F-53 CLOSED 🟢 — the first LIVE read:** this run journaled a real `pacing` entry (dogfood-051's wiring was committed to HEAD `0880806` first) and the new reducer surfaced it — `peak window 602% (compact 0 · park 1)`, payload `action park · projectedTokens 1,203,440 · remainingTokens -1,003,440 · utilization 6.0172` (executor ran at 6× the 200k window, PARK recommended and unheeded — the *act* half is §4-blocked). No new friction; F-11-closed `s0 j@0` held. | [`dogfood-052.md`](../../docs/reports/dogfood-052.md) |
| [`dogfood-053.yaml`](dogfood-053.yaml) | **WP-203/WP-207 — compaction SUMMARY in `chikory trace` totals**: the WP-207 act-half unlock now tags each `compaction` entry `trigger:"pacing"\|"count"` and renders ` (pacing)` per-entry, but the trace TOTALS line still says nothing about compaction. This adds a pure `summarizeCompaction(entries)` reducer (`runner/compaction-summary.ts`, the `summarizePacing` analog) → digest-bearing fold count + the pacing-pressure-driven subset, surfaced additively as `compactions N (pacing M)` in the same totals sub-line (byte-identical no-compaction path). New pure module + barrel re-export + renderer wire + 2 tests. Cross-file, failable on the digestRef filter / `trigger` match / byte-identical path. AC grep-pins the reducer in def + wire (F-49). Single `chikory run`, no seam env. **Builds on the act-half unlock — commit it to HEAD first.** | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; residual F-54 (telemetry not observed live)** (`run-41f2744f-82d6-4e54-825d-9704f77b1ee7`, runtime `4abb478` (WP-207 act half), delivery uncommitted byte-IDENTICAL to the run workspace; `docs/reports/dogfood-053.md`). `codex`/`gpt-5.5` one-shot all 5 files: pure `summarizeCompaction` reducer (digestRef-gated `folds`, `trigger:"pacing"` subset `pacingFolds`, non-`compaction`/digest-less ignored, empty→0/0, NO contract change) + barrel re-export + trace totals wire `compactions N (pacing M)` (additive/byte-identical no-compaction path) + 2 tests. Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1 scope ✓ (exactly 5 named files); $0.8086/$5 (16.2%), judge 0.9%, **598k/5.4k tokens**, 22 tool calls, 3m29s; AC-1 re-run green (grep-pins + vitest 27 passed + tsc + eslint exit 0). **The run itself PARKED** — `peak window 604% (compact 0 · park 1)`, **0 folds**, so the new `compactions` segment never rendered live → **F-54** (telemetry unit-proven, not yet observed live; mirror of F-52/F-53; → WP-251 seam-forced multi-step fold). F-11-closed `s0 j@0` held; no new scope/cost/loop anomaly. | [`dogfood-053.md`](../../docs/reports/dogfood-053.md) |
| [`dogfood-054.yaml`](dogfood-054.yaml) | **Agent-as-a-Judge true-positive CATCH on REAL product-WP code** (WP-215 S1) — escalates the dogfood-046/048 catch off throwaway utilities (`clamp`/`truncateDecimals`) onto real feature code. The executor builds a correct pure `scanDiffForSecrets(diff): string[]` secret-scan judge-evidence primitive (new `packages/sdk-ts/src/judge/scan-secrets.ts`, the real WP-215 half the §4 judge-harness wire consumes later); the WP-244 seam (`CHIKORY_SEED_BAD_DIFF_*`, single-line compiling `return [];`) overwrites it after step 0 → cadence-1 judge `vitest` AC red → deterministic override → AC FAILS (the catch) → executor restores from the failing-test feedback → SUCCESS in ≥2 steps. Stresses two thesis pillars on real code: judge security-evidence feature (WP-215) + judge true-positive catch (WP-244). Launch with `chikory run` + the seam env. (Was dogfood-053; re-numbered when 053 was re-pointed to the compaction-summary slice.) | **SUCCESS in 2 steps** (run-f7106c03; committed `cfb8bcd`, harvest byte-IDENTICAL; THE CATCH fired on real code — `seams fired 1`, judge AC `exited 1` at step 0 → restored at step 1; $1.3298/$5 (26.6%), judge 1.0%, family-diverse, 754k then 243k input tokens; AC re-run green vitest 469 + tsc + eslint). New **F-55 → WP-252** (uncalibrated `peak window 759%` denominator); park-saturation recurs (3rd point 602/604/759%, F-54/WP-250/251). | [`dogfood-054.md`](../../docs/reports/dogfood-054.md) |
| [`dogfood-055.yaml`](dogfood-055.yaml) | **WP-215 S2 — wire `scanDiffForSecrets` into the inner-loop judge evidence** (Agent-as-a-Judge pillar). dogfood-054 landed the pure scanner but it is consumed NOWHERE; this threads it into `collectEvidence` (`src/judge/evidence.ts`) as an additive `CollectedEvidence` field + surfaces the detected secret-kind labels in the judge prompt, so the judge mechanically extracts secret findings from the diff it already collects. Additive (no contract change); the LLM still adjudicates `no_secrets_introduced` (a live deterministic destructive override self-trips ROLLBACK on the run's own `AKIAIOSFODNN7EXAMPLE` test fixtures → deferred to WP-253 + example-key allowlist). Unit-tested on synthetic diffs. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified** (`run-73437934-9672-43d4-b453-557044ec349b`, runtime `88d2102`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-055.md`). `codex`/`gpt-5.5` one-shot all 4 files: `evidence.ts` imports `scanDiffForSecrets` + adds the REQUIRED `CollectedEvidence.secretScanLabels` over the FULL diff (before the prompt-excerpt truncation), `prompt.ts` renders the `## EVIDENCE — deterministic secret scan (added diff lines)` section (`- <label>` / `(none)`), `harness.ts` threads it through, +2-case vitest. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly 3 named files + 1 test). $0.7834/$5 (15.6%), judge 1.0%, 580k/5.0k tokens, 24 tool calls, 3m45s. AC re-run green (5 grep-pins + vitest 471 passed | 19 skipped, tsc+eslint exit 0). **NO new friction** — the section rendered live but `(none)` (the run's own diff is secret-free by design; non-empty firing unit-proven, deferred to WP-253). Park-saturation recurs (4th point 602/604/759/585%, F-54/WP-250/251); F-55/WP-252 denominator recurs. | [`dogfood-055.md`](../../docs/reports/dogfood-055.md) |
| [`dogfood-056.yaml`](dogfood-056.yaml) | **WP-253 / WP-215 S3 — the example-key allowlist that unblocks the deterministic `no_secrets_introduced` override** (Agent-as-a-Judge security pillar). A NEW pure `src/judge/secret-allowlist.ts` (`EXAMPLE_SECRET_VALUES` frozen set with AWS's canonical `AKIAIOSFODNN7EXAMPLE` built by concatenation + `isExampleSecret(value): boolean`) + a NEW `scanDiffForRealSecrets(diff): string[]` in `scan-secrets.ts` that re-uses the existing patterns but excludes allowlisted example values, leaving the evidence-facing `scanDiffForSecrets` UNCHANGED (the S1 5-case suite + dogfood-055 evidence wire are the regression guard). The override that consumes `scanDiffForRealSecrets` to flip the verdict pre-land is the §4 hand-design follow-up once this lands. Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified** (`run-37862cf7-0c24-4aec-b09a-547028bd6720`, runtime `8e4661c`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-056.md`). `codex`/`gpt-5.5` one-shot all 3 files: `secret-allowlist.ts` (`EXAMPLE_SECRET_VALUES` + `isExampleSecret`, AWS key by concatenation) + `scanDiffForRealSecrets` in `scan-secrets.ts` (`.match`/exclude allowlisted dummies; shared `getAddedDiffLines` helper extracted, `scanDiffForSecrets` behavior UNCHANGED) + a 3-case vitest. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly 2 files + 1 test). $0.4744/$5 (9.4% — cheapest WP-215-series headline), judge 2.0%, 328k/5.4k tokens, 18 tool calls, 3m15s. AC re-run green (4 grep-pins + vitest 474 passed | 19 skipped, tsc+eslint exit 0). **NO new friction** — the secret-scan evidence section again rendered `(none)` live (self-trip discipline keeps the run's own diff secret-free; non-empty live firing deferred to a dedicated assertion in the override slice). Park-saturation recurs (5th point 602/604/759/585/334%, F-54/WP-250/251); F-55/WP-252 denominator recurs. The destructive override that flips the verdict pre-land = WP-253, §4 hand-design follow-up. | [`dogfood-056.md`](../../docs/reports/dogfood-056.md) |
| [`dogfood-057.yaml`](dogfood-057.yaml) | **WP-252 — calibrate the pacing-window denominator to the executor model** (context-rot / "no magic" observability pillar). The pacing decision divides projected tokens by a hardcoded `DEFAULT_CONTEXT_WINDOW_TOKENS = 200_000` (`agent-loop.ts:63`) uncalibrated to the executor's real window, so `peak window 334%/585%/759%` is loud but its divisor is arbitrary (F-55, recurring across dogfood-052→056). A NEW pure `src/runner/context-window.ts` — `CONTEXT_WINDOW_TABLE` (model id → window tokens) + `lookupContextWindow(model, fallback)` (longest-prefix, the `lookupPricing` analog) + `resolveContextWindowForSpec(spec, fallback)` (reads the executor's `routing.stages.code.model`) — WIRED into `agent-loop.ts:355` so an un-seamed run divides by the calibrated window, the `debug.contextWindowTokens` seam still winning. Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; F-55 FIXED IN CODE** (`run-6b23da51-c440-432a-bbf8-51d4ee8a24af`, runtime `3a3dc8d`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-057.md`). `codex`/`gpt-5.5` one-shot all 3 files: a NEW pure `src/runner/context-window.ts` (`CONTEXT_WINDOW_TABLE` 14 rows mirroring `pricing.ts` family keys + `lookupContextWindow` longest-prefix, the `lookupPricing` analog + `resolveContextWindowForSpec`) WIRED into `agent-loop.ts:355` (seam still first in the `??` chain) + a 6-case vitest. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly 3 named files). $1.1870/$5 (23.7%), judge 0.8%, **898k/6.5k tokens** (NEW series-high input), 31 tool calls, 4m28s. AC-1+AC-2 re-run green (4 grep-pins + vitest 6 passed; tsc+eslint + full suite 480 passed | 19 skipped). Additive, no contract change; `decideContextWindowPacing`/journal payloads/`types.ts`/renderer untouched. **NO new friction** — F-55 (recurred dogfood-052→056) fixed in code; closure is the F-53 live-read shape (this run's own trace predates the wire, reads `peak window 904%`; first calibrated read = next run). Park-saturation recurs (6th point 602/604/759/585/334/904%, F-54/WP-250/251). | [`dogfood-057.md`](../../docs/reports/dogfood-057.md) |
| [`dogfood-058.yaml`](dogfood-058.yaml) | **WP-210 — pairwise + G-Eval scoring modes (Agent-as-a-Judge pillar), pure first slice.** The judge today emits a binary criteria/rubric verdict; WP-210 adds continuous/comparative scoring. This lands the pure scoring primitive — a NEW `src/judge/scoring.ts` with `normalizeGEvalScore(raw, scaleMin, scaleMax)` (clamp + scale a raw 1–5 criterion score to [0,1]) + `aggregateGEval(scores, opts)` (weighted mean → normalized aggregate + `passed` against a threshold) — the `buildVerdict`/`decideContextWindowPacing` pure-decision shape, local types, NO `types.ts`/contract change; re-exported from the `src/judge/index.ts` barrel; +a focused vitest. Cross-cutting failable logic (weighting, clamp edges, threshold-inclusive boundary, empty/zero-weight). The `scoringMethod` TaskSpec field + the live judge-harness wire are the SEPARATE §4 follow-up. Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; F-55 CLOSED BY OBSERVATION** (`run-67d39267-c99c-471e-b625-5de20a3bb8ca`, runtime `6292f62`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-058.md`). `codex`/`gpt-5.5` one-shot all 3 files: `src/judge/scoring.ts` (`normalizeGEvalScore` clamp→[0,1] divide-by-zero-guarded + `aggregateGEval` weighted-mean/missing-weight→1/≤0-weight-ignored/empty-degenerate/threshold-INCLUSIVE + 3 local types) re-exported from `src/judge/index.ts` + a 9-case vitest. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly 3 named files). $0.4905/$5 (9.8%), judge 1.8% ($0.0088), 355k/3.8k tokens, 16 tool calls, 2m57s. AC-1+AC-2 re-run green (4 grep-pins + vitest 9 passed; tsc+eslint + full suite 489 passed | 19 skipped). Additive, no contract change, no new dep; `buildVerdict`/`computeVerdict`/`types.ts`/journal untouched. **NO new friction** — and the FIRST calibrated live pacing read: journaled `pacing` = `action compact · projectedTokens 716,994 · utilization 1.792485` = `716994/400000` → `peak window 179%` (vs pre-wire 904%), confirming WP-252 and **closing F-55 by observation** (`compact 1 · park 0`, the dogfood-057-predicted close). | [`dogfood-058.md`](../../docs/reports/dogfood-058.md) |
| [`dogfood-059.yaml`](dogfood-059.yaml) | **WP-215 — new-dependency scan primitive (Agent-as-a-Judge security/architecture rubric), pure first slice.** The secret-scan side is fully landed (S1 `scanDiffForSecrets` dogfood-054 → S2 judge-evidence wire dogfood-055 → S3 allowlist dogfood-056); this lands the DEPENDENCY analog, WP-215's explicitly-open "dependency-scan half". A NEW `src/judge/scan-dependencies.ts` with `scanDiffForNewDependencies(diff)` — extracts module specifiers from added `import`/`from`/`require` lines (the `scanDiffForSecrets` added-lines-only/`+++`-excluded discipline), excludes relative/absolute/`node:` specifiers, normalizes a bare specifier to its package name (scoped `@scope/pkg/sub`→`@scope/pkg`, unscoped `lodash/merge`→`lodash`), sorted+de-duped+empty-safe — the `scanDiffForSecrets`/`aggregateGEval` pure-decision shape, NO `types.ts`/contract change; re-exported from `src/judge/index.ts`; +a 10-case vitest. Mechanizes the "no new dependencies unless the goal allowed them" review the dogfood loop does by hand. The live judge-evidence wire that consumes it is the SEPARATE WP-215 S5 follow-up. Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; new F-56 → WP-254** (`run-bc841ce6-ad2c-4356-bb49-355a7a7b6637`, runtime `00b31e8`, delivery uncommitted on the working tree byte-IDENTICAL to the run workspace; `docs/reports/dogfood-059.md`). `codex`/`gpt-5.5` one-shot all 3 files: a NEW `src/judge/scan-dependencies.ts` (`scanDiffForNewDependencies` — added-lines-only/`+++`-excluded; `from`/side-effect-`import`/`require` regexes; `.`/`/`/`node:` exclusion; scoped/unscoped subpath normalization; `Set`+`.sort()`, empty-safe; pure) re-exported from `src/judge/index.ts` + an 11-case vitest (10 mandated + an extra absolute-import case). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 3 named files). $0.9850/$5 (19.7%), judge 0.8%, 734k/5.9k tokens, 27 tool calls, 4m34s. AC-1+AC-2 re-run green (3 grep-pins + vitest 11 passed | full suite 500 passed | 19 skipped, tsc+eslint exit 0). Additive, no contract change, no new dep. **NEW F-56 → WP-254:** the WP-252-calibrated pacing metric STILL over-reads on codex steps — this trivial task PARKED at `peak window 370%` (`projectedTokens 1,480,248 · utilization 3.70062`, denominator still exactly 400k); the numerator sums a fresh subprocess's 27-internal-turn throughput as live occupancy, falsifying dogfood-058's "park-saturation breaks" (7th F-54 point). | [`dogfood-059.md`](../../docs/reports/dogfood-059.md) |
| [`dogfood-060.yaml`](dogfood-060.yaml) | **WP-215 S5 — wire `scanDiffForNewDependencies` into the inner-loop judge evidence** (Agent-as-a-Judge security pillar). dogfood-059 landed the pure dependency scanner but it is consumed NOWHERE; this threads it into `collectEvidence` (`src/judge/evidence.ts`) as an additive `CollectedEvidence.newDependencyLabels` field over the FULL diff + a judge-prompt evidence section (`prompt.ts` `renderNewDependencyLabels`) + the `harness.ts` thread + a new wire test — the EXACT dogfood-055 secret-scan S2-wire shape. So the judge mechanically flags the external packages a diff newly imports, in the same evidence pass it runs the secret scan. Additive (no contract change); the LLM still adjudicates the verdict (a deterministic dependency override is the §4 follow-up). 4 source files + 1 new test; cross-file, failable on the FULL-diff-before-truncation ordering. Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — delivery TEXTBOOK; but 🔴 new F-57 → WP-249 ESCALATED (green dashboard, red main)** (`run-291cf0b5-70d4-4919-8669-cef579679a56`, runtime `96df844`, landed `821cae5`; `docs/reports/dogfood-060.md`). `codex`/`gpt-5.5` one-shot all 5 files: `evidence.ts` imports `scanDiffForNewDependencies` + populates the REQUIRED `CollectedEvidence.newDependencyLabels` over the FULL diff (before `bound(...)` — the named failure surface, right), `prompt.ts` renders the `## EVIDENCE — deterministic new-dependency scan` section (`renderNewDependencyLabels`), `harness.ts` threads it, + a 3-case wire test (+1 entailed line in `secret-scan-evidence.test.ts`). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (run touched exactly the 5 files). $1.0665/$5 (21.4%), judge 0.9%, 775k/8.8k tokens, 30 tool calls, 4m39s. Section rendered `(none)` live (no NEW external pkg — unit-proven). **WP-215 S5 → 🟢; secret+dependency judge-evidence chain complete.** **🔴 F-57 → WP-249 ESCALATED:** the HARVEST commit `821cae5` swept 2 unrelated uncommitted host files (`test/cli/{cli,trace}.test.ts`, `stripAnsi` `\x1b` regex) the run never wrote into the delivery; they FAIL `eslint .` (`no-control-regex` ×2) → HEAD/main fails AC-2's own lint gate while the dashboard reads green (judge graded the clean clone; nothing re-gates the landed commit). Fixed inline (eslint-disable). 8th park data point (`peak window 392%`, F-56/WP-254). | [`dogfood-060.md`](../../docs/reports/dogfood-060.md) |
| [`dogfood-061.yaml`](dogfood-061.yaml) | **WP-249 fix-clause (c) — `chikory land --verify` re-runs the run's OWN journaled acceptance checks against the LANDED commit** (the gate-before-land thesis seam; closes dogfood-060 F-57). Today `--verify` reruns only the four generic `VERIFY_COMMANDS` (`src/cli/land.ts`); this threads the run's `acceptanceCriteria[].check`s into the same gate, run against the landed `repo` tree, FAIL-CLOSED (keep the commit, exit 1) on any red — so a land that would break the landed AC is refused, not rubber-stamped. Adds an injectable `loadAcceptanceChecks` dep (journal-reading default via `new Journal(journalPath(...)).getRun()?.task.acceptanceCriteria`, the `requireSpec` shape) + the fail-closed gate + an `acceptance: N/N checks green` count + 2 new land tests (passing + fail-closed keep-commit). 2 files; cross-file, failable (run-against-landed-tree, journal-default-vs-injectable, fail-closed keep-commit). Single `chikory run`, no seam env. | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; clause (c) LANDED, new F-58 → WP-249 de-escalated 🔴→🟡** (`run-f4dcc770-c9a6-4180-a0b6-28da39a60206`, runtime `ea29799`, delivery STAGED byte-IDENTICAL on the working tree pending harvest; `docs/reports/dogfood-061.md`). `codex`/`gpt-5.5` one-shot both files: `land.ts` adds the injectable `loadAcceptanceChecks` dep + journal-reading default (`new Journal(journalPath(dataDir ?? ".chikory", runId)).getRun()?.task.acceptanceCriteria`, the `requireSpec` shape) + the fail-closed acceptance gate AFTER `VERIFY_COMMANDS` (reuses `runCheck(check, repo)` against the landed tree, keep-commit `return 1` on red) + the `acceptance: N/N checks green` count; `land.test.ts` adds the passing + fail-closed-keep-commit cases. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 2 named files); $1.2787/$5 (25.6%), judge 0.8% ($0.0096), 965k/6.2k tokens (NEW series-high input), 27 tool calls, 4m38s; AC-1+AC-2 re-run green (4 grep-pins + vitest 10 passed; tsc+eslint + full suite 505 passed | 19 skipped). Additive, no contract change, no new dep. **WP-252 calibration CLOSED by a live PARK read** (`peak window 486%` = `projectedTokens 1,943,084 / 400,000`, first park-branch confirmation). **NEW F-58 → WP-249 de-escalated 🔴→🟡:** all three WP-249 clauses now exist in the product `chikory land` (commits only the run diff; stamps `Run-ID:` at `land.ts:122`; re-gates the landed commit), but the dogfood loop harvests via `scripts/harvest.sh` + manual `git commit` that BYPASSES `land --verify` — so F-57's failure mode is still reachable until the harvest adopts the re-gate (track-B). 9th park data point (F-56/WP-254). | [`dogfood-061.md`](../../docs/reports/dogfood-061.md) |
| [`dogfood-062.yaml`](dogfood-062.yaml) | **WP-202 Memory Pointer RECALL primitive (Memory Pointer Pattern / context-rot thesis pillar), pure first slice.** The WRITE/render half is landed (`shouldPointerize` + `formatPointerReference`, dogfood-028); this lands the READ half — a pure `parsePointerReference(line): ParsedPointerReference \| null` (the exact inverse of `formatPointerReference`: parse `[memory <kind> <idPrefix>] <bytes>B — <summary>`, malformed → null) + a pure `recallPointerExcerpt(line, excerptFn): Promise<string \| null>` (parse → call the injected `excerptFn(idPrefix, bytes)`, the `deps.runCheck` DI pattern → hermetic), in `src/runner/memory-pointer.ts` + barrel re-export + a focused vitest. The documented WP-202 "store.excerpt recall path" remainder; NO `types.ts`/contract change. Failable on the em-dash separator, the bytes suffix, the 12-char idPrefix, null-on-malformed, the round-trip with `formatPointerReference`. Single `chikory run`, no seam env. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file `memory-pointer.ts`+test+barrel, 1–3 steps, real parsing failure surface — em-dash split, bytes suffix, idPrefix, null handling, round-trip) · §1.2 ✅ (real open plan.md §6 WP-202 🟡 — the Memory Pointer Pattern named thesis pillar, the documented recall-path remainder, the pure-first shape the write half landed in, NOT throwaway scaffolding) · §1.3 ✅ PROCEED (the core context-rot mitigation this run's 486% window motivates; unblocked, not §4-walled — the WP-249 remainder is track-B harvest tooling, the dependency/secret OVERRIDE + WP-210 act + WP-250 are §4-walled, WP-251 observe-fold needs delicate multi-step seam tuning)** | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; RECALL/READ half LANDED, NO new friction (F-58/WP-254 reinforced)** (`run-01add160-0b20-49ed-9af1-6598c6c558ae`, runtime `c6f1b32`, delivery STAGED byte-IDENTICAL on the working tree pending harvest; `docs/reports/dogfood-062.md`). `codex`/`gpt-5.5` one-shot all 3 files: `memory-pointer.ts` adds `parsePointerReference` (regex `/^\[memory ([^\s]+) ([^\s]+)\] ([0-9]+)B — (.*)$/u`, literal U+2014, `Number.parseInt(bytes,10)`, null-on-malformed, exact inverse — round-trips) + the pure `recallPointerExcerpt(line, excerptFn)` (parse → injected async excerptFn; `null` without calling the fn on malformed) + the local `ParsedPointerReference` type; `index.ts` extends the existing re-export in place; `memory-pointer.test.ts` adds 5 cases (round-trip, multi-word summary, 3 malformed-null, spy recall `toHaveBeenCalledWith("abc123def456", 8192)`, malformed no-call). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 3 named files); $0.4436/$5 (8.8%), judge 1.9% ($0.0083), 319k/3.6k tokens, 12 tool calls, 2m52s; AC-1+AC-2 re-run green (5 grep-pins + scoped vitest 10 passed; tsc+eslint + full suite 510 passed | 19 skipped). Additive, no contract change, no new dep. **WP-202 RECALL/READ half → done.** **NO new friction:** F-58/WP-249 reinforced (delivery again STAGED, no `Run-ID:` trailer, harvested outside `chikory land --verify`); **WP-254 reinforced — a SPURIOUS compact** (`action compact · projectedTokens 646,090 · peak window 162%` yet true occupancy 319k/400k = 80%, UNDER the window — `646,090 ≈ 2×319k` numerator doubling). | [`dogfood-062.md`](../../docs/reports/dogfood-062.md) |
| [`dogfood-063.yaml`](dogfood-063.yaml) | **WP-210 PAIRWISE scoring mode (Agent-as-a-Judge scoring-modes thesis pillar), pure first slice.** The G-Eval half is landed (`normalizeGEvalScore` + `aggregateGEval`, dogfood-058); this lands the second half of WP-210's title — a pure `aggregatePairwise(outcomes: PairwiseOutcome[]): PairwiseResult` that tallies wins/losses/ties per candidate → `winRate = (wins + 0.5*ties)/comparisons` → a stable ranking (win-rate desc, id asc) + a clear `winnerId` or `null` (empty input OR top-tie), with `PairwiseOutcome`/`PairwiseTally`/`PairwiseResult` types, in `src/judge/scoring.ts` + barrel re-export + a focused vitest. NO `types.ts`/contract change. Failable on the tie=0.5 win-rate, the comparisons denominator, the stable sort tiebreak, top-tie→null, the empty path, and input mutation. Single `chikory run`, no seam env. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file `scoring.ts`+barrel+test, 1–3 steps, real aggregation failure surface — tie=0.5, denominator, stable sort, top-tie→null, empty, no-mutation; extends an existing module, not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-210 🟡 — the Agent-as-a-Judge scoring-modes named pillar, the documented PAIRWISE half, the pure-first shape the G-Eval half landed in, NOT throwaway scaffolding) · §1.3 ✅ PROCEED (real product-WP thesis slice; unblocked, not §4-walled — the `scoringMethod`+harness wire is the §4 follow-up exactly as G-Eval's was; WP-202 recall wire / WP-228 / WP-247 / WP-254 are §4 non-pure or semantics changes, WP-249 is track-B, WP-251 needs delicate multi-step seam tuning)** | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; PAIRWISE pure half LANDED, NO new friction (F-56/WP-254 + F-58/WP-249 reinforced)** (`run-72713667-b730-4037-ace7-468c238738c0`, runtime `3313d62`, delivery STAGED byte-IDENTICAL on the working tree pending harvest; `docs/reports/dogfood-063.md`). `codex`/`gpt-5.5` one-shot all 3 files: `src/judge/scoring.ts` adds `aggregatePairwise(outcomes): PairwiseResult` (Map-backed tally; `"a"`→a +win/b +loss, `"b"`→b +win/a +loss, `"tie"`→both +tie; `winRate = (wins + 0.5·ties)/total`; sort win-rate DESC then id ASC; `winnerId` null on empty OR top-tie; pure, no input mutation) + the `PairwiseOutcome`/`PairwiseTally`/`PairwiseResult` local types; `src/judge/index.ts` extends the existing `./scoring.js` re-export in place; `test/judge/scoring.test.ts` adds 5 cases (clear winner winRate 1, tie→0.5/null, mixed 3-candidate rank+id-tiebreak, empty degenerate, no-mutation snapshot). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 3 named files); $0.6412/$5 (12.8%), judge 1.3% ($0.0084), 466k/5.0k tokens, 20 tool calls, 2m52s; AC-1+AC-2 re-run green (6 grep-pins + scoped vitest 14 passed; tsc+eslint + full suite 515 passed \| 19 skipped). Additive, no contract change, no new dep; `aggregateGEval`/`normalizeGEvalScore`/`types.ts` untouched. **WP-210 PAIRWISE pure half → done; both pure scoring-mode primitives now exist (the §4 act-half harness wire remains).** **NO new friction:** F-56/WP-254 reinforced (~10th point — this trivial task PARKED at `peak window 236%` = `944k/400k` while true step input 466k = 116% of the 400k window; root cause pinned to `agent-loop.ts:350` feeding cumulative `spentTokens` as `currentInputTokens` → ~2× doubling); F-58/WP-249 reinforced (delivery again STAGED, no `Run-ID:` trailer, harvested outside `chikory land --verify`). | [`dogfood-063.md`](../../docs/reports/dogfood-063.md) |
| [`dogfood-064.yaml`](dogfood-064.yaml) | **WP-254 pacing-numerator fix — the pure LIVE-RESIDENT-OCCUPANCY estimator half (context-rot / pacing thesis pillar), pure first slice.** dogfood-063 pinned the F-56 root cause: `agent-loop.ts:350` feeds `decideContextWindowPacing` the run's CUMULATIVE token total as `currentInputTokens` (and the same step as `estimatedNextStepTokens`) → `projectedTokens ≈ 2× the step`, over-reading occupancy and falsely PARKING/compacting on trivial tasks (~10 data points). The pacing decision is already pure+correct and the denominator is already model-keyed; this lands the pure value the loop SHOULD feed instead — `estimateResidentContextTokens(parts: ResidentContextParts)` summing the system preamble + only the RETAINED TAIL of `recentSummaries` (folding drops the oldest), with clamp/empty/negative/no-mutation semantics — in `src/runner/pacing.ts` + barrel re-export + a focused `pacing.test.ts`. NO `types.ts`/contract change; the agent-loop feed swap is the SEPARATE §4 follow-up. Failable on the last-N-vs-first-N tail slice, the count clamp, folded-out exclusion, the empty/negative paths, and input mutation. Single `chikory run`, no seam env. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file `pacing.ts`+barrel+test, 1–3 steps, real numeric failure surface — retained-tail direction, clamp, folded-out exclusion, empty, negative clamp, no-mutation; extends an existing module, not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-254 🟡 — the context-rot/pacing named thesis pillar; directly attacks F-56, the most-reinforced friction in the corpus; the value it computes is the exact `currentInputTokens` the loop over-reads today — NOT throwaway scaffolding) · §1.3 ✅ PROCEED (real product-WP thesis slice fixing the loop's own broken pressure signal; unblocked, not §4-walled — the agent-loop feed swap is the §4 follow-up exactly as the prior pure halves' wires were; WP-210 act half / WP-202 recall wire / WP-228 / WP-247 are §4 non-pure, WP-249 is track-B, WP-251 needs delicate multi-step seam tuning)** | 🟢 **SUCCESS 1 step — clean one-shot, delivery verified; PURE NUMERATOR half LANDED, new F-59 → WP-255** (`run-6aa5081e-bda6-406a-b40f-ddf2355e17a5`, runtime `74875b9`, delivery STAGED byte-IDENTICAL on the working tree pending harvest; `docs/reports/dogfood-064.md`). `codex`/`gpt-5.5` delivered all 3 files: `src/runner/pacing.ts` adds `ResidentContextParts` + the pure `estimateResidentContextTokens(parts): number` (`Math.max(0, systemTokens + sum(recentSummaryTokens.slice(-clamp(retainedSummaryCount,[0,length]))))` — RETAINED TAIL, result clamped ≥0, empty/≤0/negative/no-mutation paths); `src/index.ts` extends the existing `./runner/pacing.js` re-export in place; `test/runner/pacing.test.ts` adds 6 cases (tail-sum 170, over-length retains all 160, 0/negative→100, empty→100, negative-`systemTokens`→0, no-mutation snapshot). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 3 named files) 4/4 rubric; $0.0084/$5 (0.2%, ALL judge), AC-1+AC-2 re-run green (4 grep-pins + scoped vitest 11 passed; tsc+eslint + full suite 521 passed \| 19 skipped). Additive — `decideContextWindowPacing` + the existing pacing exports + `types.ts` untouched; no contract change, no new dep. **WP-254 PURE NUMERATOR half → done** (the §4 agent-loop feed swap that RETIRES F-56 remains operator-landed). **NEW F-59 → WP-255:** step 1 journaled `step exceeded maxSeconds=600; killed (retriable: true)` but ran 24m32s = 2.45× the cap doing redundant post-completion verification, zeroing its telemetry (`0/0` tokens, `$0.00`) — the budget gate read $0 executor spend AND F-56 did NOT recur (`peak window 0%`) only because the kill zeroed the numerator (masked, not fixed). 🟢 Thesis WIN: durable + judge-grades-on-disk-artifacts recovered the killed executor into a correct lint-green SUCCESS (checkpoint `…@4 lastGood true`, no rollback/re-execution). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-064.md`](../../docs/reports/dogfood-064.md) |
| [`dogfood-065.yaml`](dogfood-065.yaml) | **WP-255 killed-step telemetry — the pure STEP-DEADLINE descriptor half (loop-integrity / cost-control reliability surface), pure first slice.** dogfood-064's F-59: a step was killed for exceeding `maxSeconds=600` but ran 24m32s = 2.45× the cap (root cause: `runBounded`, `src/executors/process.ts:48-55`, SIGTERMs/SIGKILLs the DIRECT child not the process GROUP, so codex's grandchildren keep the pipes open and `close` never fires until they exit naturally), and the kill zeroed the step's telemetry (`0/0` tokens, `$0.00`) so the trace makes the overrun INVISIBLE. This lands the pure value the killed-step journal + `chikory trace` SHOULD render — a `describeStepDeadline(input: StepDeadlineInput): StepDeadlineStatus` (elapsed/overran/overrunRatio/remaining) in a NEW `src/runner/step-deadline.ts` + barrel re-export + a NEW `test/runner/step-deadline.test.ts`. NO `types.ts`/contract change; the tree-reap (`process.ts`) and the killed-step journal wire are the SEPARATE §4 follow-ups. Failable on the `>` strict overran boundary at exactly-at-cap, the negative-span clock-skew clamp, the `remainingSeconds` ≥0 clamp, the `maxSeconds: 0` divide-by-zero guard, and input mutation. Single `chikory run`, no seam env. | not yet run — **gate verdict 🟡 ALLOW (fallback): §1.1 ✅ (cross-file NEW `step-deadline.ts` + barrel + NEW test, 1–3 steps, real numeric failure surface — strict overran boundary, negative-span clamp, remaining clamp, maxSeconds=0 guard, ms→s, no-mutation; not a 1-file port) · §1.2 🟡 fallback (real open plan.md §6 WP-255 🟡 code on the loop-integrity/cost-control surface, hosting the telemetry-visibility mechanism in WP-255's OWN module — the descriptor the killed-step journal/trace consume — NOT throwaway scaffolding; directly attacks F-59; the §1.2 carve-out fires because NO thesis-stressing slice on a real WP is unblocked — all §4-walled/track-B) · §1.3 🟡 ALLOW fallback (a real-WP pure slice, low on the thesis-stress axis, permitted ONLY because nothing thesis-stressing is unblocked; the blocking wall to unblock NEXT = the WP-255 tree-reap `process.ts` spawn-detached + `process.kill(-pid)` and the WP-254 agent-loop feed swap, both operator-landed §4)** | 🟡 **SUCCESS 1 step — flawless delivery but ORPHANED (🔴 new F-60 → WP-256); ran despite supersession** (`run-358273a3-dbbe-4103-8166-4f065606f309`, runtime `cfd4cba`, delivery STAGED byte-IDENTICAL on the working tree; `docs/reports/dogfood-065.md`). `codex`/`gpt-5.5` one-shot all 3 files: `src/runner/step-deadline.ts` adds `describeStepDeadline` + `StepDeadlineInput`/`StepDeadlineStatus` (`Math.max(0,(endedAtMs-startedAtMs)/1000)` elapsed, strict `>` overran, `Math.max(0,…)` remaining clamp, `maxSeconds>0?…:0` ratio guard, no rounding/mutation); `index.ts` extends the pacing-style barrel; `test/runner/step-deadline.test.ts` adds 6 cases (F-59 overrun `toBeCloseTo(2.4533)`, under-cap, exactly-at-cap, negative-span clamp, `maxSeconds:0` guard not `Infinity`/`NaN`, no-mutation snapshot). Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓ (exactly the 3 named files) 4/4 rubric; $0.7048/$5 (14.0%), judge 1.1% ($0.0078), 520k/4.7k tokens, 21 tool calls, 3m19s; AC-1+AC-2 re-run green (4 grep-pins + scoped vitest 6 passed; tsc+eslint + full suite 533 passed \| 19 skipped). Additive, no contract change, no new dep. **🔴 F-60 → WP-256:** the descriptor has ZERO runtime consumers (only the barrel + its test) — operator commit `0533a4c` (15:03) had ALREADY landed the same `elapsedSeconds`/`overrunRatio` arithmetic INLINE in `step.ts:150-163` 15 min before the run launched (15:18), and WP-255 was already 🟢 + the slice pre-labeled "now low-value". The pure-first cadence got LAPPED → manufactured duplicate dead code (the §5 standing failure mode). Fix = WP-256 launch-time staleness gate. F-61 (🟡): 520k input tokens to land a 50-line specced pure fn. | [`dogfood-065.md`](../../docs/reports/dogfood-065.md) |
| [`dogfood-066.yaml`](dogfood-066.yaml) | **FIRST durable CHAIN on REAL product code — WP-256 launch-time spec-staleness gate (the dogfood-065 F-60 fix).** Launched with `chikory chain` (NOT `run`), the goal genuinely decomposes into two dependent nodes that build the launch-time staleness decision which would have refused the orphaned dogfood-065: **Node A** (independent) a pure `parseWpStatus(planText, wpId): WpStatus \| null` in NEW `src/cli/wp-status.ts` (+ test) that reads a WP's status icon (🔴/🟡/🟢) from its plan.md table ROW — exact-id-cell match, 3rd-column extraction, prose/absent → null; **Node B** (dependent, imports node A via the WP-239 handoff) a pure `assessSpecStaleness(input): SpecStalenessReport` in NEW `src/cli/spec-staleness.ts` (+ test) with `stale === true` exactly when the target WP is already 🟢. NO seam — the durable chain (decompose → meta-judge gate → dependent handoff → per-node journal) IS the thesis vehicle, seeded into WP-256's OWN real code (closing the dogfood-047/048 scaffold-hosted gap for the chain pillar). NO barrel/`types.ts`/contract change. AC checks grep-pinned (F-49); AC-2 greps `spec-staleness.ts` for `parseWpStatus` so node B must IMPORT node A's symbol, not reimplement it. Failable on the planner decompose + gate (dogfood-041 burned 5 attempts), the dependent handoff, the exact-id/status-column parsing. `budget_usd: 12`, `max_steps: 8`. | not yet run — **gate verdict ✅ PROCEED (user-confirmed direction over the lower-risk WP-256 single-run): §1.1 ✅ (genuine 2-node `chikory chain` on the durable-execution pillar, node B imports node A; real parsing failure surface — table-row anchoring, exact-id `WP-25`≠`WP-255`, status-column, emoji→enum, not-found→null; plausibly fails, the chain path burned 5 attempts dogfood-041) · §1.2 ✅ (real open plan.md §6 WP-256 🔴, the F-60 fix queued by THIS review — the chain is the thesis mechanism seeded INTO WP-256's real code, NOT throwaway scaffolding) · §1.3 ✅ PROCEED (thesis-stressing slice on a real product WP, unblocked — chain machinery matured since dogfood-041: WP-239 handoff 🟢, resume proven dogfood-044, proxy auto-restart WP-234, planner/gate fixes WP-235/236/238/240). The §1.3 binding veto on a pure/busy headline FIRES here — WP-254/255 are both 🟢, so no pure-first fallback is permitted.** | 🟡 **FAILED at node A — chain ran, surfaced a REAL decomposition bug (the "ALSO VALID THESIS DATA" branch); WP-256 NOT delivered, new F-62 → WP-257** (`chain-15509162-b259-483c-b313-f9d7dbafbcfa`, node-a run `chain-15509162-…-node-node-a`, runtime `e423b64`; nothing harvested, working tree clean; `docs/reports/dogfood-066.md`). Plan-judge PROCEED on the decomposition; node A FAILED · 3 steps · $3.76/$6.00 (62.7%) · judge 0.3% ($0.0113) · 7m51s; node B (the WP-239 handoff) never dispatched. **Root cause F-62 → WP-257 (🔴):** the chain planner compressed node A's brief into a one-line `node.goal` ("Implement … to parse and test WP status from plan.md markdown tables.") and DROPPED the spec's verbatim grep-pinned fixtures (`WP-255`/`WP-25`), while preserving the strict AC-1 grep (`grep -q "WP-25" …`); `planNodeToTaskSpec` (`src/chain/node-spec.ts:91`) hands that paraphrase to the executor as its `goal`, so the executor built a SANE generic-table parser + 5 passing tests using REAL `plan.md` ids (WP-001/WP-210/WP-30/…) and was graded against an AC referencing literals it never received → structurally unwinnable → judge budget-waste guard HALTed after 3 consecutive AC-1 fails. F-49 discipline silently assumes a single `run` carries the brief — FALSE for a chain. F-63 (🟢 positive, no WP): the 3-strike HALT bounded the bug's cost cleanly. The chain DECOMPOSE + meta-judge PROCEED + durable node-A journal WERE exercised (✅ F-32 satisfied). **WP-256 stays 🔴 + unblocked — re-deliver via single `chikory run` (sidesteps F-62) → dogfood-067.** | [`dogfood-066.md`](../../docs/reports/dogfood-066.md) |
| [`dogfood-067.yaml`](dogfood-067.yaml) | **WP-256 launch-time spec-staleness gate (the dogfood-065 F-60 fix) — re-delivered as a SINGLE `chikory run` after dogfood-066's chain HALTed on F-62.** Same four NEW files as dogfood-066 but combined into ONE single run (NOT a chain): a pure `parseWpStatus(planText, wpId): WpStatus \| null` in `src/cli/wp-status.ts` (+ test) reading a WP's status icon (🔴/🟡/🟢) from its plan.md table ROW — exact-id-cell match (`WP-25`≠`WP-255`), 3rd-column extraction, prose/absent → null; and a pure `assessSpecStaleness(input): SpecStalenessReport` in `src/cli/spec-staleness.ts` (+ test) that IMPORTS the parser and sets `stale === true` exactly when the target WP is already 🟢 (the F-60 condition). A single run hands the FULL goal — with every mandated grep-pinned literal — straight to the executor (no planner paraphrase), so the F-62 trap that HALTed dogfood-066's chain is ABSENT; both modules share one workspace so the consumer imports the parser directly (no WP-239 handoff). NO barrel/`types.ts`/contract change. ACs grep-pinned (F-49); AC-2 greps `spec-staleness.ts` for `parseWpStatus` so the consumer must IMPORT not reimplement. `budget_usd: 5`, `max_steps: 5`. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (4 files across two pure modules + the consumer imports the parser, 1–3 steps; real parsing/decision failure surface — table-row anchoring, exact-id `WP-25`≠`WP-255`, status-column-not-notes, emoji→enum, not-found→null, the `stale===green` F-60 condition, no mutation; not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-256 🔴, the F-60 fix; four files absent on HEAD — net-new real product code, NOT a duplicate of any inline logic, NOT scaffolding) · §1.3 ✅ PROCEED (real product WP — the loop's own F-60 fix — delivered on the path that sidesteps the freshly-found F-62; not busy/scaffold-hosted, veto does not fire; the chain pillar's WP-257 is meatier but is planner-prompt work and re-chaining WP-256 is blocked on it)** | 🟢 **SUCCESS 2/2 — WP-256's pure decision DELIVERED, and (launched as `chikory chain` instead of the prescribed single `run`) became the FIRST durable chain to land real open-WP product code end-to-end** (`chain-d18a8c1b-f681-4fe3-ba35-c38fa0f54b65`, plan `plan-c4fd3fb0-…`, runtime `a4b8e7a`; 4 files STAGED uncommitted, byte-IDENTICAL to node-2 workspace; `docs/reports/dogfood-067.md`). decompose → meta-judge PROCEED → node-1 `parseWpStatus` SUCCESS (1 step, $1.6743, 1,280,082/6,912 tok) → **node-2 cloned node-1's handed-off tree (`baseCommit f7f494a == node-1 head`) and IMPORTED `parseWpStatus` (WP-239 handoff exercised LIVE — AC-2 grep forbade reimplementation)** → node-2 `assessSpecStaleness` SUCCESS (1 step, $0.8330, 624,547/4,591 tok) → chain SUCCESS. $2.5073 total · judge 0.46% ($0.0115) · 6m44s · executor `codex`/openai vs judge `gemini-3.1-pro-preview`/openai-compat (family-diverse); AC re-run green (tsc+eslint+9 vitest). **F-66 (🟢, no WP): closes the dogfood-066 handoff gap.** Two residuals: **F-65 → WP-258 (🔴)** — WP-256's gate landed ORPHANED (`assessSpecStaleness` has zero live consumers; the 4-files spec scoped out the wiring) → WP-256 → 🟡; **F-64 → WP-257 (still 🔴)** — the planner still paraphrased node-1's goal, dropping the parent's positional semantics (id-FIRST-cell/status-THIRD-cell), so the executor built a divergent header-driven parser that passed the loose AC — a chain can go green building the WRONG function; non-fatal only because the `WP-25` literal was hardened into the AC-1 description. | [`dogfood-067.md`](../../docs/reports/dogfood-067.md) |
| [`dogfood-068.yaml`](dogfood-068.yaml) | **WP-258 — WIRE WP-256's spec-staleness gate into the live `chikory run` launch path (the dogfood-067 F-65 fix: the gate landed orphaned).** Single `chikory run` (NOT a chain — avoids re-risking dogfood-067's F-64 planner paraphrase). Three changes in `packages/sdk-ts`: (1) NEW pure `src/cli/spec-staleness-precheck.ts` (+ test) — `extractTargetWpId(goal)` (first `WP-\d+`, null when absent) + `evaluateSpecStalenessPrecheck({goal, planText})` that IMPORTS WP-256's `assessSpecStaleness` and returns a non-null `warning` (containing `stale` + the id) only when the target WP is already 🟢; warn-ONLY, pure; (2) the LIVE wire in `cmdRun` (`src/cli/commands.ts`) right after `parseTaskSpec` — best-effort read `plan.md` (injectable additive `CliDeps.readPlanText`, missing plan must not break launch), call the precheck, emit any `warning` via `ioPair.err`, then proceed unchanged. NO `types.ts`/contract/barrel/workflow change; does not edit WP-256's `spec-staleness.ts`/`wp-status.ts` (consumes them). The landed diff CONSUMES `assessSpecStaleness` at a real call-site → kills the F-65 orphan + flips WP-256 → 🟢. ACs grep-pinned (F-49); AC-2 greps `commands.ts` for `evaluateSpecStalenessPrecheck`+`readPlanText` so the wire MUST exist (anti-F-65). `budget_usd: 5`, `max_steps: 5`. | 🟢 **SUCCESS 2/2 — launched as a `chikory chain` (spec was single-`run`), gate WIRED LIVE, WP-256 + WP-258 → 🟢** (`chain-aa25aa5c-c3e7-4a5c-a8cf-d33723f0655b`, plan `plan-8844e604-…`, runtime `e0da13f`, 3 files STAGED uncommitted byte-IDENTICAL to node-2 workspace; `docs/reports/dogfood-068.md`). node-1 `precheck-module` (pure module+test, $1.1489, 870k/5.7k tok) → node-2 `wire-precheck-cli` (the `commands.ts` wire, $0.5499, 409k/3.4k tok) via the WP-239 handoff (`baseCommit 4631f4d == node-1 head`); $1.6988 total, judge 0.62%, ~5m42s; AC re-run green (tsc+eslint+547 vitest). **F-69 🟢: 2nd consecutive durable chain on real product code.** Residuals: **F-67 → WP-259** (planner paraphrase dropped the mandated `stale` field + object param → contract drift past a name-only grep AC), **F-68 → WP-260** (wire reads the whole yaml, not `spec.goal`); both reinforce **F-64 → WP-257** (now triply-evidenced). | dogfood-068 |
| [`dogfood-069.yaml`](dogfood-069.yaml) | **WP-257 — the pure LITERAL-PRESERVATION VERIFIER for the chain planner (the F-64 root-cause guardrail half).** Authored single-`chikory run` (NOT a chain — a chain re-risks the very F-64 paraphrase this WP fixes). A NEW pure `src/planner/literal-preservation.ts` — `extractGoalLiterals(goalText)` (first-seen, de-duped backtick literals) + `planLiteralGaps(plan)` (parent literals NO node goal preserves, EXACT-TOKEN rule so `WP-25`≠`WP-255`, the dogfood-066 discriminator) — mirroring `coverage.ts`'s `planCoverageGaps`, type-only `Plan` import, no contract change; + a 7-case vitest. The `runPlannerPass`/`buildPlan` REVISE wire is the SEPARATE §4 follow-up. | 🟢🔴 **SUCCESS 3 steps — pure verifier LANDED + PROVEN against the live defect; the run DOGFOODED ITS OWN BUG; new F-70 → WP-261** (`chain-bdaef796-667f-4e17-8ad2-4bd49951bb7f-node-implement_literal_preservation`, plan `plan-91e4a59a-…`, runtime `361c4b4`, 2 files STAGED uncommitted byte-IDENTICAL to the node workspace; `docs/reports/dogfood-069.md`). `codex`/`gpt-5.5` delivered both files; judge `gemini-3.1-pro-preview` steps 1-2 ✓ PROCEED 1/2 (AC-1 grep pins missing), step 3 ✓ PROCEED 2/2; $2.7279/$5 (54.6%), judge 0.7% ($0.0192), 695k+595k+748k input tokens, 10m30s; AC-1+AC-2 re-run green (7 grep-pins + scoped vitest 7 passed; tsc+eslint + full suite 554 passed | 19 skipped). Delivery EXCEEDS spec (bonus exact-token-boundary case), mirrors `coverage.ts`. **🔴 F-70 → WP-261:** spec authored single-`run` (header 3× "NOT a chain") but launched as `chikory chain` (3rd consecutive launch-mode divergence) → F-64 recurred — the planner's 2-sentence single `node.goal` dropped **32 of 35** mandated literals (the DELIVERED `planLiteralGaps` over this run's own `(parent, node)` goal returns those 32 gaps); steps 1-2 burned ~$1.72 = 63% of cost failing AC-1, the run survived only because the F-49 grep pins persisted into the ACs and the judge drove re-addition by step 3. **F-71 (🟢, no WP): the landed verifier is proven correct against the LIVE defect** → the §4 `runPlannerPass`/`buildPlan` REVISE wire (operator-landed / track-B) is the top remaining WP-257 slice and would have caught this paraphrase at plan time. WP-257 → 🟡 (pure half done; wire remains). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-069.md`](../../docs/reports/dogfood-069.md) |
| [`dogfood-070.yaml`](dogfood-070.yaml) | **WP-261 — the pure LAUNCH-MODE-MISMATCH precheck (the dogfood-069 F-70 fix half).** Single `chikory run` (NOT a chain — and THIS time honor it; F-70 proved chaining a single-`run` spec re-triggers F-64). A NEW pure `src/cli/launch-mode-precheck.ts` — `detectIntendedSingleRun(specText)` (true when the spec text carries the conventional single-run marker: `NOT a chain` / `single \`chikory run\`` / `Launch with \`chikory run\``, case-insensitive) + `assessLaunchModeMismatch({ intendedSingleRun, launchedAsChain }): LaunchModeMismatch \| null` (non-null with a `warning` field EXACTLY when both booleans are true — the F-70 condition; a genuine chain spec launched as a chain is NOT a mismatch) + the `LaunchModeMismatch` interface — mirroring `spec-staleness-precheck.ts`'s `evaluateSpecStalenessPrecheck` shape, no contract change; + a 6-case vitest. **F-67-hardened:** AC-1 greps the test for `assessLaunchModeMismatch({` (object param), `.warning` (result field), and `interface LaunchModeMismatch` (the shape), not just names. The `cmdChain` wire that warns/refuses on a non-null result is the SEPARATE §4 follow-up. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file NEW module + test, 1–3 steps; real failure surface — marker-phrasing detection, the mismatch TRUTH TABLE, the genuine-chain discriminator, null-when-aligned, no-mutation; real BUG surface — F-70 cost 63% of dogfood-069; not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-261 🔴, queued by the dogfood-069 review — the launcher-guard's pure half, the `evaluateSpecStalenessPrecheck` analog; both files absent on HEAD; the `cmdChain` wire is the §4 follow-up; NOT scaffolding) · §1.3 ✅ PROCEED (fresh, triply-evidenced costly bug surface; the slice hosts the fix in WP-261's own real code; the veto does not fire — every remaining core-pillar slice is a §4 operator-landed wire, no thesis-stressing real-WP PURE slice is unblocked to displace it)** | 🟢🔴 **SUCCESS 2 nodes — pure guard LANDED + PROVEN against its OWN launch; the run DOGFOODED ITS OWN BUG A SECOND TIME (F-72), 4th consecutive launch-mode divergence** (`chain-14f72c09-debe-4f60-a6ca-1c3b3b7d0e65` nodes node-1/node-2, plan `plan-be71d6b1-…`, runtime `07f9687`, 2 files STAGED uncommitted, node-2 copies byte-IDENTICAL to the working tree; `docs/reports/dogfood-070.md`). **Launched as a `chikory chain`** (spec was single-`run`, header "NOT a chain"/"HONOR THE LAUNCH MODE THIS TIME") → planner split into node-1 (implement module + test shell, $1.2933, 988k/5.3k tok, PROCEED 1/1) → node-2 (complete test + tsc/eslint/full-suite, $0.6840, 498k/5.4k tok, PROCEED 2/2) via the WP-239 handoff; $1.9773 total (each node own $2.50 budget), judge ~0.6%, both PROCEED first-try; AC re-run green (8 grep-pins + scoped vitest 8 passed; tsc+eslint + full suite 562 passed | 19 skipped). Delivery meets spec exactly (exact `LaunchModeMismatch` shape, both markers + a benign 4th, warning contains both mandated substrings, no mutation, JSDoc). **🔴 F-72:** F-64 recurred — the two node goals dropped the marker regexes / `warning` substrings / truth table / verbatim test cases; survived first-try (cheaper than 069's $2.73) ONLY because the F-49 grep pins held the shape and the executor converged on the un-pinned `warning` substrings by luck (silent-divergence tail — reinforces WP-257). **The delivered `assessLaunchModeMismatch({true, true})` returns non-null for THIS very spec — the guard would have refused/warned its own launch.** **F-73 (🟢, no WP): proven against its own launch** → the §4 `cmdChain` wire is the top remaining WP-261 slice. WP-261 → 🟡 (pure decision done; `cmdChain` wire remains). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-070.md`](../../docs/reports/dogfood-070.md) |
| [`dogfood-071.yaml`](dogfood-071.yaml) | **WP-261 §4 — WIRE the landed launch-mode guard into `cmdChain` (the dogfood-070 F-72 fix).** Single `chikory run` (NOT a chain — one cohesive CLI edit; and a chain re-risks the very F-64 paraphrase this guard exists to catch). EDIT `src/cli/chain.ts`: import `detectIntendedSingleRun`/`assessLaunchModeMismatch` from `./launch-mode-precheck.js` and, right after `parseTaskSpec` and BEFORE `createRouter`/`planAndGateChain` (so the refusal costs ZERO LLM spend), compute `assessLaunchModeMismatch({ intendedSingleRun: detectIntendedSingleRun(yamlText), launchedAsChain: true })` over the RAW `yamlText` (the single-run marker lives in the header comment, not `spec.goal` — sidesteps F-68); when non-null and `CHIKORY_ALLOW_LAUNCH_MODE_MISMATCH` is unset, emit the `warning` + an override hint via `ioPair.err` and `return 1`; otherwise fall through unchanged. Plus a NEW behavioral test `test/cli/chain-launch-mode.test.ts` that INVOKES `cmdChain` against a temp marker spec and asserts exit 1 + both mandated `warning` substrings (F-67-hardened — the wire must actually refuse, not just mention the symbol). CONSUMES the dogfood-070 pure guard at a real call-site (kills the F-65 orphan) → closes WP-261 🟡→🟢; the direct analog of dogfood-068's `cmdRun` wire. NO new dep; no `types.ts`/contract/barrel/`commands.ts`/`launch-mode-precheck.ts` change; no CLI-flag/arg-parser edit (env override). `budget_usd: 5`, `max_steps: 5`. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file EDIT chain.ts + NEW behavioral test; real failure surface — wire BEFORE the planner so no cost is wasted, RAW `yamlText` not `spec.goal`, correct booleans, the env override, loud refusal + `return 1`, don't break the genuine-chain happy path; 1–3 steps; not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-261 🟡→🟢; consumes the just-landed pure guard at a real call-site — anti-F-65 orphan; net-new real product code, NOT scaffolding) · §1.3 ✅ PROCEED (closes the loop's OWN most-recurring, costly 4-run operator defect; neither busy nor scaffold-hosted; veto does not fire — the judge/context-rot pure surfaces are exhausted and the chain-pillar pure halves are landed, so no thesis-stressing real-WP PURE slice is unblocked to displace it)** | 🟡 **SUCCESS 1 node — WP-261 §4 `cmdChain` wire LANDED (WP-261 → 🟢), but the F-64 paraphrase caused REAL contract drift that shipped GREEN + the run DOGFOODED ITS OWN BUG a 3rd time (5th launch-mode divergence)** (`chain-fd45e5a6-6805-4898-b171-e290823c84b2-node-node-1`, plan `plan-2976e9af-…`, runtime `fc90348`, 2 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-071.md`). Launched as `chikory chain` despite the header's 4× "NOT `chikory chain`" — the run whose whole job is to REFUSE this was launched by MAKING it (F-74, 🔴→🟢: closes on commit, this wire IS the fix). `codex`/`gpt-5.5` one node ($1.1274/$5, 848k/6.1k tok, 32 tool calls, 3m38s); judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 4/4 rubric ($0.0066, 0.6%); AC re-run green (8 grep-pins + scoped vitest 1 passed; tsc+eslint + full suite 563 passed \| 19 skipped). `cmdChain` (`chain.ts:403-411`) refuses a single-`run`-marked spec launched as a chain at ZERO LLM cost → kills the F-65 orphan. **🔴 F-75 → WP-262:** the planner's one-line `node.goal` DROPPED three mandated behaviors — guard-after-`parseTaskSpec` placement (delivered BEFORE), the SECOND override-hint `ioPair.err` line (dropped; test locks it with `err.toHaveLength(1)` → the `CHIKORY_ALLOW_LAUNCH_MODE_MISMATCH` escape hatch is invisible on-screen), and the non-empty override (narrowed to `=== "1"`) — and the behavioral test the spec's F-67 HARDENING mandated was itself under-built (unparseable `not: parsed` fixture), so the drift shipped GREEN past substring-only ACs + the judge. The WP-257 silent-divergence tail realized in DELIVERY, not luck (WP-257 now quadruply-evidenced). WP-261 → 🟢 (spec-conformance remainder rides F-75 → WP-262). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-071.md`](../../docs/reports/dogfood-071.md) |
| [`dogfood-072.yaml`](dogfood-072.yaml) | **WP-233(a) — classify a transport/infra failure in the chain plan-gate distinctly from a substantive plan REVISE/rejection (the durable-execution thesis pillar; dogfood-041 attempt-2 F-33).** Single `chikory run` (NOT a chain — a chain re-risks F-64, and this run BUILDS the pure classifier, it does not exercise a live gate). A NEW pure `src/chain/plan-gate-failure.ts` — `classifyPlanGateFailure(input): PlanGateFailureClass` distinguishing an infra/transport fault ("safe to re-run") from a genuine non-PROCEED plan verdict, so `planAndGateChain` can tell "the judge was unreachable" from "the plan was rejected" (today conflated — F-33) — + a focused vitest. NO `types.ts`/contract/barrel-shape change beyond the additive re-export; the `planAndGateChain` wire + the Plan-persistence half (WP-233(b)) are the SEPARATE §4 follow-ups. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file NEW module + test on the durable-execution pillar; real failure surface — transport/`fetch failed`/timeout vs ESCALATE-as-value vs REVISE vs PROCEED discrimination, the safe-to-re-run boolean, unknown→conservative default; 1–3 steps; not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-233 🔴 — the durable/resumable chain-planning thesis pillar, queued Next-up from dogfood-041 F-33; net-new real product code, the classifier `planAndGateChain` consumes, NOT scaffolding) · §1.3 ✅ PROCEED (thesis-stressing durable-execution slice on a real product WP; the WP-261/WP-257 remainders are operator-landed/track-B CLI-harness wires, not headlines; not busy/scaffold — veto does not fire)** | 🟢 **SUCCESS 2 steps — WP-233(a) pure classifier LANDED, spec-faithful (WP-233 → 🟡); but the whole run cost was a retry tax after codex blew the 600s wall-clock cap** (`run-1ac16aa8-dc6a-4a72-bb94-d59ccb8a13d2`, runtime `e6feab9`, 3 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-072.md`). **First CORRECT single-`run` launch in 6** (the 067–071 launch-mode divergence streak broke) → full goal reached the executor verbatim, ZERO F-64 drift. `codex`/`gpt-5.5` wrote the complete delivery (`classifyPlanGateFailure` + `PLAN_GATE_INFRA_REASON_PREFIXES` + `PlanGateFailureClass`, anchored `startsWith`, PROCEED→null, no mutation) with all 8 grep-pins + the anchor-vs-includes discriminator; judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓; AC re-run green (11 grep-pins + scoped vitest 8 passed; tsc+eslint + full suite 571 passed \| 19 skipped); harvest 3/3 IDENTICAL. **🔴 F-76 → WP-263:** codex wrote the full delivery in step 1 (5765-byte diff, AC-1 ✓) then blew the 600s cap (`killed after 653.1s, 1.09×, retriable`) → the retry (step 2) had a 0-byte diff yet cost $0.4072/298k tokens re-running the ACs = **96.0% of the run** for zero incremental delivery (WP-255 fixed reaping — kill at 1.09× vs 2.45× — not the retry re-execution). **🟡 F-77 reinforces WP-255:** the killed codex step sealed $0.00/0 tokens (the KNOWN codex residual — `codex.ts:62` reads usage only at `turn.completed`). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-072.md`](../../docs/reports/dogfood-072.md) |
| [`dogfood-073.yaml`](dogfood-073.yaml) | **WP-233(b) part 1 — the plan-gate FAILURE NOTICE RENDERER + the `planAndGateChain` CONSUMER WIRE (the F-33 operator-facing fix; consumes the dogfood-072 classifier).** Single `chikory run` (NOT a chain). A NEW pure `src/chain/plan-gate-notice.ts` — `renderPlanGateFailureNotice(cls: PlanGateFailureClass): string` branching ONLY on `cls.kind` into the two verbatim operator lines (`"…INFRA fault, SAFE to re-run: "` + reason vs `"…NOT safe to re-run as-is: "` + reason), pure, no input mutation — + the barrel re-export + the `src/cli/chain.ts` wire that computes `classifyPlanGateFailure(gated.verdict)` → `renderPlanGateFailureNotice` as the gate-failure `message` (raw-rationale fallback on null) + a NEW test that composes classifier→renderer on real `PlanVerdict` literals (SAFE/NOT-safe polarity + reason preservation + no-mutation). Kills the dogfood-072 classifier's F-65 orphan by consuming it at a real call-site. NO `types.ts`/contract/`meta-judge-harness.ts`/`plan-gate-failure.ts` change. `budget_usd: 5`, `max_steps: 5`. | not yet run — **gate verdict ✅ PROCEED: §1.1 ✅ (cross-file NEW module + EDIT chain.ts + NEW test on the durable-execution pillar; real failure surface — the SAFE-vs-NOT-safe polarity, branch on `cls.kind` not the rationale, preserve the reason, wire the classifier at the gate-failure return + null-fallback, don't break the PROCEED/plan/FamilyDiversity paths; 1–3 steps; not a 1-file port) · §1.2 ✅ (real open plan.md §6 WP-233 🟡; CONSUMES the just-landed WP-233(a) classifier at a real call-site — anti-F-65 orphan; net-new real product code) · §1.3 ✅ PROCEED (thesis-stressing durable-execution slice, the operator-facing half of the F-33 fix; the WP-261/262/263 remainders are operator-landed/track-B wires, not headlines; not busy/scaffold — veto does not fire)** | 🟢 **SUCCESS 1 step — WP-233(b) part 1 LANDED spec-faithful first-pass; 2nd consecutive correct single-`run` launch; the review post-mortem re-framed dogfood-072's retry tax (new 🔴 F-78 → WP-264)** (`run-a5f8c5fe-26f8-4d33-b94b-b676039d6a8c`, runtime `008d3fd`, 4 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-073.md`). `codex`/`gpt-5.5` one-shot all 4 files ($0.7384, 548k/5.3k tok, 22 tool calls, 2m54s); judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 4/4 rubric ($0.0098, 1.3%); AC re-run green (12 grep-pins + scoped vitest 3 passed; tsc+eslint + full suite 574 passed \| 19 skipped). Exact operator polarity, kind-only branching, surgical one-return wire, real-`PlanVerdict` composed test — the WP-233(a) orphan killed. **🔴 F-78 → WP-264:** dogfood-072's post-kill AC-2 "failure" was an INFRA artifact — `runCheck` (`evidence.ts:76`) doesn't reap the check's process tree, so the 120s cap fired but the check ran 695.9s (5.8×, vitest tinypool `Failed to terminate worker`) and blocked the seal. **🟡 F-79:** the WP-263 "judge-only seal" already exists (`agent-loop.ts:470-478`) — WP-263 re-scoped, WP-264 first. **🟡 F-80** (no WP): executor ignored the bare-`pnpm exec` mandate (used `devbox run --` wrappers) + burned `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL` before self-installing. F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-073.md`](../../docs/reports/dogfood-073.md) |
| [`dogfood-074.yaml`](dogfood-074.yaml) | **WP-264 — the judge-check runner must reap the check's whole process tree at the timeout cap (the dogfood-073 F-78 fix; port the WP-255(a) group-kill to `runCheck`).** Single `chikory run` (NOT a chain). EDIT `src/judge/evidence.ts`: replace `runCheck`'s `execFileAsync("/bin/sh", ["-c", check], { timeout })` (kills only the direct `/bin/sh`; grandchildren hold the pipes — dogfood-072's AC-2 ran 695.9s on a 120s cap) with the already-proven `runBounded("/bin/sh", ["-c", check], { cwd, env, maxSeconds: timeoutMs/1000 })` from `../executors/process.js` (`spawn(detached:true)` + `process.kill(-pid)` SIGTERM→SIGKILL), preserving the `CheckRun` shape, the exact `[check timed out after ${timeoutMs}ms]` marker, `scrubExecutorEnv`, and the 64 KiB `bound()`. + a NEW behavioral test `test/judge/check-timeout-reap.test.ts` through the exported `collectEvidence`: a check spawning a backgrounded 60s pipe-holding sleeper with `checkTimeoutMs: 1000` must settle bounded-near-cap (`durationMs` < 10s, nonzero exit, marker present) — pre-fix it hangs ~60s. NO `types.ts`/contract/`process.ts`/`DEFAULT_CHECK_TIMEOUT_MS` change. `budget_usd: 5`, `max_steps: 5`. | 🟢 **SUCCESS 1 step — WP-264 LANDED spec-faithful first-pass (WP-264 → 🟢, F-78 closed); the judge-check runner now reaps the process tree; 3rd consecutive correct single-`run` launch; NO new friction; the LAST prescribed-diff headline** (`run-6063231c-402e-4eda-9881-736b39cbf876`, runtime `4112701`, 2 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-074.md`). `codex`/`gpt-5.5` one-shot both files ($1.9591, 1477k/11k tok, 48 tools, 4m50s ≪ 600s — no kill); judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 4/4 rubric ($0.0099, 0.5%); AC re-run green (5 grep-pins + scoped vitest 2 passed, the `sleep 60 & sleep 60` grandchild REAPED in 1104 ms on a 1 s cap; tsc+eslint + full suite 576 passed \| 19 skipped). `runCheck` (`evidence.ts`) now calls the group-killing `runBounded(…, { maxSeconds: timeoutMs/1000 })`, the direct-`/bin/sh` `execFileAsync` gone (the `git()` helper's stays), exact marker + 64 KiB `bound()` preserved. ℹ️ Token-economics: 1.477M input = recent high-water (2.7× dogfood-073's 548k for comparable prescribed work — WP-203/207 data, no WP). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). **Progression ⛔ STALLED → next headline is WP-265 rung 1 (loose WP-212).** | [`dogfood-074.md`](../../docs/reports/dogfood-074.md) |
| [`dogfood-075.yaml`](dogfood-075.yaml) | **WP-265 rung 1 — LOOSE-spec headline (first non-prescribed run): give the operator `chikory inject <run-id> "<guidance>"` — mid-run correction injection (WP-212, durable-execution / HITL pillar).** The runtime SUBSTRATE already exists (`injectSignal` handler → `pendingInjections` → next-step context `agent-loop.ts`; journaled kind `injection`; `RunHandle.inject(guidance)` `runner.ts:89`); the open gap is the operator-facing CLI command (no `inject` case in `cli/main.ts`, no `cmdInject`). Goal states the OUTCOME + constraints, ACs pin done (command dispatches to `handle.inject` with the exact guidance via a dependency-injected fake runner; missing run-id/guidance → nonzero exit; the existing journal→context path stays green), implementation left to the executor. Single `chikory run` (NOT a chain). `budget_usd: 6`, `max_steps: 6`. | not yet run — **gate verdict ✅ PROCEED: progression ⛔ STALLED forces the WP-265 ladder rung (binding); §1.1 ✅ (cross-file — CLI command + arg wiring + behavioral test on the durable-execution/HITL pillar; a competent agent could plausibly fail the signal dispatch, arg validation, or the fake-runner test seam) · §1.2 ✅ (real open plan.md §6 WP-212 🟡 — feature code completing the operator injection tool the longer rungs need; substrate reuse, no scaffolding) · §1.3 ✅ PROCEED (thesis-stressing HITL slice on a real product WP; not busy/scaffold — veto does not fire) · §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta ratio 1/3, cap not busted)** | 🟡🟢 **FAILED terminal, but DELIVERY COMPLETE — `chikory inject` DELIVERED + live-proven, WP-212 → 🟢; the run sealed FAILED on a self-inflicted AC-1 filename-pin** (`run-bb715500-c0d7-44af-a4e6-9d96dd4512f5`, runtime `bf0bcb1`, 3 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-075.md`). **FIRST `spec_format=loose` headline** — progression ⛔ STALLED → ✅ PROGRESSING (rung 0→1, loose 0→1, steps 2→3). `codex`/`gpt-5.5` delivered `cmdInject` (`commands.ts:448`, mirrors `cmdCancel`) + `case "inject"` dispatch/help + a LIVE Temporal test asserting the sentinel `WP-212-INJECT-SENTINEL` is journaled kind-`injection` verbatim AND reaches the next step's `context.injections`; AC-2 (tsc+eslint+full suite) green 589 passed \| 19 skipped. 3 steps $3.85/$6 (64.2%), judge `gemini-3.1-pro-preview` 0.5% ($0.0204), 17m28s; harvest 3/3 IDENTICAL. **🔴 F-82 → WP-266:** AC-1 grep-pinned `test -f test/cli/inject.test.ts` while the LOOSE goal delegated file layout and steered the test into `cli.test.ts` — the correct delivery could never satisfy the filename pin → AC-1 failed all 3 steps → budget-waste HALT guard fired (a *true* guard fire on a *false* criterion), ~$2.11 (steps 2–3) spent chasing the phantom. Thesis wins: the HALT guard worked, the inject substrate is proven end-to-end, family diversity real. Fix = loose-AC lint (grep OUTCOME symbols, never pin delegated file layout). F-58/WP-249 reinforced (STAGED, no `Run-ID:` trailer). | [`dogfood-075.md`](../../docs/reports/dogfood-075.md) |
| [`dogfood-076.yaml`](dogfood-076.yaml) | **WP-265 rung 2 — ≥10-step LOOSE run on WP-213 native raw-LLM loop executor + deliberate mid-run `kill -9` → `chikory resume` (first at-horizon per-step-reliability / compounding-error / durable-resume measurement).** Build a `native` executor adapter (`createNativeAdapter` implementing `ExecutorAdapter`, registered `native:` in the `cli/commands.ts` ADAPTERS map) that drives a router-based tool-calling loop over the workspace and completes a real multi-file coding task end-to-end. Single `chikory run`. Goal states the OUTCOME + constraints; ACs OUTCOME-anchored per the F-82 lesson (grep `createNativeAdapter`/`native:` registry symbols the goal names, NOT a new test filename). `budget_usd`/`max_steps` sized for horizon. | not yet run — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung (binding); §1.1 ✅ (≥10-step cross-file build on the native-executor / horizon pillar — a competent agent can plausibly fail a tool-calling loop, the diff capture, the adapter registration, or mid-run resume); §1.2 ✅ (real open plan.md §6 WP-213 🟡 — the benchmark-control / no-CLI-agent executor + the WP-111 conformance hedge; net-new real product code); §1.3 ✅ PROCEED (thesis-stressing horizon slice on a real product WP; not busy/scaffold — veto does not fire); §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3, cap not busted)** | 🟡🟢 **FAILED terminal, but the native adapter is BUILT + all-green@step2 — the SECOND consecutive loose headline to false-FAIL on a self-inflicted AC (new variant F-83)** (`run-17a57451-b8cb-4b52-b8e4-3101bc04b4f0`, runtime `d5d0469`, **UNHARVESTED** — delivery only in the run workspace, working tree clean; `docs/reports/dogfood-076.md`). `createNativeAdapter` (`src/executors/native.ts`, 469 lines) drives a real Router tool-loop (list/read/write/edit, terminates on `maxTurns`/`maxSeconds`, tool errors fed back, diff via `captureWorkspaceDiff`→`diffRef`, well-formed `StepRecord`, OTel span, path-traversal guards), registered `native:` in the ADAPTERS map (router threaded additively), barrel re-exported, net-new behavioral test — AC-2 (tsc+eslint+full suite 589 passed) green at step 2. 4 steps $3.71/$30 (12.4%), judge `gemini-3.1-pro-preview` 0.8%, 2h38m. **🔴 F-83 → WP-266 (LANDED this review):** AC-1's `! grep -Eq 'execFile\|spawn' native.ts` matched the doc comment "…is spawned" (code has zero such calls) → false-FAILED all 3 passes → budget-waste HALT; the loose-AC lint now ⛔s `test -f`/`test -e` + bare-word negative greps. **🔴 F-84 → WP-267:** the lint isn't enforced at launch (rung 2 launched un-linted — the mandated pre-rung-2 hand-fix was skipped). **🟡 F-85 → WP-268:** steps 3–4 ran 1.76×/1.98× past the 600s step cap (codex, F-76/F-80 family) and broke AC-2. Rung-2 KPIs (≥10-step horizon + live kill→resume) UN-measured: build was ~2 steps; run auto-terminated before the operator's kill. F-58/WP-249 reinforced. | [`dogfood-076.md`](../../docs/reports/dogfood-076.md) |
| [`dogfood-077.yaml`](dogfood-077.yaml) | **WP-265 rung 2 (RE-ATTEMPT) — ≥10-step LOOSE run on WP-206 HITL suspend/resume + deliberate mid-run `kill -9` → `chikory resume` (the un-measured rung-2 KPIs).** Give the operator `chikory suspend <run-id>` — durably PARK a healthy RUNNING run at ZERO compute (a Temporal `condition` wait mirroring the existing budget-park `agent-loop.ts:216`; status SUSPENDED, journaled) and later continue it with NO journaled step re-executed. Net-new `cmdSuspend` + `suspend` dispatch + `SIGNAL_SUSPEND`/`suspendSignal` + handler + `RunHandle` sender + a LIVE Temporal durable test (mirrors the WP-123 crash-recovery + WP-212 inject tests). Single `chikory run`. Goal states OUTCOME + constraints; ACs OUTCOME-anchored per the F-82/F-83 lesson (grep `cmdSuspend`/`SIGNAL_SUSPEND` as they appear in code, recursive test grep — NO `test -f`, NO bare-word negative grep; **WP-266 lint 🟢 PASS**). `budget_usd: 30`, `max_steps: 16`. | not yet run — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung (rung 2 still un-satisfied, 076 rung=0); §1.1 ✅ (≥10-step cross-file build on the durable-execution pillar — durable park correctness, no-step-re-execution on resume, status transitions, live Temporal test); §1.2 ✅ (real open plan.md §6 WP-206 🟡, DIRECT P2 exit-gate dependency; net-new real product code reusing the signal/condition substrate); §1.3 ✅ PROCEED (thesis-stressing horizon slice on a real product WP; not busy/scaffold — veto does not fire); §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3, cap not busted)** | 🟢🟡 **SUCCESS — WP-206 HITL suspend/resume DELIVERED clean + all-green + real live durable test (WP-206 → 🟢), but codex ONE-SHOT the 13-file feature in 1 journaled step so BOTH rung-2 KPIs (≥10-step horizon + live kill→resume) missed a 3rd straight time — now proven STRUCTURAL** (`run-d14fb74c-989d-4cce-adad-f37e06b1b3f1`, runtime `6540b51`, delivery STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-077.md`). `codex`/`gpt-5.5` delivered `cmdSuspend` (`commands.ts:464`) + `case "suspend"` dispatch/help + `SIGNAL_SUSPEND`/`SIGNAL_RESUME` + `suspendSignal`/`resumeSignal` handlers + `parkIfOperatorSuspended()` (journal `control_event` → `status="SUSPENDED"` → `await condition(resume\|\|cancel)` at loop TOP so resume re-executes nothing) + `RunHandle.suspend()` + `runner.resume()` resume-signal (guarded no-op on RUNNING → WP-123 crash-recovery test still green) + a net-new LIVE Temporal `suspend-resume.test.ts` asserting SUSPENDED, no-advance-while-parked, zero-replay resume (steps `[0,1,2,3,4]`, spend unchanged), control_events `[suspend,resume]`. New `control_event` journal kind synced across TS types + zod + py + journal-format.md. Judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 (both ACs judge-executed & exit 0; 591 passed \| 19 skipped), scope-clean, 0.3% ($0.0115). 1 step $3.79/$30 (12.6%), **2,901k/15k tokens (new campaign input high, ZERO compaction — WP-203/207 baseline)**, 7m53s, harvest 13/13 IDENTICAL. **🟡 F-86 → WP-508:** step count = judge-retry rounds (075/076/077 = 3/4/1), NOT feature size → the ≥10-step horizon is unreachable via single-goal feature-sizing; rung 2 must be CHAIN-hosted (K goals → ≥K checkpoints, kill/resume between children). **ℹ️ F-87:** live kill→resume still un-captured (durable resume IS unit-proven, just not a live dogfood artifact). F-58/WP-249 reinforced. | [`dogfood-077.md`](../../docs/reports/dogfood-077.md) |
| [`dogfood-078.yaml`](dogfood-078.yaml) | **WP-265 rung 2 (CHAIN RE-HOST, WP-508) — decomposed `chikory chain` on WP-250 pacing-`park`→durable-window-suspend, so the ≥10-step horizon comes from ≥K sequential nodes + a mid-chain `kill -9` → resume.** On a `park` pacing action the run must durably SUSPEND at zero compute (distinct `window` cause) instead of folding into a guaranteed-overflow step, then resume with no journaled step re-executed; reuse the WP-206 condition-park + WP-243 budget-park precedent + the frozen `debug.contextWindowTokens` seam. Launched with `chikory chain`. LOOSE; ACs OUTCOME-anchored (positive symbol greps + bare-toolchain build/test; **WP-266 lint 🟢 PASS**). `budget_usd: 40`, `max_steps: 8`. | launched — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung (rung 2 still un-satisfied, 077 one-shot to rung=1); §1.1 ✅ (≥10-step multi-node chain on the durable-execution pillar); §1.2 ✅ (real open plan.md §6 WP-250 🟡, DIRECT next P2 exit-gate dependency, unblocked by WP-206); §1.3 ✅ PROCEED; §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3)** | 🔴🟢 **FAILED terminal — but the WP-250 window-park is BUILT + all-green + HAND-HARVESTED + PUSHED; two harness defects, NOT a delivery failure** (`chain-6dff03ee-ebcb-4bdb-87b2-7ed7163b1a58` node `wp-250-implementation`, runtime `a698e51`; `docs/reports/dogfood-078.md`). `codex`/`gpt-5.5` built all 8 files: pure `shouldParkForWindow` (`pacing.ts:127`) → `agent-loop.ts:559` parks at `status="SUSPENDED"` on `await condition(resume\|\|cancel)` (zero-compute) journaling a distinct `cause:"window"` halt/`top_up` (`activities.ts:906` union widened) + operator/chain surfaces (`commands.ts`/`chain.ts:281`) reporting the window-park + a `chikory resume` hint; additive, no contract/StepRecord/pacing-decision shape change, no new dep. Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1; 1 step $2.80/$20 (14%), 2142k/12k tokens, 5m13s; AC-2 re-run on the harvested tree green (594 passed \| 19 skipped). **🔴 F-88 → WP-509:** the `chikory chain` planner COLLAPSED the decomposable goal into ONE node → rung-2 horizon missed a 4th time AT THE PLANNER. **🔴 F-89 → WP-510:** the writeSet gate FALSE-FAILED the delivery for writing the two test files its AC requires (planner auto-writeSet was src-only). **🟡 F-90 → WP-511:** AC-1's recursive `grep -rq contextWindowTokens test/` false-greened on incumbent files → the spec's required LIVE durable window-park test went missing (WP-250 → 🟡, owes it). Chain never reached the rung-2 mid-chain kill→resume. F-58/WP-249 reinforced. | [`dogfood-078.md`](../../docs/reports/dogfood-078.md) |
| [`dogfood-079.yaml`](dogfood-079.yaml) | **WP-265 rung 2 (CHAIN RE-HOST, WP-508) on WP-204 tiered memory — the ≥10-step at-horizon run, decomposed into 4 sequential nodes so the horizon is REAL + a mid-chain `kill -9` → `chikory chain resume`.** Tiered core/archival/recall memory with `provenance` on every write (poisoning safeguard), built as pure `src/memory/` primitives (the `src/judge/` pattern). `min_nodes: 4` (exercises WP-509 decompose floor); the writeSet gate now admits the AC-required test tree (WP-510). LOOSE; ACs OUTCOME-anchored — positive symbol greps scoped to the net-new `src/memory/` dir (F-90-safe), no `test -f`, no bare negative grep (**WP-266 lint 🟢 PASS**). Launched with `chikory chain`. `budget_usd: 50`, `max_steps: 10`. | launched — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung (rung 2 still un-satisfied after 4 misses); §1.1 ✅ (≥10-checkpoint multi-node chain on the durable-execution pillar); §1.2 ✅ (real open plan.md §6 WP-204 🔴, a P2 memory pillar; net-new real product code); §1.3 ✅ PROCEED; §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3)** | 🟢 **SUCCESS · 4/4 nodes — WP-265 rung 2 REACHED (1st time): the `chikory chain` planner DECOMPOSED WP-204 into 4 sequential judge-gated nodes (`min_nodes: 4` floor, WP-509 proven live), crossed 13 durable checkpoints, and survived a LIVE mid-chain `kill -9` → `chikory chain resume` WITHOUT re-executing the completed node — WP-204 tiered memory DELIVERED + all-green + auto-harvested + pushed (WP-204 → 🟢)** (`chain-f4b08133-77c7-47b3-ae63-8a3803bbfc14`, plan `plan-52362e84`, runtime `d4ab544`, harvested `9304c68` (4 files, 652 ins); `docs/reports/dogfood-079.md`). Nodes `tiered-memory-{core,archival,recall,provenance}` each `✓ PROCEED (2/2)`: core $1.01/1step/4m31s (sealed BEFORE the kill, seal ts `12:18:20.307` byte-identical post-resume → NOT re-executed), archival $0.93, recall $0.82, provenance $1.60 (Σ node $4.36 ≪ $50), judge `gemini-3.1-pro-preview` 0.6–1.0%; 19m9s wall spanning the kill. AC-1…AC-4 greps + AC-5 (tsc+eslint+vitest **621 passed \| 19 skipped**) re-verified green. **🟡 F-91 → WP-512:** exact-path writeSet needed FOUR false-fail fixes (test-tree/executor-named-new/cross-node-modify/barrel) — wrong primitive for LOOSE chains. **🟡 F-92 → WP-513:** WP-257 literal floor REVISE-rejected the decomposed plan (paraphrased node goals drop goal literals) — de-backticked as workaround. **🟡 F-93 → WP-514:** launch-guard false-tripped on header comment prose. **ℹ️ F-94:** each node one-shot in 1 step → horizon is inter-node only; intra-run ≥5-step reliability still owed (rung 3). F-58/WP-249 reinforced. | [`dogfood-079.md`](../../docs/reports/dogfood-079.md) |
| [`dogfood-080.yaml`](dogfood-080.yaml) | **WP-265 rung 3 — the FIRST intra-run horizon run, on WP-205 branching/rollback.** Unlike rung 2 (dogfood-079, horizon BETWEEN chain nodes), rung 3 builds MANY durable steps INSIDE ONE `chikory run` to finally stress per-step reliability / compounding-error (95%→99%) over a long single session + a live mid-run `kill -9` → `chikory resume`. Make branching first-class: a new `cmdBranch` command (reusing the pure `parseBranchTarget`/`branchNameForTarget` slices + the WP-132 `restoreCheckpoint` commit-resolution) forks a run at a checkpoint into a child run — journal seeded up to the fork + provenance, workspace forked from the checkpoint git commit (`worktree`), a judge BRANCH recommendation (additive, never replacing ROLLBACK), and a LIVE test proving the child resumes from the fork while the parent stays intact. LOOSE; ACs OUTCOME-anchored — greps on net-new `cmdBranch` (absent on HEAD → F-90-safe) + reused `parseBranchTarget`/`worktree`, no `test -f`, no bare negative grep (**WP-266 lint 🟢 PASS**). Single `chikory run`. `budget_usd: 60`, `max_steps: 40`. Design note (F-94): rung 2's horizon was inter-node; if codex still one-shots this multi-part goal, that IS the finding pushing to WP-213 (native loop) / step-forcing. | not yet run — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung = rung 3 (binding, default headline); §1.1 ✅ (multi-part durable-execution build + live branch/resume test — a competent agent can plausibly fail the journal fork, the git-commit worktree, the BRANCH-verdict wire, or the parent-intact assertion, across many steps); §1.2 ✅ (real open plan.md §6 WP-205 🟡 — completes the branching feature on the durable-execution pillar; reuses the pure slices, net-new real product code, NOT scaffolding); §1.3 ✅ PROCEED (thesis-stressing horizon slice on a real product WP; not busy/scaffold — veto does not fire); §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3, cap not busted)** | 🟢 **SUCCESS · 2 steps — but RUNG 3 NOT REACHED (the decisive negative result)** (`run-233e7d7f-540c-43c4-a5c8-a1556f8f2e1f`, uncommitted byte-IDENTICAL to workspace, `docs/reports/dogfood-080.md`). WP-205 branching landed COMPLETE: `cmdBranch` + `branch` verb (reuses `parseBranchTarget`), `forkRunAtCheckpoint` (`src/runner/branch.ts` — journal seeded through the fork checkpoint + `branch_fork` provenance + git-worktree at the checkpoint commit), an additive `"BRANCH"` verdict kind (`verdict.ts`, Rule 1 ROLLBACK still checked FIRST — never replaced), a DI-fake CLI unit suite + a real-temp-git worktree unit + a LIVE Temporal proof (drive 3 checkpoints → `cmdBranch` fork → child resumes from fork (`step-1.txt` yes / `step-2.txt` no), parent intact (`step-3.txt` + unchanged journal len)). No dep/contract change; tsc+eslint+vitest **629 passed**; $1.66/$60 (2.8%), judge 0.8%. **BUT codex ONE-SHOT the deliberately 4-part decomposed goal in a SINGLE 57-tool-call step** — the "2 steps" is 1 attempt + 1 accidental 600s step-cap auto-retry on the hanging live test; no operator kill→resume fired. 🔴 **F-95 → WP-213/step-forcing: the intra-run horizon is UNREACHABLE by goal size (dogfood-077/079/080) — it must be HARNESS-FORCED; the next headline is WP-213 native loop, NOT a bigger goal.** 🟡 **F-96 → WP-515:** the killed step reported `$0.00/0 tokens` while the retry re-billed the full context (budget undercounts; ℹ️ WP-268 hard cap held at 1.00×). WP-205 → 🟢 pending harvest. | [`dogfood-080.md`](../../docs/reports/dogfood-080.md) |
| [`dogfood-081.yaml`](dogfood-081.yaml) | **WP-269 — INTRA-RUN STEP-FORCING, the rung-3 ENABLER dogfood-080 proved is required.** dogfood-077/079/080 established (F-86/F-94/F-95) that codex ONE-SHOTS any single-run goal (self-declares `claimsComplete` → `isCompletionMilestone` fires → seal in ~1 durable step), so the intra-run ≥5-step horizon is unreachable by goal size — it must be HARNESS-forced. Build the lever: a pure `decideStepForcing` decision + an additive OPT-IN bounded-work-unit policy that DEFERS a premature completion and re-enters the durable loop with an "advance the next increment only" directive until a step floor is met AND the judge confirms the ACs, so ONE `chikory run` seals MANY checkpoints; the NO-policy path stays byte-identical; a LIVE Temporal test asserts >1 durable checkpoint in one run. LOOSE; ACs OUTCOME-anchored on net-new `decideStepForcing` (absent on HEAD → F-90-safe), no `test -f`, no bare negative grep (**WP-266 lint 🟢 PASS**). Single `chikory run`. `budget_usd: 40`, `max_steps: 30`. NOTE: this ENABLER build will itself likely one-shot (a normal feature delivery) — expected; the horizon PROOF comes next (dogfood-082 runs rung 3 + a mid-run kill→resume ON this machinery). | not yet run — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING; §1.0 carve-out FIRES (the default rung-3 HORIZON headline is BLOCKED on this missing harness capability — F-95 — so building the blocking dependency is the sanctioned next priority, DOGFOODING §1.2 fallback / TASK-PROTOCOL §4); §1.1 ✅ (a competent agent can plausibly fail the premature-completion deferral, the byte-identical no-policy guarantee, the loop re-entry, or the ≥N-checkpoint live assertion); §1.2 ✅ (real product code on the durable-execution pillar — new WP-269 runtime capability, additive to agent-loop.ts, NOT scaffolding); §1.3 ✅ PROCEED (not busy/scaffold — it unblocks the whole ladder); §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3, cap not busted)** | 🟢 **SUCCESS · 1 step — the rung-3 blocking dependency (F-95) is now BUILT** (`run-464b7b77-3c24-4566-b4bd-ecadd1eff6a3`, uncommitted byte-IDENTICAL to workspace, `docs/reports/dogfood-081.md`). WP-269 landed exactly as specced: a pure total `decideStepForcing(state, policy?)` (`src/workflow/step-forcing.ts`, sibling of `decideContextWindowPacing`, 6-case unit test) + an opt-in `boundedWorkUnit` spec-input policy (additive `TaskSpec`/schema/`bounded_work_unit` YAML field — NOT a `StepRecord`/`JournalEntry`/`Checkpoint` contract change) wired additively into `agent-loop.ts:537` so an active policy DEFERS a premature `claimsComplete` and re-enters until the durable-step floor is met AND the judge confirms the ACs; the no-policy path is byte-identical. A LIVE Temporal test (`test/runner/agent-loop.test.ts`, verified EXECUTING not skipped, 2847ms) drives ONE real run sealing **3 durable checkpoints under an active `{minDurableSteps:3}` policy** while the same goal with NO policy stays one-shot (1 checkpoint) — the first mechanically-demonstrated intra-run horizon. All-green (636 passed), scope-clean (8 files), byte-IDENTICAL. One-shot as the spec predicted (ENABLER build). $2.99/$40 (7.5%), judge 0.4%, 2267k/15k tokens. 🟡 **F-97 → WP-266/511 (track-B):** the grep ACs (`grep -rq decideStepForcing test/`) were satisfied by the UNIT test alone — nothing re-required the PART-3 LIVE test (executor over-delivered it); extend the loose-AC lint, and **dogfood-082's ACs must pin the ≥5-checkpoint horizon numerically.** WP-269 → 🟢 pending harvest. | [`dogfood-081.md`](../../docs/reports/dogfood-081.md) |
| [`dogfood-082.yaml`](dogfood-082.yaml) | **WP-265 RUNG-3 HORIZON PROOF — the first INTRA-RUN ≥5-durable-step run, driven by the WP-269 step-forcing lever dogfood-081 delivered, hosted on the genuinely-open WP-219 chain-runtime residuals (D3 halt-and-replan + structured compaction-note handoff, the two capabilities `chain-loop.ts` itself names as deferred).** ONE `chikory run` under `bounded_work_unit: { min_durable_steps: 5 }` FORCES ≥5 sealed durable checkpoints even though codex one-shots: PART 1 pure `decideReplan` (sibling of `deriveChainStatus`/`advanceChain`, bounded replan budget) → PART 2 pure structured compaction-note builder → PART 3 wire `decideReplan` into `chain-loop.ts`'s FAILED-node path (re-invoke planner, splice revised nodes, journal `node_replanned`; zero-budget = byte-identical halt) → PART 4 thread the note into the S4 handoff → PART 5 LIVE Temporal chain test (a node FAILS → replan → SUCCESS; zero-budget → FAILED). Captures BOTH rung-3 KPIs from the run's journal: ≥5 intra-run durable checkpoints + a LIVE OPERATOR mid-run `kill -9` → `chikory resume` BETWEEN sealed steps (no re-execution). LOOSE; ACs OUTCOME-anchored on net-new `decideReplan`/`node_replanned` (absent on HEAD → F-90-safe); **AC-3 is F-97-hardened** (requires a `test/chain/` file co-referencing `decideReplan` + the LIVE driver `createChainActivities` + the `node_replanned` outcome — a UNIT test cannot false-green it). No `test -f`, no bare negative grep (**WP-266 lint 🟢 PASS**). Single `chikory run`. `budget_usd: 50`, `max_steps: 30`. ⚠️ Expected finding (record either way): step-forcing DEFERS the seal but does not CHUNK the executor's work — if codex still front-loads, steps 2–5 seal thin checkpoints → a valid rung-3 result pointing at the next mechanism (per-step work-unit chunking). | not yet run — **gate verdict ✅ PROCEED: progression ✅ PROGRESSING → next WP-265 ladder rung = rung 3 (binding, pre-declared PROOF, dogfood-081 delivered the ENABLER); §1.1 ✅ (a competent agent can plausibly fail the bounded replan-budget decision, the planner re-invocation + node splice in a running Temporal chain, the additive zero-budget-halt guarantee, or the LIVE replan-to-SUCCESS assertion — cross-file, durable-execution pillar, many steps); §1.2 ✅ (real open plan.md §6 WP-219 chain-runtime residuals — D3 replan + structured compaction notes, both named DEFERRED in `chain-loop.ts`; net-new real product code, NOT scaffolding); §1.3 ✅ PROCEED (thesis-stressing rung-3 horizon slice on a real product WP; **host CORRECTED from the WP-213 first pick — WP-213 is ALREADY SHIPPED `0db60ba`, its §6 row stale; WP-211 needs Playwright (new dep + UI wall)**; WP-219 residuals are the genuinely-open no-new-dep intra-run host); §1.5 ✅ (product + LOOSE, not harness-meta; trailing-3 meta 0/3, cap not busted)** | 🟢 **SUCCESS · 6 steps — WP-265 RUNG 3 REACHED (both KPIs captured)** (`run-ef4824e2-a91c-4633-aac3-bb26608217c4`, harvested `1d2c7e5`, `docs/reports/dogfood-082.md`). ONE `chikory run` under `bounded_work_unit:{min_durable_steps:5}` sealed **6 durable checkpoints** (first intra-run horizon) AND survived a **LIVE mid-run `kill -9` → `chikory resume` landing between sealed steps with zero re-execution** (~37-min replay gap, journal byte-identical). Delivered all 5 PARTs of the WP-219 D3 replan (`decideReplan`) + S4 structured compaction-note handoff, additive/frozen-contract-safe (`node_replanned` = one additive journal kind — types.ts diff vs HEAD is EXACTLY one line), LIVE-proven (`replan-live.test.ts`: node FAILS→REPLAN→`node_replanned`→SUCCESS; zero-budget→FAILED on the byte-unchanged halt). All-green in-workspace (655 tests), 14 files, $6.76/$50 (13.5%), judge 0.6%, 57m16s. ℹ️ **F-100 → WP-270 (PRE-DECLARED, THE next headline): the horizon is REAL but HOLLOW** — seal-deferral doesn't CHUNK work, so step 1 front-loaded all product code ($4.96/28KB) and steps 2–6 sealed thin test-tweaks ($0.27–$0.49); per-step chunking is the next mechanism. 🔴 **F-99 (track-B):** a bare `chikory resume` silently lost `OPENAI_COMPAT_BASE_URL` → Temporal masked it as the ~30-min stall — add a fail-fast resume precondition. WP-219 D3/S4 → 🟢 harvested `1d2c7e5`. | [`dogfood-082.md`](../../docs/reports/dogfood-082.md) |
| [`dogfood-083.yaml`](dogfood-083.yaml) | **WP-270 — PER-STEP WORK-UNIT CHUNKING, the rung-3 QUALITY fix dogfood-082 proved is required (F-100).** dogfood-082 reached the intra-run ≥5-step horizon but HOLLOW: WP-269's `bounded_work_unit` DEFERS the seal but does not CHUNK the executor's work, so codex front-loaded all product code into step 1 and steps 2–6 sealed thin test-tweaks — the per-step-reliability / compounding-error KPI is not yet trustworthy. Build the successor lever: an opt-in mechanism that hands the executor ONE bounded dependency-ordered sub-goal per durable step (a new additive `work_chunks` list on the existing `bounded_work_unit` policy + a pure `decideWorkChunk`) so each sealed checkpoint carries REAL distinct product progress, LIVE-proven that a run distributes non-trivial work (each step a genuine diff/failure surface) across its checkpoints, and the no-chunk-list path stays byte-identical to WP-269. NOTE: the launch drives this build run's OWN horizon with the HEAD-valid `min_durable_steps:3` + directive (WP-269 lever); the not-yet-built `work_chunks` field is proven by PART 4's live test constructing the policy IN CODE (a launch YAML using it would be parse-REJECTED — verified — exactly the ENABLER-build shape of dogfood-081). LOOSE; ACs OUTCOME-anchored on a net-new decision symbol (absent on HEAD → F-90-safe) + a LIVE Temporal test asserting per-step non-trivial diffs, no `test -f`, no bare negative grep. Single `chikory run`. `budget_usd: 40`, `max_steps: 30`. | 🟢 **SUCCESS** (`run-d3879dab-c1e4-4dde-930e-27f679a75d10`, 3 steps, $4.09/$40, 10m22s) — all 4 PARTs landed additively; `decideWorkChunk` + `work_chunks` wired into `agent-loop.ts`, LIVE-proven (each step's instruction = its ordered chunk, all distinct, none = full goal; no-chunk-list variant on WP-269), 658 tests green, harvest byte-IDENTICAL. ✅ **F-101 CLOSED by re-run `run-03d161e9`** (SUCCESS · 3 steps · $2.58/$40 · 8m58s; dedicated PROCEED-gated `consumedWorkChunks` counter + `workChunkMilestone` + LIVE scripted-ROLLBACK test, 660 tests green, un-harvested). 🟡 F-102 (the re-run re-launched an already-green spec instead of a scoped PR — process note). ℹ️ the build run itself front-loaded (F-100 recurred — field unusable in its own launch YAML). **WP-270 → 🟢.** | harvest byte-IDENTICAL, un-committed at review · re-run `run-03d161e9` un-harvested · `docs/reports/dogfood-083.md` |
| [`dogfood-084-wp214-multirepo-nonhollow-horizon.yaml`](dogfood-084-wp214-multirepo-nonhollow-horizon.yaml) | **WP-214 MULTI-REPO WORKSPACES, hosting the NON-HOLLOW rung-3 HORIZON.** dogfood-082 reached the ≥5-step horizon but HOLLOW (F-100); dogfood-083 built WP-270 per-step chunking; THIS is the payoff — the FIRST launch to USE `work_chunks` in its own YAML (valid in HEAD after dogfood-083 harvest) to force a ≥5-step `chikory run` where EACH sealed checkpoint carries a DISTINCT non-trivial diff, converting the hollow horizon into a trustworthy per-step-reliability curve. Vehicle = real open WP-214: the runner today uses only `repos[0]` and hard-asserts `spec.repos.length !== 1`; make a run span N writable repos (clone all → run branch per repo → checkpoint `gitCommits` spans all → per-repo diff evidence), delivered as 5 dependency-ordered chunks (1 net-new `collectWorkspaceRepos` resolver → 2 clone-all → 3 commit/checkpoint span → 4 per-repo diff → 5 live 2-repo proof). LOOSE; ACs OUTCOME-anchored on net-new `collectWorkspaceRepos` (absent on HEAD → F-90-safe) + F-97 live-driver co-reference (`createRunnerWorker`); one-repo path byte-identical (live-asserted). `min_durable_steps: 5` + 5 `work_chunks`. Single `chikory run`. `budget_usd: 50`, `max_steps: 30`. | ⚠️ **RETIRED — NEVER RAN** (`.yaml.retired`): host WP-214 shipped OUTSIDE the loop (`fadc124`, `collectWorkspaceRepos`) so its net-new AC symbol already exists (F-106); superseded by **dogfood-085** (identical non-hollow rung-3 horizon, re-hosted on WP-215 architecture rubric). The pre-`59c57f6` 3-digit-exact glob also mis-fired on this suffixed name → the accidental 083c re-run (`run-0a285f5b`, $3.02, F-104). _Original pre-retirement gate verdict:_ **gate ✅ PROCEED: progression ✅ PROGRESSING (trailing-3 081/082/083, harness-meta 0/3, cap OK); §1.1 ✅ (5 independently-failable multi-repo increments — clone-all, per-repo commit span, checkpoint map, per-repo diff, 2-repo live proof — a competent agent can plausibly fail any); §1.2 ✅ (real open product WP-214 durable-execution feature code, NOT scaffolding; the WP-270 chunking mechanism is the VEHICLE seeded into it); §1.3 ✅ PROCEED (thesis-stressing on TWO pillars — durable multi-repo execution AND the non-hollow per-step-reliability KPI; not busy/scaffold); §1.5 ✅ (product + LOOSE, not harness-meta)** — spec parses (5 chunks, min_durable 5); format lint 🟢 | ⚠️ RETIRED, superseded by dogfood-085 |
| [`dogfood-085-wp215-arch-rubric-nonhollow-horizon.yaml`](dogfood-085-wp215-arch-rubric-nonhollow-horizon.yaml) | **WP-215 ARCHITECTURE RUBRIC, hosting the NON-HOLLOW rung-3 HORIZON** (re-host of retired dogfood-084 after WP-214 shipped outside the loop). dogfood-082 reached the ≥5-step horizon but HOLLOW (F-100); dogfood-083 built WP-270 per-step chunking; THIS is the payoff — the FIRST launch to USE `work_chunks` in its own YAML to force a ≥5-step `chikory run` where EACH sealed checkpoint carries a DISTINCT non-trivial diff. Vehicle = real open WP-215: security side (secret + dependency scans, S1–S5) is landed, but the ARCHITECTURE rubric is unbuilt ("still P2"). Add a deterministic architecture-posture judge-evidence signal — a net-new pure `scanDiffForLayeringViolations(diff)` diff-scan (the `scanDiffForSecrets`/`scanDiffForNewDependencies` analog, flagging a forbidden cross-layer import direction) → wired into `collectEvidence` (`architectureLabels`) → rendered in `prompt.ts` → answered by a new non-destructive `no_architecture_violations` standing rubric item → LIVE-proven — as 5 dependency-ordered chunks (1 pure primitive → 2 evidence wire → 3 prompt render → 4 rubric item → 5 live proof). LOOSE; ACs OUTCOME-anchored on net-new `scanDiffForLayeringViolations` (absent on HEAD → F-90-safe) + F-97 live-driver co-reference (`createRunnerWorker`); a no-violation diff leaves judge behavior byte-equivalent (live-asserted). `min_durable_steps: 5` + 5 `work_chunks`. Single `chikory run`. `budget_usd: 50`, `max_steps: 30`. | not yet run — **gate verdict ✅ PROCEED: progression ⛔ STALLED ⇒ next headline MUST be the current WP-265 ladder rung = rung-3 non-hollow horizon, which THIS spec IS (compliant); §1.1 ✅ (5 independently-failable judge-evidence increments — pure layer-scan, evidence wire, prompt render, standing rubric item, live proof); §1.2 ✅ (real open product WP-215 architecture-rubric feature code, NOT scaffolding; the WP-270 chunking mechanism is the VEHICLE seeded into it); §1.3 ✅ PROCEED (thesis-stressing on TWO pillars — the Agent-as-a-Judge software-native rubric AND the non-hollow per-step-reliability KPI); §1.5 ✅ (product + LOOSE, harness-meta 0/3)** — spec parses (5 chunks, min_durable 5); format lint 🟢 LOOSE | — |
