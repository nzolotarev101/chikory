# Dogfood task specs

One file per dogfood campaign — the TaskSpec that ran (or will run) a
plan.md work-package slice through Chikory itself. Full operating manual:
[`docs/DOGFOODING.md`](../../docs/DOGFOODING.md).

## Convention

- **Path**: `examples/dogfood/dogfood-<NNN>.yaml` — `NNN` is the campaign
  number, allocated sequentially. The paired report lives at
  `docs/reports/dogfood-<NNN>.md` (same number; the report is mandatory —
  TASK-PROTOCOL §7).
- **`name:` field**: `dogfood-<NNN>-<short-slug>` (e.g.
  `dogfood-001-memory-pointer-store`).
- **Header comment**: which WP/slice it implements, the launch command, and
  — once it has run — the run-id, outcome, landed commit, and report link.
- Specs are kept after the run (they're the reproducible half of the
  report); never rewrite the `goal`/criteria of a spec that already ran —
  a new attempt or next slice is a new `dogfood-<NNN+1>.yaml`.
- After every terminal run: `/dogfood-review <run-id>` — the standardized
  review that verifies the delivery, writes the report, updates the plan,
  and readies the next spec (DOGFOODING.md §6.1).

## Index

| Spec | WP / slice | Outcome | Report |
|---|---|---|---|
| [`dogfood-001.yaml`](dogfood-001.yaml) | WP-202 slice 1 — Memory Pointer store | SUCCESS (run 4 of 4; landed `e267e28`) | [`dogfood-001.md`](../../docs/reports/dogfood-001.md) |
| [`dogfood-002.yaml`](dogfood-002.yaml) | WP-201 slice 1 — Python contracts parity | SUCCESS (run-2899005b; landed `eb5c57e`) | [`dogfood-002.md`](../../docs/reports/dogfood-002.md) |
| [`dogfood-003.yaml`](dogfood-003.yaml) | WP-217 — completion signal → off-cadence judge pass | SUCCESS (run-b2f3504d; landed `ef4b16f`) | [`dogfood-003.md`](../../docs/reports/dogfood-003.md) |
| [`dogfood-004.yaml`](dogfood-004.yaml) | WP-218 slice 1 — pricing refresh + blind-cost-meter warning | SUCCESS (run-9edbcd28; landed `2a4dd21`) | [`dogfood-004.md`](../../docs/reports/dogfood-004.md) |
| [`dogfood-005.yaml`](dogfood-005.yaml) | WP-220 — `chikory land <run-id>` | SUCCESS (run-34926e85; diff verified on `wp-220-chikory-land`, commit pending review) | [`dogfood-005.md`](../../docs/reports/dogfood-005.md) |
| [`dogfood-006.yaml`](dogfood-006.yaml) | WP-222 slice 1 — executor subprocess env scrub | SUCCESS (run-559ea904; landed `18fae43`) | [`dogfood-006.md`](../../docs/reports/dogfood-006.md) |
| [`dogfood-007.yaml`](dogfood-007.yaml) | WP-223 — watch renders journal transitions, never sampled state | SUCCESS (run-22b337a9; diff verified, commit pending review) | [`dogfood-007.md`](../../docs/reports/dogfood-007.md) |
| [`dogfood-008.yaml`](dogfood-008.yaml) | WP-224 — `land --verify` + git-stderr capture | SUCCESS (run-86c4b628; diff verified, commit pending review) | [`dogfood-008.md`](../../docs/reports/dogfood-008.md) |
| [`dogfood-009.yaml`](dogfood-009.yaml) | WP-225 — de-flake the WP-217 milestone test (durable-state waitFor) | SUCCESS (run-841bc838; diff verified, commit pending review on `main`) | [`dogfood-009.md`](../../docs/reports/dogfood-009.md) |
| [`dogfood-010.yaml`](dogfood-010.yaml) | WP-209 slice 1 — process metrics in `chikory trace` (issues-found:changes-made ratio) | SUCCESS (run-c9df353b; diff verified, commit pending review on `main`) | [`dogfood-010.md`](../../docs/reports/dogfood-010.md) |
| [`dogfood-011.yaml`](dogfood-011.yaml) | WP-209 slice 2 — components-over-time (executor/judge timeline footer in `chikory trace`) | SUCCESS (run-59e0166c; diff verified, commit pending review on `main`) | [`dogfood-011.md`](../../docs/reports/dogfood-011.md) |
| [`dogfood-012.yaml`](dogfood-012.yaml) | WP-208 slice 1 — pure `notificationsFor` derivation (journal entries + NotificationPolicy → ordered messages; delivery deferred) | SUCCESS (run-ea31f96c; diff verified, harvested uncommitted on `main`) | [`dogfood-012.md`](../../docs/reports/dogfood-012.md) |
| [`dogfood-013.yaml`](dogfood-013.yaml) | WP-208 slice 2 — pure `slackPayloadFor` formatter (`Notification` → Slack `{ text }` with trigger emoji prefix; webhook POST + call-site deferred) | SUCCESS (run-048635b1; diff verified, harvested uncommitted on `main`) | [`dogfood-013.md`](../../docs/reports/dogfood-013.md) |
| [`dogfood-014.yaml`](dogfood-014.yaml) | WP-208 slice 3 pure half — pure `desktopPayloadFor` formatter (`Notification` → `{ title, body }` desktop-ping body; side-effectful delivery + call-site deferred) | SUCCESS (run-6b8f648c; landed `3e1336f`; surfaced+fixed F-20 harvest bug) | [`dogfood-014.md`](../../docs/reports/dogfood-014.md) |
| [`dogfood-015.yaml`](dogfood-015.yaml) | WP-219 S3 pure half — `readyNodes(plan, completed)` chain sequencing (pure dependency resolution over `Plan`/`PlanNode`; first consumer of the ADR-005 contracts) | SUCCESS (run-56d95ffc; landed `40ada45`; surfaced F-21/WP-226) | [`dogfood-015.md`](../../docs/reports/dogfood-015.md) |
| [`dogfood-016.yaml`](dogfood-016.yaml) | WP-219 S3 pure precondition — `hasDependencyCycle(plan)` (pure acyclic guard over `Plan` deps; the executor's check before looping `readyNodes`) | SUCCESS (run-2418f473; harvested byte-identically + staged, pending commit) | [`dogfood-016.md`](../../docs/reports/dogfood-016.md) |
| [`dogfood-017.yaml`](dogfood-017.yaml) | WP-227 — final journal drain before terminal return (dogfood-016 F-23; closes the remaining F-15 observer race) | **FAILED** (run-19c25609; spec redundant — WP-227 already hand-landed `26b9964`; true-positive judge ESCALATE on empty-diff-vs-claim) | [`dogfood-017.md`](../../docs/reports/dogfood-017.md) |
| [`dogfood-018.yaml`](dogfood-018.yaml) | WP-229 — surface the ESCALATE reason in `--watch` (dogfood-017 F-27) | SUCCESS (run-59115f35; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-27 closed; surfaced F-28) | [`dogfood-018.md`](../../docs/reports/dogfood-018.md) |
| [`dogfood-019.yaml`](dogfood-019.yaml) | WP-221 pure trigger half — `isCompletionMilestone(record)` ORs `claimsComplete` into the WP-217 empty-diff judge trigger (dogfood-003/018 F-11) | SUCCESS (run-d836635b; functional delivery exact, harvested IDENTICAL + staged on `main`, pending commit; surfaced **F-29** — test fixtures violate `ArtifactRef`, unseen by the `src`-only typecheck gate → WP-230) | [`dogfood-019.md`](../../docs/reports/dogfood-019.md) |
| [`dogfood-020.yaml`](dogfood-020.yaml) | WP-230 — typecheck gate covers `test/**` + fix dogfood-019 fixtures (dogfood-019 F-29) | SUCCESS (run-3575ba23; gate verified to trip on a bad fixture, F-29 closed; harvested IDENTICAL + staged on `main`, pending commit; surfaced **F-30** — duplicate launch `run-f24af22c`, ~$1 wasted, no WP) | [`dogfood-020.md`](../../docs/reports/dogfood-020.md) |
| [`dogfood-021.yaml`](dogfood-021.yaml) | WP-221 Slice B — runner detects the executor completion marker → `claimsComplete` (the F-11 cost win; protocol scaffolded by hand). _(Was WP-218 token-budget math, reprioritized 2026-06-14; that slice preserved unscheduled in the plan.md WP-218 row.)_ | SUCCESS (run-91eced6b; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-11 cost win complete in code, probe-retirement one observation away; no new friction, F-30 did not recur) | [`dogfood-021.md`](../../docs/reports/dogfood-021.md) |
| [`dogfood-022.yaml`](dogfood-022.yaml) | WP-219 S2 Slice 1 — pure goal-planner prompt half (`buildPlannerMessages` + `PLANNER_SYSTEM_PROMPT` + `PLAN_RESPONSE_SCHEMA`, mirroring `judge/prompt.ts`; the `decompose` router call + parse deferred). _(The old dogfood-022 WP-218 pure-token-math spec was **withdrawn 2026-06-14** — the whole token gate was hand-landed in the architect wall-clear `821d67f`; this slot is reused. WP-203 S2 digest wiring was the other candidate but is blocked on WP-202 Memory Pointer for `CompactionResult.digestRef`.)_ | **SUCCESS** (run-499218ef; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; **F-11 closed by observation** — first marker-emitting run, ONE step, no probe step; no new friction) | [`dogfood-022.md`](../../docs/reports/dogfood-022.md) |
| [`dogfood-023.yaml`](dogfood-023.yaml) | WP-219 S2 Slice 2 — pure plan-assembly half (`buildPlan(reply, input, opts): Plan`, mirroring the judge's `buildVerdict`; assembles a validated `Plan` from the planner's schema-valid `{ nodes }` reply; JSON parse + id + clock stay in the non-pure `decompose` wrapper) | **SUCCESS** (run-2d40ded5; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; S2's pure surface complete; F-11-closed `s0 j@0` shape held for a second straight run; no new friction; input tokens fell to 451k, ~half the 022 high) | [`dogfood-023.md`](../../docs/reports/dogfood-023.md) |
| [`dogfood-024.yaml`](dogfood-024.yaml) | WP-219 S2b — pure plan meta-judge prompt half (`buildPlanJudgeMessages` + `PLAN_JUDGE_SYSTEM_PROMPT` + `PLAN_VERDICT_RESPONSE_SCHEMA`, mirroring `judge/prompt.ts`; grades a `Plan` against the goal criteria → `PlanVerdict`; the non-pure plan-judge harness + verdict assembly deferred) | **SUCCESS** (run-28073328; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; schema verified field-for-field against frozen `PlanVerdict`; F-11-closed `s0 j@0` shape held for a third straight run; no new friction; input tokens set a new campaign high at 976k, ~2× the 023 low — confirms cost is noisy not monotonic) | [`dogfood-024.md`](../../docs/reports/dogfood-024.md) |
| [`dogfood-025.yaml`](dogfood-025.yaml) | WP-219 S2b — pure plan meta-judge verdict-assembly half (`buildPlanVerdict`, mirroring the judge's `buildVerdict`; folds the landed pure `planCoverageGaps` into `PlanVerdict.uncoveredCriteria` and assembles a validated `PlanVerdict` from a schema-valid `{ kind, rationale }` reply; the non-pure plan-judge harness + `decompose` wrapper deferred) | **SUCCESS** (run-0d39fd12; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; deterministic coverage override verified; `PlanVerdict` shape checked field-for-field against `types.ts:475`; F-11-closed `s0 j@0` shape held for a fourth straight run; no new friction; **WP-219's pure surface now complete**; input tokens fell back to 467k, the low band next to 023's 451k — sawtooth confirms cost is noise not ratchet) | [`dogfood-025.md`](../../docs/reports/dogfood-025.md) |
| [`dogfood-026.yaml`](dogfood-026.yaml) | WP-203 S4 — pure compaction-trace renderer (`formatEntryLine` gains a `case "compaction"` rendering the `CompactionResult` payload `tokensBefore→tokensAfter` + digest-pointer presence; the WP-209 trace-renderer pattern, pure over a synthetic `JournalEntry[]` fixture; not blocked on WP-202 — the S2 digest wiring stays deferred) | **SUCCESS** (run-f9d699d4; diff byte-for-byte to spec, harvested IDENTICAL, uncommitted on `main`; `CompactionResult` shape checked field-for-field against `types.ts:362`; both digest-present and digest-absent branches tested; F-11-closed `s0 j@0` shape held for a fifth straight run; no new friction; **WP-203 S4 pure trace surface complete**; input tokens 807k, mid-band — sawtooth confirms cost is noise not ratchet) | [`dogfood-026.md`](../../docs/reports/dogfood-026.md) |
| [`dogfood-027.yaml`](dogfood-027.yaml) | WP-228 S1 — pure launch-baseline-precheck decision (`evaluateBaselinePrecheck(results): BaselinePrecheckResult`, the analog of `buildVerdict`/`buildPlanVerdict`; turns collected acceptance-check exit codes into a `satisfied`/`passedIds`/`failedIds`/`summary` verdict so a redundant run can be warned/refused, dogfood-017 F-25; local result types, no `types.ts` change; the check-execution + `--force` wiring stays non-pure hand-design) | **SUCCESS** (run-f97a0e63; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; F-11-closed `s0 j@0` shape held for a sixth straight run; no new friction; input tokens 527k, low band) | [`dogfood-027.md`](../../docs/reports/dogfood-027.md) |
| [`dogfood-028.yaml`](dogfood-028.yaml) | WP-202 / CM-3 — pure Memory Pointer decision + reference rendering (`shouldPointerize(bytes, policy)` + `formatPointerReference(ref)` in a new `src/runner/memory-pointer.ts`, the analog of `buildVerdict`/`evaluateBaselinePrecheck`; decides when a tool output is large enough to store externally and renders the short context-facing reference, project.md Memory Pointer Pattern / CM-3; local policy type, imports only the frozen `ArtifactRef`, no `types.ts` change; the interception + `store.put` + injection wiring stays non-pure hand-design — first step toward unblocking WP-203 S2) | **SUCCESS** (run-7681a607; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; `ArtifactRef` imported type-only, no `types.ts` change; F-11-closed `s0 j@0` shape held for a seventh straight run; no new friction; **WP-202's pure decision+renderer surface complete**; input tokens 410k, a new series low) | [`dogfood-028.md`](../../docs/reports/dogfood-028.md) |
| [`dogfood-029.yaml`](dogfood-029.yaml) | WP-203 S2 — pure compaction digest-prompt half (`DIGEST_SYSTEM_PROMPT` + `buildDigestMessages(toDigest)` in a new `src/runner/compaction-prompt.ts`, the analog of `planner/prompt.ts`/`judge/prompt.ts`; builds the pure prompt for the compaction LLM digest call over the frozen `CompactionPlan.toDigest` and `Message`; the router call + `store.put` of the digest behind a Memory Pointer + `CompactionResult` journal write stays non-pure hand-design) | **SUCCESS** (run-74f88081; diff byte-for-byte to spec, harvested IDENTICAL + staged on `main`, pending commit; `Message` imported type-only, no schema/contract change; F-11-closed `s0 j@0` shape held for an eighth straight run; no new friction; **WP-203's entire pure surface now exhausted**; input tokens 462k, low band) | [`dogfood-029.md`](../../docs/reports/dogfood-029.md) |
| [`dogfood-030.yaml`](dogfood-030.yaml) | WP-201 Python-SDK parity — port the pure compaction digest-prompt half (`DIGEST_SYSTEM_PROMPT` + `build_digest_messages(to_digest) -> list[Message]` in a new `packages/sdk-py/src/chikory/compaction_prompt.py`, the Python parity of dogfood-029; mirrors the TS `compaction-prompt.ts` source-of-truth, `Message` already ported `chikory/types.py:87`, no contract change; the digest wiring stays non-pure hand-design) | **SUCCESS** (run-1a97e2ca; harvested IDENTICAL + staged on `main`, pending commit; F-11-closed `s0 j@0` shape held for a ninth straight run; no new friction; input tokens 434k, low band) | [`dogfood-030.md`](../../docs/reports/dogfood-030.md) |
| [`dogfood-031.yaml`](dogfood-031.yaml) | WP-207 context-window pacing decision — pure `decideContextWindowPacing(usage, policy)` runner helper (`continue`/`compact`/`park`) over projected context-window pressure; local types only, no `types.ts`/schema/runtime wiring; the non-pure runner/journal integration stays deferred | **SUCCESS** (run-c4aa79d3; run diff exact; landed `67eb167` with **F-31** commit-scope contamination → WP-231; F-11-closed `s0 j@0` shape held for a tenth straight run; input tokens 375k, new low) | [`dogfood-031.md`](../../docs/reports/dogfood-031.md) |
| [`dogfood-032.yaml`](dogfood-032.yaml) | WP-231 — dogfood landing-scope audit: compare run workspace diff with landed commit and flag extra/missing/different paths (dogfood-031 F-31) | **SUCCESS** (run-4f1fbe0a; landed `ded282b`; step-1 judge ESCALATE caught verifier abort risk, step 2 fixed it; **F-31 closed**) | [`dogfood-032.md`](../../docs/reports/dogfood-032.md) |
| [`dogfood-033.yaml`](dogfood-033.yaml) | WP-205 — pure branch-target parser for `chikory branch <run-id>@<step>` (`parseBranchTarget`, no workflow forking yet) | **SUCCESS** (run-41dd7c98; harvested IDENTICAL + staged uncommitted on `main`; F-11-closed `s0 j@0` shape held; no new friction; zero-step `$0.00` launch residue `run-1d4718db` ignored) | [`dogfood-033.md`](../../docs/reports/dogfood-033.md) |
| [`dogfood-034.yaml`](dogfood-034.yaml) | WP-205 — pure default branch-name helper for parsed branch targets (`branchNameForTarget`, no CLI command or git/worktree side effects yet) | **SUCCESS** (run-1634171d; harvested IDENTICAL + staged uncommitted on `main`; +5 tests, F-11-closed `s0 j@0` held for an eleventh straight run; no new friction; **WP-205 pure surface complete**; input tokens 594k mid-band) | [`dogfood-034.md`](../../docs/reports/dogfood-034.md) |
| [`dogfood-035.yaml`](dogfood-035.yaml) | WP-201 Python-SDK parity — port the two branch-target pure helpers (`parse_branch_target` + `branch_name_for_target` in a new `packages/sdk-py/src/chikory/branch_target.py`, mirroring the TS `src/cli/branch-target.ts` source-of-truth; the dogfood-030 parity pattern, no contract change) | **SUCCESS** (run-b0bc3865; harvested IDENTICAL + committed `88e496c` on `main`, landing-scope MATCH; 16 pytest cases, F-11-closed `s0 j@0` held for a twelfth straight run; no new friction; input tokens 318k, series low) | [`dogfood-035.md`](../../docs/reports/dogfood-035.md) |
| [`dogfood-036.yaml`](dogfood-036.yaml) | WP-201 Python-SDK parity — port the Memory Pointer pure surface (`should_pointerize` + `format_pointer_reference` + local `MemoryPointerPolicy` in a new `packages/sdk-py/src/chikory/memory_pointer.py`, mirroring the TS `runner/memory-pointer.ts` source-of-truth from dogfood-028; project.md Memory Pointer Pattern / CONTRACTS.md CM-3; `ArtifactRef` reused, no contract change) | **SUCCESS** (run-51645fbb; harvested IDENTICAL + uncommitted on the working tree; byte-for-byte parity verified incl. 12-char id truncation + em dash; 5 pytest cases, 72 py tests green; F-11-closed `s0 j@0` held for a thirteenth straight run; no new friction; input tokens 398k) | [`dogfood-036.md`](../../docs/reports/dogfood-036.md) |
| [`dogfood-037.yaml`](dogfood-037.yaml) | WP-219 S6 — the pure chain-trace renderer (`renderChainTrace(record, entries)` in a new `packages/sdk-ts/src/chain/trace.ts`, the chain-level analog of the per-run `renderTrace`; renders a `ChainRecord` plan tree + per-node run + outcome from the chain journal, the forensics surface unblocked by the hand-landed S3-wiring substrate; no contract change) — _refocused 2026-06-20 from the deprioritized WP-207 pacing parity port onto the critical-path chain executor_ | **SUCCESS** (run-295b2947; harvested IDENTICAL + uncommitted on the working tree; six-line render order + terminal-reason branch verified line-by-line against the spec & the per-run `renderTrace`; type-only contract imports, re-export at `index.ts:73`; 5 vitest cases, 365 TS tests green; F-11-closed `s0 j@0` held for a fifteenth straight run; no new friction; input tokens 793k) | [`dogfood-037.md`](../../docs/reports/dogfood-037.md) |
| [`dogfood-038.yaml`](dogfood-038.yaml) | WP-219 S3-pure — the chain-state reducer (`advanceChain` + `deriveChainStatus` in a new `packages/sdk-ts/src/chain/advance.ts`, the `computeVerdict` analog per ADR-005 §S3 transition rules; sibling of the landed pure `readyNodes`/`hasDependencyCycle`; consumes the hand-frozen `NodeOutcome` + `ChainRecord.nodeOutcomes` contract, no contract change in the slice) | **SUCCESS** (run-61e8b0a1; harvested IDENTICAL + uncommitted on the working tree; four-rule precedence + pure immutable fold verified line-by-line against ADR-005 §S3; type-only contract imports, re-export at `index.ts:72`; 6 vitest cases, 345 TS tests green; F-11-closed `s0 j@0` held for a fourteenth straight run; no new friction; input tokens 625k) | [`dogfood-038.md`](../../docs/reports/dogfood-038.md) |
| [`dogfood-039.yaml`](dogfood-039.yaml) | WP-201 Python-SDK parity — port the WP-219 S3 chain-state reducer (`advance_chain` + `derive_chain_status` in a new `packages/sdk-py/src/chikory/chain_advance.py`, mirroring the TS `src/chain/advance.ts` source-of-truth from dogfood-038; the four-rule ADR-005 §S3 precedence + pure immutable fold; `ChainRecord`/`NodeOutcome`/`ChainStatus` already in `chikory/types.py`, no contract change) | **SUCCESS** (run-6bb8ac7e; harvested IDENTICAL + uncommitted on the working tree; four-rule precedence + pure immutable fold verified line-by-line against the TS `advance.ts`; 6 pytest cases, 80 py tests green; F-11-closed `s0 j@0` held for a sixteenth straight run; no new friction; input tokens 755k, series high) | [`dogfood-039.md`](../../docs/reports/dogfood-039.md) |
| [`dogfood-040.yaml`](dogfood-040.yaml) | WP-201 Python-SDK parity — port the WP-207 context-window pacing decision (`decide_context_window_pacing` + local `ContextWindowUsage`/`ContextWindowPacingPolicy`/`ContextWindowPacingDecision` frozen dataclasses in a new `packages/sdk-py/src/chikory/pacing.py`, mirroring the TS `src/runner/pacing.ts` source-of-truth; the `continue`/`compact`/`park` decision over projected context-window pressure; all local types, no contract change) | not yet run — **demoted to track-B regression coverage** behind dogfood-041 (parity ports are not thesis evidence; land as a normal PR or run after the chain dogfood) | — |
| [`dogfood-041.yaml`](dogfood-041.yaml) | **FIRST CHAIN DOGFOOD** (WP-219 S3 end-to-end) — launched with the new `chikory chain` verb: decompose a goal (wire `chikory trace <chain-id>` to the pure `renderChainTrace`) into a multi-node Plan, gate it with the different-family plan meta-judge, run each node as a judge-gated child run through the durable `chainLoop`, halt on first FAILED node. Tests durable multi-run execution + compounding error + a real judge surface — what the first 39 pure-leaf runs never did. | 🔴 **DELIVERY SUCCESS, CHAIN NOT EXERCISED** (run-a28655c9; **launched with `chikory run`, NOT `chikory chain`** — single-run SUCCESS that delivered the `chikory trace <chain-id>` branch correctly (harvested IDENTICAL + staged uncommitted) but never invoked the planner/plan-meta-judge/`chainLoop`: no `.chikory/chains/`, no node runs, goal ran verbatim in 1 step. **F-32 → WP-232**. **Attempt 2** (Path A: delivery reverted, docs landed `a6880f3`, re-launched with `chikory chain`) engaged the chain path — planner decomposed a real goal into a multi-node Plan — but died at the plan meta-judge gate on a transient `transport error: fetch failed`, host-side before `startChain`, with zero persisted state → **F-33** (non-durable planning layer → WP-233) + **F-34** (`dogfood.sh` proxy not health-checked → WP-234). **Attempt 3** (proxy now health-checked, WP-234 done) got the furthest yet — planner AND plan meta-judge LLM calls BOTH succeeded (family-diverse `gpt-5.5` + `gemini-3.1-pro-preview`) — then the gate died deterministically on its own schema contradiction (`unrecognized_keys 'uncoveredCriteria'`) → **F-35 → WP-235, FIXED this session** (`PlanJudgeReplySchema` now accepts the `uncoveredCriteria` the response schema requires). **Attempt 4** (F-35 fixed) — planner decomposed a clean 2-node plan AND the meta-judge LLM PROCEEDed with a correct node→AC mapping — then the DETERMINISTIC coverage floor overrode PROCEED→REVISE because the planner's invented per-node criterion ids didn't equal the goal ids `AC-1/AC-2/AC-3` → **F-36 → WP-236, FIXED this session** (`PLANNER_SYSTEM_PROMPT` + `buildPlannerMessages` now instruct the planner to reuse each goal criterion id VERBATIM; floor unchanged as a safety net). The last GATING blocker is removed — re-launch `devbox run dogfood`; a PROCEED can finally flow into `startChain`/`chainLoop` and the nodes can execute. **🎉 Attempt 5 — THE CHAIN RAN END-TO-END** (`chain-d3794c24-…`): plan gate PROCEED → node 1 `resolve-id-and-render-trace` SUCCESS (changes made 1) → node 2 `verify-and-test` FAILED/HALT (changes made 0) → chain FAILED 1/2. Durable 2-node chain, per-node judge gating, the judge HALTing a stuck empty-diff node (true positive), and halt-on-FAILED all worked — **the thesis pillar is PROVEN to run**. It FAILED only on the deferred S4 context handoff → **F-37 → WP-237** (node 2 ran against fresh HEAD without node 1's diff; now the critical-path keystone). Secondary: planner emitted a verification-only node → **F-38 → WP-238, FIXED this session** (planner prompt now forbids verify-only nodes). Next: build WP-237 (S4 handoff), then re-launch for the first GREEN dependent chain) | [`dogfood-041.md`](../../docs/reports/dogfood-041.md) |
| [`dogfood-042.yaml`](dogfood-042.yaml) | WP-237 — first dependent 2-node chain: node A adds `formatUsd`; node B imports it to add `formatCostShare`, mechanically proving S4 predecessor git-state handoff. | **SUCCESS 2/2** (`chain-1cde6ae3-d05f-438e-b818-8af76419d6ae`; durable quota ESCALATE→approve→resume; harvested `b1b825d`; F-39/F-40/F-41/F-42) | [`dogfood-042.md`](../../docs/reports/dogfood-042.md) |
| [`dogfood-043.yaml`](dogfood-043.yaml) | WP-239 — artifact-backed fan-in proof: two independent predecessor modules must both reach one dependent importer; canonical runtime test gates deterministic merge/conflict behavior and non-shared-workspace transport. | **READY — prerequisites implemented; not yet run** | — |
