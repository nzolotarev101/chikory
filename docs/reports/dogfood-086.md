# dogfood-086 — WP-271 CHUNK-SCOPED JUDGE ADJUDICATION + UNATTENDED-SAFE ESCALATE. The F-107 unblock: closes the ⑦ overnight-unattended rung's named blocker. Threads the active `work_chunk` directive into the Agent-as-a-Judge so an intermediate chunked step is judged against the CURRENT chunk (later deferred-by-design PARTs are NOT flagged as omissions), AND makes ESCALATE unattended-safe (opt-in policy → seal a resumable terminal state instead of blocking forever on an untimed approval park). Delivered in FOUR dependency-ordered durable steps, each a distinct non-trivial diff. All 4 ACs green, harvest byte-IDENTICAL, cross-language contract parity (TS + Python + shared fixture). NO spurious ESCALATE fired this run (all 4 steps PROCEEDed) — confirming F-107 is NON-DETERMINISTIC LLM-judge variance (it fired in 085 on the same unfixed judge), which is exactly why the fix is needed.

- **WP:** WP-271 — chunk-scoped judge adjudication + unattended-safe ESCALATE policy (JD-3/JD-4 judge-trust pillar). A real open plan.md §6 product WP, spawned by 🔴 F-107 (dogfood-085). Feature code on `src/judge/*` + `src/workflow/agent-loop.ts` — the WP-270 chunking mechanism is the VEHICLE seeded INTO this real judge code (§1.2: no fresh throwaway utility). Named blocker for the ⑦ overnight-unattended rung.
- **Date:** 2026-07-04
- **Spec:** `examples/dogfood/dogfood-086-wp271-chunk-scoped-judge.yaml` (LOOSE — goal states OUTCOME + constraints, ACs pin done via OUTCOME symbols the goal NAMES; module/test layout left to the executor)
- **Run-id:** `run-88235198-2aea-4e29-a8b9-5fb9ab78930d`
- **Landed commit:** un-harvested (delivery on the working tree, byte-IDENTICAL to the run workspace — pack §5 all 18 files IDENTICAL)
- **Mode:** `chikory run` (single durable run, ATTENDED per the operator note — but no approval was needed since no escalate fired)

## Trace

```
run run-88235198-2aea-4e29-a8b9-5fb9ab78930d · SUCCESS · 4 steps · $5.72 / $50.00 · 20m 30s · executor codex(openai) · judge openai-compat/gemini-3.1-pro-preview
 #   step (chunk)                         tokens(in/out)  cost      diff B   verdict
 1   PART 1 thread active chunk           517k/4.3k       $0.6895    2464    ✓ PROCEED (1/4)   AC-1/2/3 fail = not-yet-rendered/tested, by design (WIP tolerance)
 2   PART 2 render + scope prompt         921k/6.6k       $1.2176    6650    ✓ PROCEED (4/4)
 3   PART 3 unattended-safe escalate      2376k/13k       $3.0979   17611    ✓ PROCEED (4/4)   biggest sub-goal (contract + parity + wiring + unit tests)
 4   PART 4 live Temporal proof           494k/5.4k       $0.6721    6252    ✓ PROCEED (4/4)
 totals: decisions 4 · judge passes 4 ($0.04, 0.7%) · rollbacks 0 · escalations 0 · injections 0 · checkpoints 4 (@4/@9/@14/@19) · pacing 4 · peak window 1%
```

- **Loop integrity 🟢 (no divergence):** 4 distinct sealed checkpoints (`@4/@9/@14/@19`, commits `42d6a3c0`/`ec965340`/`97cc4451`/`7da2a288`), `lastGood true` each, no duplicate journal entry, no re-executed committed step, 0 crash-resumes, 0 rollbacks, **0 escalations** (contrast 085's 1 spurious escalate + hollow re-hand). Chunk pointer advanced cleanly: step1 PART1→PROCEED (consumed 0→1) · step2 PART2 (1→2) · step3 PART3 (2→3) · step4 PART4 (3→4, all chunks consumed + ACs met → SUCCESS). **Non-hollow 4/4** — every sealed checkpoint carries a distinct non-trivial diff (2464 / 6650 / 17611 / 6252 bytes); no empty-diff probe step (F-11 did not recur).

## Delivery quality (human review, post-run workspace = working tree) — 🟢 COMPLETE, all-green, contract-additive, cross-language parity

All 4 numbered PARTs landed additively. Reviewed line-by-line against the goal.

| Goal PART | Delivered | ✓ |
|---|---|---|
| **1 — THREAD ACTIVE CHUNK (additive):** optional `activeWorkChunkDirective?: string` on the judge-pass input in `harness.ts`; pass it from the loop's judge invocation when `decideWorkChunk` returned `use_chunk`, else `undefined`; `undefined` leaves judge input unchanged; no frozen-contract reshape | `harness.ts`: added `activeWorkChunkDirective?: string` to `RunJudgePassInput` (`:171`), spread into `buildJudgeMessages` input ONLY when `!== undefined` (`:211`, preserves byte-equivalence). `agent-loop.ts:498`: spread `{ activeWorkChunkDirective: activeWorkChunk.chunk.directive }` into the `runJudgePass` call only when `activeWorkChunk.action === "use_chunk"`. Purely additive. | 🟢 |
| **2 — RENDER + SCOPE PROMPT (dep 1):** net-new pure `renderActiveWorkChunkScope(directive?: string): string` in `prompt.ts`, threaded into `buildJudgeMessages`; active chunk → a `## ACTIVE WORK CHUNK (this step's scope)` section stating later-goal parts absent from THIS diff are DEFERRED BY DESIGN; `undefined` → byte-equivalent prompt | `prompt.ts`: `renderActiveWorkChunkScope` (`:106`) returns `""` for `undefined`, else the scope header + directive + "DEFERRED BY DESIGN … must NOT be treated as omissions"; wired into `buildJudgeMessages` (`:161`) — appended after the GOAL section ONLY when non-empty (byte-equivalence guaranteed). Re-exported from `index.ts`. LLM still adjudicates. | 🟢 |
| **3 — UNATTENDED-SAFE ESCALATE (durable half):** pure total `decideEscalationWait(state, policy?)` in `src/workflow/` (sibling of `decideStepForcing`, no I/O/clock); opt-in policy → seal a resumable terminal instead of blocking forever; wire additively into BOTH agent-loop ESCALATE paths so default is byte-identical; exhaustive unit tests | NEW `src/workflow/escalation-wait.ts`: `decideEscalationWait` returns `await_approval`/`AWAITING_APPROVAL` unless `policy.escalation === "seal_resumable_failed"`, then `seal_resumable_failed`/`FAILED` with `failureReason: "unattended {source} escalation — {reason}"` (blank-reason normalized, input-immutable). Wired into `agent-loop.ts` **both** the judge-ESCALATE path (`:584`) and the runner-loop-breaker path (`:615`): default `undefined` policy → unchanged untimed `await condition(...)`. NEW additive contract: `UnattendedPolicy` type (`types.ts:178`) + `UnattendedPolicySchema` (zod, `schemas.ts:168`, in `ContractTypeChecks` + `contractSchemas`) + raw-YAML `unattended` (`taskspec.ts:155`) + `TaskSpec.unattended?` field. **Python parity:** `UnattendedPolicy` pydantic model (`types.py:149`) + `__init__.py` export + `test_contracts.py` registry. Shared fixture `fixtures/contracts/UnattendedPolicy.valid.json`. 6-case unit test `test/runner/escalation-wait.test.ts` (no-policy, explicit await_approval, judge+runner seal, blank-reason normalize, input-immutability). | 🟢 |
| **4 — LIVE PROOF (dep 1–3):** LIVE Temporal test (reuse `createRunnerWorker`/`createTemporalRunner`/scripted registry/`waitFor`/`describe.skipIf`/`makeJudgedSpec`) — a multi-chunk run whose EARLY step defers a LATER chunk is PROCEEDed (no ESCALATE / no `AWAITING_APPROVAL`) → SUCCESS; no-active-chunk prompt byte-equivalent; a GENUINE escalate under the unattended policy seals a resumable state. Co-reference `renderActiveWorkChunkScope` + `createRunnerWorker` (F-97) | `test/runner/work-chunk.test.ts` (extended): (a) unit — `renderActiveWorkChunkScope()` empty + `buildJudgeMessages` byte-equal with `undefined`; renders scope + "DEFERRED BY DESIGN". (b) LIVE — deferred early chunk → SUCCESS, `verdictKinds === ["PROCEED","PROCEED"]`, first judge wire body contains the rendered chunk scope; no-active vs baseline judge user content **byte-equal** and no `## ACTIVE WORK CHUNK` section; genuine escalate under `unattended:{escalation:seal_resumable_failed}` → `status FAILED`, `failure.reason` contains `unattended judge escalation` + the concern, 1 checkpoint preserved, `lastVerdict ESCALATE`. Co-references BOTH symbols (F-97-safe). | 🟢 |

- **Frozen contracts held:** `activeWorkChunkDirective` is an ADDITIVE OPTIONAL field; `renderActiveWorkChunkScope` + `decideEscalationWait` are NEW pure symbols; `UnattendedPolicy` + the `unattended` spec-input is an ADDITIVE OPTIONAL. No shape change to `StepRecord` / `JournalEntry` / `Checkpoint` / `RubricItem`. No new dependency. WP-123/WP-206/WP-269/WP-270 paths untouched; scan evidence unchanged.
- **Additivity is LIVE-PROVEN, not claimed:** PART 4 asserts the no-active-chunk judge user-message content is **byte-equal** to the baseline (no-chunk) run — the additive guarantee is an assertion, not a comment.
- **Scope (`git status --short`):** 19 files. New: `escalation-wait.ts`, `escalation-wait.test.ts`, `UnattendedPolicy.valid.json`. Modified additively: judge (`harness.ts`, `index.ts`, `prompt.ts`), workflow (`agent-loop.ts`), contracts (`types.ts`, `schemas.ts`, `taskspec.ts`, `runner/activities.ts`), tests (`work-chunk.test.ts`, `budget-gate.test.ts`, `verdict-gating.test.ts`, `taskspec.test.ts`), Python parity (`types.py`, `__init__.py`, `test_contracts.py`). All entailed by the goal (the contract + parity spread is the cost of an additive spec-input field). No out-of-scope file.

## Independent verify — the run's own green confirmed

- Pack §3 re-ran all 4 ACs against the WORKING TREE: **AC-1/2/3/4 PASS**. AC-4 = `tsc --noEmit && eslint . && vitest run` → **713 passed / 19 skipped, 107 files**, exit 0. Delivery on the tree (un-harvested but present) → tree-grep ACs trustworthy.
- Pack §5 byte-diffed all 18 changed `packages/…` files vs the run workspace: **all IDENTICAL** — the working tree IS the run's output; no divergence.
- The run's OWN judge executed all 4 ACs against the workspace each step (`judge-executed check … exited 0`); family diversity real: executor **codex (OpenAI)** vs judge **gemini-3.1-pro-preview (Gemini, via openai-compat shim)** — structurally distinct families.

## Anomaly hunt

- **Wasted/filler steps:** none. 4/4 non-hollow, no empty diff, no "already done" re-verify. Step 1's PROCEED at 1/4 criteria is correct WIP tolerance (PART 1 threads the field; AC-1/2/3 require the render/test that PARTs 2/4 add) — not a wasted step; the diff is 2464 real bytes.
- **Cost telemetry 🟢:** every step nonzero cost with nonzero tokens; no `.00`-with-tokens. Total exact $5.7171 / $50 = **11.4%**; judge share **0.7%** ($0.04). codex(openai) + gemini priced correctly.
- **Token economics:** step 3 = **2376k input / 13k output, 52 tool calls, $3.10** — the single biggest step, and justified: PART 3 touched 10 files (the additive `UnattendedPolicy` contract across TS type+zod+rawYAML, Python pydantic+export+test, the shared fixture, both agent-loop wire sites, the 6-case unit test). 2.4M input reflects context accumulation over the chunked horizon — a data point for WP-203/WP-207 (context-rot / pacing). Steps 1/2/4 = 517k/921k/494k, in line with 085.
- **Judge behavior 🟢:** checks executed live each step (`exited 0`); rubric justifications sane; **0 escalations, 0 rollbacks**; `judge_catches = 0` (no seeded seam — this was a straight feature-delivery run, not a judge-catch drill). Family diversity real.
- **Human ceremony:** attended (operator on hand per the spec note) but **no `chikory approve` needed** — the feared spurious park never fired. Human work = slice + launch + harvest-pending only.
- **Loop integrity 🟢:** see Trace — clean chunk-pointer advance, no dupes, no resumes.

## New friction

Friction numbering is global + sequential; highest prior = **F-109** (test-concurrency OOM, ledger commit `690bff1`). Continue at F-110.

### 🟡 F-110 → track-B / WP-271-adjacent (NEW, design-semantics, latent): the unattended-safe ESCALATE seals `status = FAILED`, conflating a policy-park with a genuine failure

- **Evidence:** `decideEscalationWait` under `escalation: seal_resumable_failed` returns `status: "FAILED"` with `failureReason: "unattended {source} escalation — {reason}"`, and `agent-loop.ts` calls `seal("FAILED", …)`. PART 4 asserts exactly this (`escalateReport.status === "FAILED"`). The executor chose FAILED to stay additive — a distinct terminal (e.g. `HALTED`/`AWAITING_APPROVAL`-durable) would reshape the frozen `RunStatus`/terminal-state contract, which the goal forbade.
- **Impact (low, latent):** an operator waking after an ⑦ overnight run sees `FAILED` and must parse `failure.reason` for the `unattended … escalation` prefix to distinguish a policy-park (resume-and-approve) from a real defect (investigate). Autonomy is preserved (no infinite hang — the primary F-107 harm is closed), but the terminal-state signal is lossy. WP-206 resume from a sealed FAILED checkpoint is the recovery path; whether resume correctly re-enters the escalated step (vs replaying) is untested here and couples to **F-108** (`consumedWorkChunks` not restored on resume, still open).
- **WP it spawns:** track-B note under WP-271 / a future terminal-state refinement (needs a frozen-contract ADR to add a distinct resumable-park status). NOT a headline — the primary F-107 harm (untimed hang) is closed and the ⑦ rung is unblocked.

### Still-open carry-forward (not new this run)
- 🟡 **F-108** (WP-206×WP-270): `consumedWorkChunks` not restored on crash→resume → a resumed chunked run replays from chunk 0. Untouched by this run; now coupled to F-110's resume path. Track-B.
- ℹ️ **F-107 is NON-DETERMINISTIC (confirmed):** this run exercised the STILL-unfixed chunk-unaware judge (the workspace clones HEAD; the fix takes effect only on the NEXT run) yet drew **zero** spurious escalates across 4 chunked steps — where 085 drew one. Same mechanism, opposite outcome ⇒ the false-positive is LLM-judge variance, not deterministic. This strengthens the WP-271 rationale (you cannot rely on the bug not firing on any given overnight run) and is captured, not filed as new friction.

## Verdict on the thesis

🟢 **Strong.** This run is the judge-trust pillar paying rent: a structurally-diverse judge (Gemini) delivering feature code that makes ITS OWN adjudication chunk-aware and makes an autonomous run un-hangable — the exact reliability lever the thesis claims (durable execution + real-time judge quality gates over long horizons). The delivery closed the named blocker (F-107) for the ⑦ overnight-unattended rung: an unattended run can no longer hang forever on a spurious approval park, and the judge no longer measures an intermediate chunked step against the whole goal. 4/4 non-hollow, 100% per-step reliability, contract-additive with cross-language parity — clean. The one residual (F-110, FAILED-conflation) is a cosmetic terminal-state signal, not an autonomy defect. **The ⑦ overnight-unattended rung is now unblocked.**
