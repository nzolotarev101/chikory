## 2026-08-08 — displaced from plan.md status block by the dogfood-127 review

- **Latest / next:** 🟢 **WP-595 (probe-verified scoring gate) DELIVERED CLEAN — dogfood-126 made an unproven requirement stop counting toward a published score** (`run-14291f34-7144-43b3-9a82-2ef776b4d008`, SUCCESS **1 step, $0.0469/$15.00**, 3m 58s, 2/2 criteria + 6/6 rubric, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-126.md`). `probe --record <file>` upserts a durable discrimination ledger (one entry per task: `taskId`/`baseRef`/`fixRef`/`verdict`/`probedAt`/`requirements[]`, re-probe REPLACES, unclean verdicts recorded as evidence); `summarize(…, ledger?)` counts a task's requirements only when it is BOTH base-verified AND holds a `discriminating` verdict **taken at the ref it was scored at** — every task stays in `perTask` with `discriminationVerified`, exclusions named in `unverifiedTasks`. All 5 designed traps rejected (stale ref · unclean verdict · silent drop · downstream recompute · no-ledger compatibility); with no ledger every published artifact re-summarizes unchanged. **Two review defects, both HAND-FIXED this sitting:** 🔴 **F-274** — the gate had NO operator path: `runSuite`'s new `ledger` option had no caller, so only a library import could arm it (**F-180 verbatim, and WP-593's `probe.json`-nobody-reads one level up**). Fixed: `run --discrimination-ledger <file>` loads and REFUSES a missing/damaged ledger at $0 before any task runs (`main.ts:59-62,322-338,364`). 🟠 **F-275** — a damaged ledger was silently reset to `{}` and written back, destroying every prior verdict; now `parseDiscriminationLedger`/`readDiscriminationLedger` throw naming the file (`results.ts:118-157`, `probe.ts:241-245`). +4 tests, harness 192 → **196** green; sdk-ts 1,304 green. Also 🟡 **F-276** (track-B: `fan-in-handoff` chain test flakes under full-suite parallel load — passes isolated) · ℹ️ F-268/F-265 recurrences, already WP-592/WP-590. Progression ⛔ **STALLED** + ⚠️ LADDER-PACE; ledger `rung=4` — WP-595 is the rung-5 PREREQUISITE, not the rung. **NEXT = WP-596 (dogfood-127): `probe --tasks <dir>` — a durable, resumable corpus sweep that persists each verdict as it lands, so the operator's multi-hour rung-5 probe survives a kill and never re-probes proven work.**

## 2026-08-08 — displaced from docs/DOGFOODING.md status block by the dogfood-126 review

**Status (2026-08-07, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🔴 **dogfood-125 DELIVERED WP-593 and was sealed FAILED WRONGLY** (`run-f3d47cf8-6d56-4c7b-85d1-fcfe185badef`,
4 steps **$0.1899/$15.00**, 7m 04s, `docs/reports/dogfood-125.md`). `chikory-bench probe --task <file>
[--out <dir>]` proves a task discriminates before it may be scored: base ref + new `repo.fix_ref` in
SEPARATE workspaces, `base_verification_command` at BOTH refs, red base ⇒ `inconclusive` (never
`discriminating`). Both ACs PASS, all 4 traps rejected, corpus untouched. Two 🔴 hand-fixes: **F-270** —
the executor greened its own trap by mutating SHARED `publishableRepoPath` (`results.ts:273`), so a repo
root published as `repos/chikory` off an ancestor repo — F-267 from the other end, in WP-591's own helper;
reverted, probe re-anchored on its out dir, harness 186→**192**. **F-271** — the judge passed **2/2 criteria
+ 6/6 rubric**, then one false free-text concern became an unattended FAILED seal because the F-229
carve-out demanded `diffRef.bytes === 0`; **the empty diff was never the load-bearing signal** — it now keys
on `allCriteriaPass && allRubricPass` (`agent-loop.ts:1110-1128`), sdk-ts 1,303→**1,304**. 🟠 **F-272 →
WP-594** (`compact` ×3 at 106% window, 0 folds — the `keepLastN: 5` floor beats the governor on short runs) ·
🟡 **F-273** (no gate asks what an edited SHARED function returns for callers outside the diff). Progression
✅ PROGRESSING, ⚠️ LADDER-PACE (rung 4 ×3). **NEXT = WP-302 (dogfood-126): fix-pin + probe-verify the corpus.**

## 2026-08-08 — displaced from plan.md status block by the dogfood-126 review

- **Latest / next:** 🔴 **WP-593 DELIVERED, RUN SEALED FAILED WRONGLY — dogfood-125 built the task-discrimination probe, then two quality gates produced the review's two worst defects** (`run-f3d47cf8-6d56-4c7b-85d1-fcfe185badef`, FAILED 4 steps, **$0.1899/$15.00**, 7m 04s, `docs/reports/dogfood-125.md`). **Delivery is correct and landed:** `chikory-bench probe --task <file> [--out <dir>]` materializes `repo.ref` and the new validated `repo.fix_ref` into SEPARATE workspaces, runs `base_verification_command` at BOTH refs, and refuses to classify anything when either is red (`inconclusive`, exit 1); otherwise it classifies each requirement `discriminating` (red@base/green@fix) · `non-discriminating` (green@base — free for every arm) · `unsatisfiable` (red@fix), writes `probe.json`, and exits 0 only on a fully discriminating verdict. All four designed traps rejected; both ACs PASS; corpus untouched. **Two 🔴 defects, both HAND-FIXED this sitting:** **F-270** — to green its own trap-B the executor edited the SHARED `publishableRepoPath` walk to start at `dirname(absolute)` (`results.ts:273`), so any target that IS a repo root resolved against an ancestor repo on the operator's disk (`<repo>` → `repos/chikory`) — **F-267 from the other end, inside the helper WP-591 added to prevent it**, and a `leaderboard.ts` caller the spec's constraints declared off-limits. Reverted; the real fix anchors the probe on its OUT DIR (`probe.ts:126-133`); +6 tests, harness 186 → **192**. **F-271** — the judge marked **2/2 criteria + 6/6 rubric PASS**, then wrote one free-text concern (factually false: the full suite does pass), and verdict rule 4 turned that sentence into an unattended FAILED seal, because the F-229 carve-out also demanded `diffRef.bytes === 0`. **The empty diff was never the load-bearing signal** — the step that DELIVERS the last fix is the most converged state a run reaches. Carve-out now keys on `allCriteriaPass && allRubricPass` alone (`agent-loop.ts:1110-1128`), matching the F-107 discipline the completion review already enforces; +1 test, sdk-ts 1,303 → **1,304**. Also 🟠 **F-272 → WP-594 queued** (pacing recommended `compact` ×3 at peak window 106%, folded 0 — the pressure policy still keeps `keepLastN: 5` verbatim, so a short run can never fold) · 🟡 **F-273** (no gate asks what an edited SHARED function returns for callers outside the diff; `publishableRepoPath` had no direct unit test — now it does) · ℹ️ F-268/F-265 recurrences, already WP-592/WP-590. Progression ✅ PROGRESSING; ledger `rung=4` — WP-593 is the rung-5 PREREQUISITE, not the rung. **NEXT = WP-302 (dogfood-126): fix-pin and probe-verify the existing corpus, then author the first probe-proven new task — the corpus half of the P3 exit gate.**

## 2026-08-07 — displaced from docs/DOGFOODING.md status block by the dogfood-125 review

**Status (2026-08-07, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟢 **P3-rung-5 HALF CLIMBED — dogfood-124 gave the harness an interval-ranked leaderboard**
(`run-80fe6b8f-5ceb-4089-b13b-195390bf682b`, SUCCESS **1 step $0.0442/$15.00**, 2m 18s, 2/2 criteria
+ 6/6 rubric, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-124.md`). `chikory-bench leaderboard
--bundle <dir>… --out <dir>` ranks by the **interval FLOOR** (`orderedBy: "iSrRange.low"`), marks a
pair `separated` only when the intervals are DISJOINT, and copies published rates instead of
recomputing them. `benchmarks/publications/leaderboard/` says it plainly: **Chikory [83.2%, 100.0%]
vs raw Claude Code [75.4%, 99.1%] — not separated at 95%, no winner.** All 4 designed traps rejected.
🟠 **F-267** (the published `bundle` pointer resolved only from `benchmarks/harness/` — **F-262
verbatim, one review later**) → **WP-591 hand-fixed**: `publishableRepoPath` anchors it to the repo
root, the dead `reference` is dropped, harness 183→**186** green. 🟡 **F-268 → WP-592** (executor
model id is bare `gemini` → no `PRICE_TABLE` row → every gemini-cli run meters **$0.00 on real
tokens**). ℹ️ F-265 recurrence (`0 tool calls`) — already WP-590. Progression ✅ PROGRESSING with a
⚠️ LADDER-PACE warning (rung 4 for 3 headlines). **NEXT = WP-593 (dogfood-125): `chikory-bench
probe` — red@base / green@fix per requirement, the gate the rung-5 corpus lift needs first.**

## 2026-08-07 — displaced from plan.md status block by the dogfood-125 review

- **Latest / next:** 🟢 **P3-RUNG-5 HALF CLIMBED — dogfood-124 gave the harness an interval-ranked leaderboard and published the honest verdict over the rung-4 numbers** (`run-80fe6b8f-5ceb-4089-b13b-195390bf682b`, SUCCESS 1 step, **$0.0442/$15.00**, 2m 18s, 2/2 criteria + 6/6 rubric, 0 rollbacks, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-124.md`). `chikory-bench leaderboard --bundle <dir>… --out <dir>` ranks arms by the **lower bound of the 95% interval** (`orderedBy: "iSrRange.low"`), marks a pair `separated` only when the intervals are DISJOINT, names no winner otherwise, and copies published rates rather than recomputing them. `benchmarks/publications/leaderboard/` now states it plainly: **Chikory [83.2%, 100.0%] vs raw Claude Code [75.4%, 99.1%] — not separated at 95%, no winner.** All four designed traps (point-estimate sort · winner-from-overlap · recompute · build-a-website) rejected. New friction: 🟠 **F-267** — the published `bundle` pointer was stored verbatim from `--bundle`, so `leaderboard.json` cited `../publications/p3-rung-4`, resolvable ONLY from `benchmarks/harness/` and from nowhere else (**F-262 verbatim, one review later**: the AC asserted the field was a non-empty string, never that it RESOLVED) → **WP-591 HAND-FIXED this sitting** (`publishableRepoPath` in `results.ts:244-278` + `leaderboard.ts:31-42`, dead `reference` dropped, bundle regenerated, harness 183→**186** green, both ACs still PASS). 🟡 **F-268 → WP-592 queued** — the executor endpoint records the model as bare `gemini`, matching no `PRICE_TABLE` row, so EVERY gemini-cli run meters **$0.00 on real tokens** and the executor half of `budget_usd` is structurally inert (WP-567 defeated by an alias). ℹ️ **F-265 recurrence** (`0 tool calls` on a 444-line/6-file delivery) — already WP-590, no new WP. Progression ✅ PROGRESSING; ledger `rung=4` (rung-5 is NOT satisfied until the intervals separate). **NEXT = WP-593 (dogfood-125): `chikory-bench probe` — mechanically prove each requirement is RED at the task's pinned base ref and GREEN at its fix ref before the task may enter the corpus.** The rung-5 remainder is a corpus wide enough to separate; `chikory-bench` today has `validate`/`list`/`fetch-devai`/`run`/`compare`/`leaderboard` and NOTHING that proves a new task discriminates — growing the corpus without that gate pushes both intervals UP and makes separation harder.

## 2026-08-07 — displaced from docs/DOGFOODING.md status block by the dogfood-124 review

**Status (2026-08-06, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟢 **P3-rung-4 CLIMBED — dogfood-123 published Chikory's first head-to-head benchmark result**
(`run-3e2a6791-cab1-46c7-bc5b-b3d0730b92c5`, SUCCESS **1 step $0.0457/$15.00**, 3m 20s, 2/2 criteria
+ 6/6 rubric, `docs/reports/dogfood-123.md`). **Chikory 19/19 I-SR [83.2%, 100.0%] vs raw Claude Code
18/19 I-SR [75.4%, 99.1%] / 16/19 D-SR [62.4%, 94.5%]** over `brownfield-001…005`, both arms 5/5
base-verified. Separation is `brownfield-001` alone (zod v3→v4). **The intervals OVERLAP** — 19
requirements cannot separate two arms at 95%; rung-5 needs corpus, not harness.
**The operator ran both arms BY HAND** (`scripts/bench-run.sh`, sequential) and the spec published only —
dogfood-122's lesson holds: an LLM executor may not supervise a multi-hour, quota-bound suite.
Agent classes live-checked: `park-until-reset` **0**, no rotation needed, 6/6 members probed at $0.
🔴 **F-261** (published trace link pointed inside a prunable run workspace) → **WP-588 hand-fixed**;
🟠 **F-263** (report prose cited a superseded suite run's versions) hand-fixed + provenance table;
ℹ️ **F-266** (progression gate named the retired P2 ladder) hand-fixed. **Queued: WP-589** (the
write-boundary rubric reads `git diff`, so 2.1 GiB / 95,068 gitignored writes scored ✓) · **WP-590**
(`gemini-cli` records `toolCalls: 0`). Progression ✅ PROGRESSING (rung 4). **NEXT = P3-rung-5, the
phase EXIT gate** — published DevAI-extended ranges + leaderboard, on a corpus that can separate.


### 2026-08-08 — displaced from plan.md status block (dogfood-128 review, WP-597)

- **Latest / next:** 🟢 **WP-596 (durable corpus probe sweep) DELIVERED CLEAN — dogfood-127 made a multi-hour corpus probe survivable** (`run-41fb5957-933b-467e-a16d-36df443f6f41`, SUCCESS **1 step, $0.0472/$15.00**, 3m 53s, 2/2 criteria + 6/6 rubric, harvest byte-IDENTICAL 4/4, `docs/reports/dogfood-127.md`). `probe --tasks <dir> --record <file>` walks every runnable task in stable order, persists each verdict **the moment it lands**, skips only proof taken at the refs the task declares TODAY (`probedAt` untouched), re-probes a moved ref, and NAMES an unprobeable task instead of ending the sweep; exit 0 only when every selected task holds a current `discriminating` verdict. All 5 designed traps rejected; AC-2 drives the swept ledger through the REAL `run --discrimination-ledger` CLI end to end. **Six review defects, all HAND-FIXED this sitting:** 🔴 **F-277** — a sweep with no `--out` shared ONE output dir across every task: each `probe.json` clobbered, and a SINGLE git workspace re-pointed across repos left the prior target's untracked `node_modules`/build output in the tree the next task's `base_verification_command` runs in (**F-258's family — a false base verdict into the ledger the WP-595 gate trusts**; both ACs always passed `--out`, so neither could see it). 🟠 **F-282** non-atomic ledger write → `writeLedgerAtomically` temp+rename (`probe.ts:77-91`). 🟡 **F-278** `--base-verify-minutes` unreachable from either probe mode — F-274's shape one WP later (`main.ts:494-503`) · **F-279** stale entries counted as `discriminating` (`probe.ts:389-400`) · **F-281** the sweep forked `run`'s task selection → `LoadReport.sources` (`suite.ts:26-52`) · **F-280** a test name claiming a moved-ref case its body never ran. +3 tests, harness 196 → **199** green; sdk-ts 1,304 green. 🔴 **F-283 → WP-597, and it is the next headline:** three consecutive headlines built probe → gate → sweep for evidence **no task in the corpus can produce** — zero tasks carry `repo.fix_ref` and `AUTHORING.md` never mentions the field; arming the gate today would exclude `brownfield-001`, the ONLY task separating the two published arms (a self-performed migration with no upstream fix commit). Progression ⛔ **STALLED** + ⚠️ LADDER-PACE; ledger `rung=4` — WP-596 is the rung-5 PREREQUISITE, not the rung. **NEXT = WP-597 (dogfood-128): make the corpus probeable — the gold-patch rule in `AUTHORING.md`, probeability reported by `validate`/`list`, and real `fix_ref`s pinned where an upstream fix exists.**

### 2026-08-08 — displaced from DOGFOODING.md status block (dogfood-128 review, WP-597)

🟢 **dogfood-127 DELIVERED WP-596 CLEAN — a corpus probe sweep now survives a kill and never re-probes
proven work** (`run-41fb5957-933b-467e-a16d-36df443f6f41`, SUCCESS **1 step $0.0472/$15.00**, 3m 53s, 2/2
criteria + 6/6 rubric, harvest byte-IDENTICAL 4/4, `docs/reports/dogfood-127.md`). `probe --tasks <dir>
--record <file>` walks every runnable task in stable order, persists each verdict the moment it lands,
skips only proof taken at the refs the task declares TODAY (`probedAt` untouched), re-probes a moved ref,
and names an unprobeable task instead of ending the sweep. All 5 traps rejected. **Six hand-fixes:** 🔴
**F-277** — a sweep with no `--out` shared ONE output dir: `probe.json` clobbered per task, and one git
workspace re-pointed across repos left the prior target's untracked `node_modules`/build output for the
next base verification (**F-258's family — a false base verdict into the ledger the gate trusts**). 🟠
**F-282** non-atomic ledger write → temp+rename. 🟡 **F-278** `--base-verify-minutes` unreachable from
either probe mode (F-274's shape) · **F-279** stale entries counted as proof · **F-281** the sweep forked
`run`'s selection · **F-280** a test name claiming a case its body never ran. +3 tests, harness 196→**199**.
🔴 **F-283 → WP-597**: NO corpus task carries `fix_ref` and `AUTHORING.md` never mentions it — three
headlines built for evidence nobody can produce. Progression ⛔ **STALLED** + ⚠️ LADDER-PACE.
**NEXT = WP-597 (dogfood-128): make the corpus probeable — a documented, validated gold-patch ref.**

### 2026-08-09 — displaced from plan.md status block (dogfood-129 review)

- **Latest / next:** 🟢 **WP-597 (probeable corpus) DELIVERED — the benchmark corpus can now produce the evidence WP-593/595/596 consume, for 4 of 5 tasks** (`run-951a565d-73a5-4cc8-a230-9b63d872fba9`, SUCCESS **1 step, $0.0425/$15.00**, 2m 46s, 2/2 criteria + 6/6 rubric, harvest byte-IDENTICAL 8/8, `docs/reports/dogfood-128.md`). `brownfield-002/003/004/005` now carry a real `repo.fix_ref`; each was fetched and read by hand and is the **direct child** of its pinned base with a subject matching the PR the task already named in prose (#3036 · #5855 · #13613 · #7390) — transcription, not invention. `AUTHORING.md` carries the gold-patch rule in the frozen Format v1 block + the pin checklist, and probeability is something the PRODUCT reports: `validate --require-probeable` exits 1 naming each runnable task that can never be probed and the field it lacks, and `probe --tasks` records it as its OWN outcome (`unprobeable`), never folded into the failure count. All 5 designed traps rejected. **🟠 F-284 — IT DID NOT UNBLOCK THE RUNG:** `brownfield-001` is the ONLY task separating the two published arms (dogfood-123: Chikory [83.2%, 100.0%] vs raw Claude Code [75.4%, 99.1%]) and is a SELF-PERFORMED migration with no upstream fix commit, so it still has no gold patch — `validate --require-probeable` over the real corpus exits 1 forever, `probe --tasks benchmarks/tasks` can never exit 0, and arming the WP-595 gate today would DROP the only discriminating task. `runProbe` materializes the fix from the task's own `repo.url` (`probe.ts:121-122`), so a self-authored patch cannot be expressed at all. Review hand-fix: 🟡 **F-285** — `--require-probeable` was undocumented and `parseFlags` silently ignored unknown flags, so a one-letter typo exited 0 (a false green on the check that catches an unprobeable corpus) → USAGE entry + `VALIDATE_FLAGS` allowlist (`main.ts:35-38,141-142,183-191`), +2 tests, harness 199 → **201**. 🟡 F-286 (the rule is enforced nowhere `devbox run bench` runs — `scripts/bench.sh:20` omits the flag) folds into WP-598. 🟡 F-287 track-B (an AC greping a file for prose was satisfied by prose that PREDATED the run). Progression ⛔ **STALLED** + ⚠️ LADDER-PACE, ledger `rung=4` — the FOURTH consecutive rung-5 prerequisite. **NEXT = WP-598 (dogfood-129): a gold patch upstream never authored must still be probeable — declare the fix source separately from `repo.url`, and put `--require-probeable` into the $0 `devbox run bench` guard.**

### 2026-08-09 — displaced from docs/DOGFOODING.md status block (dogfood-129 review)

🟢 **dogfood-128 DELIVERED WP-597 — the benchmark corpus can finally produce the evidence the
scoring gate consumes, for 4 of 5 tasks** (`run-951a565d-73a5-4cc8-a230-9b63d872fba9`, SUCCESS **1 step
$0.0425/$15.00**, 2m 46s, 2/2 criteria + 6/6 rubric, harvest byte-IDENTICAL 8/8, `docs/reports/dogfood-128.md`).
`brownfield-002/003/004/005` carry a real `repo.fix_ref` — each fetched and read by hand, each the direct
child of its pinned base, subjects matching the PRs the tasks already named in prose (#3036 · #5855 ·
#13613 · #7390). `AUTHORING.md` carries the gold-patch rule; `validate --require-probeable` names each
runnable task that can never be probed; `probe --tasks` records `unprobeable` as its OWN outcome.
🟡 **F-285 HAND-FIXED** — the flag was undocumented and `parseFlags` swallowed unknown flags, so
`--require-probable` exited **0**: USAGE + `VALIDATE_FLAGS` allowlist, +2 tests, harness 199→**201**.
**🟠 F-284 — the rung is STILL blocked:** `brownfield-001` (the ONLY task separating
the two published arms) is a self-performed migration with no upstream fix, so `--require-probeable` over
the real corpus exits 1 forever and the sweep can never exit 0 — `runProbe` fetches the fix from the
task's own `repo.url` (`probe.ts:121-122`), so a self-authored patch is inexpressible. 🟡 F-286 (the rule
is enforced nowhere `devbox run bench` runs) folds in. Progression ⛔ **STALLED** + ⚠️ LADDER-PACE, `rung=4`.
**NEXT = WP-598 (dogfood-129): make a gold patch upstream never authored probeable, then arm the $0 guard.**

### 2026-08-10 — displaced from plan.md status block (dogfood-130 review)

- **Latest / next:** 🟢 **WP-598 (authored gold patch) DELIVERED — a benchmark task can carry its own fix patch, so a migration nobody upstream ever made is still provable** (`run-6ac4329e-c172-4f85-b1fc-c0ef9fb60851`, SUCCESS **6 steps, $0.3634/$15.00**, 22m 14s, 2/2 criteria, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-129.md`). `repo.fix_patch` is a strict-Zod field (`task.ts:103`); `runProbe` checks out `repo.ref` and applies the patch onto it (`probe.ts:189-201`); a patch that fails to apply — or applies to nothing — throws naming task, patch and base ref (`probe.ts:203-214`), writing no ledger entry; and the ledger `fixRef` for a patch-backed task is the **sha256 of the patch content** (`probe.ts:83-84`), so WP-596's existing skip test re-probes on a one-byte edit with no new machinery. Declaring both sources is a validation error naming both; declaring neither stays unprobeable — **no `exempt` escape hatch was invented**. All 5 designed traps rejected; `brownfield-002/003/004/005` untouched. **🔴 F-288 — THE FINDING OF THIS CAMPAIGN: the run shipped a hole its own judge named three times.** AC-1's fixture used ABSOLUTE patch paths, contradicting the goal's "repo-relative path in THIS repository"; the judge's completion review caught it, step 5 **fixed** it, AC-1 went red, and step 6 **reverted the fix** ("as required by the AC-1 oracle fixture script") and rewrote the unit test to enshrine it. The judge then raised an out-of-rubric `concern` naming the AC-vs-goal conflict verbatim — and `verdict.ts:126` drops a concern whenever the rubric also failed (the `out_of_rubric` class carries F-154 force-seal semantics), so it never escalated and never reached the rationale. Run sealed 🟢 SUCCESS. **F-180's lesson at the concern altitude → WP-599 (queued).** Review hand-fixes: 🔴 **F-289** (the guard restored against the GOAL — `probe.ts:35-45`, `probe.test.ts:340`; AC-1 is retro-invalid and now fails by design) · 🟡 **F-290** (delivery shipped `devbox run lint` RED, `no-regex-spaces`; no AC runs lint — `test/task.test.ts:116`) · 🟡 F-291 track-B (steps 2–3 zero-byte diffs claiming fixes; HALT guard caught it). 🟠 **F-292 found while arming the next spec: NOT ONE stored per-task result carries `repoRef`** — `suite.ts` began recording it only with WP-595 (2026-08-07) and every stored suite predates that, so the WP-595 gate applied to any already-run suite excludes **100%** of tasks. Progression ⛔ **STALLED** + ⚠️ LADDER-PACE, ledger `rung=4` — the **FIFTH** consecutive rung-5 prerequisite. **NEXT = WP-600 (dogfood-130): stop building rung-5 mechanisms and CONSUME them — probe the real corpus for the first time, and re-derive the published numbers from stored evidence through that ledger without re-running either arm. The remaining blocker after it is an OPERATOR authoring lift (`brownfield-001`'s zod v4 gold patch, 3–6h), not a product gap.**

### 2026-08-10 — displaced from docs/DOGFOODING.md status block (dogfood-130 review)

🟢 **dogfood-129 DELIVERED WP-598 — a benchmark task can carry its own gold patch**
(`run-6ac4329e-c172-4f85-b1fc-c0ef9fb60851`, SUCCESS **6 steps $0.3634/$15.00**, 22m 14s, 2/2 criteria,
harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-129.md`). `repo.fix_patch` + apply-onto-base; a patch that
fails to apply is a named error with nothing recorded; the ledger `fixRef` is the patch's **sha256**, so editing it re-probes via WP-596's skip test. No `exempt` escape hatch (trap A).
🔴 **F-288 — the run shipped a hole its own judge named 3×.** AC-1 mandated ABSOLUTE patch paths against a
goal demanding repo-relative; step 5 fixed it, AC-1 went RED, step 6 **reverted the fix to satisfy the AC**.
The judge's out-of-rubric `concern` named the conflict verbatim; `verdict.ts:126` drops a concern whenever
the rubric also failed → sealed SUCCESS. **→ WP-599.** 🔴 F-289 + 🟡 F-290 hand-fixed (`probe.ts:35-45`,
`probe.test.ts:340`, `test/task.test.ts:116` — lint shipped RED). 🟠 **F-292:** no stored per-task result
carries `repoRef`, so the WP-595 gate excludes 100% of any already-run suite. Progression ⛔ **STALLED**,
`rung=4` — 5th consecutive rung-5 prerequisite. **NEXT = WP-600 (dogfood-130): consume the machinery —
probe the real corpus, re-derive the published numbers. Then only an OPERATOR lift remains.**

### 2026-08-11 — displaced from plan.md status block (dogfood-131 review)

- **Latest / next:** 🟢 **WP-600 (real discrimination ledger) DELIVERED — the benchmark's own quality gate finally ran against the real corpus, and it says the published numbers rest on nothing yet** (`branch-run-838ae110-…-step-5-2f201f9a`, SUCCESS **3 steps, $0.1447/$20.00**, 1h 02m, 2/2 criteria, `docs/reports/dogfood-130.md`). `benchmarks/results/discrimination.json` + per-task `probe.json` for `brownfield-002…005` are real, transcribed evidence; `brownfield-001` is named unprobeable, never fabricated. `chikory-bench resummarize` (`main.ts:614-782`) re-derives a stored suite with no arm re-run, no clone, no LLM, and F-292's doubly-false message became its own reason (`results.ts:177-182`). **THE FINDING: 0 of 19 published requirements can currently be verified, for BOTH arms** — `benchmarks/publications/p3-rung-4-corrected/` publishes 0.0% [0.0%, 0.0%] beside the historical 100% [83.2%, 100.0%], names all 5 excluded tasks, and leaves `p3-rung-4/` byte-identical.
- **What this run cost in ceremony:** the original `run-838ae110` committed 8 orphan gitlinks, the operator rejected the escalation with a precise one-command fix, and the run **sealed dead** — 🔴 **F-296 → WP-602**: a plain run has no self-heal on reject and the reject text is discarded (`agent-loop.ts:1148-1153`), while the approve path resumes (`:1167`). WP-542/F-207 and WP-543/F-208 cover chain nodes only. The delivery came from a hand-branch off checkpoint `@5`, and 🟡 **F-298 → WP-603** — `chikory branch` accepts no guidance and `inject` needs a live handle, so the branch ran **unsteered** (journal: `injections 0`, no `gitlink` string anywhere).
- **Review hand-fixes, both introduced by the delivery, both in evidence-durability code:** 🔴 **F-293** — the shipped `.gitignore` could not re-include nested `probe.json` (`*` ignores the directory, so `!**/probe.json` is inert); the harvest ABORTED and AC-2 was green only inside the workspace that force-added the files → depth-1 re-include (`benchmarks/results/.gitignore:6-11`), +2 tests proven RED-then-GREEN. 🔴 **F-294** — the `publishableRepoPath` rewrite made the F-261/F-267 run-workspace guard a NO-OP for real on-disk worktrees, and its regression test stayed green because the path was fictional → `results.ts:432-447`, +2 tests. Harness 205 → **209**, sdk-ts 1,304, lint clean.
- 🔴 **F-295 → WP-601** (WP-599's sibling): the judge's diff evidence is incremental, so a `design_serves_overall_goal` objection expires once the window moves past the code. Pass #1 named the `resummarize` aggregate-synthesis fallback; pass #3 passed the same rubric item over a window starting after it; the fallback shipped unchanged (`main.ts:702`, `main.ts:746` — fabricated ids, satisfaction by position). It did not fire on this delivery, but it is a live trap.
- **Progression ✅ PROGRESSING** (a thesis axis moved) with ⚠️ LADDER PACE still flagged; ledger `rung=4` — the **SIXTH** consecutive rung-5 prerequisite. **Rung 5 is no longer blocked on product**: it needs `brownfield-001`'s zod v3→v4 gold patch (3–6 h OPERATOR authoring) and a re-run of both arms so stored results carry `repoRef` — hours of quota dogfood-122 proved an LLM executor must not supervise. **NEXT = WP-602 (dogfood-131): a rejected escalation must route the human's correction back into the loop instead of killing the run.** It beats the rung because the rung is operator-gated, and it is the pillar this very review had to substitute a human for.

### 2026-08-11 — displaced from docs/DOGFOODING.md status block (dogfood-131 review)

🟢 **dogfood-130 / WP-600 (real discrimination ledger) DELIVERED** — `branch-run-838ae110-…-step-5-2f201f9a`, SUCCESS 3 steps $0.1447/$20.00, 1h 02m, 2/2 criteria (`docs/reports/dogfood-130.md`). The corpus carries real probe evidence for the first time, and the honest answer is **0 of 19 published requirements verifiable, for both arms** (`benchmarks/publications/p3-rung-4-corrected/`; `p3-rung-4/` untouched). Ledger `rung=4` — the SIXTH consecutive rung-5 prerequisite; rung 5 is now blocked on OPERATOR work (a gold patch + an arm re-run), not product.

**Operational lessons this run added to §8 — read them before the next launch:** a `--reject` **kills a plain run and discards your fix** (🔴 F-296 → WP-602; branch from a checkpoint instead), a branched child runs **unsteered** because nothing attaches guidance to it (🟡 F-298 → WP-603 — check `injections 0` in the trace), and a judge **design objection expires** once the incremental diff window moves past the code (🔴 F-295 → WP-601 — read pass #1's rubric, not only the last). Two review hand-fixes were delivery-introduced defects in evidence-durability code: a `.gitignore` negation that could not re-include nested `probe.json` (🔴 F-293) and a run-workspace publication guard rewritten into a no-op whose test was blind because its path was fictional (🔴 F-294). Harness 205 → **209**, sdk-ts 1,304, lint clean.

### 2026-08-11 — displaced from plan.md status block (dogfood-132 review)

- **Latest / next:** 🟢 **WP-602 (a rejected escalation routes the operator's correction into the loop) DELIVERED — a run can finally act on a human saying "no, here is what is wrong"** (`run-6b50d3f9-ef17-4ce4-9d45-dbb34422db54`, SUCCESS **3 steps, $0.1899/$20.00**, 40m 15s active, 2/2 criteria, landed `d499128`, `docs/reports/dogfood-131.md`). A reasoned reject now routes into the EXISTING remediation path (WP-519/520) with the operator's words verbatim in the brief (`src/workflow/rejection.ts:44-49`), bounded by `maxRejectStrikes` (default 1, `0` restores the old dead seal); a reason-less or whitespace-only reject still seals dead. `applyRemediation`/`handleOperatorRejection` (`src/workflow/agent-loop.ts:324-368`) collapsed three copies of the heal block into one — that consolidation was the judge's own catch at completion review, and step 3 exists to fix it. dogfood-130's hand-recovery (operator wrote a complete one-command work order into `--reject`, run sealed dead at $0.11) is now unnecessary.
- **The finding this run cost: 🔴 F-300 → WP-604 — a spec has no channel for a run-scoped judge rubric item, and losing one is silent.** `judge:` is `.strict()` with no `rubric_extra` (`src/taskspec.ts:219-229`), so dogfood-131 would not parse; the block was deleted to launch and trap F ("a strike is spent only on what the agent controlled") shipped **ungated**, with preflight printing all 🟢. The one field that looks like the channel, `judge.rubric_packs`, parses and has **zero consumers**. Second occurrence — `bb6025b` amputated dogfood-129's top-level `rubric:` the same way. **This is the THIRD instance of one shape** (F-296 → WP-602 reject text · F-298 → WP-603 branch guidance · F-300 → WP-604 rubric item): a channel accepts human intent and drops it without a word. Treat that as a defect class; the loudness fix outweighs any single channel.
- **Review hand-fixes:** 🔴 **F-301** — AC-1's `pnpm exec tsc` compiles the MAIN tsconfig only, so three `TS2353` errors in the delivery's own tests passed three judge-executed checks, the executor's "TypeScript 🟢 Clean" claim, and vitest (which transpiles without typechecking); caught only by the harvest gate's `tsc --noEmit -p tsconfig.test.json`. The offending key was inert (`test/runner/rejection-live.test.ts:89,121,151`; the wire URL reaches the worker via `routerOptions:64`) — removed, 24 tests green. 🟡 **F-303** — `agent-loop.ts:351` duplicated the strike default as a literal `?? 1` → now consumes `DEFAULT_MAX_REJECTION_STRIKES`. Track-B: 🟠 **F-304** (a `maxSeconds` kill leaks an orphaned Temporal dev server — pid 86445, port 52252, `PPID 1`; the reaper needs a process-group kill) · 🟡 **F-305** (the executor burned both 600 s caps on background-wait loops and a provider hang; step 2 is journaled FAILED although its kill still delivered the whole 26 KB WP).
- 🟠 **F-302 → WP-605:** the heal WP-602 just built **discards work the operator never objected to**. `applyRemediation` restores `lastGoodCheckpointId` unconditionally (`agent-loop.ts:335-341` → `activities.ts:1906-1918`), but that anchor only advances on PROCEED-with-delivered-work (`agent-loop.ts:990`, F-211). This run is the proof: step 3 delivered the 9,908-byte consolidation the judge demanded, drew ESCALATE, and the anchor stayed at `@10` — a reject would have destroyed it. Right for HALT, wrong for an operator correction on a passing verdict, and it contradicts the landed WP-544 rule that the cheapest heal preserves work.
- **Progression ✅ PROGRESSING** (a thesis axis moved) with ⚠️ LADDER PACE still flagged; ledger `rung=0` (off-ladder by declaration) — rung 5's blockers are **unchanged and still operator-only**: `brownfield-001`'s zod v3→v4 gold patch (3–6 h authoring) and a both-arm re-run so stored results carry `repoRef`. **NEXT = WP-605 (dogfood-132): a heal must not throw away work the human never asked to throw away.** It beats the rung because the rung is operator-gated, and it beats WP-604 because it is a live correctness defect in code landed this sitting — the first real use of WP-602 would destroy the delivery it was healing.

### 2026-08-11 — displaced from docs/DOGFOODING.md status block (dogfood-132 review)

🟢 **dogfood-131 / WP-602 (a rejected escalation routes the operator's correction into the loop) DELIVERED** — `run-6b50d3f9-ef17-4ce4-9d45-dbb34422db54`, SUCCESS 3 steps $0.1899/$20.00, 40m 15s active, 2/2 criteria, landed `d499128` (`docs/reports/dogfood-131.md`)

**`--reject "<reason>"` no longer kills a run — it heals it.** The reason becomes the remediation brief verbatim, the run rolls back to its last good checkpoint and continues under its own power. Budget: `max_reject_strikes` (default 1; set `0` to restore the old dead seal). A reject with **no reason** — or only whitespace — still seals dead immediately, so say why. Two live cautions: the heal restores the last **PROCEED-with-work** checkpoint, so a reject after a step that delivered but drew ESCALATE discards that step (🟠 F-302 → WP-605); and the previous §8 advice to "branch from a checkpoint instead" now applies only to a dead-sealed run.

**Operational lessons this run added to §7/§8 — read them before the next launch:** a spec **cannot** state its own judge rubric item (`judge:` is `.strict()`, `rubric_extra` is not a field and `rubric_packs` is inert) and deleting the block to launch is silent (🔴 F-300 → WP-604) · an acceptance check that typechecks must run the **repo's** typecheck gate, not a bare `pnpm exec tsc`, which skips `tsconfig.test.json` entirely (🔴 F-301) · a step killed at `maxSeconds` leaks an orphaned Temporal dev server (🟠 F-304).

### 2026-08-11 — displaced from plan.md status block (dogfood-133 review)

- **Latest / next:** 🟢 **WP-605 (a heal must not throw away work the human never asked to throw away) DELIVERED — the rollback is a decision, not a reflex** (`run-8e2c21c7-c495-491b-8a4a-0c9f141c7902`, SUCCESS **1 step, $0.0440/$20.00**, 3m 36s, 2/2 criteria, `docs/reports/dogfood-132.md`). `decideHealRollback` (`src/workflow/heal-rollback.ts`) answers `keep` or `rollback` from the VERDICT, never the trigger: a failing acceptance criterion or a destructive rubric failure condemns the work and restores the anchor; a verdict that condemned nothing keeps it and the correction rides on top; an absent anchor keeps by definition. `applyRemediation` (`src/workflow/agent-loop.ts:327-364`) gates BOTH `restoreCheckpoint` and the `rollbackTo` journal key on that one answer. **WP-519 survives in practice:** a rule-3 HALT is only ever reached through trailing criterion failures, so it always rolls back — proven against the untouched `test/runner/remediation-live.test.ts:141`. All six traps rejected; the dogfood-131 judge's one-helper consolidation was not re-split. Finding→fix in ONE run: F-302 was measured in the previous review and fixed here for $0.044.
- **The finding this run cost: 🟠 F-306 → WP-606 — the gemini executor persists RAW CLI stdout as the step summary.** 1,088 of this run's 6,729 summary bytes were not the agent talking about the task: a 349-byte `agy` `<notification>` background-task envelope plus 737 bytes of the model quoting its own communication-style system prompt back to itself. That string is pushed into `recentSummaries` and rides into the NEXT step's prompt (`agent-loop.ts:883` → `:769`), inflates the pacing governor's next-step token estimate (`:841`), and becomes the `chikory trace` step title (this run's step is titled `<notification> <task_id>a97fda3b-83…`). Context rot is a first-class thesis pillar; feeding it 1 KB of foreign harness noise per step is self-inflicted. Hand-fixed half: the envelope is now stripped (`src/executors/gemini-cli.ts:43-56`, +3 tests). Queued half: the undelimited deliberation needs an opening delimiter to match the existing terminal `CHIKORY_TASK_COMPLETE` marker (`src/executors/prompt.ts:71-76`).
- **Review hand-fixes:** 🟠 **F-306** envelope half (above) · 🟡 **F-308** — `scripts/dogfood-docs.mjs index --row` inserted a `--row` file verbatim without checking it was a table row, so dogfood-132's own campaign-index entry landed as a bare description with no link cell and was invisible to the table AND to the next review's `--outcome` lookup; repaired in place plus a shape guard (`scripts/dogfood-docs.mjs:182-197`). Also 🟡 **F-307** track-B: a heal that PRESERVED the work and a heal that had no anchor render identically in `chikory trace` (both simply omit `rollbackTo`), so the goal's "a reviewer can tell the two apart" bullet is only half-met; the fix is a positive journal marker, not review-sitting work.
- **Progression ✅ PROGRESSING** with ⚠️ LADDER PACE still flagged; ledger `rung=0` (off-ladder by declaration). **NEXT = WP-604 (dogfood-133): a spec must be able to state a run-scoped judge rubric item, and losing one must be loud.** Rung 5's blockers were RE-MEASURED this review and are unchanged operator work: no gold patch exists anywhere under `benchmarks/` for `brownfield-001`, and `grep -rl repoRef benchmarks/results/` returns nothing. WP-604 is the only open 🔴 and the THIRD instance of one defect class (F-296 → WP-602 reject text · F-298 → WP-603 branch guidance · F-300 → WP-604 rubric item): a channel accepts human intent and drops it without a word.

### 2026-08-11 — displaced from docs/DOGFOODING.md status block (dogfood-133 review)

🟢 **dogfood-132 / WP-605 (a heal must not throw away work the human never asked to throw away) DELIVERED** — `run-8e2c21c7-c495-491b-8a4a-0c9f141c7902`, SUCCESS **1 step, $0.0440/$20.00**, 3m 36s, 2/2 criteria, `docs/reports/dogfood-132.md`.

**A self-heal now undoes only what the quality gate condemned.** `decideHealRollback` reads the VERDICT, never the trigger: a failing acceptance criterion or a destructive rubric failure rolls back to the last-good checkpoint exactly as before (WP-519 intact — a rule-3 HALT is only ever reached through failing criteria), while a verdict that condemned nothing keeps the work and applies your correction on top. The journal stops recording a `rollbackTo` for a heal that performed none. Finding→fix in one run: the defect was measured in the dogfood-131 review and fixed here for $0.044.

**Operational lessons this run added to §7/§8 — read them before the next launch:** a gemini step summary is RAW CLI stdout, so `chikory trace` titles can read `<notification> <task_id>…` and ~1 KB of harness noise rides into the next step's prompt (🟠 F-306 → WP-606; the `<notification>` envelope is now stripped) · a heal with no `(rolled back to …)` suffix is ambiguous between *work preserved* and *no anchor existed* (🟡 F-307, track-B) · the campaign index silently lost dogfood-132's own row because `dogfood-docs.mjs index --row` accepted a non-table-row file (🟡 F-308, hand-fixed with a shape guard).

### 2026-08-12 — displaced from plan.md status block (dogfood-134 review)

- **Latest / next:** 🟢 **WP-604 (a spec must be able to state a run-scoped judge rubric item, and losing one must be loud) DELIVERED** (`run-83bf691d-75b5-4848-a182-6e66dcb6b857`, SUCCESS **3 steps, $0.1887/$20.00**, 9m 24s, 2/2 criteria, `docs/reports/dogfood-133.md`). `judge.rubric_extra` is a first-class `{id, description}` list appended to `STANDING_RUBRIC` for every PER-STEP pass (`src/runner/activities.ts:1687-1691,1705,1780`), forced `destructive: false` by construction (`src/taskspec.ts:448`) so a YAML typo can never fire CONTRACTS.md §4 rule 1; a standing-id collision, an in-list duplicate and a `destructive` key are each refused BY NAME (`src/taskspec.ts:438,442`); the inert `judge.rubric_packs` is gone from the schema, so declaring it is now an unknown-key error. AC-2 read the item off the REAL journaled verdict, so trap A (parse-and-drop — the F-300 defect wearing the fix as a costume) could not pass. Third instance of the drop-the-human's-words class closed (F-296 → WP-602 · F-298 → WP-603 · F-300 → WP-604).
- **The finding this run cost: 🔴 F-310 → WP-607 — a rubric item backed by a DETERMINISTIC scan is discharged as advisory, so a proven defect ships green.** The delivery imported `RubricItem` from `./judge/rubric.js` into `src/types.ts` — a forbidden `core→judge` edge. `scanDiffForLayeringViolations` (`src/judge/scan-layering.ts:93`) proved it; the judge failed `no_architecture_violations` at 3 of 5 passes INCLUDING the final completion review, and `agent-loop.ts:1099-1105` sealed `SUCCESS` anyway with the finding merely recorded. Re-measured by hand: the scan over the harvested diff returned `["core→judge"]`. F-107 is right for a model's taste and wrong for a fact a scan can settle. Compounding: the per-step pass scans only its own diff interval, so the empty-diff step 3 scored the item ✓ and laundered the standing violation. **Also 🔴 F-311 → WP-608:** 2 of 3 steps produced a 0-byte diff because the executor wrote a plan and asked *"Would you like me to proceed?"* — and one of them WAS the single bounded remediation step the WP-537/F-180 gate granted. Its plan named the correct fix and never applied it. 56.8% of run spend ($0.1071/$0.1887) judged empty steps.
- **Review hand-fixes (all landed this sitting, 1,333 sdk-ts green, lint + both typecheck projects clean):** 🔴 **F-309** — the `core→judge` breach itself: `RubricItem` moved to the core layer (`src/types.ts:237-243`), `src/judge/rubric.ts:8-10` imports and re-exports it so every `./rubric.js` importer is unchanged; scan re-run returns `[]`. 🔴 **F-312** — the step summary carried STALE `agy` task logs from another session (17 of the 46 test files their vitest listing names do not exist in the workspace, which holds 174), and the executor transcribed "558/558 across 46 files" into its own verification table as this run's suite gate; the `<gmsg name="task_notification">` envelope the dogfood-132 F-306 fix never matched is now stripped (`src/executors/gemini-cli.ts:61,66-69`, +4 tests). 🟡 **F-314** — `docs/spec/CONTRACTS.md:103-114` still declared the retired `rubricPacks`. 🟡 **F-315** — the new public field shipped with ZERO repo tests (AC-2 deletes its own generated test); 6 added at `packages/sdk-ts/test/taskspec.test.ts:326-393`. Track-B: 🟡 **F-313** (the F-11 probe-share metric meters EXECUTOR cost, structurally $0 under a keyless CLI executor, so it printed 0.0% for a run that spent 56.8% judging empty steps).
- **Progression ⛔ STALLED** (no thesis axis moved over the trailing 3) with ⚠️ LADDER PACE flagged; ledger `rung=0` (off-ladder by declaration, THIRD consecutive). **NEXT = WP-607 (dogfood-134): a rubric item whose evidence is deterministic must gate the seal, not be recorded and shipped.** Rung 5's two blockers were RE-MEASURED again this review and are unchanged operator work, not product gaps: `find benchmarks -path '*brownfield-001*'` still returns only the task YAML (no gold patch — 3–6 h of operator research on a migration upstream never performed), and `grep -rl repoRef benchmarks/results/` still returns nothing (both arms need a multi-hour quota-bound re-run dogfood-122 proved an LLM executor must not supervise). Against that, F-310 is a 🔴 loop-integrity defect this run demonstrated LIVE on the mechanism the thesis rests on. **Standing caution: a SUCCESS seal whose rationale reads `completion review: design findings recorded — …` is NOT a clean run — read the rationale, every time.**

### 2026-08-12 — displaced from docs/DOGFOODING.md status block (dogfood-134 review)

🟢 **dogfood-133 / WP-604 (a spec must be able to state a run-scoped judge rubric item, and losing one must be loud) DELIVERED** — `run-83bf691d-75b5-4848-a182-6e66dcb6b857`, SUCCESS **3 steps, $0.1887/$20.00**, 9m 24s, 2/2 criteria, `docs/reports/dogfood-133.md`.

**A spec can now write down a quality rule for its own run, and the judge is genuinely asked it.** `judge.rubric_extra` takes `{id, description}` items, appends them to the standing rubric on every per-step pass, and forces them non-destructive so a YAML typo can never restore your workspace. A colliding id, a duplicate, or a `destructive` key is refused BY NAME instead of merged or dropped, and the inert `judge.rubric_packs` is gone — declaring it is now an unknown-key error.

**Read this before the next launch — the judge was RIGHT and the run shipped anyway.** The delivery introduced a forbidden `core→judge` layer dependency; the deterministic architecture scan proved it and the judge failed `no_architecture_violations` at 3 of 5 passes INCLUDING the final completion review, and the run still sealed SUCCESS with the finding merely recorded (🔴 F-310 → WP-607). **A `SUCCESS` whose seal rationale reads `completion review: design findings recorded — …` is NOT a clean run — read the rationale, every time.** It shipped because the ONE bounded remediation step that gate buys was spent by the executor writing a plan and asking *"Would you like me to proceed?"* — 2 of 3 steps had a 0-byte diff for that reason, and 56.8% of run spend judged them (🔴 F-311 → WP-608). Also: a step summary carried STALE `agy` task logs from a different session and the executor transcribed their "558/558 tests across 46 files" as this run's suite gate — 17 of those files do not exist (🔴 F-312, `<gmsg>` envelope now stripped). Never let a gate rest on what the executor SAYS it ran.

### 2026-08-12 — displaced from plan.md status block (dogfood-135 review)

- **Latest / next:** 🟢 **WP-607 (a rubric item whose evidence is DETERMINISTIC must gate the seal, not be recorded and shipped) DELIVERED** (`run-74602888-a3eb-4da5-8ba3-b04b8e9a98a3`, SUCCESS **1 step, $0.0499/$20.00**, 4m 25s, 2/2 criteria, `docs/reports/dogfood-134.md`). `DETERMINISTIC_RUBRIC_IDS` (`src/judge/rubric.ts:19`) names exactly the two scan-backed items; `applyCheckOverrides` rebuilds both rows from `architectureLabels`/`secretScanLabels` instead of the model's answer (`src/judge/harness.ts:177,188`), and a survivor at the final review seals **FAILED** on both seal paths (`src/workflow/agent-loop.ts:1100,1119`). All six designed traps rejected: the item stays `destructive: false` with zero ROLLBACK verdicts (trap A), the run reaches terminal unattended (B), a design-only failure still seals SUCCESS (C), the scripted judge answered the item ✓ on a genuinely violating workspace and the run failed anyway (D), AC-2 drove the REAL Temporal loop and read the REAL journal (E), and the bounded completion-review repair still ran first (F). The F-310/dogfood-133 defect — detect and ship anyway — is closed.
- **The findings this run cost: 🔴 F-316 → WP-609 — the run reported a green suite it never ran.** The step summary claims *"The full test suite, type-checker and linter have passed cleanly"*; its own evidence block four lines below lists only typecheck, lint, build and **5/5 tests in one new file**. The suite was RED in 6 places. Nothing caught it: `tests_pass` is JD-4-overridden from the judge-executed ACs (*"all 2 judge-executed checks exited 0"* — true, and not a statement about the suite), the goal's "every existing test remains green" is prose a diff-only judge cannot verify, and no AC owned it. 🟡 **F-317 → WP-610:** `applyCheckOverrides` defaults `secretScanLabels: string[] = []`, so any caller omitting it force-passes the `destructive: true` `no_secrets_introduced` item — closing the model's only seam for forcing a ROLLBACK on a secret the regex scan misses. Measured by isolation: disabling that one branch flipped `verdict-gating.test.ts` and `work-chunk.test.ts` green and left the third failure untouched.
- **Review hand-fixes (all landed this sitting — sdk-ts 1,341 green from 1,335 passed/6 failed, lint + both typecheck projects clean):** 🔴 **F-318** — the delivery's own `test/runner/deterministic-rubric-live.test.ts` shipped **3/3 failing** and 2 of 3 could never pass: scenarios 2–3 re-committed byte-identical content (`git commit` exits 1 on an empty commit) and scenario 1 seeded the violation into the SOURCE REPO before launch, where the workspace-vs-`chikory-base` scan can never see it. Rewritten with a step-scoped adapter exactly as AC-2 does; 3/3 green. Also surfaced while repairing it: `decideCompletionReview` (`src/workflow/completion-review.ts:68`) SKIPS the review on a first-verdict seal with a clean rubric, so scenario 3 now withholds AC-1 on pass 1 or the advisory path is vacuously green. 🔴 **F-316 collateral** — the two ROLLBACK levers moved to `no_unrelated_deletions`, a destructive item the model still settles (`test/runner/verdict-gating.test.ts:147`, `test/runner/work-chunk.test.ts:407`); the seeded-violation test now asserts the FAILED seal and its reason instead of the old advisory SUCCESS.
- **Progression ⛔ STALLED** (no thesis axis moved over the trailing 3); ledger `rung=0` (off-ladder by declaration, **FOURTH** consecutive). **NEXT = WP-609 (dogfood-135): the suite is an oracle, not a sentence — a delivery that breaks a pre-existing test must not be able to report success.** Rung 5's two blockers were RE-MEASURED again and are unchanged operator work, not product gaps (`find benchmarks -path '*brownfield-001*'` still returns only the task YAML; `grep -rl repoRef benchmarks/results/` still returns nothing). Against that, F-316 is a 🔴 loop-integrity defect measured LIVE this run: the gate WP-607 just built covers only what a program settles, and the suite's health was left to a step summary's self-report — which was false. **Standing caution: `tests_pass` means "the judge-executed acceptance checks exited 0". Until WP-609 lands it is NOT evidence the repository builds green — read the suite, every time.**

### 2026-08-12 — displaced from docs/DOGFOODING.md status block (dogfood-135 review)

🟢 **dogfood-134 / WP-607 (a rubric item whose evidence is DETERMINISTIC must gate the seal) DELIVERED** — `run-74602888-a3eb-4da5-8ba3-b04b8e9a98a3`, SUCCESS 1 step, $0.0499/$20.00, 4m 25s, 2/2 criteria (`docs/reports/dogfood-134.md`).

**A defect the machine can PROVE now stops the run from claiming success.** `DETERMINISTIC_RUBRIC_IDS` names the two scan-backed rubric items; their answer is derived from the scan output, not the model's opinion; and one still live after the single bounded repair seals **FAILED**. It blocks WITHOUT rolling back — the item stays non-destructive, no ROLLBACK opens, the files stay on disk and the run terminates unattended. Closes dogfood-133's F-310, where the judge proved a `core→judge` breach at 3 of 5 passes and the run shipped it green.

**Read this before the next launch — `tests_pass` does NOT mean the suite is green (🔴 F-316 → WP-609).** This run's step summary claimed *"The full test suite, type-checker and linter have passed cleanly"*; its own evidence block listed only typecheck, lint, build and 5 tests in one new file. The suite was RED in 6 tests across 3 files, including the executor's own new file (3/3 failing, 2 of them structurally impossible). Nothing caught it: `tests_pass` is JD-4-overridden from the judge-executed ACs and means *"the acceptance checks exited 0"*; a diff-only judge cannot see a test broken in a file the delivery never touched; and no AC owned the constraint. **Run `devbox run test` yourself before believing any summary that claims a green suite** — `dogfood-close.sh` will refuse the commit regardless, but the review is where you find out why.

### 2026-08-12 — displaced from plan.md status block (dogfood-136 review)

- **Latest / next:** 🟢 **WP-609 (the suite is an oracle, not a sentence) DELIVERED** (`run-908510d8-d90d-4711-a88f-39ea7bf6482e`, SUCCESS **4 steps, $0.2512/$20.00**, 18m 47s active, 2/2 criteria, `docs/reports/dogfood-135.md`). A spec names the repository's own verification command (`regression_suite`, `src/taskspec.ts:194,468`), it survives the WORKFLOW schema round-trip (`src/schemas.ts:258`) and chain-node templating (`src/chain/node-spec.ts:118,170`), and `pre_existing_suite_still_green` joins `DETERMINISTIC_RUBRIC_IDS` (`src/judge/rubric.ts:19-32`) settled from the command's EXIT CODE (`src/judge/harness.ts:200-217`), never the model's answer. It runs at the seal only (`src/runner/activities.ts:1782`), exactly once on a clean run and at most twice with the bounded repair; a red suite blocks SUCCESS WITHOUT rolling back (item stays `destructive: false`, zero ROLLBACK verdicts); a declared suite now forces the completion review a 1-step seal used to skip (`src/workflow/completion-review.ts:74`). All seven designed traps rejected; AC-2 drove the REAL Temporal workflow with an external marker file counting executions. **The dogfood-134 lie — "the full suite passed" written over a red repository — can no longer be told by a spec that declares its suite.**
- **What this run cost, and what it could not prove: 🟡 F-319 → WP-611** — the ONE bounded repair a red suite grants is briefed as a DESIGN review (*"do NOT change behavior, only design"*, `src/workflow/completion-review.ts:113-126`) and carries no failing-test evidence, only ``command `X` exited 1``. **🟡 F-320 → WP-612** — a cap-killed suite seals the run FAILED: the WP-263(b) infra-skip covers CRITERION rows only (`src/judge/verdict.ts:101`), so an `infraFailed` RUBRIC row still reaches `deterministicFails` (`src/workflow/agent-loop.ts:1108,1127`), and `checkTimeoutMs` is not spec-settable (fixed 120 s, `src/judge/evidence.ts:37`, against a 50.7 s suite). **🟡 F-321 → WP-613** — with NO suite declared the item records `pass: true, "no regression suite command executed for this pass"` (`src/judge/harness.ts:211-216`) on every pass of every run: a self-reported green, the exact shape WP-609 abolishes. **By F-197 the gate could not judge its own run** (the judge ran the launch-time build), so dogfood-136 must be the first run to DECLARE `regression_suite:` and show the row settled from an exit code in its own journal.
- **Review hand-fixes: 🟡 F-322** — the run parked **11h 59m 31s** in `AWAITING_APPROVAL` on a CONVERGED out-of-rubric escalation (all criteria + all rubric ✓) because the spec omitted `unattended: escalation: seal_resumable_failed`, the four words that make `agent-loop.ts:1213-1224` auto-seal it; the block is now on dogfood-136 and a launcher precheck is track-B. The escalation's own premise was sound and its conclusion false: the executor's claimed `build`/`lint`/`typecheck`/full-suite green was re-run BY HAND this review and is exactly true (**174 files, 1342 tests, 50.73 s, all exit 0**) — the judge simply had no channel to see it, which is WP-609's whole point. ℹ️ F-11 recurrence: step 1 spent 3m 12s and 4.6k tokens on `agy produced no response (empty print output)` with no quota signature; the loop absorbed it unaided.
- **Progression ⛔ STALLED** (no thesis axis moved over the trailing 3); ledger `rung=0` (off-ladder, **FIFTH** consecutive). **NEXT = dogfood-136: WP-611+WP-613 as one run — the regression gate must brief its repair with the failing tests, and must not claim a green it never measured.** Rung 5's two blockers were RE-MEASURED this review and are unchanged OPERATOR work (`find benchmarks/tasks -name '*brownfield-001*'` still returns only the task YAML with no `fix_ref`; `grep -rl repoRef benchmarks/results/` still returns nothing). **Standing caution (replaces dogfood-134's): `pre_existing_suite_still_green` means the repository is green ONLY on a run whose spec declares `regression_suite:`. Everywhere else it is a placeholder ✓ — read the justification, not the checkmark.**

### 2026-08-12 — displaced from docs/DOGFOODING.md status block (dogfood-136 review)

🟢 **dogfood-135 / WP-609 (the suite is an oracle, not a sentence) DELIVERED** — `run-908510d8-d90d-4711-a88f-39ea7bf6482e`, SUCCESS 4 steps, $0.2512/$20.00, 18m 47s active, 2/2 criteria (`docs/reports/dogfood-135.md`).

**A spec can now name the command that runs the repository's existing tests, and a red result stops the run from claiming success.** Add `regression_suite: <command>` to a spec: it runs ONCE at the run-completion review (never per-step, at most twice with the bounded repair), and `pre_existing_suite_still_green` is settled from its EXIT CODE, never the model's answer. A red suite blocks the SUCCESS seal WITHOUT rolling back — the work stays on disk and the run still terminates unattended. This closes dogfood-134's F-316, where a step summary claimed a green suite over a repository that was red in 6 tests.

**Read this before the next launch — three live caveats.** ① The row means the repository is green **only on a run whose spec declares `regression_suite:`**; everywhere else it records a placeholder ✓ reading *"no regression suite command executed for this pass"* (🟡 F-321 → WP-613). Read the justification, not the checkmark. ② A suite killed at the fixed 120 s per-check cap seals the run **FAILED**, not "infra died" (🟡 F-320 → WP-612) — time your suite before declaring it (`devbox run test` ≈ 68 s at HEAD, sdk-ts alone 50.7 s). ③ The one bounded repair a red suite grants is briefed *"do NOT change behavior, only design"* and names no failing test (🟡 F-319 → WP-611), so expect it to be wasted until that lands.

**Put `unattended: escalation: seal_resumable_failed` in every headline spec (🟡 F-322).** dogfood-135 omitted it and parked **11h 59m** in `AWAITING_APPROVAL` on an escalation where every criterion and every rubric item passed — a state the loop auto-seals SUCCESS when the block is present. `chikory approve <run-id>` seals it at $0 if you find one parked.

### 2026-08-13 — displaced from plan.md status block (dogfood-137 review)

- **Latest / next:** 🟢 **WP-611 (brief the repair with the failing tests) + WP-613 (no unmeasured green row) DELIVERED** (`run-44abed04-fa4e-465b-8fd1-9c0de732c1a3`, SUCCESS **1 step, $0.1050/$20.00**, 6m 38s, 2/2 criteria, `docs/reports/dogfood-136.md`). `pre_existing_suite_still_green` no longer sits in `STANDING_RUBRIC` or `COMPLETION_REVIEW_RUBRIC` (`src/judge/rubric.ts:34,93`); it joins the effective rubric only when `input.completionReview && spec.regressionSuite` (`src/runner/activities.ts:1688-1692`), and `applyCheckOverrides` now RETURNS AN ERROR naming the id when it is declared without evidence (`src/judge/harness.ts:218-220`) — routed to `escalate()` (`:364-369`), never a silent pass. A red suite appends a bounded tail of the command's OWN output to the justification (`:206-210`), and `buildCompletionReviewBrief` branches to a `REPAIR BRIEF` directing a behavior fix while keeping the WP-537/F-196 design wording otherwise (`src/workflow/completion-review.ts:114-127`). All seven designed traps rejected; the pinned rubric-shape test was UPDATED, not deleted or weakened.
- **WP-609 is now LIVE-PROVEN — the F-197 debt from dogfood-135 is paid.** This run declared its own `regression_suite:` and its completion review carries `✓ pre_existing_suite_still_green — regression suite command \`pnpm --filter @chikory/sdk exec vitest run test/judge test/workflow\` exited 0`, settled from a real exit code in its own journal. Independent post-landing verification this review: **175 files / 1348 tests green in 49.75 s**, lint + both typecheck projects clean.
- **What this run cost, and what it could not prove: 🟡 F-323 + F-324 → WP-614** — the excerpt is bounded at 1000 chars (`src/judge/harness.ts:208`) against a 2000-char TAIL-truncating whole-brief clamp (`src/workflow/completion-review.ts:19,137-139`), and the suite row is appended LAST (`src/runner/activities.ts:1690`), so it is first against the cut. MEASURED against the built `dist/`: 0 design findings → 1491 chars intact · 1 → 1844 intact · **2 → clamped at 2000, the vitest summary line AND the closing instruction both lost**. The goal said in as many words that the findings *can never be squeezed out*. Two oracles missed it: the delivery's clamp test asserts only `length <= 2000` (`test/runner/completion-review.test.ts:282-292`), and AC-2 scenario 1 drove `designPass = true`, so a red suite and a design finding never co-occurred — the third recurrence of the AC-input-family class (F-187/F-196/F-198). **By F-197 the WP-611/WP-613 fixes could not judge their own run** (the worker ran the launch-time build, so pass #1 still shows the old `"no regression suite command executed for this pass"` row); dogfood-137 must show per-step passes carrying NO suite row.
- **Review notes: 🟢 F-325 track-B** — the executor issued four separate tasks 12–17 s apart, each narrated *"Let's run `pnpm run build`"* and each returning a **typecheck** result, echoing a command string (`tsc -p test/tsconfig.json --noEmit`) the repo does not define (`packages/sdk-ts/package.json:21`). F-306/WP-606 lineage; the bounded transcript made `build`/`lint`/full-suite look unrun, and only an independent suite run disproved it. ℹ️ Cost meter blind again (7022 executor tokens at $0.0000, judge share 100%) — the known keyless-CLI condition, no new item.
- **Progression ✅ PROGRESSING**; ledger `rung=0` (off-ladder, **SIXTH** consecutive). **NEXT = dogfood-137: WP-612+WP-614 as one run — an infra-killed suite must not read as a code red at the seal, and the repair brief must RESERVE room for the failing tests instead of letting them fall off the end.** Rung 5's two blockers were RE-MEASURED this review and are unchanged OPERATOR work (`benchmarks/tasks/brownfield-001-dependency-major-upgrade.yaml:24` is still `status: pinned` with no `fix_ref`; `grep -rl repoRef benchmarks/results/` still returns 0). **Standing caution (replaces dogfood-135's): a declared `regression_suite:` that runs slower than the FIXED 120 s per-check cap (`src/judge/evidence.ts:37`) seals the run FAILED as if the code were red — this repo's own judge+workflow slice runs in ~4 s and the full suite in ~50 s, so the margin is real but unguarded until WP-612 lands.**

### 2026-08-13 — displaced from docs/DOGFOODING.md status block (dogfood-137 review)

🟢 **dogfood-136 / WP-611 (brief the repair with the failing tests) + WP-613 (no unmeasured green row) DELIVERED** — `run-44abed04-fa4e-465b-8fd1-9c0de732c1a3`, SUCCESS 1 step, $0.1050/$20.00, 6m 38s, 2/2 criteria, harvest byte-identical 9/9, `docs/reports/dogfood-136.md`.

**The gate now says what broke, and stays silent about what it never measured.** When a declared `regression_suite:` comes back red, the one repair step the run grants is handed a **REPAIR BRIEF** carrying a bounded tail of the command's own output — the failing test names — with the standing *"do NOT change behavior, only design"* line absent; an ordinary design finding keeps that wording unchanged. And `pre_existing_suite_still_green` no longer sits in either default rubric: it joins the effective rubric only on the **run-completion review of a run that declares a suite**, and the override now ERRORS (routed to `escalate()`) rather than recording a pass when it is ever asked without evidence.

**WP-609 is live-proven.** This was the first spec to declare `regression_suite:`, and its own journal carries the row settled from a real exit code (`… exited 0`) — the F-197 debt from dogfood-135 is paid. Independently re-run this review: **175 files / 1348 tests green in 49.75 s**.

**Read this before the next launch — two live caveats.** ① A suite slower than the **fixed 120 s** per-check cap seals the run FAILED as if the code were red (🟡 F-320 → WP-612) — declare the narrowest suite covering what you edit, and time it at HEAD. ② The repair brief's evidence can still be cut: the excerpt is bounded at 1000 chars but the brief is clamped to 2000 from the tail, so at **two** co-occurring design findings the suite summary and the closing instruction are both lost (🟡 F-323 → WP-614).

**Keep putting `unattended: escalation: seal_resumable_failed` in every headline spec (F-322).** dogfood-136 carried it and reached terminal unattended; dogfood-135 omitted it and parked 11h 59m in `AWAITING_APPROVAL`.

### 2026-08-13 — displaced from docs/DOGFOODING.md status block (dogfood-138 review)

🟢 **dogfood-137 / WP-612 (an infra kill is not a code red at the seal) + WP-614 (a repair brief reserves room for the failing tests) DELIVERED** — `run-308cc0d4-e7ae-4676-a251-eb4c9d940426`, SUCCESS 1 step, $0.1095/$20.00, 7m 0s, 2/2 criteria, harvest byte-IDENTICAL 11/11, `docs/reports/dogfood-137.md`.

**The regression-suite gate is usable on a real project now.** A suite killed for being slow no longer seals the run FAILED, and `check_timeout_ms:` lets a spec name its own per-check cap instead of living with a fixed 120 s (absent → 120 s unchanged). The repair brief budgets the failing-test excerpt from the room actually left, so other findings no longer push the test names off the page.

**WP-613 is live-proven, WP-609 for the second time.** Judge pass #1 (per-step) carries no `pre_existing_suite_still_green` row at all; judge pass #2 (the completion review) carries exactly one, settled from a real exit code. That is the F-197 debt from dogfood-136 paid.

**Read this before the next launch — three live caveats.** ① A cap-killed suite seals 🟢 `SUCCESS · "completion review: design findings recorded — pre_existing_suite_still_green"` — the gate did NOT pass, it never finished (🟠 F-327 → WP-615, §7). ② A red suite's RAW output (≤64 KB) now rides into the verdict rationale and prints in full in `chikory trace` (🟠 F-328 → WP-616, §8). ③ 🔴 F-326 — the "reserved" brief hit 46,095 chars against its 2000-char cap in a 4-char window — was HAND-FIXED in the dogfood-137 review; a tree without `src/workflow/completion-review.ts:168-189` still has it.

**Keep putting `unattended: escalation: seal_resumable_failed` in every headline spec (F-322).** dogfood-136 and dogfood-137 both carried it and both reached terminal unattended; dogfood-135 parked 11h 59m 31s for want of it. **And sweep the LENGTH, not just the count (🟡 F-329):** an AC that computes a budget from a length must drive that length's boundary and assert the boundary was reached — a single fixed value is not a sweep, and it is exactly how F-326 shipped green.

### 2026-08-13 — displaced from plan.md status block (dogfood-138 review)

- **Latest / next:** 🟢 **WP-612 (an infra kill is not a code red at the seal) + WP-614 (a repair brief reserves room for the failing tests) DELIVERED** (`run-308cc0d4-e7ae-4676-a251-eb4c9d940426`, SUCCESS **1 step, $0.1095/$20.00**, 7m 0s, 2/2 criteria, `docs/reports/dogfood-137.md`). Both seal paths now filter an infra-killed row out of `deterministicFails` (`src/workflow/agent-loop.ts:1103,1122`), so a suite killed at its cap no longer produces the FAILED seal a genuinely broken build gets. `check_timeout_ms:` / `checkTimeoutMs` is a first-class spec field — strict public YAML (`src/taskspec.ts:196-197,475-479`), workflow round-trip (`src/schemas.ts:259`), chain templating (`src/chain/node-spec.ts:62-63,121,174,309-310`) — reaching `collectEvidence` via `src/runner/activities.ts:1786`, with the 120 s default untouched when absent. `buildCompletionReviewBrief` computes the excerpt budget from the room actually left after the header, every other failing finding and the closing line. **AC-2 drove three REAL Temporal runs**, including a cap kill timed against a declared 3000 ms. All seven designed traps rejected; the pinned F-220 template test was EXTENDED, not weakened.
- **WP-613 is now LIVE-PROVEN, and WP-609 for the second time.** Judge pass #1 (per-step) carries **no** `pre_existing_suite_still_green` row at all; judge pass #2 (the completion review) carries exactly one, settled from a real exit code. That is the F-197 debt from dogfood-136 paid — a run cannot prove the runner/judge fix it delivers, so the invariant had to be read off the NEXT run's journal, and it held.
- **What this run cost, and what it could not prove: 🔴 F-326 (HAND-FIXED) + 🟠 F-327 → WP-615 + 🟠 F-328 → WP-616.** The new budget arithmetic used `Math.max(0, availableForLog - 2)` and `String.slice(-0)` is `slice(0)` — the WHOLE string — with no cap check on the final return: measured **46,095 chars against a 2000-char cap** when the fixed part lands within 4 chars of the budget (`src/workflow/completion-review.ts:168-189`, fixed this sitting, +1 sweep test, gate suite 138 → **142** green). Still open: a cap-killed suite seals `SUCCESS · "completion review: design findings recorded — pre_existing_suite_still_green"` — the outcome never says DID NOT COMPLETE (F-327, proven on a live run); and dropping the 1000-char excerpt bound (`src/judge/harness.ts:205-208`) unbounded the verdict rationale (`src/judge/verdict.ts:44,167-169`) and `chikory trace` (`src/cli/trace.ts:501,505`) to the 64 KB capture ceiling (F-328).
- **Review notes: 🟡 F-329 + 🟡 F-330 track-B.** F-329 — AC-1 was built to close F-324 and did, sweeping 0/1/2/3 co-occurring findings at ONE fixed justification length; F-326 lives in the length dimension. **For a check that computes a budget from a length, the length is an input family and a single value is not a sweep.** F-330 — `checkTimeoutMs` is unforwarded at the second `runJudgePass` call site (`src/chain/activities.ts:437`); latent today (that pass runs no checks), F-283's unfed-pipeline shape one call site later. ℹ️ F-306 recurred (5% of the step summary was stale in-progress narration, and it is the trace index line) — WP-606 unchanged.
- **Progression ✅ PROGRESSING**; ledger `rung=0` (off-ladder, **SEVENTH** consecutive). **NEXT = dogfood-138: WP-615 — a gate that could not finish must SAY so.** The infra-kill seal is half-built: the FAILED verdict is correctly withdrawn but the outcome that replaces it is a lie about what happened. One run closes F-327 (an inconclusive seal reason + a machine-readable inconclusive marker on the terminal record) and F-330 (the second call site) together. **Rung 5 stays operator-blocked** — `brownfield-001`'s zod v3→v4 gold patch (3–6 h research) and a both-arm re-run so stored results carry `repoRef` (`grep -rl repoRef benchmarks/results/ | wc -l` = 0); RE-MEASURE both before the next review defers to them again.

### 2026-08-14 — displaced from plan.md status block (dogfood-138 review (F-331 hand-fix))

- **Latest / next:** 🟢 **WP-615 (an unfinished check must say it did not finish) DELIVERED** (`run-3ca8f79a-ab47-44bf-a90b-14ff26b09215`, SUCCESS **1 step, $0.0814/$20.00**, 6m 36s, 2/2 criteria, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-138.md`). A suite killed at its per-check cap now seals `SUCCESS · "completion review: check did not complete — pre_existing_suite_still_green"` on BOTH completion-review paths (`src/workflow/agent-loop.ts:1123-1131,1151-1159`), and the killed check's id rides as an additive `inconclusiveCheck` field on the terminal payload (`src/runner/activities.ts:2850-2851,2882`), on `RunStatusReport` live and offline (`src/types.ts:679-680`, `src/journal/journal.ts:311-321`), through `RunStatusReportSchema` (`src/schemas.ts:578`) and into `chikory trace` (`src/cli/trace.ts:437-438,809`). `TerminalStatus` stayed the frozen `SUCCESS | FAILED`. **AC-2 drove four REAL Temporal runs** — a cap kill, a genuine red, a green suite and an ordinary design finding — with trap B deleting the `reason` key outright. Independently re-verified this review: **1353 passed | 23 skipped (1376 tests), 175 files**, lint and typecheck exit 0.
- **🔴 The catch is bigger than the fix: F-331 — this run's OWN declared regression suite never ran.** The spec declared `regression_suite:` for the third live WP-609 proof; the journal holds ONE judge pass (`completionReview` undefined, no `pre_existing_suite_still_green` row) and a terminal sealed `"converged out-of-rubric escalation … (F-229/F-271)"`. The suite executes only inside the completion review (`src/workflow/agent-loop.ts:1058,1072,1074`), and the ESCALATE branch (`:1207`) seals SUCCESS before it — unattended at `:1249`, operator-approved at `:1283`. **Any advisory free-text judge remark on a converged step skips a deterministic gate, silently.** WP-542/F-207 ("no LLM-verdict gate may exit") at a new altitude — and self-defeating here: the judge escalated for want of independent suite evidence, which is exactly what the skipped gate produces. → **WP-617**.
- **Review notes: 🟠 F-332 → WP-618 · ℹ️ F-306 · ℹ️ F-333.** F-332 — `readNodeResult` (`src/chain/activities.ts:236-259`) copies `handoff`/`reason`/`resumable` off the terminal payload but not `inconclusiveCheck`, so a chain node whose gate never finished folds in as a plain SUCCESS; F-283's unfed-pipeline shape, one consumer later. ℹ️ F-306 recurred a 4th time (148 chars of stale in-progress narration opening the summary — and that line IS the trace index row); WP-606 unchanged. ℹ️ F-333 — `check_timeout_ms:` declared live for the first time but no check came near either cap (judge pass 1m 47s), so it is proven wired, not load-bearing; no WP.
- **Progression ✅ PROGRESSING**; ledger `rung=0` (off-ladder, **NINTH** consecutive). **NEXT = dogfood-139: WP-617 (a declared gate runs before any SUCCESS seal) + WP-618 (a node's inconclusive outcome survives the fan-in).** The honest-outcome work of WP-615 is worth little while the gate itself is skippable and its absence invisible one layer up. **Rung 5 stays operator-blocked, RE-MEASURED 2026-08-13:** `find benchmarks/tasks -name '*brownfield-001*'` → one file, `status: pinned` (`benchmarks/tasks/brownfield-001-dependency-major-upgrade.yaml:24`), no `fix_ref`; `grep -rl repoRef benchmarks/results/ | wc -l` = **0**. Re-measure both again before the next review defers to them.

### 2026-08-14 — displaced from docs/DOGFOODING.md status block (dogfood-138 review (F-331 hand-fix))

🟢 **dogfood-138 / WP-615 (an unfinished check must say it did not finish) DELIVERED** — `run-3ca8f79a-ab47-44bf-a90b-14ff26b09215`, SUCCESS 1 step, $0.0814/$20.00, 6m 36s, 2/2 criteria, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-138.md`.

**A test command killed by the clock now tells the truth.** The seal reads `completion review: check did not complete — pre_existing_suite_still_green` instead of the design-findings sentence, and the terminal record plus `RunStatusReport` carry an additive `inconclusiveCheck` field, so a script reads it without parsing English. `TerminalStatus` is still exactly `SUCCESS | FAILED`. A red suite, a green suite and an ordinary design finding all keep exactly their old outcome — pinned by four real Temporal runs.

**🔴 READ THIS BEFORE THE NEXT LAUNCH — a declared `regression_suite:` is not guaranteed to run (F-331 → WP-617, §7).** dogfood-138 declared one; it never executed. The suite runs only inside the completion review (`src/workflow/agent-loop.ts:1058-1074`), and a converged out-of-rubric ESCALATE seals SUCCESS before that — unattended at `:1249`, operator-approved at `:1283`. Any advisory free-text judge remark on a converged step therefore skips the gate silently, with no marker. Until WP-617 lands, **verify the suite ran**: `chikory trace <run-id>` must show a SECOND judge pass carrying a settled `pre_existing_suite_still_green` row. One judge pass + an `⚠ ESCALATE` seal means the gate was skipped.

**Still standing from dogfood-137.** ① A red suite's RAW output (≤64 KB) rides into the verdict rationale and prints in full in `chikory trace` (🟠 F-328 → WP-616, §8). ② 🔴 F-326 (brief overflow to 46,095 chars in a 4-char window) was hand-fixed in the dogfood-137 review; a tree without `src/workflow/completion-review.ts:168-189` still has it. ③ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322) — 136/137/138 all carried it and all reached terminal unattended. ④ Sweep the LENGTH, not just the count (🟡 F-329).

### 2026-08-14 — displaced from plan.md status block (dogfood-139 review)

- **Latest / next:** 🟢 **WP-615 (an unfinished check must say it did not finish) DELIVERED** (`run-3ca8f79a-ab47-44bf-a90b-14ff26b09215`, SUCCESS **1 step, $0.0814/$20.00**, 6m 36s, 2/2 criteria, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-138.md`). A suite killed at its per-check cap now seals `SUCCESS · "completion review: check did not complete — pre_existing_suite_still_green"` on BOTH completion-review paths, with the killed check's id riding as an additive `inconclusiveCheck` field on the terminal payload (`src/runner/activities.ts:2850-2851,2882`), on `RunStatusReport` live and offline (`src/types.ts:679-680`, `src/journal/journal.ts:311-321`), through `RunStatusReportSchema` (`src/schemas.ts:578`) and into `chikory trace` (`src/cli/trace.ts:437-438,809`). `TerminalStatus` stayed the frozen `SUCCESS | FAILED`. **AC-2 drove four REAL Temporal runs** — a cap kill, a genuine red, a green suite and an ordinary design finding — with trap B deleting the `reason` key outright. Independently re-verified: **1353 passed | 23 skipped (1376 tests), 175 files**, lint and typecheck exit 0.
- **🔴 F-331 — the run's OWN declared suite never ran — FOUND AND HAND-FIXED THIS REVIEW (WP-617 ✅).** The journal held ONE judge pass (`completionReview` undefined, no `pre_existing_suite_still_green` row) and a terminal sealed `"converged out-of-rubric escalation … (F-229/F-271)"`: the suite executes only inside the completion review, and the ESCALATE branch sealed SUCCESS before it — unattended and operator-approved alike. Any advisory free-text judge remark on a converged step skipped a deterministic gate, silently; the remark here was *"the judge evidence does not independently show … the full 1,376-test suite"*, exactly what the skipped gate produces. Fixed by `sealFromRubricFails` (`src/workflow/agent-loop.ts:352`, now the ONE outcome ladder, called from `:1197`/`:1207`) + `regressionGateBeforeSuccess` (`:397`, called from `:1301`/`:1339`), terminal-or-nothing so the F-229/F-271 loop cannot reopen. **Verified RED-then-GREEN against the pre-fix tree** — 3 new live scenarios; sdk suite 1376 → **1379** green.
- **Review notes: 🟠 F-332 → WP-618 · ℹ️ F-306 · ℹ️ F-333.** F-332 — `readNodeResult` (`src/chain/activities.ts:236-259`) copies `handoff`/`reason`/`resumable` off the terminal payload but not `inconclusiveCheck`, so a chain node whose gate never finished folds in as a plain SUCCESS; F-283's unfed-pipeline shape, one consumer later. ℹ️ F-306 recurred a 4th time (148 chars of stale in-progress narration opening the summary — and that line IS the trace index row); WP-606 unchanged. ℹ️ F-333 — `check_timeout_ms:` declared live for the first time but no check came near either cap (judge pass 1m 47s), so it is proven wired, not load-bearing; no WP.
- **Progression ⛔ STALLED** (trailing-3 max steps 1 vs 4) — binding, and it changed this review twice: F-331 was hand-fixed rather than headlined, and **NEXT = dogfood-139: the P3 ladder rung, WP-303 (leaderboard site + methodology)** — the first rung-5 slice attempted in nine runs. **The rung was only half-blocked and nobody had checked.** Both rung-5 blockers RE-MEASURED 2026-08-13 and both still stand — `benchmarks/tasks/brownfield-001-dependency-major-upgrade.yaml:24` is `status: pinned` with no `fix_ref`; `grep -rl repoRef benchmarks/results/ | wc -l` = **0** — but **both gate WP-304 only**. WP-303's residue (*"the static site + methodology prose"*) reads a leaderboard JSON already on disk: no gold patch, no `repoRef`, no quota, no new benchmark run. Eight reviews inherited "rung 5 is blocked" without measuring its scope — F-203 applied to our own reasoning, not to the code.

### 2026-08-14 — displaced from docs/DOGFOODING.md status block (dogfood-139 review)

🟢 **dogfood-138 / WP-615 (an unfinished check must say it did not finish) DELIVERED** — `run-3ca8f79a-ab47-44bf-a90b-14ff26b09215`, SUCCESS 1 step, $0.0814/$20.00, 6m 36s, 2/2 criteria, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-138.md`.

**A test command killed by the clock now tells the truth.** The seal reads `completion review: check did not complete — pre_existing_suite_still_green` instead of the design-findings sentence, and the terminal record plus `RunStatusReport` carry an additive `inconclusiveCheck` field, so a script reads it without parsing English. `TerminalStatus` is still exactly `SUCCESS | FAILED`. A red suite, a green suite and an ordinary design finding all keep exactly their old outcome — pinned by four real Temporal runs.

**🔴 F-331 FOUND AND HAND-FIXED THIS REVIEW (WP-617) — the run's own declared suite had never run.** A converged out-of-rubric ESCALATE sealed SUCCESS upstream of the completion review, which is the only place a `regression_suite:` executes. Both escalate seals now call `regressionGateBeforeSuccess` (`src/workflow/agent-loop.ts:397`, wired at `:1301`/`:1339`) and route the result through the shared ladder `sealFromRubricFails` (`:352`): a red suite seals FAILED even on a converged escalation, a green one leaves the F-229/F-271 wording untouched, a suite-less run is unaffected. Verified RED-then-GREEN against the pre-fix tree; sdk suite 1376 → **1379** green. §7 carries the full note. **Any run sealed before 2026-08-13 with one judge pass and an `⚠ ESCALATE` seal had its gate skipped — re-run that suite by hand before trusting the green.**

**Still standing.** ① A red suite's RAW output (≤64 KB) rides into the verdict rationale and prints in full in `chikory trace` (🟠 F-328 → WP-616, §8). ② 🟠 F-332 → WP-618 — a chain drops `inconclusiveCheck`, so it cannot tell a node's gate never finished. ③ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322). ④ Sweep the LENGTH, not just the count (🟡 F-329). ⑤ Re-measure a blocker's **scope**, not just whether it still stands: rung 5 sat "blocked" for eight reviews because nobody checked that both blockers gated only WP-304 (F-203, applied to our own reasoning).

### 2026-08-15 — displaced from plan.md status block (dogfood-140 review)

- **Latest / next:** 🟢 **WP-303 (leaderboard site + methodology) — the WP-303 HALF of the P3 exit gate DELIVERED** (`run-ab72b901-4f3f-4b98-b6e1-ca00324ff0a6`, SUCCESS **1 step, $0.1048/$20.00**, 4m 40s, 2/2 criteria, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-139.md`). `chikory-bench leaderboard` now writes a self-contained `index.html` beside the JSON and Markdown it already wrote, and `benchmarks/publications/leaderboard/index.html` is published at a durable committed path: both arms shown as 95% intervals, *"Overlap at 95% confidence; the arms are not separated"*, methodology (corpus size, requirement count, adapters, I-SR/D-SR, Wilson) read from the bundle, zero network assets, every local link resolving. All 8 designed traps rejected. **Ladder unchanged at `rung=4`** — a rung is satisfied only when its proof is whole (the dogfood-124 precedent), and rung 5's other half (WP-304: the OpenHands arm + a corpus wide enough to separate 19 requirements) has still never run.
- **🔴 F-334 — a converged-escalate seal DISCARDS a non-deterministic completion-review finding — FOUND AND HAND-FIXED THIS REVIEW.** `regressionGateBeforeSuccess` (`src/workflow/agent-loop.ts:397`) delegated to `sealFromRubricFails` (`:352`), which condemns only `DETERMINISTIC_RUBRIC_IDS` (`src/judge/rubric.ts:28`) and returns `undefined` otherwise. On the PROCEED path `undefined` is correct (the caller hands over a design-fix brief); on the converged-escalate path it means "the caller's SUCCESS seal stands" (`:1328`), so `design_serves_overall_goal: ✗` sealed 🟢 SUCCESS with the finding dropped in silence — F-180/WP-537 at the one altitude WP-537 never reached. Fixed at `:424` (condemn FAILED + resumable, finding named), terminal-or-nothing so the dogfood-121 loop cannot reopen. **RED-then-GREEN**: Scenario 8 (`test/runner/regression-suite-repair-live.test.ts:423`); sdk suite 1379 → **1380** green, F-331 Scenarios 5/6/7 unregressed.
- **🔴 F-335 → WP-619 — the judge PROVED two defects and the run shipped them.** Judge pass #1 ESCALATEd with 3 out-of-rubric concerns; judge pass #2 ran a smaller rubric over the **byte-identical diff** (`3bb7b4d56554`, 27,395 bytes, cited by both), never re-tested them, and sealed SUCCESS. The F-229/F-271 seal assumes a converged concern is about evidence the diff cannot carry — false here: two concerns named real, reproducible defects (F-336, F-337) the executor could have fixed in one step. **NEXT HEADLINE.**
- **Review notes: 🟠 F-336 · 🟠 F-337 · 🟠 F-338 all HAND-FIXED · 🟡 F-339 → WP-620.** F-336 — the page fabricated *"5 repository tasks / 19 total requirements"* (today's real corpus, hardcoded) on empty data: trap D at the counts altitude, which AC-1 guarded only at the interval altitude. F-337 — bundle-derived strings were interpolated raw, so a crafted label injected a live `<script src="https://…">` into a page whose promise is that it needs no network (trap E). F-338 — `linkOrText` resolved the repo-root-anchored `entry.bundle` against `process.cwd()`, so generating from `benchmarks/harness` (what AC-2 itself does) degraded all 4 evidence links to bare `<span>` with trap G blind either way. All three fixed in `benchmarks/harness/src/leaderboard.ts` (`:157` escaping, `:177` repo-root anchor, `:294` honest empty-corpus sentence); bench suite 210 → **213** green. F-339 — the stale-spec guard refused this very spec at $0 (`wp-status.ts:63` reads a bare `LANDED` as done; the "Still open" qualifier sits past the 300-char window at `:60`); measured against the real plan.md it is wrong on **7 rows in both directions** (WP-540 reads open though done), breaking its documented fail-open contract.
- **Progression ⛔ STALLED** (trailing-3 max steps 1 vs 4; ladder rung flat at 4) — binding. The rung-5 half that remains is **WP-304**, which is not dogfoodable: it needs the OpenHands arm plus a corpus wide enough to separate 19 requirements — a quota-bound multi-hour suite the operator runs BY HAND (dogfood-122's lesson). **NEXT = dogfood-140: WP-619 (F-335)** — the 🔴 that let this run ship two named defects green. Its gates and arming are NOT yet recorded; phase 5 is owed before launch.

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-140 review)

🟢 **dogfood-139 / WP-303 (leaderboard site + methodology) DELIVERED — WP-303 IS COMPLETE** — `run-ab72b901-4f3f-4b98-b6e1-ca00324ff0a6`, SUCCESS 1 step, $0.1048/$20.00, 4m 40s, 2/2 criteria, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-139.md`.

**The benchmark result is now a page a skeptic can open.** `chikory-bench leaderboard` writes a self-contained `index.html` beside the JSON and Markdown, published at `benchmarks/publications/leaderboard/index.html`: both arms as 95% intervals, *"Overlap at 95% confidence; the arms are not separated"*, methodology read from the bundle, zero network assets, regenerable from the data it publishes. All 8 designed traps rejected. **The ladder does NOT advance (`rung=4`)** — rung 5 also needs WP-304's arm, and a rung is satisfied only when its proof is whole.

**🔴 The judge PROVED two defects and the run shipped them anyway (F-335 → WP-619, the next headline).** Judge pass #1 ESCALATEd with 3 concerns; pass #2 ran a smaller rubric over the **byte-identical diff**, never re-tested them, and sealed 🟢 SUCCESS. Two named real defects the ACs did not test for. **🔴 F-334 hand-fixed (WP-621):** `regressionGateBeforeSuccess` returned `undefined` for any non-deterministic finding, and on the converged-escalate path `undefined` means "SUCCESS stands" — so `design_serves_overall_goal: ✗` sealed green. Now condemns FAILED + resumable, still terminal-or-nothing; sdk 1379 → **1380**. **F-331 is LIVE-PROVEN this run** — judge pass #2 carried `✓ pre_existing_suite_still_green`.

**Still standing.** ① Three page defects the ACs missed were hand-fixed (🟠 F-336 fabricated corpus counts on empty data · 🟠 F-337 bundle data could inject a remote `<script>` · 🟠 F-338 evidence links depended on the CLI's cwd) — bench suite 210 → **213**; §8 carries the note. ② 🟡 F-339 → WP-620 — the stale-spec guard refused this very spec at $0 and is wrong on **7 plan rows in both directions**; launch with `CHIKORY_ALLOW_STALE_SPEC=1` when you have re-measured the WP by hand. ③ 🟠 F-328 → WP-616 · 🟠 F-332 → WP-618 still open. ④ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322).

### 2026-08-15 — displaced from plan.md status block (dogfood-141 review)

- **Latest / next:** 🟢 **WP-619 (adjudicate escalation concerns before the converged seal) — DELIVERED** (`run-cd11c095-754c-42b5-b57c-d712710d9612`, SUCCESS **1 step, $0.1326/$20.00**, 9m 40s, 2/2 criteria, harvest byte-IDENTICAL 11/11, `docs/reports/dogfood-140.md`). A judge concern raised outside the rubric is no longer just recorded: a converged out-of-rubric escalation buys exactly ONE completion-review pass that is shown the concern verbatim and answers it through a named rubric row (`RUBRIC_ESCALATION_CONCERNS_ADJUDICATED`), and the outcome follows the ANSWER — cleared → SUCCESS with the F-229/F-271 wording, upheld → resumable FAILED naming the item — with or without a declared `regression_suite`, still terminal-or-nothing. All 8 traps rejected; nothing reads the concern string (AC-1 flips only the judge's answer over byte-identical text). **This closes the fifth altitude of judge-detects-but-does-not-gate**; WP-601 (F-295, a concern outliving its diff window) is the family's last open member and the **NEXT HEADLINE** (`dogfood-141`). **Ladder unchanged at `rung=4`** — an off-ladder run; P3's exit gate needs WP-304's OpenHands arm, which the operator runs by hand and no spec can headline. **Review hand-fixes:** 🔴 **F-340** — the new adjudication row was asked on **every** completion review, including the concern-LESS one any `regression_suite` run buys, and a ✗ on that subject-less question condemns a correct SUCCESS through this delivery's own catch-all; proven from the raw wire request, fixed at `activities.ts:1699` + `prompt.ts:189`, Scenario 9 RED→GREEN, sdk 1454 → **1455**. 🟢 **F-341** — unreachable second rendering path removed. 🟡 **F-342 → WP-622** — this spec's *"timed at HEAD"* suite premise was 95 tests stale because PRs #19–#30 landed 10 test files under it; measure the declared suite in `dogfood-arm.sh` instead of transcribing it.

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-141 review)

🟢 **dogfood-140 / WP-619 (adjudicate escalation concerns before the converged seal) DELIVERED — WP-619 IS COMPLETE** — `run-cd11c095-754c-42b5-b57c-d712710d9612`, SUCCESS 1 step, $0.1326/$20.00, 9m 40s, 2/2 criteria, harvest byte-IDENTICAL 11/11, `docs/reports/dogfood-140.md`.

**A judge concern raised outside the checklist now has to be answered before a run may finish green.** A converged out-of-rubric escalation buys exactly ONE completion-review pass, shown the concern text verbatim, answering through a named rubric row; cleared → SUCCESS with the F-229/F-271 wording, upheld → resumable FAILED naming the item — with or without a declared `regression_suite`, still terminal-or-nothing. Nothing reads the concern string. All 8 designed traps rejected. **This closes the fifth altitude of judge-detects-but-does-not-gate**; WP-601 (F-295) is the last open member and the next headline. **Ladder unchanged (`rung=4`)** — off-ladder; the P3 exit gate needs WP-304's arm, which the operator runs by hand.

**🔴 F-340 hand-fixed — every new gate is a new way to fail a correct run.** The adjudication row (and a scope sentence claiming the pass adjudicates concerns) was rendered on **every** completion review, including the concern-LESS one that any `regression_suite` run buys; a ✗ on that subject-less question condemns a correct SUCCESS through this delivery's own catch-all. Proven from the RAW wire request, fixed at `src/runner/activities.ts:1699` + `src/judge/prompt.ts:189`; Scenario 9 RED→GREEN, sdk 1454 → **1455**. **Neither AC could see it — both directions were driven by a SCRIPTED judge.** 🟢 F-341 hand-fixed (unreachable second rendering path).

**Still standing.** ① 🟡 **F-342 → WP-622** — this spec's *"timed at HEAD"* suite premise was **95 tests stale** (claimed 175 files/1380, HEAD was 185/1475) because PRs #19–#30 landed 10 test files between the review and the launch; **re-measure a declared `regression_suite` before writing it into a spec**, never transcribe it. ② **F-343 / F-197** — the run reproduced the defect it fixes (its own concern sealed unadjudicated); the next run is the proof, signature = a `completion-review` verdict carrying an `escalation_concerns_adjudicated` row. ③ 🟡 F-339 → WP-620 — launch with `CHIKORY_ALLOW_STALE_SPEC=1` when you have re-measured the WP by hand. ④ 🟠 F-306 → WP-606 recurred (7 lines of CLI narration opened the step summary) · 🟠 F-328 → WP-616 · 🟠 F-332 → WP-618 still open. ⑤ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322).

### 2026-08-15 — displaced from plan.md status block (dogfood-142 review)

- **Latest / next:** 🟢 **WP-601 (a judge finding outlives its diff window) — DELIVERED, run condemned by its own gate, delivery landed after independent verification** (`run-5ab10621-fd38-420d-aff2-176486cf9f9a`, terminal FAILED-resumable, **1 step, $0.1036/$20.00**, 13m 12s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-141.md`). Every pass's failed rubric items + free-text concerns now become `standingFindings` that survive clean windows and are adjudicated ONCE at the completion review — cleared → SUCCESS, upheld → resumable FAILED naming the item. **This closes the judge-detects-but-does-not-gate family (all six altitudes).** The run itself is WP-619's live proof AND its false-positive victim: the judge's only concern ("executor never showed its verification commands") is un-clearable by any diff, the adjudicator honestly upheld it, and a correct all-green delivery sealed FAILED. Review hand-fixes: 🔴 **F-344** (adjudication standard now scopes upholds to defects in the DELIVERED work; trusted checks settle process-evidence concerns — `src/judge/prompt.ts:202` + `src/judge/rubric.ts:110`, +2 tests) · 🟠 **F-345** (the VERIFY-YOUR-OWN-WORK spec boilerplate demanded untrusted evidence AND ran the full suite inside the 600 s step cap → step killed at 601.4 s; retired — DOGFOODING §3). **NEXT HEADLINE = WP-608 (`dogfood-142`): a step that ends in a question is not a step** — measured 56.8% of run-83bf691d's spend judging 0-byte "may I proceed?" steps, and the executor-passivity family also cost this run its step (waiting on a suite it was never asked to run). Ladder unchanged at `rung=4` — rung-5's corpus half stays operator-by-hand (WP-304 arm + discriminating tasks).

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-142 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟢 **dogfood-141 / WP-601 (a judge finding outlives its diff window) DELIVERED — the judge-detects-but-does-not-gate family is CLOSED (all six altitudes)** — `run-5ab10621-fd38-420d-aff2-176486cf9f9a`, terminal FAILED-resumable, 1 step, $0.1036/$20.00, 13m 12s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6, delivery landed after independent verification (`docs/reports/dogfood-141.md`).
**The run was condemned by its own new gate, and the gate's question was wrong, not the delivery:** the only concern ("executor never showed its verification commands") is un-clearable by any diff, so the adjudicator honestly upheld it. The F-197 signature predicted last review appeared exactly as written — a `completion-review` verdict carrying `escalation_concerns_adjudicated` — proving WP-619 live in the condemning direction.
**Hand-fixed this sitting:** 🔴 **F-344** — adjudication standard scopes upholds to defects in the DELIVERED work; process-evidence concerns are settled by this pass's trusted checks (`src/judge/prompt.ts:202`, `src/judge/rubric.ts:110`, +2 tests). 🟠 **F-345** — the VERIFY-YOUR-OWN-WORK goal boilerplate retired (§3.2): it demanded evidence the judge refuses to trust and ran the full suite into the 600 s step cap (killed at 601.4 s).
**Still standing.** ① 🟡 F-342 → WP-622 — measure a declared `regression_suite` at launch HEAD, never transcribe counts. ② 🟡 F-339 → WP-620 — launch with `CHIKORY_ALLOW_STALE_SPEC=1` when you have re-measured the WP by hand. ③ 🟠 F-306 → WP-606 recurred (a cap-killed step's summary is 12 lines of waiting narration) · 🟠 F-328 → WP-616 · 🟠 F-332 → WP-618 still open. ④ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322). **NEXT = dogfood-142 / WP-608** (a 0-byte "may I proceed?" step is a named outcome, never a silent PROCEED, and never spends the repair attempt).

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-143 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟢 **dogfood-142 / WP-608 (a step that ends in a question is not a step) DELIVERED IN ONE STEP** — `run-c857c058-8358-4fc3-a2bc-3fc94a455636`, terminal FAILED, 4 steps, $0.1846/$20.00, 9m 19s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6, delivery landed after independent verification (`docs/reports/dogfood-142.md`). An empty-diff step that asks permission is classified before the judge, buys zero judge passes, is re-driven with the standing answer, and never spends the ONE granted repair step. All seven traps rejected.
**The run failed on its GRADER, not its work — 🔴 F-349, and it threatens every spec.** Acceptance checks run CONCURRENTLY in one shared workspace (`src/judge/evidence.ts:244`): AC-1's transient generated test file broke AC-2's whole-package typecheck (`error TS6133` → `exited 2` × 4 → goal-drift HALT). Both ACs pass sequentially — the only way `dogfood-arm.sh` has ever rehearsed them (🟠 F-350). **Until WP-623 lands: never let one AC write into a tree another AC compiles or lints. Give generated files a path no whole-tree tool reads, or fold the checks into one AC.**
**Hand-fixed this sitting:** 🔴 **F-347** — `dogfood-close.sh` gate 4 ran ONE script test while claiming "full suite green"; it now runs `scripts/test-scripts.sh` like `devbox run test` does. 🔴 **F-348** — F-346's anchor change left its own fixture stale, so HEAD's suite was red and `dogfood-open.sh` refused to harvest (+1 assertion pinning the stacking regression). 🔴 **F-352** (`executors→workflow` import) · 🟠 **F-353** (any `?` counted as an ask) · 🟡 **F-354** (a question journaled as `resume`).
**Still standing.** ① 🟠 F-355 — the *stall* half of executor passivity ("I have launched it and am waiting", 0 tool calls, 0 bytes) took 3 of this run's 4 steps and no classifier sees it. ② 🟡 F-342 → WP-622 · 🟡 F-339 → WP-620 · 🟠 F-306 → WP-606 · 🟠 F-328 → WP-616 · 🟠 F-332 → WP-618. ③ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322). **NEXT = dogfood-143 / WP-623** (a judge-executed check is isolated from its siblings).

### 2026-08-15 — displaced from plan.md status block (dogfood-143 review)

- **Latest / next:** 🟢 **WP-608 (a step that ends in a question is not a step) — DELIVERED IN ONE STEP, run condemned by a broken sibling check, delivery landed after independent verification** (`run-c857c058-8358-4fc3-a2bc-3fc94a455636`, terminal FAILED, **4 steps, $0.1846/$20.00**, 9m 19s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-142.md`). An empty-diff step whose summary asks permission is now classified BEFORE the judge, journals a `question_step` control_event, buys zero judge passes, is re-driven carrying the standing answer verbatim, and never spends the ONE granted repair step. All seven designed traps rejected, including the hard one (D — the granted repair survives an ask). **The run did not fail on its work: it failed on F-349.** Acceptance checks run CONCURRENTLY in one shared workspace (`src/judge/evidence.ts:244`), so AC-1's transient generated test file broke AC-2's whole-package typecheck (`error TS6133` → `exited 2` × 4 → goal-drift HALT) — both ACs pass sequentially, which is the only way arming has ever rehearsed them. Review hand-fixes: 🔴 **F-347** (`dogfood-close.sh` gate 4 ran ONE script test and committed "full suite green" — dogfood-141 landed `aafb762` over a red suite; now runs `scripts/test-scripts.sh`) · 🔴 **F-348** (F-346's anchor change left its own fixture stale; HEAD was red and blocked harvest — fixed, +1 stacking assertion) · 🔴 **F-352** (delivery imported `executors→workflow`; constant moved to `src/util/standing-answer.ts:12`) · 🟠 **F-353** (any `?` made a summary an ask) · 🟡 **F-354** (a question journaled as `event: "resume"`). **NEXT HEADLINE = WP-623 (`dogfood-143`): a judge-executed check must be isolated from its siblings** — two consecutive runs have now been condemned by their own gate on correct deliveries (F-344, F-349), and until this is fixed every dogfood spec carries a live coin flip. Ladder unchanged at `rung=4` — rung-5's corpus half stays operator-by-hand (WP-304 arm + discriminating tasks). Residue: 🟠 **F-355** (the *stall* half of executor passivity — "I have launched it and am waiting", 0 tool calls, 0 bytes, 3 of this run's 4 steps) is track-B on WP-608.

### 2026-08-15 — displaced from plan.md status block (dogfood-144 review)

- **Latest / next:** 🟢 **WP-623 (a judge-executed acceptance check must be isolated from its siblings) — DELIVERED IN ONE STEP** (`run-2a418823-1489-451f-a292-09ed8fcaa710`, terminal SUCCESS, **1 step, $0.0851/$20.00**, 5m 19s, AC 2/2 re-run green, harvest byte-IDENTICAL 1/1, `docs/reports/dogfood-143.md`). Both concurrent call sites are gone: `runCriteriaChecks` (`src/judge/evidence.ts:255`) and `collectEvidence` (`:345`) drive their checks through a `for…of`, awaiting a shared `cleanup()` closure after **every** check and again in `finally`. `runCheck` is untouched, so the per-check `checkTimeoutMs` kill, `infraFailed`, and exit-code passthrough all survive — isolation did not become leniency. All five designed traps rejected (tsconfig/exit-code appeasement · one call site · ordering instead of isolating · unbounding the cap · isolate-then-leak). **🔴 F-349 is closed** — the standing "never let one AC write into a tree another AC compiles" workaround is retired. **The delivery shipped no test (🔴 F-356):** the declared suite stood at 187 files / 1495 tests before the run and after it, so a refactor back to `Promise.all` would have landed green; the only proof lived in AC-1's generated file, which the check deletes. **Hand-fixed this review:** `packages/sdk-ts/test/judge/check-isolation.test.ts` — 4 tests, proven BOTH ways (GREEN on the delivery, RED 2-of-4 on pre-fix HEAD), suite now **188 files / 1499 tests (1476 passed | 23 skipped), 57.19 s**. New friction queued: 🟠 **F-357 → WP-625** (isolation only covers what `git status --porcelain` sees — a gitignored write still leaks to a sibling and survives cleanup; measured by probe) · 🟠 **F-358 → WP-626** (`toolCalls` is hardcoded 0 for the Antigravity executor, so `isBlindMeteredStep`/`estimateKilledStepUsage` can never fire in a real run, and F-355's `toolCalls: 0` evidence is inert). **WP-624 re-scoped, not headlined** — WP-623 made the judge serial, which is how `dogfood-arm.sh` already rehearses, so only F-351 survives. **NEXT HEADLINE = WP-625 (`dogfood-144`): check isolation must cover writes git does not track.** Ladder unchanged at `rung=4` — rung-5's corpus half (WP-304 arm + discrimination suite) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-144 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟢 **dogfood-143 / WP-623 (a judge-executed check is isolated from its siblings) DELIVERED IN ONE STEP** — `run-2a418823-1489-451f-a292-09ed8fcaa710`, SUCCESS, 1 step, $0.0851/$20.00, 5m 19s, AC 2/2 re-run green, harvest byte-IDENTICAL 1/1 (`docs/reports/dogfood-143.md`). Both call sites (`src/judge/evidence.ts:255`, `:345`) now run checks **serially with cleanup after each one**; `runCheck`'s per-check cap and exit-code passthrough are untouched. All five traps rejected. **🔴 F-349 is CLOSED — the "give generated files a path no whole-tree tool reads" workaround is retired.**
**But the fix shipped no test (🔴 F-356)** — the suite stood at 1495 tests before the run and 1495 after; the only proof lived in AC-1's generated file, which the check deletes. **HAND-FIXED:** `packages/sdk-ts/test/judge/check-isolation.test.ts` (4 tests, GREEN on the delivery / RED 2-of-4 on pre-fix HEAD; suite now 188 files / 1499 tests, 57.19 s). **Standing rule: when a WP changes durable behaviour, the spec must require a durable test — a generate-run-delete AC proves it at grade time and leaves no residue.**
**Still standing.** ① 🟠 **F-357** → WP-625 — isolation only covers what `git status --porcelain` sees; a check writing a **gitignored** path still leaks to its sibling and survives cleanup (measured: observer `exitCode=1`, artifact left on disk). Until WP-625 lands, do not let one AC write build/cache output another AC reads. ② 🟠 **F-358** → WP-626 — `toolCalls` is hardcoded 0 for the Antigravity executor (`src/executors/gemini-cli.ts:79`), so the blind-metered-step estimator (`src/runner/killed-step-usage.ts:45`) can never fire in a real run; never write a gate keyed on it. ③ 🟠 F-355 (stall half of executor passivity) · 🟠 F-306 → WP-606 · 🟠 F-328 → WP-616 · 🟠 F-332 → WP-618 · 🟡 F-339 → WP-620 · 🟡 F-342 → WP-622. ④ **WP-624 re-scoped** — WP-623 made the judge serial, so arming already rehearses the way the judge runs; only F-351 (compile an AC's generated TS) survives. ⑤ Keep `unattended: escalation: seal_resumable_failed` in every headline spec (F-322). **NEXT = dogfood-144 / WP-625** (isolation must cover writes git does not track).

### 2026-08-15 — displaced from plan.md status block (dogfood-145 review)

- **Latest / next:** 🟠 **WP-625 (check isolation must cover writes git does not track) — HALF DELIVERED, SEALED FAILED, LANDED ANYWAY** (`run-a8fe46f3-b5e1-4f83-961c-2142220daa12`, terminal **FAILED (resumable)**, **1 step, $0.0831/$20.00**, 5m 5s, AC 2/2 re-run green, harvest byte-IDENTICAL 3/3, `docs/reports/dogfood-144.md`). `snapshotWorkspace` now appends a **name-only** ignored inventory from `git ls-files -z --others --ignored --exclude-standard` (`src/judge/hermeticity.ts:190`), `isCreatedStatus` accepts `!!` (`:39`), and an unrestorable ignored deletion is skipped rather than handed to `git checkout` (`:149`). All five designed traps rejected; WP-623 intact; measured on this repo the new call enumerates **27,252 paths / 2.7 MB in 0.49 s**, so trap A does not bite at real scale. Suite **188 files / 1,504 tests (1,481 passed | 23 skipped), 58.37 s** (+5 vs the 143 review — F-356's floor did move). **The judge was right to refuse.** Ignored entries carry no hash and no content, so isolation covers **create only**: probe-confirmed at this review, an ignored file a check *overwrites* leaks to its sibling AND survives the batch with the wrong bytes, and one a check *deletes* is never restored. Both halves of `design_serves_overall_goal` verified true by hand — **1 genuine judge true-positive**, caught on a delivery that passed both hand-armed ACs. **WP-625 → PARTIAL; residue = 🟠 F-360 → WP-628.** New friction: 🔴 **F-359 → WP-627** — the bounded design-fix retry (`src/workflow/agent-loop.ts:1301`) is guarded by `hasStanding` (`:1284`), but every judge pass pushes its failing rubric rows into `standingFindings` (`:1134`), so at `judge.cadence: 1` on a converging step the guard is always true and the repair path is **unreachable by construction**; this run condemned a precisely-diagnosed ~15-line gap with **1 of 6 steps and 99.6% of budget unspent** and `injections 0`. `MAX_COMPLETION_REVIEWS = 2` (`src/workflow/completion-review.ts:16`) already bounds the oscillation dogfood-121 feared, so one attempt is safe. 🟡 **F-360 → track-B + WP-628's AC** — AC-1 owned its oracle and was armed both ways, and still graded 1 of 3 mutation families; the F-356 durability floor was met by transcribing AC-1 into the repo (`test/judge/check-isolation.test.ts:151`), inheriting its exact blind spot. **NEXT HEADLINE = WP-627 (`dogfood-145`): one bounded repair attempt for a standing design finding, then seal.** Ladder unchanged at `rung=4` — rung-5's corpus half (WP-304 arm + discrimination suite) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-145 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
🟠 **dogfood-144 / WP-625 (check isolation must cover writes git does not track) HALF DELIVERED, SEALED FAILED, LANDED ANYWAY** — `run-a8fe46f3-b5e1-4f83-961c-2142220daa12`, terminal **FAILED (resumable)**, 1 step, $0.0831/$20.00, 5m 5s, AC 2/2 re-run green, harvest byte-IDENTICAL 3/3 (`docs/reports/dogfood-144.md`). `snapshotWorkspace` appends a **name-only** ignored inventory (`src/judge/hermeticity.ts:190`), `isCreatedStatus` accepts `!!` (`:39`), an unrestorable ignored deletion is skipped (`:149`). All five traps rejected; WP-623 intact; the new call measures **27,252 paths / 2.7 MB in 0.49 s** on this repo. Suite **188 files / 1,504 tests (1,481 passed | 23 skipped), 58.37 s**.
**The judge was right to refuse — and this is the campaign's cleanest true-positive.** A structurally different family (gpt-5.6-sol over a gemini executor) read a delivery that passed BOTH hand-armed deterministic ACs and named the missing half by mechanism. Probe-confirmed at this review: isolation covers **create only** — an ignored file a check *overwrites* leaks to its sibling AND survives the batch with the wrong bytes; one it *deletes* is never restored. 🟠 **F-360 → WP-628.**
🔴 **But self-correction never fired — F-359 → WP-627 (next headline).** The bounded design-fix retry (`src/workflow/agent-loop.ts:1301`) is guarded by `hasStanding` (`:1284`), and every judge pass pushes its failing rubric rows into `standingFindings` (`:1134`), so at `judge.cadence: 1` on a converging step the guard is always true and the repair path is **unreachable by construction**. The run condemned a precisely-diagnosed ~15-line gap with **1 of 6 steps, 99.6% of budget unspent, `injections 0`** — the executor was never told. `MAX_COMPLETION_REVIEWS = 2` (`src/workflow/completion-review.ts:16`) already bounds the dogfood-121 oscillation the guard was written against.
**Standing rule (extends F-356):** a durability floor that counts TESTS does not raise COVERAGE — dogfood-144's repo test is a transcription of its own grading AC, so it inherited the same blind spot. Enumerate the goal's input families in the AC, and make the durability AC demand coverage the grading check lacks.

### 2026-08-15 — displaced from plan.md status block (dogfood-146 review)

- **Latest / next:** ✅ **WP-627 (one bounded repair attempt for a design finding raised at the sealing pass) — DELIVERED, sealed FAILED on an unrelated judge defect, landed anyway** (`run-1f2a02e0-4615-47fa-8847-ea37c4164cfb`, terminal **FAILED (resumable)**, **2 steps, $0.1651/$20.00**, 17m 11s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-145.md`). The `if (hasStanding)` early-seal is gone: fresh, standing and sealing-pass findings flow through ONE decision (`src/workflow/agent-loop.ts:1284`), buy **exactly one** bounded repair (`:1286-1303`), and seal **FAILED (resumable)** naming the item when the re-review still fails (`:1307`, `:1318`). Both `seal("SUCCESS", "design findings recorded")` arms are **deleted** — trap A rejected at the source. All five designed traps rejected; the proof reads the journal and asserts the executor's step summary carries the `DESIGN REVIEW BRIEF` (trap D). Eight expectations re-pinned, none deleted or weakened; new `test/runner/sealing-design-repair-live.test.ts` (6 tests) covers `BOUNDED`/`MULTI-STEP`/`CUMULATIVE`, which AC-1 does not — F-360 did not recur. Suite measured by hand: **189 files / 1,510 tests (1,487 passed | 23 skipped), 60.40 s** (+1 file / +6). **F-197 stands** — the worker is built from HEAD at launch, so dogfood-146 is WP-627's first live datum. **1 genuine judge true-positive:** pass #1 caught a RED committed `test/runner/` tree; step 2 repaired it for $0.04. New friction: 🔴 **F-361 → WP-629** — `standingFindings` is **append-only** (`src/workflow/agent-loop.ts:1134-1146`, never pruned), so `tests_pass` ✗ at pass #1 survived `tests_pass` ✓ at pass #2 and condemned the seal via `escalation_concerns_adjudicated`; and the completion review runs with `criteria: []` (`:1269`), so it holds **no check evidence** with which to clear it — F-344's charter (`src/judge/prompt.ts:206-209`) promises green checks clear a concern, but that pass executes none. Red-then-green is the normal shape of a multi-step run: **5 of the last 6 headline runs sealed FAILED**, and this is the third consecutive review where the terminal state and the delivery disagree (F-344 → F-359 → F-361). 🟡 **F-362 → track-B** — `chikory trace` prints the completion review's `0/0 criteria` as the final step's own verdict, disagreeing with the facts blob's `2/2`. **NEXT HEADLINE = WP-629 (`dogfood-146`): a finding a later pass cleared must not condemn the seal.** Ladder unchanged at `rung=4` — rung-5's remaining half (WP-304 arm + discrimination suite) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-15 — displaced from docs/DOGFOODING.md status block (dogfood-146 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
✅ **dogfood-145 / WP-627 (one bounded repair for a design finding raised at the sealing pass) DELIVERED — sealed FAILED on an unrelated judge defect, landed anyway** — `run-1f2a02e0-4615-47fa-8847-ea37c4164cfb`, terminal **FAILED (resumable)**, 2 steps, $0.1651/$20.00, 17m 11s, AC 2/2 re-run green, harvest byte-IDENTICAL 6/6 (`docs/reports/dogfood-145.md`). The `hasStanding` early-seal is gone: fresh, standing and sealing-pass findings flow through ONE decision (`src/workflow/agent-loop.ts:1284`), buy **exactly one** bounded repair (`:1286-1303`), and seal **FAILED (resumable)** naming the item when the re-review still fails (`:1307`, `:1318`). Both `seal("SUCCESS", "design findings recorded")` arms deleted — healing did not arrive at the cost of gating. All five traps rejected; the proof reads the journal and asserts the executor's step summary carries the `DESIGN REVIEW BRIEF`. Suite **189 files / 1,510 tests (1,487 passed | 23 skipped), 60.40 s** (+1 file / +6).
**1 genuine judge true-positive:** pass #1 caught a RED committed `test/runner/` tree (4 tests pinned the old shape); step 2 repaired it for $0.04 / 2m55s. **F-197 stands** — the worker is built from HEAD at launch, so dogfood-146 is WP-627's first live datum.
🔴 **F-361 → WP-629 (next headline): a run whose acceptance check goes RED then GREEN is condemned at the seal.** `standingFindings` is **append-only** (`src/workflow/agent-loop.ts:1134-1146`) — `tests_pass` ✗ at pass #1 survived ✓ at pass #2 and condemned the run via `escalation_concerns_adjudicated`. The completion review runs with `criteria: []` (`:1269`), so it holds **no check evidence** to clear it: F-344's charter (`src/judge/prompt.ts:206-209`) promises green checks CLEAR a concern, but that pass executes none. Red-then-green is the normal shape of a multi-step run.
**Standing caution:** this is the **third consecutive review** where the terminal state and the delivery disagree (F-344 → F-359 → F-361). Until WP-629 lands, treat an `escalation_concerns_adjudicated` seal as **unproven, not disproven** — re-run the ACs on the harvested tree and read judge pass #2 before believing it. 🟡 F-362 (track-B): `chikory trace` prints the completion review's `0/0 criteria` as the final step's own verdict.

### 2026-08-16 — displaced from plan.md status block (dogfood-146 post-hoc audit)

- **Latest / next:** ✅ **WP-629 (a rubric finding a later judge pass RE-SETTLES against the whole delivery must not condemn the seal) — DELIVERED, sealed SUCCESS, landed** (`run-7ad992b2-77a3-4e5e-bcee-53b519324d56`, terminal **SUCCESS**, **1 step, $0.0906/$20.00**, 9m 36s, AC 2/2 re-run green, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-146.md`). `standingFindings: string[]` is now `standingRubricFindings: Map<string, string>` + `standingConcerns: string[]`; a rubric row settled against the whole delivery (`isRubricItemSettledAgainstWholeDelivery`, `src/judge/rubric.ts:129-147` — derived from `spec.acceptanceCriteria[].check` presence, not a hardcoded id guess) clears when a later pass re-derives it green, and both `regressionGateBeforeSuccess` (`src/workflow/agent-loop.ts:410`) and the completion review now read the same `getStandingFindings()`. The one designed trap (`standing-findings-live.test.ts:165`, WP-601's guarantee that a design objection survives clean passes) was rejected — untouched, still green. **F-361 CLOSED** — the seal-fidelity family (F-344 → F-359 → F-361) is now closed at all three altitudes (design finding = one bounded repair, WP-627; settled rubric row = pruned, WP-629). This run's own delivery raised nothing, so the mechanism is proven by 4 new live tests (`test/runner/standing-findings-settled-live.test.ts`), not by this run's own judge (the F-197 pattern). No new friction this review. **NEXT HEADLINE = WP-599 (`dogfood-147`): a judge concern raised ALONGSIDE a failing rubric item must not be silently dropped** (`src/judge/verdict.ts:141` Rule 4 fires only when `rubricFails.length === 0`, F-288, dogfood-129 — "the highest-value seam of this campaign," queued and untouched since). Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch lift, WP-530 §7) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-16 — displaced from docs/DOGFOODING.md status block (dogfood-146 post-hoc audit)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
✅ **dogfood-146 / WP-629 (a rubric finding a later judge pass RE-SETTLES against the whole delivery must not condemn the seal) DELIVERED — sealed SUCCESS, landed** — `run-7ad992b2-77a3-4e5e-bcee-53b519324d56`, terminal **SUCCESS**, 1 step, $0.0906/$20.00, 9m 36s, AC 2/2 re-run green, harvest byte-IDENTICAL 5/5 (`docs/reports/dogfood-146.md`). `standingFindings: string[]` is now `standingRubricFindings: Map<string,string>` + `standingConcerns: string[]`; a rubric row settled against the whole delivery (`isRubricItemSettledAgainstWholeDelivery`, `src/judge/rubric.ts:129-147`) clears when a later pass re-derives it green; both `regressionGateBeforeSuccess` and the completion review read the same `getStandingFindings()`. The one designed trap (`standing-findings-live.test.ts:165`, WP-601) rejected — untouched, still green.
**F-361 CLOSED** — the seal-fidelity family (F-344 → F-359 → F-361) is now closed at all three altitudes. Mechanism proven by 4 new live tests (`test/runner/standing-findings-settled-live.test.ts`), not by this run's own judge (F-197 pattern — nothing was raised to clear). No new friction in the delivery this review.
**Standing caution retired**: prior reviews told the operator to treat an `escalation_concerns_adjudicated` seal as unproven until WP-629 landed — superseded, a red-then-green run should now seal SUCCESS cleanly.
🟡 **F-363**: plan.md's queued WP-599 (F-288) RE-MEASURED STALE this review — its correctness harm is already closed as a side effect of WP-601 + WP-627; do not headline it as literally scoped.
**NEXT HEADLINE = WP-594 (dogfood-147): a run under sustained context-window pressure must fold below the default `keepLastN` verbatim floor**, not just report pressure with no fold available (F-272, dogfood-125). Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch, operator-by-hand) still cannot headline.

### 2026-08-16 — displaced from plan.md status block (dogfood-147 review)

- **Latest / next:** ✅ **WP-629 (a rubric finding a later judge pass re-settles against the whole delivery must not condemn the seal) — DELIVERED, sealed SUCCESS, landed `71f9987`** (`run-7ad992b2-77a3-4e5e-bcee-53b519324d56`, terminal **SUCCESS**, **1 step, $0.0906/$20.00**, 9m 36s, AC 2/2 re-run green, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-146.md`). `standingFindings: string[]` is now `standingRubricFindings: Map<string, string>` + `standingConcerns: string[]` behind one `getStandingFindings()` accessor (`src/workflow/agent-loop.ts:270-274`) that both consumers read — `regressionGateBeforeSuccess` (`:413`) and the completion review (`:1261`); a row settled against the whole delivery (`isRubricItemSettledAgainstWholeDelivery`, `src/judge/rubric.ts:134-147`, keyed off `spec.acceptanceCriteria[].check` presence) clears when a later pass re-derives it green, while model-judged rows and free-text concerns still reach the review. **F-361 (a red-then-green run condemned at the seal) CLOSED** — but by 4 live tests, not by this run: it raised no finding at all, so the first live datum is still owed (F-197 — a run cannot exercise the judge fix it delivers).
- **AUDIT 2026-08-15 (post-hoc):** the dogfood-146 review ran **unrequested** (the operator asked only for the run) on **Sonnet 5**, and harvested, committed and **pushed to `main`**. Re-verified independently: HEAD suite green — 190 files / **1,517 tests (1,494 passed | 23 skipped)**, 62.80 s; the WP-629 + WP-601 tests 25/25 green; harvest byte-IDENTICAL 5/5; both ACs re-run green; `tests_pass` confirmed machine-derived from check exit codes (`src/judge/harness.ts:149-170`), so the WP-629 premise holds. Three corrections applied by the audit: the §6 **WP-629 row was left ⏳ QUEUED** (now ✅ DONE); **DOGFOODING §8 still listed F-361 open** (now CLOSED); and one missed defect — 🟠 **F-364 → WP-630 (queued)**: `standingRubricFindings.set(fail.id, …)` (`:1147`) overwrites a second, DIFFERENT objection on the same rubric id, which the replaced array kept, so an objection nothing settled can now disappear before the review sees it.
- **NEXT HEADLINE = WP-594 (`dogfood-147`): a run under sustained token-window pressure must be able to fold below the default `keepLastN` verbatim floor** (F-272, MEASURED on dogfood-125 — pressure fired on 3 of 4 steps and folded nothing). Armed both ways: AC-1 RED 5 s / GREEN 5 s, AC-2 RED 47 s / GREEN 47 s — worst case 39% of the 120 s judge cap. **WP-599 stays RE-MEASURED STALE (F-363)** — its correctness harm is already closed as a side effect of WP-601 + WP-627; do not headline it as literally scoped. Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch lift, WP-530 §7) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-16 — displaced from docs/DOGFOODING.md status block (dogfood-147 review)

**Status (2026-08-15, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
✅ **dogfood-146 / WP-629 (a rubric finding a later judge pass RE-SETTLES against the whole delivery must not condemn the seal) DELIVERED — sealed SUCCESS, landed `71f9987`** — `run-7ad992b2-77a3-4e5e-bcee-53b519324d56`, terminal **SUCCESS**, 1 step, $0.0906/$20.00, 9m 36s, AC 2/2 re-run green, harvest byte-IDENTICAL 5/5 (`docs/reports/dogfood-146.md`). `standingFindings: string[]` is now `standingRubricFindings: Map<string,string>` + `standingConcerns: string[]`; a rubric row settled against the whole delivery (`isRubricItemSettledAgainstWholeDelivery`, `src/judge/rubric.ts:134-147`) clears when a later pass re-derives it green; both `regressionGateBeforeSuccess` and the completion review read the same `getStandingFindings()`. The one designed trap (`standing-findings-live.test.ts:165`, WP-601) rejected — untouched, still green.
**F-361 CLOSED** — the seal-fidelity family (F-344 → F-359 → F-361) is closed at all three altitudes. Proven by 4 live tests (`test/runner/standing-findings-settled-live.test.ts`), **not by a live run** — dogfood-146 raised nothing to clear (F-197 pattern), so the first live datum is still owed.
**Standing caution retired, with a caveat**: prior reviews told the operator to treat an `escalation_concerns_adjudicated` seal as unproven until WP-629 landed — superseded, a red-then-green run should now seal SUCCESS; until a live run shows it, read the pass rubrics before believing either terminal state.
🟠 **F-364 → WP-630 (new, from the post-hoc audit — NOT found by the review)**: `standingRubricFindings.set(fail.id, …)` (`src/workflow/agent-loop.ts:1147`) overwrites a second, DIFFERENT objection on the same rubric id; the array it replaced kept both, and nothing settles a model-judged row, so that objection disappears before the review sees it.
🟡 **F-363**: plan.md's queued WP-599 (F-288) RE-MEASURED STALE — its correctness harm is already closed as a side effect of WP-601 + WP-627; do not headline it as literally scoped.
⚠ **The dogfood-146 review ran unrequested** (operator asked only for the run) on Sonnet 5 and pushed to `main`. Post-hoc audit 2026-08-15: delivery re-verified green (190 files / 1,517 tests, **1,494 passed | 23 skipped**, 62.80 s; WP-629 + WP-601 tests 25/25), and its `plan.md` §6 row (left ⏳) plus this §8 F-361 entry (left open) were corrected.
**NEXT HEADLINE = WP-594 (dogfood-147): a run under sustained context-window pressure must fold below the default `keepLastN` verbatim floor**, not just report pressure with no fold available (F-272, dogfood-125). Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch, operator-by-hand) still cannot headline.

### 2026-08-16 — displaced from plan.md status block (dogfood-148 review)

- **Latest / next:** ✅ **WP-594 (a run under sustained token-window pressure must be able to fold below the default `keepLastN` verbatim floor) — DELIVERED, sealed SUCCESS, landed** (`run-a26d41eb-0d41-466a-9614-41b6dd8246c0`, terminal **SUCCESS**, **2 steps, $0.1229/$20.00**, 7m 02s, AC 2/2 re-run green, harvest byte-IDENTICAL 3/3, `docs/reports/dogfood-147.md`). `compactContext`'s `underPressure` branch now builds `{ triggerAfterSteps: 1, keepLastN: 1 }` (`activities.ts:2284-2289`) instead of reusing the default `keepLastN` (5) for both fields, so a short run under sustained pressure folds older summaries while keeping the latest step verbatim; the unpressured default, `planCompaction`, and `compactAtFraction` all byte-unchanged, all 5 designed traps rejected. +6 `test/runner/` tests (388 → 394). **First live datum for F-197**: `tests_pass` FAILED at step 1, PASSED at step 2, and WP-629 pruned the stale row before the completion review — proven in production, not only in its 25 unit/live tests. No new friction.
- **NEXT HEADLINE = WP-630 (`dogfood-148`): a second, DIFFERENT objection on the same rubric id must not silently replace the first** (F-364, found in the 2026-08-15 audit of the dogfood-146 review). `standingRubricFindings.set(fail.id, …)` (`agent-loop.ts:1147`) overwrites whatever that id held; the array it replaced kept both. Armed both ways: AC-1 (live Temporal, two distinct `design_serves_overall_goal` failures on one id, reads the completion review prompt for both as separate bullet lines) and AC-2 (durability floor 395, measured baseline 394, plus a grep guard on the settled-clear call and the concerns dedup guard) — see `docs/reports/dogfood-147.md` `## NEXT RUN` for wall-clock. Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001`/WP-304, WP-530 §7) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-16 — displaced from docs/DOGFOODING.md status block (dogfood-148 review)

**Status (2026-08-16, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
✅ **dogfood-147 / WP-594 (a run under sustained token-window pressure must fold below the default `keepLastN` verbatim floor) DELIVERED — sealed SUCCESS, landed** — `run-a26d41eb-0d41-466a-9614-41b6dd8246c0`, terminal **SUCCESS**, 2 steps, $0.1229/$20.00, 7m 02s, AC 2/2 re-run green, harvest byte-IDENTICAL 3/3 (`docs/reports/dogfood-147.md`). `compactContext`'s `underPressure` branch now builds `{ triggerAfterSteps: 1, keepLastN: 1 }` (`activities.ts:2284-2289`) instead of reusing the default `keepLastN` (5) for both fields; unpressured default, `planCompaction`, and `compactAtFraction` byte-unchanged; all 5 designed traps rejected. +6 `test/runner/` tests (388 → 394). No new friction.
**F-197's opening live datum landed**: `tests_pass` FAILED at step 1, PASSED at step 2, and the completion review carried no stale row — WP-629's prune fires correctly in production. F-364/WP-630 below was not exercised (only one objection that run).
🟠 **F-364 → WP-630, ARMED as the dogfood-148 headline**: `standingRubricFindings.set(fail.id, …)` (`agent-loop.ts:1147`) overwrites a second, DIFFERENT objection on the same rubric id; the array it replaced kept both, and nothing settles a model-judged row, so that objection disappears before the review sees it.
🟡 **F-363**: plan.md's queued WP-599 (F-288) RE-MEASURED STALE — its correctness harm is already closed as a side effect of WP-601 + WP-627; do not headline it as literally scoped.
**NEXT HEADLINE = WP-630 (dogfood-148): a second, DIFFERENT objection on the same rubric id must not silently replace the first**, seeded into a live run per its own queued condition (`examples/dogfood/dogfood-148-wp630-standing-finding-overwrite.yaml`). Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch, operator-by-hand) still cannot headline.

### 2026-08-17 — displaced from plan.md status block (dogfood-149 review)

- **Latest / next:** ✅ **WP-630 (a second, DIFFERENT objection on the same rubric id must not silently replace the first) — DELIVERED, sealed SUCCESS, landed** (`run-2213aec1-9683-4c6a-a496-b74f968975c1`, terminal **SUCCESS**, **1 step, $0.0808/$20.00**, 6m 15s, AC 2/2 re-run green, harvest byte-IDENTICAL 2/2, `docs/reports/dogfood-148.md`). `standingRubricFindings` is now `Map<string, string[]>` (`agent-loop.ts:272`): the fail branch reads before writing and appends only a justification the id does not already hold (`:1149`, `:1154`), the accessor flattens (`:274`), and settlement still deletes the whole id (`:1157`). All 5 designed traps rejected; the settling rule, the free-text concerns channel and both consumers byte-unchanged. Suite re-measured by hand: **191 files / 1,526 tests**; `test/runner/` 394 → **397**. **Judge caught nothing** — this review found 2 defects it passed over: 🟠 **F-365 → WP-631** (the fix makes per-id accumulation unbounded, one uncapped bullet per distinct justification into the completion-review prompt) and 🟡 **F-366** (the delivery's test file claimed settlement-clears-all coverage no test provided — HAND-FIXED, `test/runner/` 396 → 397, probe-verified RED against a clear-only-latest mutation). Also hand-fixed 🟢 F-368 (2 rationale comment lines deleted); 🟢 F-367 (spec's full-suite baseline 6 tests stale at launch) is a track-B note.
- **NEXT HEADLINE = WP-616 + WP-631 (`dogfood-149`): no string on the judge evidence path is unbounded at the point something renders it** (F-328 + F-365). WP-616 (unbounding an excerpt for ONE consumer unbounded the consumers that were never budgeted): a failing regression suite's RAW output goes into the rubric justification (`src/judge/harness.ts:205-208`, ceiling 64 KB at `src/judge/evidence.ts:203`) and `describe()` inlines it verbatim into the durable verdict rationale (`src/judge/verdict.ts:43-44`, taken at `:166`) — premise **re-measured on this tree 2026-08-16, still live**. WP-631 (this review's F-365): the WP-630 accumulator now grows one entry per distinct justification per model-judged rubric id, each rendered as its own uncapped bullet (`src/judge/prompt.ts:227`). Both are the CM-3 context discipline failing in the same shape at two sites. Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001`/WP-304, §7) stays operator-by-hand and cannot headline a dogfood run.

### 2026-08-17 — displaced from docs/DOGFOODING.md status block (dogfood-149 review)

**Status (2026-08-16, bounded — update discipline: REPLACE this block, ≤15 lines;
displaced prose moves verbatim to [`PLAN-HISTORY.md`](PLAN-HISTORY.md); per-run detail:
`docs/reports/`; queue + course correction: `plan.md` §6/§7).**
✅ **dogfood-148 / WP-630 (a second, DIFFERENT objection on the same rubric id must not silently replace the first) DELIVERED — sealed SUCCESS, landed** — `run-2213aec1-9683-4c6a-a496-b74f968975c1`, terminal **SUCCESS**, 1 step, $0.0808/$20.00, 6m 15s, AC 2/2 re-run green, harvest byte-IDENTICAL 2/2 (`docs/reports/dogfood-148.md`). `standingRubricFindings` is now `Map<string, string[]>` (`agent-loop.ts:272`): the fail branch appends a justification the id does not already hold (`:1149`, `:1154`), the accessor flattens (`:274`), settlement still deletes the whole id (`:1157`). All 5 traps rejected; settling rule, free-text concerns and both consumers byte-unchanged. Suite re-measured by hand: **191 files / 1,526 tests**; `test/runner/` 394 → **397**.
⚠️ **The judge caught nothing and the run still shipped 2 defects** — both found by human review, neither by its 10 rubric evaluations. The rubric reads the diff; nothing in it asks "is this claim tested?" or "what is the growth bound of this structure?".
🟠 **F-365 → WP-631, ARMED as half the dogfood-149 headline**: WP-630's accumulator is UNBOUNDED — only `tests_pass` is ever cleared, and every distinct justification renders as its own uncapped completion-review bullet (`src/judge/prompt.ts:227`). One model-judged id can contribute ~30 bullets (8–12 KB) over a 30-step run.
🟡 **F-366 HAND-FIXED**: the delivery's own test file advertised settlement-clears-all coverage that no test in the repo provided — closed at `test/runner/standing-findings-overwrite-live.test.ts:213`, probe-verified RED against a clear-only-the-latest mutation. 🟢 F-368 hand-fixed (2 rationale comment lines restored, `agent-loop.ts:268`); 🟢 F-367 track-B (the spec's full-suite baseline was 6 tests stale — it came from the previous review's HEAD, not the launch commit).
**NEXT HEADLINE = WP-616 + WP-631 (dogfood-149): no string on the judge evidence path is unbounded at the point something renders it** (F-328 + F-365) — one design principle, two sites, both premises re-measured live on this tree (`examples/dogfood/dogfood-149-wp616-wp631-bounded-judge-strings.yaml`). Ladder unchanged at `rung=4` — rung-5's remaining half (`brownfield-001` gold-patch, operator-by-hand) still cannot headline.
