# Requirements Traceability Matrix

Every requirement in [`project.md`](../project.md), assigned a stable ID, mapped to the work packages (WPs) in [`plan.md`](../plan.md) that satisfy it. **If a requirement has no WP, that's a plan bug — file it.**

- **Phase** = where it is first satisfied (later phases may extend it).
- Status values: `planned` · `in-progress` · `done` · `deferred (reason)`.
- When implementing a WP, reference the requirement IDs it closes in the PR description.

## RT — Routing & vendor neutrality (spec §5.1)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| RT-1 | Sits in front of one or many LLMs; routes tasks | WP-101 | P1 | done |
| RT-2 | Manages retries, handles failures | WP-103 | P1 | done |
| RT-3 | Coordinates multi-agent workflows | WP-121, WP-132 (executor+judge coordination); richer multi-agent in WP-203 sub-agents; **WP-618 (a node's inconclusive outcome survives the fan-in)**; **WP-636 (live/restored chain record agreement) ✅**; **WP-589 half (1) (a node's write boundary sees gitignored writes) ✅** | P1→P2 | in-progress (executor+judge coordination done; sub-agents P2). **WP-618 DONE — dogfood-156 (`run-3ff7c55a-ba68-48af-a125-218236bbdbca`, terminal SUCCESS, 2 steps, $0.1925/$20.00, AC 3/3 re-run green, harvest byte-IDENTICAL 12/12, `docs/reports/dogfood-156.md`), landed this review.** A chain node whose declared check never finished no longer folds in as an ordinary success: the run-level `inconclusiveCheck` marker WP-615 built now crosses every hop to the **persisted** `node_sealed` chain-journal entry (`packages/sdk-ts/src/chain/activities.ts:307`, typed `packages/sdk-ts/src/chain/store.ts:82`), is folded onto the reconstructed outcome (`packages/sdk-ts/src/chain/store.ts:301`), and is named by three operator renderers (`packages/sdk-ts/src/chain/read-trace.ts:45`, `packages/sdk-ts/src/chain/trace.ts:17`, `packages/sdk-ts/src/cli/chain.ts:470`). `NodeOutcome`'s value set is unchanged — the marker is additive, not a fourth status — and a node whose checks really ran gains no key. 🟢 **WP-636 DONE — dogfood-157 (`run-8b00e28a-3abf-4f91-bd92-4438c79087b0`, terminal SUCCESS, 2 steps, $0.1428/$20.00, AC 3/3 re-run green, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-157.md`), landed this review.** The coordination contract has ONE truth again: the marker is attached where the outcome is produced (`deriveNodeOutcome`, `packages/sdk-ts/src/chain/node-spec.ts:350`), so `readNodeResult` (`packages/sdk-ts/src/chain/activities.ts:254`) hands every `advanceChain` consumer the same object `chainRecordFrom` rebuilds (`packages/sdk-ts/src/chain/store.ts:301`) — a chain that never resumed no longer knows less about its own nodes than one that did, pinned by a test driving BOTH constructors off real journals (`packages/sdk-ts/test/chain/activities-inconclusive.test.ts:61`). The WP-521 seeded-fail drill branch was the one path still folding a bare literal (F-399); hand-fixed at the same review (`packages/sdk-ts/src/chain/chain-loop.ts:339`). 🟢 **WP-589 half (1) DONE — dogfood-158 (`run-71087607-eca4-41ca-b73b-febb0f484b8e`, terminal SUCCESS, 2 steps, $0.1891/$20.00, AC 3/3 re-run green, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-158.md`), landed this review.** The writeSet a planner grants a node is now a real boundary: `publishChainHandoff` measures the node's writes from four git streams including `git ls-files --others --ignored --exclude-standard` (`packages/sdk-ts/src/runner/activities.ts:2884`) and admits them through the unchanged F-218 rules, so a node can no longer write 2.1 GiB into a gitignored tree and seal SUCCESS. `RepoHandoff.changedPaths` — the bounded channel the next agent reads — is untouched. Toolchain output is exempt by a named segment set (`packages/sdk-ts/src/chain/write-set.ts:133`) after 🔴 F-401, which shipped an exemption covering only `node_modules` and would have failed the seal on any node that built the package (hand-fixed at the review). 🟡 Residue: **WP-638** (the ignored stream is a working-tree snapshot with no `BASE_TAG`, so a file predating the node is blamed on it) and **WP-589 half (2)** (`chikory-bench compare` reading arm summaries from outside the sandbox). |
| RT-4 | Bring-your-own-model, swap freely | WP-101, WP-104 | P1 | done |
| RT-5 | Explicit per-stage policies (planning/coding/review/judge) | WP-104 | P1 | done |
| RT-6 | Dynamic routing layer across providers | WP-103 (failover), WP-104 | P1 | done |
| RT-7 | Teams keep existing observability stack | WP-105 (OTel/OTLP, no proprietary sink) | P1 | done |
| RT-8 | Sits above existing frameworks/coding agents, not replacing them | ADR-003, WP-111, WP-112 | P1 | done |
| RT-9 | Runs locally; cloud only as orchestrated process | WP-004 (local Temporal), constraint on all P4 WPs | P1 | planned |
| RT-10 | Drive CLI coding agents: Claude Code, Codex, Jules, Antigravity | WP-112, WP-113, WP-216 | P1→P2 | in-progress (Claude Code + Codex + Antigravity `agy`/Gemini done 2026-07-23 via `gemini-cli` adapter; Jules pending, WP-216) | ⚠️ 🔴 **F-192 (dogfood-115):** the Antigravity (`agy`) arm ran under `--dangerously-skip-permissions` and delivered OUTSIDE its workspace; driving a CLI agent is not sandboxing one. Detector landed; **WP-539** is the containment fix.
| RT-11 | Launch providers: Anthropic, OpenAI, Gemini, open models (OpenAI-compat) | WP-101, WP-102 | P1 | done |
| RT-12 | Limit-aware intelligent scaling across inference endpoints (CLI-subscription / API-key / OpenAI-compat) — headroom failover + work-conserving scheduling before any sleep; a park lasts exactly the learned reset; and a pacing governor that spends headroom proportionally to window quota (rolling-5h AND weekly) and task horizon, so a weekly limit is never exhausted mid-task | WP-307 (capability model) ✅, WP-308 (scheduler) ✅, WP-309 (limit telemetry), WP-310 (pacing governor) | P3 | in progress — added 2026-07-10. **WP-307 ✅ DONE (dogfood-097 `run-d44ff996`, 2026-07-11, uncommitted working tree):** `src/endpoint-capability.ts` — descriptors for CLI-subscription/API-key/openai-compat endpoints (auth mode, limit semantics, cost linkage, model family), per-stage resolution, invariant #2 family feed, replay-safe journal + trace line. **WP-308 (scheduler) ✅ DONE (brain dogfood-098 `run-6ef24fb7` + hands dogfood-099 `run-03cd4c21`, 2026-07-12, uncommitted):** `classifyLimitSignal` + `decideLimitResponse` ordered plan (failover → limit-independent → park last, invariant #2 preserved) + `CHIKORY_LIMIT_AT_STEP` seam + replay-safe `limit_signal` journal + `limit-slept vs conserved` trace totals; **execution half landed (F-136 fixed):** net-new `applyLimitResponse` re-dispatches failover / runs real limit-independent work then defers the throttled item / parks via a workflow-side durable timer — the fabricated `claimsComplete:true` seam is gone. Live proof: forced-limit run seals SUCCESS with `conserved 5000ms > 0`, `slept 0`, every plan item genuinely done. **WP-309 (limit telemetry + reset learning) ✅ DONE (test-complete) — cross-run half landed dogfood-102 (`run-cd98de3e`, uncommitted, 2026-07-14, `docs/reports/dogfood-102.md`, SUCCESS 7 steps $11.25/$45):** net-new `appendEndpointLimitObservations` (`activities.ts`, inside `if (observation !== undefined)`) persists the ledger's in-window consumption at a real hit as the learned window capacity to `<dataDir>/ledger/endpoints.db`; a fresh `EndpointLedger` reads it as `windowState().capacityTokens` and `decideLimitPacing` throttles against it with zero declared/injected capacity (F-97 co-reference test); injected seam provably writes nothing; 904 TS green — **🔴 F-138 CLOSED.** Cross-run KPI proven DETERMINISTICALLY (spec-scoped), NOT yet live across two real runs (ℹ️ F-142: no non-injected deterministic hit seam). New friction 🟡 F-141 (AC-3 full-suite over judge per-check cap ×2, infra-classed) · 🟡 F-134 recurrence (front-load ROLLBACK ×1, $2.19 tax). PER-RUN HALF landed earlier (dogfood-100 `run-1caffe21`, committed `8a330a4`, 2026-07-13): a REAL non-injected call site (`activities.ts:1007-1011`, `rawLimitSignalFromStepRecord` on a FAILED step record — 429 + CLI-stderr) feeds `classifyLimitSignal`; replay-safe `limit_observation` journal kind; net-new pure `learnEndpointReset` (median of observed resets, abstains <2 samples) drives a LEARNED-duration park; forced-park end-to-end proof closes ℹ️ F-137; `chikory trace` renders learned resets; 898 TS green. **✅ RESIDUE (F-138) CLOSED (dogfood-102):** the write is wired — `appendLimitObservation` now has a real `src/` caller and the WP-310 governor paces against ledger-learned capacity cross-run (test-proven). **WP-310 (pacing governor) ✅ CORE HAND-LANDED track-B 2026-07-12 (uncommitted working tree):** WP-308 made limit handling spend-first, which raised per-hour subscription burn with zero weekly-quota awareness — the governor closes that. Declared `quotaWindows` (rolling-5h + weekly) on the `claude-code`/`codex` descriptors; cross-run consumption ledger `<dataDir>/ledger/endpoints.db` (`src/journal/endpoint-ledger.ts`, the chain.db precedent — capacity LEARNED from `limit_observations`, never vendor-declared); pure `decideLimitPacing` (`src/runner/limit-pacing.ts`): push ≤½ sustainable pace / steady / throttle (durable `control_event source:"pace"` sleep, clamped 30min, never below the `horizon` deadline pace — `paceConflict` journaled loudly) / predict-limit (next step would blow the tightest window → routed through the WP-308 `applyLimitResponse` path BEFORE the provider fires 429); pre-step gate in `agent-loop.ts` beside the budget/token gates; replay-safe `limit_pace` journal + `pace …` trace totals (no-pace runs byte-identical); `CHIKORY_QUOTA_STATE` forced-state + `CHIKORY_QUOTA_WINDOWS` compressed-window seams; TaskSpec `horizon` field. LIVE Temporal proof (`agent-loop.test.ts` WP-310): forced quota pressure → push@0, throttle@1 with a real durable pace sleep, SUCCESS, `limitPaceThrottles:1, limitSignals:0`. **✅ REAL-CODEX LIVE PROOF LANDED — dogfood-101 (`run-6c9873e3`, SUCCESS 4 steps $2.90/$45, `docs/reports/dogfood-101.md`): under a compressed weekly window (20M cap) the governor pushed@0 then throttled@1/2/3 (`limitingWindow:"weekly"`), 3× `control_event source:"pace"` durable sleeps (450571/256662/199037ms), pace-throttled 15m 6s, sealed SUCCESS at 1.92M ≪ 20M (90% left), `pace` footer renders — first observation of the governor pacing a real agent.** Slice 2 (learned resets/capacity feeding the governor from real signals) rides WP-309. Replaces sleep-first limit handling (dogfood-090 parks, `soak.sleep_ms`, DOGFOODING §7 manual CLI switch) |

## DX — Durable execution (spec §5.2)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| DX-1 | State, memory, task context across minutes → weeks | WP-121, WP-122; cross-session memory WP-204; intra-run step-forcing WP-269; per-step chunking WP-270 | P1→P2 | in-progress — cross-session tiered memory WP-204 → 🟢 DONE (dogfood-079); durable journal/checkpoint state (WP-121/122) live; **intra-run step-forcing WP-269 → 🟢 DONE (dogfood-081, `run-464b7b77`, uncommitted byte-IDENTICAL pending harvest, `docs/reports/dogfood-081.md`)** — the rung-3 ENABLER that makes ONE `chikory run` accumulate MANY sealed durable checkpoints instead of collapsing into one executor session: a pure total `decideStepForcing(state, policy?)` + an opt-in `boundedWorkUnit` spec-input policy that DEFERS a premature `claimsComplete` and re-enters `agent-loop.ts` until a durable-step floor is met AND the judge confirms the ACs; contract-additive (no `StepRecord`/`JournalEntry`/`Checkpoint` change), no-policy path byte-identical, LIVE-proven (one run seals 3 durable checkpoints under an active policy while no-policy stays one-shot). **WP-265 rung-3 intra-run horizon PROOF REACHED (dogfood-082, `run-ef4824e2-a91c-4633-aac3-bb26608217c4`, 2026-07-03, harvested `1d2c7e5`, `docs/reports/dogfood-082.md`):** ONE `chikory run` under `bounded_work_unit:{min_durable_steps:5}` sealed **6 durable checkpoints** AND survived a LIVE mid-run `kill -9` → `chikory resume` between sealed steps with zero re-execution (journal byte-identical). ⚠️ the horizon is real but HOLLOW — seal-deferral without work-CHUNKING front-loaded all product code into step 1, so steps 2–6 sealed thin test-tweak checkpoints and the per-step-reliability KPI was not yet trustworthy. **WP-270 per-step work-unit chunking → 🟢 DONE (dogfood-083, `run-d3879dab-c1e4-4dde-930e-27f679a75d10`, harvest byte-IDENTICAL / un-committed at review, `docs/reports/dogfood-083.md`):** opt-in ordered `work_chunks` on `bounded_work_unit` + pure `decideWorkChunk` (`src/workflow/work-chunk.ts`) wired into `agent-loop.ts` step-forcing so each forced step's instruction is EXACTLY the next chunk's directive; additive (no list = byte-for-byte WP-269), frozen contracts held, LIVE-proven (real Temporal run asserts distinct ordered per-step work + the WP-269 fallback), 658 tests green. ✅ **F-101 CLOSED (dogfood-083 re-run `run-03d161e9`, 2026-07-04, un-harvested):** chunk counter replaced with a dedicated `consumedWorkChunks` incremented only on PROCEED + `use_chunk`, so a rolled-back chunk is re-handed rather than skipped; `workChunkMilestone` forces a judge pass on each chunk step; LIVE scripted-ROLLBACK regression test asserts step instructions `[chunk0, chunk0, chunk1]`, 660 tests green. → the NON-hollow rung-3 horizon on `work_chunks` (dogfood-084) is the next headline. |
| DX-2 | Journal/replay: each LLM/tool call a deterministic journaled step | WP-121 | P1 | done | ⚠️ **INTEGRITY GAP FOUND 2026-07-27 (🔴 F-192, dogfood-115 `run-c19147fe`) — the guarantee is only as good as the workspace boundary:** the graded clone lives INSIDE the repo it clones and its `origin` points back at it, so an executor wrote a complete delivery into the operator's checkout instead of the sandbox. Journal, checkpoints, rollback and replay all kept working — correctly, on the wrong tree — while the real work sat outside the audit trail entirely. Detection hand-landed (`executors/step.ts` fails an empty-diff step whose source repo gained paths; 6 tests); **prevention is 🔴 WP-539** (clone to `os.tmpdir()` as the bench harness already does).
| DX-3 | Crash → resurrect from point of failure via memoized results | WP-123 | P1 | done | ⚠️ **INTEGRITY GAP FOUND 2026-07-27 (🔴 F-192, dogfood-115 `run-c19147fe`) — the guarantee is only as good as the workspace boundary:** the graded clone lives INSIDE the repo it clones and its `origin` points back at it, so an executor wrote a complete delivery into the operator's checkout instead of the sandbox. Journal, checkpoints, rollback and replay all kept working — correctly, on the wrong tree — while the real work sat outside the audit trail entirely. Detection hand-landed (`executors/step.ts` fails an empty-diff step whose source repo gained paths; 6 tests); **prevention is 🔴 WP-539** (clone to `os.tmpdir()` as the bench harness already does). **Hardened 2026-08-20 (WP-645, dogfood-163 `run-12dfe421-7633-4199-9e3c-18bc32a410c5`, `docs/reports/dogfood-163.md`):** a resurrect is only as good as the classification of what killed the step. A CRASH — the executor dying without producing an answer — used to read as the agent's own failure, so its half-written files were committed, judged and sealed as the run's answer (dogfood-162 lost a fully-green delivery that way). `isInfraStepFailure` now recognises it **structurally** — a FAILED step that metered `tokens.output === 0` (`packages/sdk-ts/src/executors/infra-failure.ts:17`, flagged at `packages/sdk-ts/src/executors/step.ts:284`) — so a new adapter inherits the protection without code. ⚠️ Not yet live-exercised (F-197): the harness runs the build from launch HEAD. |
| DX-4 | Checkpoint per LLM/tool call; pause, inspect, resume | WP-122, WP-141 | P1 | done | ⚠️ **INTEGRITY GAP FOUND 2026-07-27 (🔴 F-192, dogfood-115 `run-c19147fe`) — the guarantee is only as good as the workspace boundary:** the graded clone lives INSIDE the repo it clones and its `origin` points back at it, so an executor wrote a complete delivery into the operator's checkout instead of the sandbox. Journal, checkpoints, rollback and replay all kept working — correctly, on the wrong tree — while the real work sat outside the audit trail entirely. Detection hand-landed (`executors/step.ts` fails an empty-diff step whose source repo gained paths; 6 tests); **prevention is 🔴 WP-539** (clone to `os.tmpdir()` as the bench harness already does). ⚠️ **REOPENED (dogfood-162, F-424 → WP-646):** every checkpoint carries a `lastGood` boolean and the FAILED terminal entry records `lastCheckpoint` — the most recent one, not the good one. `run-75794008-83a8-4ca8-893d-ae3000df754a` sealed on `@35` (`lastGood: false`, 7 red tests) while `@28` (`lastGood: true`) held a tree passing all three ACs with the suite at 1,787 passed. The flag is journaled and never read at the seal, and `scripts/harvest.sh` takes the workspace as it stands, so the operator is handed the worst state the run reached. Inspect/resume works; *which* state a failed run offers does not. **Extended 2026-08-20 (WP-646, dogfood-163 `run-12dfe421-7633-4199-9e3c-18bc32a410c5`):** "inspect, resume" now means the BEST tree the run reached, not the last one it touched. An infra-failed step's partial writes are reverted to `lastGood` **before the judge grades** (`packages/sdk-ts/src/workflow/agent-loop.ts:943`), a FAILED terminal names `lastGood` instead of the most recent checkpoint (`:327`), and `chikory resume` follows that name and moves its diff baseline with it (`packages/sdk-ts/src/runner/activities.ts:2105`). The rewind-everything trap is rejected: a FAILED run whose final step DELIVERED under PROCEED keeps that work. |
| DX-5 | Branching of execution paths as first-class op | WP-205 | P2 | **DONE (dogfood-080, `run-233e7d7f`, 2026-07-03, uncommitted byte-IDENTICAL to workspace pending harvest — `docs/reports/dogfood-080.md`).** The operator-facing `cmdBranch` command (wired into the `branch` verb, reusing `parseBranchTarget`) forks a run at a committed checkpoint into a child run: `forkRunAtCheckpoint` (`src/runner/branch.ts`) seeds the child journal through the fork checkpoint + records a `branch_fork` control-event with parent-id/checkpoint provenance, and creates the child workspace as a git worktree at the checkpoint's recorded commit (WP-132 commit-resolution reused). Tested via a DI-fake unit suite, a real-temp-git worktree unit test, and a LIVE Temporal proof (drive 3 checkpoints → `cmdBranch` → child resumes from the fork while the parent stays intact). Pure surface (parser dogfood-033, name helper dogfood-034) reused, not re-implemented; no dep/contract change; 629 tests green. |
| DX-6 | Rollback as first-class op | WP-132 (judge-triggered), WP-205 (manual) | P1→P2 | **DONE** (judge-triggered rollback done WP-132; manual `chikory branch` DONE via dogfood-080 — `cmdBranch` + `forkRunAtCheckpoint` fork a run at a checkpoint into an isolated child worktree, parent left intact, live-Temporal-proven; uncommitted pending harvest, `docs/reports/dogfood-080.md`) |
| DX-7 | Budget-aware continuation | WP-124 (hard gate), WP-207 (reasoned continuation) | P1→P2 | in-progress (hard gate done; reasoned continuation P2) |
| DX-8 | Suspend/resume for HITL; sleep hours/days, resume on approval | WP-206 (full); WP-132 ESCALATE is the P1 stopgap | P2 | **DONE (dogfood-077, `run-d14fb74c-989d-4cce-adad-f37e06b1b3f1`, WP-206 → 🟢, 13 files STAGED uncommitted byte-IDENTICAL to the workspace; `docs/reports/dogfood-077.md`).** `chikory suspend <run-id>` durably parks a HEALTHY running run at ZERO compute: `cmdSuspend` (`cli/commands.ts:464`, mirrors `cmdCancel`) + `case "suspend"` dispatch/help + a durable `SIGNAL_SUSPEND`/`suspendSignal` + `setHandler` in `agent-loop.ts` whose `parkIfOperatorSuspended()` journals a `control_event`(suspend), sets `status="SUSPENDED"`, and `await condition(() => resumeRequested \|\| cancelRequested)` (a Temporal zero-compute wait, NOT a poll/sleep) — placed at the loop TOP so NO journaled step re-executes on resume; `RunHandle.suspend()` sender + `runner.resume()` resume-signal (guarded no-op on a RUNNING run, so WP-123 crash-recovery resume still works). A net-new LIVE Temporal `test/runner/suspend-resume.test.ts` proves SUSPENDED, no-advance-while-parked, and zero-replay resume (steps `[0,1,2,3,4]`, spend unchanged, control_events `[suspend,resume]`); full suite green (591 passed \| 19 skipped). New additive `control_event` journal kind synced across TS types + zod schema + py `types.py` + `docs/spec/journal-format.md`. ⚠️ The run was a WP-265 rung-2 attempt; the ≥10-step horizon + live kill→resume KPIs went un-measured — codex one-shot the feature in 1 step (F-86 → WP-508, not a DX-8 gap). **The live at-INTRA-run kill→resume is now PROVEN (dogfood-082, `run-ef4824e2`, 2026-07-03): a real mid-run `kill -9` → `chikory resume` landed BETWEEN sealed steps and journal-replayed the sealed steps byte-identically with zero re-execution.** ⚠️ **NEW F-99 (track-B, WP-206-adjacent):** the resume was masked as a ~30-min stall — a bare `chikory resume` from a shell missing `OPENAI_COMPAT_BASE_URL` silently lost judge routing, and Temporal's ~65-attempt activity retry looped silently instead of failing loud. Add a fail-fast resume precondition that validates the configured provider env BEFORE entering the durable retry loop. **WP-272 EXTENDS DX-8 into AUTONOMOUS time-paced durable re-entry (dogfood-088, `run-eeb0d5d7`, hand-landed uncommitted — `docs/reports/dogfood-088.md`):** where DX-8 parks until a HUMAN resumes, WP-272's opt-in `spec.soak` policy makes the loop durably SUSPEND between steps and RE-ENTER on a durable `sleep(sleepMs)` Temporal timer (no operator), producing honest wall-clock endurance for the ⑦ overnight rung. Net-new pure `decideSoakDelay` + `SoakPolicy` (`src/workflow/soak.ts`), additive `control_event` `source:"soak"`, `re-entries N · soak-slept <duration>` trace telemetry, resume rehydrates `soakState`; additive `soak` contract w/ Python parity; no frozen-contract shape change, 744 TS + 52 py green, LIVE-proven. WP-272 → 🟢. |
| DX-9 | Self-heal: every non-infra failure gets ≥1 bounded, journaled automated heal attempt before terminal seal — run, chain, AND the plan gate above both | WP-519 (remediation-before-HALT), WP-520 (resumable-FAILED terminal), WP-521 (chain heal-by-default + chain resume), **WP-542 (tier-0 plan-gate repair)**, **WP-543 (no chain ends un-sealed)**; ADR-009 | P2 → post-C-1 | in-progress — **run level DONE (hand-land 2026-07-08, track-B):** rule-3 HALT now grants ONE journaled remediation attempt (brief composed from the judge's own form + rollback to last-good + off-cadence re-judge; pure `src/workflow/remediation.ts`, additive `remediation` journal kind); exhausted/unattended seals carry `resumable: true` and `chikory resume` RE-STARTS the workflow over the same journal (reopen `control_event` `source:"failed_seal"`, failure evidence rides as feedback, fresh heal budget per incarnation); dead seals refuse resume with the `chikory branch` path. LIVE Temporal proof `test/runner/remediation-live.test.ts` (heal→SUCCESS; exhaust→resumable seal→resume→reopen→SUCCESS; dead refuses); 765 TS + 84 py green. **WP-521 (chain level) (a)+(b) HAND-LANDED track-B 2026-07-15 (committed); (c) = P3-rung-2.** The post-C-1 gate (needed WP-232 chain e2e evidence) is CLEARED by dogfood-103 (`chain-9d189c1a`). **✅ (a) heal-by-default:** `startChain` defaults `maxReplans` to 1 (`runner.ts`; explicit 0 = legacy halt). **✅ (b) self-contained evidence-enriched replan:** pure `src/chain/replan-plan.ts` (`buildReplanBrief`/`buildRetryPlan`) RETRIES the failed node with its seal-reason evidence; `replanRemaining` fires in the REAL worker without the test-only injected dep; additive `node_replanned.brief`. `CHIKORY_SEED_CHAIN_FAIL_NODE` force-fail seam + LIVE proof `test/chain/chain-heal-live.test.ts` (923 TS + 84 py green). **✅ LIVE-CHAIN PROVEN (P3-rung-1) — dogfood-105 (`chain-da846d34`, seam ARMED `=1`, `docs/reports/dogfood-105.md`): a real 3-node chain force-failed its MIDDLE node → `node_replanned` (evidence-enriched) → N-B-r1 recovered to SUCCESS, journal-read (both KPI halves; first chain-recovery tick since 097). Ledger `rung=1`.** The chain did not fully SEAL — bonus node N-C snagged in an out-of-rubric approve loop (🔴 F-154) after a genuine judge true-positive (🟡 F-153); self-heal path NOT implicated. (dogfood-104 was the unarmed first attempt, F-146; WP-531 guard landed 2026-07-16.) **✅ (c) SUBSTRATE HAND-LANDED 2026-07-19 (`af9ce6b`):** `chikory chain resume <chain-id>` re-enters a sealed-FAILED resumable chain, retries the failed node (reopen `control_event source:"chain_failed_seal"` + `restoreChain` journal-restore + `ChainJournal.reopenChain`; node-failure FAILED is resumable, malformed-plan FAILED is dead; template persisted); the F-154 approve force-seal fix landed alongside (`81066d3`). LIVE proof `test/chain/chain-resume-live.test.ts`; 944 TS + 84 py green. **✅ DRILL HAND-LANDED 2026-07-19 (WP-532):** two-phase `CHIKORY_CHAIN_RESUME_DRILL=1` `chain-dogfood` mode (phase 1 seals FAILED via host-side `CHIKORY_CHAIN_MAX_REPLANS=0` heal-off seam + armed force-fail, phase 2 `chikory chain resume` → SUCCESS; guard 1d-ter validates --chain + seam at $0); `test/cli/chain-resume-drill-seam.test.ts`, 948 TS green. **✅ P3-rung-2 CLIMBED — dogfood-106 (`chain-babfabb0-4f4b-46c0-a5c3-42ac27153709`, `docs/reports/dogfood-106.md`, SUCCESS 4 steps $5.55, 2026-07-20):** the operator `chikory chain resume` CLI path recovered a real sealed-FAILED chain — first live proof of the (c) substrate on an actual dogfood chain (only ever unit/integration-tested before). Surfaced + fixed 🔴 **F-155** same session: `followChain` raced the freshly-started `chainLoop` workflow and honored the pre-resume journal's stale FAILED tail, so `chikory chain resume` silently no-op'd on the 1st launch; fixed with a `sinceIdx` baseline (`9e01e09`). Re-ran: `N-B-r1` retried for real, chain SUCCESS, `chikory chain trace` renders the resume history (WP-532 delivery `a7fc10b`). Moves the chain-recovery KPI (kill→resume, flat at 0 through 097…103, first ticked 1 at dogfood-105 self-heal) to include the operator-resume axis too; DX-9/NF-7 self-heal doctrine now proven at BOTH the run level and both chain sub-levels (internal self-heal + operator resume). **✅ WP-542 TIER-0 HAND-LANDED 2026-07-28 (F-207):** the one layer with no heal tier at all was the plan gate ABOVE both loops — any rejection discarded the decomposition and exited (five `dogfood-120` launches, zero nodes run, each repaired by a human editing the goal spec). `planAndGateChain` now loops classify → decide → brief → re-decompose over all five plan-phase rejection classes (planner transport, write-set topology, `min_nodes` floor, coverage/literal floors, meta-judge REVISE), bounded at 3 attempts / 10% of the chain budget (`CHIKORY_PLAN_REPAIR_ATTEMPTS=0` restores the old stop); brief composed deterministically from `verdict.uncoveredCriteria` + `planLiteralGaps` + the rejected outline (no extra LLM call); one `plan_verdict` chain-journal entry per attempt. Config errors and substantive ESCALATE stay unrepaired by design, and an exhausted budget still stops the launch with the full trail — the gate is never softened. Pure tier `src/heal/gate-repair.ts` + binding `src/planner/plan-repair.ts`; 1061 TS green. **✅ WP-543 HAND-LANDED 2026-07-28 (F-208):** the doctrine's other half — a chain that never *seals* is worse than one that seals badly. A node sealed `{FAILED, ESCALATE}` parked the chain in `AWAITING_PLAN_APPROVAL` (ADR-005 §S3 rule 1) after its escalation had ALREADY been answered, and `chainLoop` — which seals only on SUCCESS/FAILED — returned journaling nothing, so `chain resume` found no sealed state, `chain approve` found no in-flight node, and `--watch` waited forever (dogfood-120 `chain-0723ac0b`). Pure `resolveAnsweredEscalationPark` (`src/chain/escalation-park.ts`) now converts that park into the resumable FAILED seal in the ORCHESTRATOR, leaving the four-rule reducer + `chain_advance.py` parity byte-unchanged; `chikory chain resume` additionally repairs chains already orphaned by the pre-fix build, fail-closed on a live workflow / in-flight node / unreachable server. LIVE proof `test/chain/chain-escalation-seal-live.test.ts`, verified RED pre-fix; 1089 TS green. **✅ WP-544 HAND-LANDED 2026-07-29 (F-209…F-214):** a heal budget is only legitimate over what the agent controls, and the cheapest heal must run first. dogfood-120's `N-2` was HALTed as a budget-waster on $0.20 of $15 — two of its three strikes were an infra cap-kill and a verdict a ROLLBACK had reverted — then refused any chain heal because a lineage that had SUCCEEDED still debited a chain-wide budget hardcoded to 1. Root cause under both: `ChainNodeTemplate` forwarded six fields, so the goal spec's `step_limits`/`unattended`/`pacing` were parsed and dropped (that `unattended` drop is also why F-208's node parked for a human at all). Delivered: enumerated template contract with a compile-time exhaustiveness assertion; `StepRecord.infraFailed` so a cap-killed step spends no rule-3 strike; rule-3 history truncated at the last ROLLBACK (never at a remediation restore — that would un-bound WP-519); a rollback anchor must contain work; ROLLBACK feedback carries the failing criteria; `ReplanBounds` (per-lineage budget + chain ceiling over LIVE plan nodes, spec-configurable); and ADR-009 tier 4 — `decideNodeHeal` RE-ENTERS a child that sealed `resumable` before any replan, preserving its checkpoint and journal. Replay-proven on the real `N-2` journal (RED on HEAD) + LIVE `test/chain/node-resume-live.test.ts`; 1118 TS green. **✅ WP-554 HAND-LANDED 2026-07-30 (F-229):** the doctrine's last dead end was a run that had CONVERGED and had no way to say so. dogfood-121's `N-3` sealed FAILED twice on zero-byte diffs with every acceptance criterion AND every rubric item passing, because the judge objected only in free text about evidence the INCREMENTAL diff cannot carry (a launcher committed one checkpoint earlier). Unattended, the auto-resume could only re-judge the same empty tree and re-raise the same concern — two rounds exhausted the node's replan budget and killed a 5-node chain. F-154's carve-out stated this verbatim but sat BELOW the unattended seal in `workflow/agent-loop.ts`. Delivered: the rule is hoisted above `decideEscalationWait`, gated on `record.diffRef.bytes === 0` (reusing `isCompletionMilestone`'s signal, not a second concept) plus all-criteria and all-rubric passing, and scoped to `unattended.escalation === "seal_resumable_failed"` so an attended operator still adjudicates; the concern rides the seal reason verbatim. Three LIVE Temporal tests (empty→SUCCESS, non-empty→still FAILED, attended→still awaits), RED proven by stashing the fix; 1184 TS green. **✅ WP-553 HAND-LANDED 2026-07-30 (F-228, F-234):** an infra wall is not a failure to heal — it is a wait. A provider quota exhaustion (`Individual quota reached … Resets in 1h0m8s`) reached the CG-1 strike counter instead of the limit scheduler, because `StepRecord.limitSignal` was read by `runner/activities.ts:320` and set by NO adapter, making park-until-reset injection-only. Delivered: the field is declared and populated by `runCliStep` for every CLI adapter, `quota (reached|exceeded|exhausted)` is classified, and the compact duration `1h0m8s` no longer parses as 8 seconds.  **🔴 REOPENED at the RUN level by dogfood-130 (F-296 → WP-602):** the self-heal guarantee has a hole nobody had named — a **rejected escalation** on a plain run seals `FAILED` immediately and non-resumably (`agent-loop.ts:1148-1153`), while the approve path resumes (`:1167`). No heal attempt is made, and the operator's reject **reason** — the single richest correction signal in the system — is interpolated into the seal string and discarded. WP-542/F-207 (no LLM-verdict gate may exit) and WP-543/F-208 (no incarnation ends un-sealed) both cover chain nodes only. dogfood-130 is the live cost: a correct one-command work order (*untrack 8 orphan gitlinks*) killed a $0.11 run, and the operator hand-branched from checkpoint `@5` to recover. **✅ CLOSED at the run level 2026-08-10 by WP-602 (dogfood-131, `run-6b50d3f9-ef17-4ce4-9d45-dbb34422db54`, SUCCESS 3 steps $0.1899/$20.00, landed `d499128`, `docs/reports/dogfood-131.md`):** a reject carrying a reason now routes into the SAME WP-519/520 remediation path — pure `decideRejection` (`src/workflow/rejection.ts:52-78`) beside `decideRemediation`, driven by `handleOperatorRejection` + `applyRemediation` (`src/workflow/agent-loop.ts:324-368`) from BOTH the judge-escalate (`:1173-1181`) and loop-breaker (`:1219-1225`) branches, so the run rolls back to its last good checkpoint, receives the operator's words VERBATIM as the brief, and continues under its own power to its own terminal state with no further operator command. Bounded by `maxRejectStrikes` (default 1; `0` restores the exact pre-WP-602 dead seal); a reject whose reason is absent, empty or whitespace-only still seals dead immediately. The dead seal is now reserved for exactly the two cases the requirement names. **✅ RESIDUAL F-302 CLOSED 2026-08-11 by WP-605 (dogfood-132, `run-8e2c21c7-c495-491b-8a4a-0c9f141c7902`, SUCCESS 1 step $0.0440/$20.00, 2/2 criteria, `docs/reports/dogfood-132.md`):** the heal no longer destroys work the operator never objected to. `decideHealRollback` (`src/workflow/heal-rollback.ts`) is a pure, replay-safe decision read from the VERDICT rather than the trigger — a failing acceptance criterion or a destructive rubric failure condemns the work and restores `lastGoodCheckpointId`; a verdict that condemned nothing keeps the work and the correction brief is applied on top; an absent anchor keeps because there is nothing to restore. `applyRemediation` (`src/workflow/agent-loop.ts:327-364`) gates BOTH `restoreCheckpoint` and the `rollbackTo` journal key on that single answer, so the journal no longer records a rollback it did not perform. WP-519's guarantee is narrowed, not removed: a rule-3 HALT is only ever reached through trailing criterion failures, so it still always rolls back (`test/runner/remediation-live.test.ts:141`, untouched and green). The heal remains bounded and journaled as DX-9 requires. ⚠️ **BOUND MEASURED TOO TIGHT 2026-08-20 (dogfood-159, F-408 → WP-640).** The heal is bounded and journaled, but the completion-review bound is a CONSTANT — exactly one repair attempt per run (`packages/sdk-ts/src/workflow/agent-loop.ts:1348`), independent of the run's remaining headroom. `run-ec5c4bb8-de16-4c28-beba-a8cd09795fa1` spent its grant after step 1; step 2's review raised a **new** finding and the run sealed FAILED (resumable) at 21:37:24 with **3 of 6 steps and 98.3% of the $20 budget unspent**. A hand-typed `resume` 1h 39m 52s later produced step 3, which cleared the finding in 3m 12s and sealed SUCCESS — the run could heal itself and was not allowed to. DX-9's "≥1 bounded attempt" is satisfied; what fails is the spirit of an UNATTENDED loop, since 81% of that run's wall clock was a human-latency stall. **WP-640** scales the grant with remaining steps/budget and counts an attempt only when the finding is UNCHANGED (a new finding is progress, not oscillation), without weakening the F-223…F-226 anti-oscillation bound. Compounding measurement defect → **WP-641** (F-409): the reopen IS journaled as `control_event {event: "resume", source: "failed_seal"}`, but `chikory trace` renders neither the FAILED seal nor the reopen, and `scripts/dogfood-verify.sh:163` counts resumes by grepping that output for `resumed` — so the ledger recorded `resumes 0` for a run that demonstrably resumed. **WP-640 (repair grant scales with headroom) DONE — dogfood-160 (`run-de555224-1de9-491b-b809-6c063e621f86`, terminal FAILED (resumable), 4 steps, $0.2995/$20.00, `docs/reports/dogfood-160.md`), landed 2026-08-20 after a hand-completion.** The self-heal budget at the completion-review gate is no longer a constant: an objection the last repair attempt was NOT given buys another attempt while steps and budget remain, while a repeated objection still seals FAILED (resumable) within 2 review passes. This closes the F-408 case where dogfood-159 stopped with 3 of 6 steps and 98.3% of budget unspent and then waited 1h 39m 52s for a hand-typed `resume` that fixed it in 3m 12s — i.e. DX-9's "bounded automated heal attempt" was bounded by the wrong quantity. **WP-643 (a repeat is recognised however it is worded) DONE — dogfood-161 (`run-eef8a03d-c6b6-4165-9738-d002cef3d56d`, terminal FAILED (resumable), 4 steps, $0.4229/$20.00, 48m 13s, `docs/reports/dogfood-161.md`), landed 2026-08-20 at that review.** The heal budget's anti-oscillation test is no longer a string comparison: `areMateriallySameObjections` (`packages/sdk-ts/src/workflow/completion-review.ts:478`) compares what an objection is ABOUT — code entities and their sub-tokens, a defect category, a focus subject and a trigger condition — while staying pure and replay-safe inside the Temporal workflow and reading `{ id, justification }` only. LIVE-PROVEN on prose the delivery never saw: that run's own four completion reviews restate ONE complaint against four revisions of the code, and driven through the accumulating history (`packages/sdk-ts/src/workflow/agent-loop.ts:263`, pushed at `:1406`) the byte-equality it replaced granted a repair attempt on all four while the delivered instrument stops at review #2. So a judge that rewords its complaint can no longer buy a run's whole headroom, and DX-9's bounded-heal guarantee is bounded by the right quantity at last. **Residue → WP-644 (open):** the instrument is neither complete nor sound, both measured at the dogfood-161 review. It recognises **2 of 6** pairs of that same four-way restatement, and review #4's wording matches none of its predecessors — so recognition is wording-order dependent and `MAX_PROGRESS_GRANTS = 2` remains a live backstop (F-416). And a `true` is no longer always right: two objections on `flushBatchWriter`, "drops metadata on retry" vs "loses the checksum on retry", compare as the same objection (`completion-review.ts:560`), and over 298 real cross-run objection pairs from 13 journals the false-positive rate is **1.3%** where byte-equality's was 0% (F-417) — calling a genuinely new finding a repeat strands a run that could have healed itself, which is the DX-9 failure mode inverted. F-418 names the harder half: reviews #1→#4 each faulted a different concrete shortcut in the then-current code and the executor removed each in turn, so the run was CONVERGING while the objection stayed the same, and the progress model cannot separate *same wall, new code* from *no progress*. DX-9 stays **in-progress** on that residue. ⚠️ **BOUNDARY DEFECT (dogfood-162, F-423 → WP-645):** "non-infra failure" is decided by `isInfraStepFailure` (`packages/sdk-ts/src/runner/strike-accounting.ts:38`), which recognises only a wall-clock cap kill — `infraFailed` is set solely on `proc.timedOut` (`packages/sdk-ts/src/executors/step.ts:241`). An executor CRASH (`packages/sdk-ts/src/executors/gemini-cli.ts:87`, `ok: false, retriable: true`, no flag) is therefore treated as delivered work: dogfood-162 step 6 died with a 64-byte transcript and had its 12,589-byte half-written file committed, judged 0/3 and sealed as the run's answer. A crash and a cap kill are the same class of event and only one gets the infra treatment. ✅ **WP-644 + WP-647 DONE — dogfood-165 (`run-99c6aad9-fee6-47cb-abcb-80737959032e`, SUCCESS 1 step $0.0954/$20.00, 11m 45s, `docs/reports/dogfood-165.md`).** The repeat decision is now defensible in BOTH directions on prose it was not fitted to: **7 of 8** labelled restatements from this campaign's own judge (incumbent 4), **0/3** on same-run same-rubric-id collisions, **25/1501 (1.67%)** cross-family — every number re-measured at the review. This closes F-417's inverted half in the measurable direction: the run's answer to "have I already been told this?" no longer strands a converging run on a reworded complaint, and a strict SUPERSET restatement (the first complaint repeated with a second point added) is now decided in code (`packages/sdk-ts/src/workflow/completion-review.ts:563`). WP-647 shipped the ORACLE rule that made it gradeable — recall floor and both ceilings in ONE check over a population no delivery can enumerate — after dogfood-162 passed every criterion it had at a 44.06% soundness collapse. ⚠️ **Residue → WP-650 (F-437):** two objections that differ only in the identifier naming WHICH thing is faulted still merge — **60/120 (50.0%)** of a repo-enumerated locus-substitution population, measured IDENTICAL on the pre-WP-644 comparator, so this is long-standing rather than newly introduced (the narrow `AC-1`/`AC-2` case is the new part, pinned to the loosened counts at `:529` by ablation). DX-9 stays **in-progress** on the same F-418 axis — the loop can now tell *reworded* from *new*, but not yet *same complaint about a different target*. F-418's harder half (same wall, new code) is untouched. |

## JD — Agent-as-a-Judge (spec §5.3)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| JD-1 | Built-in eval layer: one agent validates another's work | WP-131 | P1 | done |
| JD-2 | Inner loop: evaluates every N actions / at milestones | WP-132 (every N); WP-217 (empty-diff completion milestone — landed `ef4b16f`, dogfood-003); WP-221 (explicit `claimsComplete` signal, dogfood-003 F-11); WP-225 (de-flaked the agent-loop test exercising the WP-217 path — waitFor now gates on the durable verdict, delivered via dogfood-009 `run-841bc838`, commit pending review; **F-19 closed**, 8/8 host runs) | P1→P2 | **done** (cadence + empty-diff milestone triggers done, milestone test deterministic; explicit completion claim WP-221 complete. **WP-221 Slice A** (dogfood-019 `run-d836635b`): `agent-loop.ts:211`'s trigger is the pure `isCompletionMilestone(record)` ORing `claimsComplete` into the empty-diff condition. **Slice B** (dogfood-021 `run-91eced6b`): the runner reads the marker via pure `claimsCompleteFromSummary` → `StepRecord.claimsComplete` on the SUCCESS branch, judging the productive step directly. **F-11 CLOSED by observation** (dogfood-022 `run-499218ef`): the first marker-emitting real run sealed SUCCESS in ONE step with no empty-diff probe (`components over time: s0 j@0`); the probe-step tax — one inference step across twenty data points dogfood-002…021, cost share 5.4 %–35.1 %, dogfood-017 the sharpest case F-26 — is retired). **WP-608 (F-311) DONE — dogfood-142** (`run-c857c058-8358-4fc3-a2bc-3fc94a455636`, delivery landed this review): the milestone triggers now have a NEGATIVE partner. An empty-diff step whose summary asks for permission is classified BEFORE the judge (`decideQuestionStep`, `src/workflow/agent-loop.ts:1050`), journals a `control_event` with `source: "question_step"`, buys ZERO judge passes, is re-driven carrying `STANDING_APPROVAL_ANSWER` verbatim (`src/util/standing-answer.ts:12`) and does NOT increment `completionReviewAttempts` — so the one bounded repair step survives an ask (the F-311 harm: on `run-83bf691d` the granted fix step was itself a question, and the defect shipped). Measured waste retired: 56.8% of that run's spend. **Not yet closed — F-355:** the *stall* form of the same passivity ("I have launched it and am waiting", 0 tool calls, 0 bytes) is not an ask, fires no classifier, and took 3 of dogfood-142's own 4 steps.) 🔴 **F-355 REOPENED AND MEASURED, → WP-632 (dogfood-149, `branch-run-f57c3f17-a3d7-4273-b62f-82beb8c63a03-step-5-770afe4b`, `docs/reports/dogfood-149.md`).** The stall form named above is not merely unclassified — it corrupts the milestone signal this requirement rests on. MEASURED: step 2 produced a **0-byte diff** and 0 tool calls in 100.4s, its summary claimed completion with a fabricated verification table ending `CHIKORY_TASK_COMPLETE`, and because `decideQuestionStep` (`packages/sdk-ts/src/workflow/question-step.ts:55-65`) only classifies an empty diff whose summary ASKS for approval, the step went to the judge as a normal step. The judge then answered `design_serves_overall_goal` **PASS** — *"There are no design changes in the supplied diff to judge adversely"* — for a rubric row that had FAILED at the preceding pass and FAILED again at the following one, over the same defect. Cost: 2 judge passes, **$0.09997 = 36.2% of run spend**, for zero bytes. JD-2 says the inner loop evaluates at milestones; an empty step is not a milestone, and letting one write a PASS into the per-step rubric history makes the loop's own record of whether an objection is being answered unreliable. **WP-632** carries the fix: carry forward the previous answer for model-judged rows on an empty-diff pass (never for machine-derived rows like `tests_pass`, which are re-derived against the whole tree), and never spend the one bounded repair attempt. 🟢 **WP-632 DONE — dogfood-150 (`run-17010886-9ade-4937-8d9f-db0a67b143a7`, SUCCESS 1 step, $0.1019/$20.00, 12m 16s, AC 3/3 re-run green, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-150.md`), landed this review.** JD-2 says the inner loop evaluates **at milestones**; an empty step is not one, and the loop no longer records it as if it were. A judge pass whose whole window delivered nothing now carries the previous pass's answer for every rubric row `isRubricItemSettledAgainstWholeDelivery` does NOT call settled, keeps the freshly derived machine answer for every row it does, and invents nothing when there is no previous answer (`reconcileEmptyStepRubric`, `packages/sdk-ts/src/judge/rubric.ts:158`); the reconciled form is rebuilt through `buildVerdict` BEFORE the journal writes, so the verdict, `standingRubricFindings`, `computeVerdict` and `chikory trace` read ONE form, and the crash-window path rebuilds from the already-reconciled journaled form. A zero-byte step also no longer consumes a completion-review grant, so a run whose executor stalls once still gets the repair it was promised — `MAX_COMPLETION_REVIEWS` is untouched at 2. **F-355 is CLOSED**: both halves of executor passivity are now classified — WP-608 intercepts the empty step that ASKS, WP-632 handles the empty step that ASSERTS. **Three 🔴 defects in the delivery's own stall path were found by human review and hand-fixed this sitting, all probe-verified both ways:** F-373 (the stall branch briefed the executor with a `DESIGN REVIEW BRIEF` naming zero findings and skipped the WP-619 adjudication review — now gated on the failing rubric alone, `packages/sdk-ts/src/workflow/agent-loop.ts:1300`), F-374 (the repair grant was bounded only by `max_steps`: 5 consecutive 0-byte stalls all granted, 8 judge passes — now capped by `stallRepairGrants`, `:271`/`:1302`), and F-375 (at `cadence: 2` a pass that had just read and cleared an objection against earlier REAL diffs had its fresh answer overwritten by a stale one — now keyed on every step in the pass's window, `packages/sdk-ts/src/runner/activities.ts:853`). Pinned by `packages/sdk-ts/test/runner/empty-step-stall-boundaries-live.test.ts`; suite 1,540 → **1,551**. **The judge scored 0 true-positives on this run** — 2 passes, both PROCEED 3/3 — because all three defects lived in input families neither the ACs nor the delivered tests ever instantiated. |
| JD-3 | Gates next action: halt/rollback/branch/escalate before bad change lands | WP-132 (halt/rollback/escalate); branch verdict WP-205; **WP-244 (deterministic judge-catch seam — DOGFOOD-PROVEN dogfood-046)** | P1→P2 | in-progress (halt/rollback/escalate done; **branch verdict DONE — dogfood-080 added an additive `"BRANCH"` verdict kind (`verdict.ts`, Rule 1 ROLLBACK still checked FIRST, never replaced), `docs/reports/dogfood-080.md`**; WP-205 branch command/journal-fork/worktree side effects now also DONE (dogfood-080). **The real-time true-positive catch (a judge blocking a genuinely wrong diff *before* it lands) is the product's core thesis and is now DOGFOOD-PROVEN — and reproducible on demand — via dogfood-046** (`run-b024565e-…`, runtime `ebab493`, delivery committed `5b6ca24`, `docs/reports/dogfood-046.md`): the WP-244 `debug.seedBadDiff` seam overwrote a correct `clamp` with `return value;` after the executor finished but before the judge ran → the cadence-1 judge's `vitest` AC went red (AC-1 `exited 1`) → the deterministic override (`harness.ts:105`) blocked the SUCCESS seal (the catch) → the executor restored a correct impl from the failing-test feedback → SUCCESS in 2 steps. The DOGFOODING §1.1 KPI ("regressions the judge caught pre-land") is sealed and forceable. **Nuance:** the catch came from the judge-*executed* acceptance test, not the LLM's diff reading — the LLM verdict on the seeded bug was PROCEED, its rubric (`no_unrelated_deletions`/`scope_matches`/`no_secrets`) all passed; only running the test caught it. This confirms the CLAUDE.md invariant *Agent-as-a-Judge runs tests, not text grading*. **Predecessor (dogfood-045 F-46):** "hope the executor fails" is non-deterministic — `codex`/`gpt-5.5` one-shot a five-edge-trap `truncateMiddle` → no catch; the WP-244 seam removed the luck. **Follow-ups:** F-47 → WP-245 (the seam fires with no journaled telemetry; the trace's `injections 0` masks the catch); WP-246 (per-node seam wiring) **landed `3fc27bb` but the chain-level catch is STILL UNPROVEN** — dogfood-047 (`chain-989b31b9-…`, `docs/reports/dogfood-047.md`) was launched **without** the `CHIKORY_SEED_BAD_DIFF_*` env, so the seam never armed (node B `task_json` has no `debug.seedBadDiff`, trace `injections 0`) → clean SUCCESS 2/2, no catch (the F-32 "path not exercised" wasted-run mode the spec header warned about). Two new gaps block an honest re-run: **F-48 → WP-247** (no pre-flight guard refusing/warning when a seam-requiring spec is launched disarmed) and **F-49 → WP-248** (the AC `check` gates on the executor's *self-authored* tests — dogfood-047's executor deviated from the spec's mandated formula/assertions and the judge passed it `scope_matches_instruction ✓`; a circular gate). **Re-run dogfood-047 with the seam armed once WP-247/WP-248 land.** The durable-chain machinery itself worked: 2-node SUCCESS, real WP-239 dependent handoff, family-diverse, harvested IDENTICAL `37cddb1`) **→ NOW PROVEN AT CHAIN LEVEL (dogfood-048, `chain-b7665e97-…`, runtime `3fc27bb`, delivery committed `2c516d5`, `docs/reports/dogfood-048.md`): the armed re-attempt landed the FIRST chain-level true-positive catch. Inside a dependent node of a durable chain, node B wrote a correct `truncateToCents` (importing node A's `truncateDecimals` via the WP-239 handoff) at step 0, the WP-246 seam (`CHIKORY_SEED_BAD_DIFF_NODE_INDEX=1`) overwrote it with `return value;`, node B's cadence-1 judge re-ran AC-2 → `vitest exited 1` → deterministic override → AC-2 FAILED (0/1) → node B refused to seal SUCCESS (the chain-level catch) → executor restored a correct impl from the failing-test feedback → node B SUCCESS → chain SUCCESS 2/2. Verified armed (`debug.seedBadDiff` in node B `task_json`, node B 2 steps, step-2 diff base = the seeded `return value;`); family-diverse (`codex`/`openai` vs Google `gemini-3.1-pro-preview`). Both dogfood-047 gaps closed at spec-authoring time: F-48 (env baked into the launch header + verified armed) and F-49 (AC `check` grep-pins the mandated literals — the circular gate broken, the executor could not rewrite the test). WP-246 → 🟢 DOGFOOD-PROVEN; the §1.1 KPI now holds one level up, in a durable multi-run chain. Residual: the seam still journals no telemetry (F-47 → WP-245; node B trace reads `injections 0`); minor F-50 (graded gate enforces behaviour + assertions but not mandated prose — node B dropped the spec's JSDoc; folds into WP-248).** **Update (dogfood-049, `run-26e74ad3-…`, delivery committed `dde765b`, `docs/reports/dogfood-049.md`): WP-247's pure pre-flight decision LANDED — `describeSeamArming(env): SeamArmingReport` (`packages/sdk-ts/src/cli/seam-precheck.ts`, re-exported `index.ts:114`), the `evaluateBaselinePrecheck`/`precheck.ts` analog reading the four `CHIKORY_SEED_BAD_DIFF_*` keys → armed/disarmed + optional 0-based `nodeIndex` + empty-CONTENT warning, mirroring the host-side readers `cli/chain.ts:158-171`/`cli/commands.ts:235`. `codex`/`gpt-5.5` one-shot SUCCESS, grep-pinned assertions verbatim (F-49), judge ✓ PROCEED 1/1, full suite 449 passed. WP-247 → 🟡 (the launcher banner wire + the structural disarmed-spec guard still owed). New F-51 → WP-249 (harvest-commit hygiene: `dde765b` bundled an unrelated operator `land.test.ts` edit + cites no run-id → `dogfood-verify §6`/`git log --grep <run-id>` can't resolve the landed commit). Top remaining seam debt is now F-47 → WP-245 (seam telemetry) — the chosen next headline (dogfood-050).** **Update (dogfood-050, `run-55eb5422-…`, runtime `a4e9665`, delivery uncommitted byte-IDENTICAL to the run workspace, `docs/reports/dogfood-050.md`): F-47 → WP-245 CLOSED in code — the seam is now self-documenting.** `codex`/`gpt-5.5` one-shot all 7 files (6 named + the entailed `schemas.ts` Zod-enum mirror) in 1 step: additive `"seam"` `JournalEntryKind`, a `recordSeamEvent` activity (idempotent `appendOnce` keyed on `seamEventIndex`, payload carries byteCount NOT content — CM-3), the agent-loop wire inside the seam-fire guard, and the `chikory trace` ` · seams fired N` surfacing (additive, no-seam path byte-identical). Judge `gemini-3.1-pro-preview` ✓ PROCEED 1/1 scope `✓`, all 5 grep-pins verbatim (F-49), $1.0340/$5 (20.6%), judge 0.7%, 770k/6.5k tokens; full SDK suite 451 passed re-verified incl. the `verdict-gating` "seedBadDiff ARMED" Temporal path (the new activity is registered, not just unit-mocked). The seeded-catch proof now survives in a durable `seam` journal entry + the trace, not three-blob byte-archaeology. Minor residual F-52 (telemetry unit-proven but not yet observed live — this dogfood instruments-not-arms by design; confirm `seams fired 1` on the next armed run or add a one-line `journal.entries("seam")` assertion to the ARMED test, NOT a scaffold-hosted re-run; folds into WP-245). **The seam saga is settled (dogfood-046/048/050); the chosen next headline pivots to the context-rot pillar — WP-207 pacing telemetry (dogfood-051, see FA-3/SE-2).** **Update (dogfood-054, `run-f7106c03-…`, committed `cfb8bcd`, `docs/reports/dogfood-054.md`): the true-positive catch now lands on REAL product-WP code, off the throwaway scaffolding.** The corrupted file was WP-215 S1's real `scanDiffForSecrets` (not a disposable `clamp`/`roundTo`/`truncateDecimals`): `codex`/`gpt-5.5` wrote a correct scanner → the seam stubbed it to always-`[]` (102 bytes) after step 0 → cadence-1 judge `vitest` AC `exited 1` → deterministic override → AC FAILED (the catch, pre-land) → executor restored from the failing-test feedback → SUCCESS in 2 steps, $1.33/$5, judge 1.0%, family-diverse. The §1.1 KPI is now sealed on a genuine plan.md deliverable, closing the dogfood-review §5 scaffold-hosted failure mode for this pillar. **Update (dogfood-086, `run-88235198`, `docs/reports/dogfood-086.md`): the ESCALATE half of JD-3 is now CHUNK-AWARE + UNATTENDED-SAFE (WP-271 → 🟢).** The judge previously received the full `spec.goal` and would spuriously ESCALATE an on-track intermediate chunked step for a later PART deferred BY DESIGN, then block on an untimed approval `await condition(...)` (an infinite hang for an unattended run). WP-271 threads the active `work_chunk` directive into the judge prompt via a net-new pure `renderActiveWorkChunkScope` (a `## ACTIVE WORK CHUNK` scope section telling the judge deferred later parts are not omissions), and adds a pure `decideEscalationWait(state, policy?)` wired into both agent-loop ESCALATE paths so an opt-in `UnattendedPolicy{escalation:"seal_resumable_failed"}` seals a resumable terminal instead of hanging (default `undefined` policy = byte-identical to the prior approval wait). Additive across TS+Python contracts + shared fixture, LIVE-proven; F-107 closed, the ⑦ overnight-unattended rung unblocked. Residual 🟡 F-110 (the escalate seals `FAILED`, conflating a policy-park with a genuine failure — a distinct resumable terminal needs a frozen-`RunStatus` ADR; track-B). **Update (dogfood-087 review, F-112 → WP-273): WP-271 made the judge chunk-aware only in the LLM PROMPT — the DETERMINISTIC `computeVerdict` rules were still whole-goal-scoped.** The history-keyed rules assume a stable goal, but WP-270 chunking makes criteria non-monotonic by design (a terminal AC a later chunk satisfies fails the earlier chunks). 🔴 **F-112 (Rule 3 consecutive-fail HALT) was hand-fixed** — dogfood-087 (`run-a94253fd`) died at step 3/6 because AC-2/AC-3 failed the first 3 chunks by design and tripped the goal-drift HALT; an additive `workChunkInProgress` flag (threaded `agent-loop`→`judgeStep`→`runJudgePass`→`computeVerdict`) now suppresses Rule 3 on non-final chunks and resumes it on the final chunk + completion re-verification (+2 tests, 715 green). **WP-273 → 🟢** completed the rest: Rule 5 flip-flop ESCALATE fixed with the same `workChunkInProgress` flag (a criterion oscillating across chunks is state-change, not judge drift; +1 test, 716 green), and Rule 4 out-of-rubric-concern ESCALATE decided NO code change — it stays firing as the human-escalation safety net (WP-271's prompt scope is trusted, and a residual misfire now seals resumable-FAILED under the unattended policy, not a hang). Rule 1 ROLLBACK (diff-keyed) + the completion seal are confirmed chunk-safe, no change. **Update (dogfood-110, `docs/reports/dogfood-110.md`): 🔴 WP-533/F-159 — the judge's gate decision is currently OVERRIDDEN by a cruder signal.** `packages/sdk-ts/src/executors/step.ts:167` marks a step FAILED on any non-zero executor process exit; on a "fix-until-green" benchmark task the agent's own final test command exits non-zero every step until the suite is green, so 3 consecutive steps trip the runner's escalation and seal the run FAILED **even when the judge just voted PROCEED** (dogfood-110 `brownfield-001` step 3: judge PROCEED 2/3, run FAILED). This inverts the JD-3 invariant (the judge should gate the next action, not a raw exit code). Fix (WP-533): make step success judge/AC-driven — treat "executor exit non-zero WITH a real diff and a PROCEED judge" as IN_PROGRESS, reserve FAILED for adapter/tooling errors. **✅ SUBSTRATE LANDED 2026-07-22:** `runCliStep` dropped the standalone `exitCode !== 0 → FAILED` branch — the adapter parser's structured `ok` is now authoritative (SUCCESS on a completed turn even with a non-zero process exit); `parsed.ok=false` stays FAILED with exit-code/stderr forensics. 956 TS green. NOT yet live-proven — dogfood-111 (`brownfield` re-run) is the proof.  **🟢 **WP-601 DONE (dogfood-141, `run-5ab10621-fd38-420d-aff2-176486cf9f9a`, delivery landed after independent verification, `docs/reports/dogfood-141.md`) — the gate can no longer be outrun:** every pass's failed rubric items + free-text concerns become `standingFindings` (`src/workflow/agent-loop.ts:266`) that survive clean diff windows and are adjudicated ONCE at the completion review — cleared → SUCCESS, upheld → resumable FAILED naming the item. This closes the judge-detects-but-does-not-gate family (all six altitudes). Its first live adjudication also exposed 🔴 F-344 (a process-evidence concern is un-clearable by any diff — the adjudication standard now scopes upholds to defects in the DELIVERED work, `src/judge/prompt.ts:202` + `src/judge/rubric.ts:110`, hand-fixed 2026-08-15). _History (🔴 dogfood-130, F-295 → WP-601 — a gate that could be OUTRUN):** the judge's diff evidence is incremental (`workspace diff since <last checkpoint commit>`), so a `design_serves_overall_goal` objection raised against code committed at step N is simply absent from the evidence at step N+2, and nothing carries it forward. Measured: pass #1 failed the item naming the `resummarize` aggregate-synthesis fallback (*"reconstructs requirement identities and satisfaction positions rather than preserving actual per-requirement evidence"*), pass #3 passed the same item over a window starting after that code, and the fallback shipped unchanged (`main.ts:684-717`, `main.ts:728-768`). **Gating the next action is not enough if the objection evaporates before the seal** — JD-3 needs unresolved concerns to persist as standing verdict input until explicitly discharged. Sibling of WP-599 (a concern DROPPED when the rubric also fails); together they are the two ways an objection dies unadjudicated. **Update (dogfood-144/145/146): the seal-fidelity half of this family is now CLOSED.** F-344's fix (above) proved incomplete on its own live run — 🔴 F-359 (dogfood-144, `docs/reports/dogfood-144.md`): a design finding raised at the SEALING pass (not an earlier pass) had no repair path at all, condemning a run the judge never got to re-examine; **WP-627 fixed it** (dogfood-145, `run-1f2a02e0-…`): fresh/standing/sealing-pass findings now flow through ONE decision buying exactly one bounded repair attempt before sealing FAILED. That same run then surfaced 🔴 F-361 (dogfood-145): `standingFindings` was append-only, so a `tests_pass` failure a LATER pass re-derived green (over the whole delivery, not an incremental diff) still condemned the seal; **WP-629 fixed it** (dogfood-146, `run-7ad992b2-…`, `docs/reports/dogfood-146.md`): `standingRubricFindings: Map<string,string>` clears a rubric row when `isRubricItemSettledAgainstWholeDelivery` says a later pass is authoritative, while a model-judged objection (WP-601's own guarantee, `standing-findings-live.test.ts:165`) and every free-text concern (WP-599's future fix) still persist untouched. **WP-599 (a concern dropped when it arrives ALONGSIDE a rubric failure) is now DONE — dogfood-153 (`run-8113a98d-53e2-42b6-bed0-41e7d08920ca`, SUCCESS 1 step $0.0852/$20.00, `docs/reports/dogfood-153.md`), closing the family.** The dogfood-153 premise re-measurement relocated it: the concern was never lost (WP-619/WP-630 landed `standingConcerns` after WP-599 was written, and the completion review adjudicates it), so the real defect was that the loop would CONDEMN a run at the seal over a diagnosis it never handed to the agent that could still fix it. `buildCriterionFeedback` (`src/workflow/remediation.ts:91`) now carries blocking out-of-rubric concerns into the next step via the one `blockingConcerns` policy, with rule 4's `rubricFails.length === 0` guard deliberately untouched. 🔴 **F-384 hand-fixed at that review** (the two sections were joined then clamped as one 2,000-char string against 2,851–6,532-byte justifications, so the concerns section was dropped in every combined case); 🟡 residual F-385 → WP-635. 🟢 **WP-630 DONE — dogfood-148 (`run-2213aec1-9683-4c6a-a496-b74f968975c1`, SUCCESS 1 step $0.0808/$20.00, 6m 15s, 2/2 criteria, harvest byte-IDENTICAL 2/2, `docs/reports/dogfood-148.md`), landed this review:** a gate can only gate what still exists at adjudication time. `standingRubricFindings` is now `Map<string, string[]>` (`packages/sdk-ts/src/workflow/agent-loop.ts:272`), so a second, DIFFERENT objection on the same rubric id APPENDS instead of overwriting the first (`:1149`, `:1154`) — closing F-364, the last member of the objection-dies-without-adjudication family (F-288 concern dropped, F-295 objection outrun by more steps, F-361 cleared finding condemns the seal). Identical justifications still collapse to one; settlement still deletes the whole id at once (`:1157`), now pinned live at `packages/sdk-ts/test/runner/standing-findings-overwrite-live.test.ts:213`. 🟡 **Residual: F-365 → WP-631 (queued)** — the accumulation this WP created is UNBOUNDED, and every entry renders as its own uncapped completion-review bullet (`packages/sdk-ts/src/judge/prompt.ts:227`); a gate that floods its own adjudicating prompt trades one failure mode for another. |
| JD-4 | Software-native: PR diffs, tests, UI snapshots, acceptance criteria, security posture, architecture rubric | WP-131 (diffs+tests+criteria); WP-211 (UI); WP-215 (security+architecture) | P1→P2 | in-progress (diffs+tests+criteria done; UI P2). **WP-215 security S1 LANDED (dogfood-054, `run-f7106c03-…`, committed `cfb8bcd`, `docs/reports/dogfood-054.md`):** the pure secret-scan judge-evidence primitive `scanDiffForSecrets(diff): string[]` (`src/judge/scan-secrets.ts`) — added-lines-only, `/AKIA[0-9A-Z]{16}/`→`aws-access-key`, `/sk-[A-Za-z0-9]{20,}/`→`openai-key`, sorted+de-duped; pure, no contract change. **S2 LANDED (dogfood-055, `run-73437934-…`, runtime `88d2102`, delivery uncommitted byte-IDENTICAL on the working tree, `docs/reports/dogfood-055.md`):** the scanner is wired into the inner-loop judge evidence — `collectEvidence` calls `scanDiffForSecrets` over the FULL diff into a new REQUIRED `CollectedEvidence.secretScanLabels` field (before the prompt-excerpt truncation), `prompt.ts` renders a `## EVIDENCE — deterministic secret scan (added diff lines)` section (labels `- <label>` / `(none)`), `harness.ts` threads it through; additive, no contract change, LLM still adjudicates `no_secrets_introduced`. `codex`/`gpt-5.5` one-shot all 4 files in 1 step; judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2; vitest 471 passed, tsc+eslint exit 0. The section rendered live but `(none)` (the run's own diff is secret-free by design — non-empty firing deferred to the WP-253 dogfood). **S3 LANDED (dogfood-056, `run-37862cf7-…`, runtime `8e4661c`, delivery uncommitted byte-IDENTICAL on the working tree, `docs/reports/dogfood-056.md`):** the pure example-key allowlist shipped — new `src/judge/secret-allowlist.ts` (`EXAMPLE_SECRET_VALUES` frozen set + pure `isExampleSecret(value): boolean`, AWS's `AKIAIOSFODNN7EXAMPLE` built by concatenation) + a NEW `scanDiffForRealSecrets(diff): string[]` in `scan-secrets.ts` (`.match`es the existing patterns, EXCLUDES allowlisted dummies; the evidence-facing `scanDiffForSecrets` behavior UNCHANGED — the 5-case S1 suite is the regression guard) + a 3-case vitest. `codex`/`gpt-5.5` one-shot all 3 files in 1 step; judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓; $0.4744/$5 (9.4%), judge 2.0%, 328k/5.4k tokens; AC-1+AC-2 re-run green (4 grep-pins + vitest 474 passed | 19 skipped, tsc+eslint exit 0). Additive, no contract change. This unblocks the deterministic `no_secrets_introduced` override (a live override would otherwise self-trip ROLLBACK on example keys in test fixtures). **The override itself = WP-253, the §4 hand-design follow-up (operator-landed).** **S4 dependency-scan primitive LANDED (dogfood-059, `run-bc841ce6-…`, runtime `00b31e8`, `docs/reports/dogfood-059.md`):** a pure `scanDiffForNewDependencies(diff): string[]` (`src/judge/scan-dependencies.ts`) — the `scanDiffForSecrets` analog that flags external packages a diff newly imports (added-lines-only, import/require specifier extraction, relative/absolute/`node:` exclusion, scoped/unscoped package-name normalization, sorted+de-duped); `codex`/`gpt-5.5` SUCCESS in 1 step, judge ✓ PROCEED 2/2, 11-case vitest, full suite 500 passed. **S5 evidence WIRE LANDED (dogfood-060, `run-291cf0b5-70d4-4919-8669-cef579679a56`, runtime `96df844`, `docs/reports/dogfood-060.md`):** the scanner is threaded into the inner-loop judge evidence — `collectEvidence` calls `scanDiffForNewDependencies` over the FULL diff (BEFORE the prompt-excerpt truncation) into a new REQUIRED `CollectedEvidence.newDependencyLabels` field, `prompt.ts` renders a `## EVIDENCE — deterministic new-dependency scan (added diff lines)` section (`renderNewDependencyLabels`, labels `- <label>` / `(none)`), `harness.ts` threads it through; additive, no contract change, the LLM still adjudicates the verdict. `codex`/`gpt-5.5` one-shot all 5 files in 1 step; judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2; $1.0665/$5 (21.4%), judge 0.9%, 775k/8.8k tokens; the section rendered live but `(none)` (the run's own diff adds no NEW external package — non-empty firing unit-proven). **The secret (S1→S2→S3) + dependency (S4→S5) judge-evidence chain is now complete; the deterministic dependency/secret OVERRIDE that flips a verdict on a non-empty scan = the WP-253-style §4 follow-up (operator-landed).** ⚠️ **dogfood-060 F-57 → WP-249 ESCALATED 🔴:** the harvest commit `821cae5` bundled 2 unrelated host files the run never wrote that fail `eslint .` → `main` went RED on AC-2's own lint gate while the judge read green (the judge graded the clean clone; nothing re-gates the landed commit) — fixed inline, the durable fix is a post-harvest AC re-gate on the landed tree. **dogfood-061 LANDED that durable fix → WP-249 de-escalated 🔴→🟡 (`run-f4dcc770-c9a6-4180-a0b6-28da39a60206`, runtime `ea29799`, delivery STAGED byte-IDENTICAL pending harvest, `docs/reports/dogfood-061.md`):** `chikory land --verify` now re-runs the run's OWN journaled `acceptanceCriteria[].check`s against the LANDED commit's tree (fail-closed, keep the commit, exit 1 on red) via an injectable `loadAcceptanceChecks` dep + journal-reading default + an `acceptance: N/N checks green` count; `codex`/`gpt-5.5` SUCCESS 1 step, judge ✓ PROCEED 2/2, vitest 505 passed, tsc+eslint exit 0 — the product realization of JD-3's "gate before a bad change lands" on the loop's own land step. **Residual F-58 (why WP-249 stays 🟡):** the fix lives in `chikory land`, but the dogfood loop harvests via `scripts/harvest.sh` + a manual `git commit` that bypasses `land --verify` — so F-57's failure mode is still reachable until the harvest path adopts the re-gate (track-B operational adoption). Architecture rubric still P2. **✅ dogfood-073 F-78 → WP-264 LANDED (🟢, dogfood-074, `run-6063231c-402e-4eda-9881-736b39cbf876`, runtime `4112701`, delivery STAGED uncommitted byte-IDENTICAL, `docs/reports/dogfood-074.md`): the judge-check runner now reaps the check's whole process tree at the cap.** `runCheck` (`src/judge/evidence.ts`) replaced the direct-`/bin/sh` `execFileAsync` with the group-killing `runBounded(…, { maxSeconds: timeoutMs/1000 })` (`spawn(detached:true)` + `process.kill(-pid)` SIGTERM→SIGKILL), preserving the `CheckRun` shape, the exact `[check timed out after ${timeoutMs}ms]` marker, and the 64 KiB `bound()`; a timed-out check is always nonzero. New behavioral test through the public `collectEvidence` proves a `sleep 60 & sleep 60` pipe-holding grandchild is REAPED in **1104 ms** on a 1 s cap (pre-fix ~60 s). `codex`/`gpt-5.5` SUCCESS 1 step, judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2, full suite 576 passed | 19 skipped. An infra hang can no longer masquerade as a substantive red AC. Unblocks the WP-263 residual. **✅ ARCHITECTURE RUBRIC LANDED (dogfood-085, `run-17b5ef57-7064-4e35-95cc-e3edfbad7dea`, 2026-07-04, delivery uncommitted byte-IDENTICAL on the working tree, `docs/reports/dogfood-085.md`):** the deterministic architecture-posture chain shipped end-to-end, the exact `scanDiffForSecrets`/`scanDiffForNewDependencies` analog — a net-new pure `scanDiffForLayeringViolations(diff): string[]` (`src/judge/scan-layering.ts`: added-lines-only import extraction, a 10-layer declared `LAYER_ORDER` core→providers→…→cli, flags a lower-layer file importing a higher layer as a forbidden `from→to` edge, sorted+de-duped, empty-safe, pure) → wired into `collectEvidence` (`evidence.ts`) over the FULL diff into the additive REQUIRED `CollectedEvidence.architectureLabels`, threaded through `harness.ts` → rendered in `prompt.ts` (`renderArchitectureLabels`, `## EVIDENCE — deterministic architecture scan (added diff lines)`) → answered by a NON-destructive `no_architecture_violations` `STANDING_RUBRIC` item (`rubric.ts:41`, `destructive:false`, NOT a rule-1 auto-ROLLBACK, `RubricItem` shape unchanged) → LIVE Temporal proof (`test/judge/architecture-scan-evidence.test.ts`, co-references `scanDiffForLayeringViolations` + `createRunnerWorker`, F-97-safe). Additive, no `StepRecord`/`JournalEntry`/`Checkpoint`/`RubricItem` shape change, no new dep; tsc+eslint + full suite **699 passed | 19 skipped (104 files)**, ACs re-run green, harvest byte-IDENTICAL. Also the NON-HOLLOW rung-3 horizon host (WP-270 `work_chunks`, 5/6 non-hollow, 100% per-step reliability). **The WP-215 secret (S1→S3) + dependency (S4→S5) + architecture judge-evidence chains are ALL complete → JD-4 security-posture + architecture-rubric done** (the destructive secret/dependency OVERRIDE that flips a verdict on a non-empty scan remains the WP-253-style §4 operator-landed follow-up). ✅ **F-107 → WP-271 CLOSED (dogfood-086, `run-88235198`, `docs/reports/dogfood-086.md`):** the judge was chunk-unaware — it ESCALATEd an intermediate chunked step (all ACs+rubric PASS) on a deferred-by-design later PART, parking the run for approval; WP-271 threaded the active `work_chunk` into the judge prompt (`renderActiveWorkChunkScope`) so an intermediate step is judged against the CURRENT chunk, and made ESCALATE unattended-safe (`decideEscalationWait`/`UnattendedPolicy` seals a resumable terminal instead of an untimed hang) — the ⑦ overnight-unattended rung is now unblocked. (This re-hosted the RETIRED dogfood-084, whose original host **WP-214 multi-repo workspaces SHIPPED outside the loop — `fadc124`, `collectWorkspaceRepos` at `src/runner/workspace-repos.ts:72` wired `activities.ts:314`, LIVE `multi-repo-workspace.test.ts`; FA-1 WP-214 → DONE**.) **✅ WP-311 (big-picture design judging) HAND-LANDED track-B 2026-07-12:** the architecture rubric grew from the deterministic layering scan to LLM-adjudicated DESIGN judgment — non-destructive `design_serves_overall_goal` standing rubric item on every pass, chain `plan.goal`+outline plumbed into node judge prompts (`ChainLink.planGoal/planOutline` → `## OVERALL GOAL (big picture)` section), and a run-completion holistic review over the CUMULATIVE run diff (`COMPLETION_REVIEW_RUBRIC` + `cumulative_design_coherent`, journaled `source:"completion-review"`, one bounded design-fix retry then SUCCESS-with-findings; all items non-destructive — a finished run is never rolled back or parked on a design finding). LIVE Temporal proof `test/runner/completion-review-live.test.ts`; 894 TS green. **✅ CHAIN-COMPLETION AGGREGATE REVIEW hand-landed 2026-07-14 (uncommitted; the dogfood-103 prerequisite):** the fourth altitude — pure `decideChainCompletionReview` + a self-contained `reviewChainCompletion` chain activity (builds its own judge via `createRouter` so it fires in the real worker; reuses `runJudgePass`+`COMPLETION_REVIEW_RUBRIC`, dedicated 15-min proxy) runs ONE meta judge pass at the chain SUCCESS seal over `plan.goal`+every `NodeOutcome`+the cumulative cross-node diff, journaling additive `chain_completion_review` (kind synced TS/zod/py/CONTRACTS) + a `chikory trace` `review:` line; NON-DESTRUCTIVE (F-107 — findings recorded, sealed nodes never re-judged, chain still seals SUCCESS). LIVE Temporal proof `test/chain/chain-completion-review-live.test.ts` (3 tests); 911 TS + 84 py green. **✅ LIVE-PROVEN ON A REAL 3-NODE CHAIN — dogfood-103 (`chain-9d189c1a`, SUCCESS 3 nodes $4.46 12m 31s, `docs/reports/dogfood-103.md`): the chain journal carried EXACTLY ONE `chain_completion_review` (PROCEED, `diffBase:"chain-base"` — true cross-node cumulative diff, not the node-local fallback), all 3 nodes reviewed, 3 `node_sealed` verdicts UNCHANGED. WP-311 DONE (all four design-judging altitudes proven).** Residue: fan-out cumulative-merge deferred (linear-chain diff). **🔴 REOPENED for check hygiene 2026-07-23 → WP-535 (F-164):** judge-executed `check`s are NOT hermetic — anything a check writes into the workspace is swept into the next checkpoint commit (`agent-loop.ts:743-793` judges before `writeCheckpoint`), proven on `brownfield-003` `run-58b48706` (executor diff 2 files / 1,729 B vs commit `c6a99d74` 3 files). A checkpoint commit must byte-match the executor's own step diff. `docs/reports/bench-brownfield-20260723-222341.md`. **✅ WP-535 DONE at the substrate 2026-07-24 (dogfood-112, `run-4bd7ddc0`, landed `15649d1`, `docs/reports/dogfood-112.md`):** pure `planCheckSideEffectCleanup(before, after)` (`src/judge/hermeticity.ts`) + snapshot-before/restore-after wiring around `collectEvidence` (`src/judge/evidence.ts:248-286`, try/finally) — a check's created files are deleted and modified files restored before `writeCheckpoint`, while the executor's uncommitted work (derived strictly from the snapshot DIFFERENCE) is left untouched; the F-164 probe path (`!b && a` created → delete) is neutralized. 9 tests (8 unit + 1 live). **Residue:** 🟡 F-169 (restore path is byte-identical for text only — binary/symlink/mode fidelity + a `mkdir -p`/try-catch on the content-restore branch owed before any binary-touching check enters the corpus; the judge caught this pre-land, non-blocking, track-B). **✅ LIVE-PROVEN IN THE WILD 2026-07-25** (suite `benchmarks/results/20260725-130418-chikory`, `docs/reports/bench-brownfield-20260725-130418.md`): on the real F-164 path the `brownfield-003` R4 probe appears in **0** checkpoint commits, the graded commit `5ca79a5` holds exactly the agent's 2 files (+22), and the grader's R2 matched only the agent's own `default.test.ts` — the ⏳ closes. **New residue ℹ️ F-175:** the cleanup wraps the JUDGE (`collectEvidence`) only; the harness grader (`benchmarks/harness/src/grade.ts` `runCheck`) has no equivalent wrap, so R4's probe survives on disk into the archived workspace (mtime = grader phase). Harmless for the run (R4 grades last) but the artifact is not re-gradable — wrap `runCheck` in `planCheckSideEffectCleanup` (track-B WP-535 residue). **🔴 JD-4 PARTIALLY REOPENED 2026-07-26 → WP-537 (F-180, dogfood-113 `run-5da56db2`, `docs/reports/dogfood-113.md`): the design rubric DETECTS but does not GATE.** The design-fix retry loop ALREADY EXISTS and works (`workflow/agent-loop.ts:874-900`: completion review over the cumulative diff → `buildCompletionReviewBrief` → one bounded fix step, re-reviewed, `MAX_COMPLETION_REVIEWS=2`). It was SKIPPED because `workflow/completion-review.ts:38-43` skips a *first-verdict seal* (`sealingDiffBase === baseCommit`) — exactly what a 1-step run is — while the sealing verdict itself comes from `decideVerdict`, where a rubric-only failure falls through to a branch that also returns `PROCEED` (`judge/verdict.ts:131-149`). On a first-verdict seal the rubric result therefore has **no consumer at all**: the path that would act on it was skipped precisely because the other path had already looked. Blast radius = every 1-step dogfood (108/112/113). On dogfood-113 the judge's `design_serves_overall_goal ✗` named three concrete design defects — **two of which the review independently confirmed as real** (inverted engine-range semantics; the run/grade paths not sharing one plan, the spec's own named trap) — and the run sealed 🟢 SUCCESS at step 1 with them intact. WP-311 proved design judging at four altitudes; **at the run-SEAL altitude the verdict is inert**, so JD-4's "architecture rubric" is advisory rather than load-bearing there. Fix (WP-537), ~5 lines: `decideCompletionReview` must not skip a first-verdict seal when the SEALING VERDICT ITSELF carried rubric failures — skip only when the cumulative diff was covered AND the rubric was clean. `verdict.form.rubricResults` is already in scope at the seal site (`agent-loop.ts:843`); the brief, the bounded retry, the re-review and the F-107 "never park a run whose criteria all pass" discipline are all already built. Mid-run rubric fails stay non-destructive (dogfood-048/112 lineage must not regress). **✅ WP-537 DONE 2026-07-27 (dogfood-116, `run-688134b0-f6f0-41ed-a831-cab9dc9a8c38`, `docs/reports/dogfood-116.md`) — JD-4's architecture rubric is LOAD-BEARING at the run-seal altitude again.** `decideCompletionReview` no longer skips a first-verdict seal whose SEALING VERDICT carried rubric failures: the objection now takes the bounded design-fix path (brief → one fix step → re-review → SUCCESS either way). A CLEAN first-verdict seal still SKIPS (no cost regression), a rubric-only failure still seals SUCCESS (F-107 intact), exhaustion still wins (`MAX_COMPLETION_REVIEWS = 2`). 27 completion-review tests (2×2×3 matrix + 2 live), sdk-ts 1007/23. ⚠️ **Shipped half-wired and the JUDGE caught it (🔴 F-196):** the sealing verdict's failing items never reached `buildCompletionReviewBrief` — a second, independent review had to ALSO fail, so a clean review dropped the objection after paying the extra pass (the goal's trap C). Hand-completed in the review sitting: pure `mergeDesignFindings` unions both sides' failing items deduped by id, `agent-loop.ts:895-906` builds the brief from the union; 6 net-new tests, the live one proven RED with the merge reverted. **This is the strongest JD-4 evidence to date** — four ACs including the loop's best oracle passed a half-wired delivery, and only the design rubric saw it. ⏳ **NOT YET PROVEN IN THE WILD (🟡 F-197):** the Temporal worker is built from HEAD at launch, so the run that delivered the fix sealed SUCCESS on a rubric ✗ with nothing gating it (F-180's fifth consecutive recurrence). First live datum = **dogfood-117**, whose review must read the journal for the mechanism's signature (rubric ✗ → a `completion-review` pass + a `DESIGN REVIEW BRIEF` step summary; rubric clean → exactly one judge pass).  **dogfood-130 datum — the judge refused executor prose as evidence, correctly:** at pass #3 it escalated out-of-rubric because *"the supplied judge checks do not execute the full unit-test suite or ESLint; their success appears only in an executor-authored step summary, which the judging rules do not permit treating as evidence."* Hand-verified against the landed tree: the claim was accurate (sdk-ts 1,304 · harness 205 · lint clean). This is the discipline the AC layer keeps failing to enforce — and the same run shows why it matters, since AC-2 certified `probe.json` durability by running `git ls-files` **inside the workspace that had force-added the files** (🔴 F-293). **An AC must run where the artifact will LIVE, not inside the run's own workspace.** **✅ WP-604 (dogfood-133, `run-83bf691d-75b5-4848-a182-6e66dcb6b857`, SUCCESS 3 steps $0.1887/$20.00, `docs/reports/dogfood-133.md`) — the rubric is no longer fixed in code.** A spec states run-scoped quality rules as `judge.rubric_extra` (`{id, description}`), appended to `STANDING_RUBRIC` for every PER-STEP pass (`src/runner/activities.ts:1687-1691`), forced `destructive: false` by construction (`src/taskspec.ts:448`), and refused BY NAME on a standing-id collision, an in-list duplicate or a `destructive` key (`src/taskspec.ts:438,442`). The inert `judge.rubric_packs` is removed, so declaring it is an unknown-key error rather than a silent no-op. AC-2 proved the item reaches the judge by reading it off the REAL journaled verdict, which a stored-and-ignored field cannot do. This is the expressiveness half of the architecture rubric WP-215 reserved. 🔴 **BUT JD-4's gating half is REOPENED by F-310 → WP-607:** a rubric item scored from a DETERMINISTIC primitive is still discharged as advisory. On this very run the `no_architecture_violations` scan (`src/judge/scan-layering.ts:93`) proved a forbidden `core→judge` import, the judge failed the item at 3 of 5 passes **including the final completion review**, and `agent-loop.ts:1099-1105` sealed SUCCESS with the finding merely recorded — the breach landed on disk and was hand-fixed in review. F-180's shape, one level up: *the judge detects and does not gate*, now for deterministic evidence rather than acceptance criteria. Compounded because the per-step pass scans only its own diff interval, so an empty-diff step scores the item ✓ and launders a standing violation. ✅ **WP-607 CLOSES that gating half (dogfood-134, `run-74602888-a3eb-4da5-8ba3-b04b8e9a98a3`, SUCCESS 1 step $0.0499/$20.00, `docs/reports/dogfood-134.md`) — deterministic rubric evidence is now LOAD-BEARING at the seal.** `DETERMINISTIC_RUBRIC_IDS` (`src/judge/rubric.ts:19`) declares exactly the two scan-backed standing items; `applyCheckOverrides` rebuilds both rows from `collected.architectureLabels`/`secretScanLabels` rather than the model's answer (`src/judge/harness.ts:177,188`) — the JD-4 discipline `tests_pass` already followed, applied to evidence the judge was already computing; and a failure still live after the ONE bounded completion-review repair seals **FAILED** on both seal paths (`src/workflow/agent-loop.ts:1100,1119`). Crucially it BLOCKS without ROLLING BACK: the item stays `destructive: false`, no ROLLBACK verdict opens, the work stays on disk and the run terminates unattended — so F-107 (never park a passing-criteria run) and WP-605 (a heal must not discard work nobody condemned) both hold. Proven on the REAL Temporal loop with the scripted judge answering the item ✓ on a genuinely violating workspace, so only a delivery that lets the SCAN settle it can pass. A model's design opinion still only advises. **Residue 🟡 F-317 → WP-610:** `applyCheckOverrides` defaults `secretScanLabels: string[] = []`, so a caller that omits it force-passes the `destructive: true` `no_secrets_introduced` item — closing the model's only seam for a rollback on a secret the regex scan misses. **And the same run exposed the gate's boundary (🔴 F-316 → WP-609):** it left the suite RED in 6 tests while its step summary claimed the full suite passed. `tests_pass` is JD-4-overridden from the judge-executed ACs — it means "the acceptance checks exited 0", NOT "the repository builds green" — and a diff-only judge cannot see a test broken in a file the delivery never touched. JD-4's next slice is a deterministic pre-existing-tests-green gate on the same footing as the layering scan. ✅ **WP-609 DELIVERS EXACTLY THAT (dogfood-135, `run-908510d8-d90d-4711-a88f-39ea7bf6482e`, SUCCESS 4 steps $0.2512/$20.00, `docs/reports/dogfood-135.md`) — "the repository is still green" is now a program, not a sentence in a goal.** A spec names its own verification command (`regression_suite`, `src/taskspec.ts:194,468`), it survives the workflow schema round-trip (`src/schemas.ts:258`) and chain-node templating (`src/chain/node-spec.ts:118,170`), and the new `pre_existing_suite_still_green` standing item joins `DETERMINISTIC_RUBRIC_IDS` (`src/judge/rubric.ts:19-32`), executed through the existing bounded `runCheck` inside the hermeticity snapshot (`src/judge/evidence.ts:331-341`) and settled from the EXIT CODE with the command named in the justification (`src/judge/harness.ts:200-217`). **Cost-shaped correctly:** the command is passed only on a completion-review pass (`src/runner/activities.ts:1782`) — never per-step, exactly once on a clean run, at most twice with the bounded repair — and a declared suite forces the review a first-verdict seal would otherwise skip (`src/workflow/completion-review.ts:74`). A red suite blocks SUCCESS WITHOUT a ROLLBACK (item stays `destructive: false`), so F-107 and WP-605 both hold. **Residue: 🟡 F-319 → WP-611** (the one bounded repair is briefed as a DESIGN review and carries no failing-test evidence) · 🟡 **F-320 → WP-612** (an `infraFailed` RUBRIC row still seals FAILED — `src/judge/verdict.ts:101` skips infra only for CRITERION rows — and the 120 s cap is not spec-settable) · 🟡 **F-321 → WP-613** (with no suite declared the item records an unmeasured ✓ on every pass of every run). **Not yet proven in the wild (F-197 shape):** the judge ran the launch-time build, so the gate never judged its own run; dogfood-136 is the first spec to declare `regression_suite:` and must show the row settled from an exit code in its own journal. | **WP-613 (dogfood-136, `run-44abed04-fa4e-465b-8fd1-9c0de732c1a3`, `docs/reports/dogfood-136.md`) — a JD-4 evidence row may now exist only where its evidence does.** `pre_existing_suite_still_green` was removed from both default rubrics (`src/judge/rubric.ts:34,93`) and joins the effective rubric only when `input.completionReview && spec.regressionSuite` (`src/runner/activities.ts:1688-1692`); `applyCheckOverrides` returns an ERROR naming the id if it is ever declared without a `CheckRun` (`src/judge/harness.ts:218-220`), routed to `escalate()` (`:364-369`). The placeholder ✓ row that every run used to carry — *"no regression suite command executed for this pass"* — no longer exists, so a JD-4 checkmark now means a program ran. First live proof in the same run: the completion review settled the row from a real exit code. **WP-612 + the spec-chosen per-check cap LANDED — dogfood-137 (`run-308cc0d4-e7ae-4676-a251-eb4c9d940426`, SUCCESS 1 step $0.1095/$20.00, 7m 0s, `docs/reports/dogfood-137.md`):** a regression suite killed at its per-check cap is no longer a verdict on the code. Both sealing paths filter an infra-killed deterministic row out of `deterministicFails` (`src/workflow/agent-loop.ts:1103,1122`), extending the WP-263(b) rule that already governs criterion rows (`src/judge/verdict.ts:99-103`) to the machine-settled RUBRIC rows WP-609 introduced; the row itself is untouched — still `pass: false`, still `infraFailed: true`, still saying DID NOT COMPLETE. A genuine nonzero exit still seals FAILED on both paths (live-proven). `check_timeout_ms:` / `checkTimeoutMs` is now a declarable spec field (`src/taskspec.ts:196-197,475-479` → `src/schemas.ts:259` → `src/chain/node-spec.ts:62-63,121,174,309-310` → `src/runner/activities.ts:1786` → `collectEvidence`), so a project whose suite outruns the 120 s default can finally use the gate; absent, `DEFAULT_CHECK_TIMEOUT_MS` is unchanged. **WAS REOPENED → WP-615, NOW CLOSED (dogfood-138, `run-3ca8f79a-ab47-44bf-a90b-14ff26b09215`, SUCCESS 1 step $0.0814/$20.00, 6m 36s, `docs/reports/dogfood-138.md`):** the outcome that REPLACES the withdrawn FAILED seal is wrong — a cap-killed suite seals `SUCCESS · "completion review: design findings recorded — pre_existing_suite_still_green"` (proven verbatim on a live run), so the run never says the check did not complete (🟠 F-327). JD-4 is NOT fully satisfied for a killed check until the outcome is honest — **it now is**: `seal("SUCCESS", "completion review: check did not complete — <id>", { inconclusiveCheck: <id> })` on both paths (`src/workflow/agent-loop.ts:1123-1131,1151-1159`), the killed check's id carried as an ADDITIVE field on the terminal payload (`src/runner/activities.ts:2882`), on `RunStatusReport` live and offline (`src/types.ts:679-680`, `src/journal/journal.ts:311-321`), through `RunStatusReportSchema` (`src/schemas.ts:578`) and into `chikory trace` (`src/cli/trace.ts:437-438,809`), with `TerminalStatus` frozen. A red, a green and an ordinary design finding each keep exactly their prior outcome — four real Temporal runs in AC-2. **WP-613 LIVE-PROVEN here for the first time** (F-197: a run cannot prove the judge fix it delivers): judge pass #1 carries no `pre_existing_suite_still_green` row at all, judge pass #2 carries exactly one, settled from a real exit code. **REOPENED AGAIN one altitude up, then CLOSED BY HAND the same sitting → WP-617 ✅ (🔴 F-331):** a JD-4 evidence row can be absent entirely and the run still seal SUCCESS. The suite executes only inside the completion review (`src/workflow/agent-loop.ts:1058,1072,1074`), and the ESCALATE branch (`:1207`) seals SUCCESS before it — unattended at `:1249`, operator-approved at `:1283`. Measured on this very run, which declared `regression_suite:` and produced ONE judge pass carrying no `pre_existing_suite_still_green` row at all: a checkmark meant a program ran, but the ABSENCE of the row meant nothing at the outcome. Both escalate seals now call `regressionGateBeforeSuccess` (`src/workflow/agent-loop.ts:397`, wired at `:1301`/`:1339`), which runs a declared suite once and routes the result through the shared outcome ladder `sealFromRubricFails` (`:352`, also now the single implementation behind both completion-review sites at `:1197`/`:1207`). A red suite seals FAILED even on a converged escalation; a green one leaves the F-229/F-271 wording intact; a suite-less run buys no extra pass. Verified RED-then-GREEN against the pre-fix tree — 3 new live scenarios, sdk suite 1376 → **1379** green. **JD-4 is now satisfied in all three states: the row passes, the row fails, or the row could not answer — and each reaches the outcome.** **Also → WP-618 (🟠 F-332):** `readNodeResult` (`src/chain/activities.ts:236-259`) drops `inconclusiveCheck`, so at chain scope the honest outcome regresses to prose. **✅ THE FIFTH AND LAST OPEN GATING ALTITUDE CLOSED — WP-619 (dogfood-140, `run-cd11c095-754c-42b5-b57c-d712710d9612`, SUCCESS 1 step $0.1326/$20.00, 9m 40s, `docs/reports/dogfood-140.md`): a judge finding expressed as FREE TEXT rather than a rubric row now gates too.** Every prior fix in this family settled a *structured* answer — `tests_pass` (JD-4 override), `no_architecture_violations`/`no_secrets_introduced` (WP-607), `pre_existing_suite_still_green` (WP-609/612/613/615/617), a non-deterministic completion-review item (WP-621). The one thing the judge could say that nothing consumed was its `concerns` array: a converged out-of-rubric ESCALATE copied the text into the terminal `reason` and sealed 🟢 SUCCESS. dogfood-139 shipped two named, reproducible defects that way. Now the run's OWN completion-review pass is shown the concern text verbatim (`renderCompletionReviewScope`, `src/judge/prompt.ts:187`) and answers it through the named `escalation_concerns_adjudicated` row (`src/judge/rubric.ts:87`): cleared → SUCCESS with the F-229/F-271 wording intact, upheld → **resumable FAILED naming the item** (`src/workflow/agent-loop.ts:444`), whether or not a `regression_suite` is declared (`:397`). The decision is the JUDGE'S — nothing in the runner reads the concern string, proven by driving both outcomes over **byte-identical** concern text. Terminal-or-nothing at exactly one extra pass, so dogfood-121's dead 5-node chain cannot reopen; a concern-less run, a PROCEED, a rollback and a rubric-failing run all keep today's behavior and cost. **Review residue 🔴 F-340, hand-fixed the same sitting — the gate's own false-positive surface:** the adjudication row and the scope sentence claiming it were rendered on EVERY completion review, including the concern-LESS one any `regression_suite` run buys, so a judge answering "cannot determine" to a question with no subject condemned a correct SUCCESS through this delivery's own catch-all. Proven from the RAW wire request, fixed at `src/runner/activities.ts:1699` + `src/judge/prompt.ts:189`, Scenario 9 RED→GREEN (`test/runner/regression-suite-repair-live.test.ts:510`), sdk 1454 → **1455**. Neither AC could see it: both directions were driven by a SCRIPTED judge, so no probe ever observed the vacuous question being ASKED — the F-187/F-196/F-198 input-family discipline, now owed by every gate this family adds. **Not yet proven in the wild (F-197):** the worker is built from HEAD at launch, so dogfood-140's own true concern sealed unadjudicated; the first live datum is dogfood-141, whose journal must carry a `completion-review` verdict with an `escalation_concerns_adjudicated` row. **WP-601 (F-295) CLOSED — dogfood-141** (`standingFindings` carry every pass's failed rubric items and free-text concerns past a clean window and adjudicate them once at the completion review), which retires the judge-detects-but-does-not-gate family. **JD-4's new hole is upstream of judgment entirely — WP-623 (F-349, dogfood-142): the judge-EXECUTED half of this requirement is not isolated.** Every acceptance check runs concurrently in one shared workspace (`src/judge/evidence.ts:244`), so a check that writes a temp file can fail a sibling that compiles or lints the whole tree. Measured: dogfood-142's AC-2 died with `error TS6133` naming AC-1's transient generated test, `exited 2` on all four passes, goal-drift HALT, correct delivery sealed FAILED. JD-4 grades on tests and acceptance criteria — a criterion result that depends on which sibling was running is not a grade. Companion: WP-624 (F-350/F-351) — the $0 arming gate rehearses checks serially, so this class is invisible to it. **WP-623 DONE — dogfood-143 (`run-2a418823-1489-451f-a292-09ed8fcaa710`, SUCCESS 1 step $0.0851/$20.00, 5m 19s, `docs/reports/dogfood-143.md`), landed this review.** Both `Promise.all` batches are gone: `runCriteriaChecks` (`packages/sdk-ts/src/judge/evidence.ts:255`) and `collectEvidence` (`:345`) run their checks through a `for…of` awaiting a shared `cleanup()` after each one and again in `finally`; `runCheck` is untouched so the per-check cap, `infraFailed` and exit-code passthrough survive — a criterion now answers for the delivery alone. **Two carve-outs recorded, not closed:** 🟠 **F-357 → WP-625** — isolation covers only what `git status --porcelain` reports (`src/judge/hermeticity.ts:168`), so a check writing a **gitignored** path still leaks to its sibling and survives cleanup (measured by probe at this review: `observer exitCode=1`, artifact left on disk); and 🔴 **F-356** — the delivery pinned none of this in a test (suite 1495 → 1495), HAND-FIXED this review in `packages/sdk-ts/test/judge/check-isolation.test.ts` (4 tests, GREEN on the delivery / RED 2-of-4 on pre-fix HEAD). **WP-624 re-scoped**: WP-623 made the judge serial, which is how `dogfood-arm.sh` already rehearses, so F-350 is closed and only F-351 survives. **WP-625 PARTIAL — dogfood-144 (`run-a8fe46f3-b5e1-4f83-961c-2142220daa12`, FAILED (resumable), 1 step, $0.0831/$20.00, 5m 5s, `docs/reports/dogfood-144.md`), landed this review.** `snapshotWorkspace` now appends a name-only ignored inventory from `git ls-files -z --others --ignored --exclude-standard` (`packages/sdk-ts/src/judge/hermeticity.ts:190`), `isCreatedStatus` accepts `!!` (`:39`), and an unrestorable ignored deletion is skipped rather than handed to `git checkout` (`:149`); measured 27,252 paths / 2.7 MB in 0.49 s on this repo, so the cheapness bound holds at real scale. **Scoped to the CREATE family.** Ignored entries carry no hash and no content, so the `b && a` compare (`:138`) can never fire for one: probe-confirmed at this review, an OVERWRITTEN ignored file leaks to its sibling and survives the batch with the wrong bytes, and a DELETED one is never restored. A criterion whose result still depends on a sibling's overwrite is still not a grade → 🟠 **F-360 → WP-628** (create/modify/delete matrix; `stat` size+mtime detects cheaply, only restoration needs content). Notable for JD-4 itself: the LLM judge named both halves unprompted on a delivery that passed BOTH hand-armed deterministic checks — 1 genuine true-positive, and the clearest evidence yet that the rubric altitude catches what the criteria altitude cannot. **WP-628 DONE — dogfood-159 (`run-ec5c4bb8-de16-4c28-beba-a8cd09795fa1`, SUCCESS, 3 steps, $0.3358/$20.00, 2h 2m, AC 3/3, harvest byte-IDENTICAL 5/5, `docs/reports/dogfood-159.md`), landed this review — F-360 CLOSED.** Isolation now covers all three ways a check can change an ignored file. `snapshotWorkspace` `stat`s every ignored path (O(inventory)) and preserves content under a 64 KiB-per-file / 32 MiB-total budget (`packages/sdk-ts/src/judge/hermeticity.ts:11`, `:12`); unpreserved paths keep a `stat:size:mtime:inode` fingerprint so change is still DETECTED, and an unrepairable change is reported into the batch's own `CheckRun.output` (`packages/sdk-ts/src/judge/evidence.ts:292`) instead of being passed off as a clean tree. Content is `Uint8Array`, so non-UTF-8 artifacts round-trip byte-identical. Re-probed on a real 17,372-path judge workspace: 2.3 s per three-check batch. **Again the decisive JD-4 datum: all 3 acceptance checks were green at step 1 and the `design_serves_overall_goal` rubric found three real defects the criteria altitude could not see** (UTF-8 round-tripping, a deleted unpreserved path yielding no plan entry, budget-exceeded corruption reaching only `console.warn`); a fourth followed at pass #4. All four were repaired in-run — the fifth consecutive run where the judge caught AND the loop healed. **Still open at the JD-4 altitude:** the judge's fourth finding named a real symptom with the wrong mechanism (it blamed concurrent workers; the cause was a hash-kind mismatch across the moving budget boundary), so the repair step it bought did not repair — F-406, hand-fixed at this review. A rubric that is right about the symptom and wrong about the cause spends a step and clears itself. Residue → **WP-639** (884/17,372 ignored paths are symlinks to directories and are dropped from the snapshot entirely — detection, not restoration, is the gap). ⚠️ **THE EVIDENCE CHANNEL ITSELF BROKE AND WAS FIXED 2026-08-20 (F-410, dogfood-159 review).** JD-4 is "software-native evidence" — diffs, tests, acceptance criteria — and for one window on `main` the acceptance-criterion half produced no evidence at all while reporting success. `5bed806` (#90) replaced `runCheck`'s `/bin/sh -c` with tokenize-and-exec-the-first-word; every acceptance check in this repo is a multi-line shell script, so the script became arguments to its own first word and exited 0 without running. Measured against the real `runCriteriaChecks`: `exitCode: 0`, no file written, no assertion evaluated — the judge would have recorded "judge-executed check `AC-1` exited 0" for every criterion of every run. Restored at `packages/sdk-ts/src/judge/evidence.ts:203`, with the two tests that encoded the vacuous pass replaced by two pinning the real contract. **The JD-4 lesson: a check's exit code is only evidence if the check RAN — an exit status is not a measurement of anything unless the executor is pinned.** Compounding gap → **WP-642** (F-411): arming executes checks through bash and the judge executes them through `runCheck`, so dogfood-159's ACs armed and re-ran fully green while the judge path was dead. |
| JD-5 | Structurally diversified: different model family / prompt regime / memory than executor | WP-133, ADR-002; WP-222 slice 1 (executor env allowlist — landed `18fae43` via dogfood-006 `run-559ea904`) | P1→P2 | done (declared-family check); seam hardened — `scrubExecutorEnv` gives executor children only their own family key; F-14 closure **confirmed** by dogfood-007 `run-22b337a9` (zero shim noise in the executor transcript); TaskSpec pass-through opt-in rides WP-221's contracts PR |
| JD-6 | Scoring: pointwise + pairwise; CoT/G-Eval; optional debate/specialized evaluator | WP-131 (pointwise+CoT); WP-210 (pairwise+G-Eval); debate deferred (cost — ADR-002) | P1→P2 | in-progress (pointwise+CoT done; WP-210 BOTH pure scoring primitives now landed — G-Eval `aggregateGEval`/`normalizeGEvalScore` dogfood-058 `run-67d39267`, PAIRWISE `aggregatePairwise`/`PairwiseOutcome`/`PairwiseTally`/`PairwiseResult` dogfood-063 `run-72713667`; the pure scoring-modes surface is EXHAUSTED; `scoringMethod` field + live judge selection wire = §4 act-half follow-up; debate deferred) |
| JD-7 | Judge guardrails: binary/low-precision scores, explicit rubrics, drift/reward-hacking awareness, visible latency/cost overhead | WP-131 (binary rubric), WP-134 (cost visibility), **WP-548 (concern severity floor — DONE, dogfood-152, `src/judge/verdict.ts:72`)**, **WP-599 (a blocking concern now reaches the executor — DONE, dogfood-153, `src/workflow/remediation.ts:91`)**; drift monitoring extends in WP-306; **WP-635 DONE** (dogfood-154 — a failing check's justification carries the check's OUTPUT, not its command; `src/judge/harness.ts:130`) | P1→P3 | in-progress (binary rubric + cost visibility done; an out-of-rubric concern now carries a `minor`/`blocking` severity and only blocking ones stop the run, with unmapped ⇒ blocking so a judge cannot silence itself — WP-548; **and a blocking concern now rides into the executor's next-step feedback beside the failing-criterion evidence instead of waiting to condemn the run at the seal — WP-599, dogfood-153**. ⚠️ The channel that carries it is bounded at 2,000 chars while this run's own judge justifications measured 6,532 / 3,757 / 2,851 bytes: F-384 (joined-then-clamped, concerns dropped entirely) was hand-fixed at the dogfood-153 review. **WP-635 (dogfood-154, `run-f7735f50-5c99-4d5a-8cb8-74f84a48f964`, SUCCESS 1 step $0.1335/$20.00, `docs/reports/dogfood-154.md`) settled the sizing question by changing the PAYLOAD, not the bound:** the justification now carries the check's OUTPUT — the assertion and the author's `AC-n FAIL: …` sentence — instead of the 6,624-byte command echo (`src/judge/harness.ts:130`), with the tail kept and the cut marked; a passing check still costs ~40 bytes and a killed check still reads `DID NOT COMPLETE` (WP-263(b)/F-76/F-79). `REMEDIATION_BRIEF_MAX_CHARS = 2000` was deliberately NOT raised. F-385 closed with it: `withCriterionFeedback` is now an importable named export (`src/workflow/remediation.ts:183`) and the completion-milestone site composes rationale + criteria instead of displacing (`src/workflow/agent-loop.ts:1409`). 🔴 **F-388 hand-fixed at the dogfood-154 review** — the harness kept the output's TAIL and `clampBrief` then kept the HEAD, so at any check output ≥1,890 bytes the executor received build-banner noise and no diagnosis at all (a fully GREEN suite run prints 17,403 bytes); `clampSectionKeepingTail` (`src/workflow/remediation.ts:166`) keeps each section's header verbatim and tail-clamps its body behind a `… [head truncated]` marker, pinned by 5 tests (`test/runner/remediation.test.ts:267`, 3 RED pre-fix). 🟡 Residual F-389 (the 64 KiB collection bound `src/judge/evidence.ts:205` also keeps the head — track-B) · 🟡 F-391 (three straight runs with 0 judge concerns, so `concernSeverities` still has no live datum). Drift monitoring P3) 🔴 **GUARDRAIL GAP FOUND 2026-08-20 (F-429/F-430 → WP-648 + WP-649, dogfood-163 `run-12dfe421-7633-4199-9e3c-18bc32a410c5`):** the guardrails are one-directional — a deterministic rubric row can be raised but never retracted. `mergeDesignFindings` (`packages/sdk-ts/src/workflow/completion-review.ts:665`) skips passing rows outright (`:672`), so the completion review's re-measurement on a WIDER diff cannot clear the sealing pass's FAIL, and `sealFromRubricFails` (`packages/sdk-ts/src/workflow/agent-loop.ts:375`) then seals FAILED with no appeal. Compounded by a scanner with no baseline (`packages/sdk-ts/src/judge/scan-layering.ts:93`), which scores a RESTORED pre-existing import as a new violation — 11 such lines in 6 committed files. MEASURED: dogfood-163's judge pass #4 reported "no layering violations" with all 5 rows green and the run sealed FAILED on that row 17 ms later, with 4 of 6 steps and 98.9% of budget unspent. A gate that cannot take yes for an answer converts every true positive into a permanent one. ✅ **CLOSED 2026-08-21 by WP-648 + WP-649 (dogfood-164, `run-be874c29-6b55-4cb6-a9cc-9784170c64f5`, SUCCESS 1 step $0.113/$20.00 8m 8s, AC 3/3, harvest byte-IDENTICAL 5/5, declared suite 1,806 passed, `docs/reports/dogfood-164.md`):** the guardrails are now two-directional. A deterministic rubric row the completion review re-measures as PASS clears the sealing pass's FAIL for that id (`packages/sdk-ts/src/workflow/completion-review.ts:704`) while an LLM-judged row keeps its union intact, and the reverse disagreement (sealing PASS, review FAIL) is decided in code so a cumulative-only violation still condemns; the acquittal reaches the real seal through the single call site at `packages/sdk-ts/src/workflow/agent-loop.ts:1387`. The scanner now holds the diff's own pre-image (`packages/sdk-ts/src/judge/scan-layering.ts:35`) so a RESTORED import is no longer scored as introduced. 🔴 **F-431 hand-fixed at that review** — as delivered the exoneration keyed on the layer-pair LABEL rather than the import, which silenced a genuinely new forbidden import whenever another import of the same pair sat in the hunk as `git diff` context: measured on a real diff, HEAD reported `workflow→runner` and the delivery reported nothing, across 7 files / 12 lines of `packages/sdk-ts/src`. Re-keyed on the resolved import PATH (`packages/sdk-ts/src/judge/scan-layering.ts:181`), 3 committed tests RED pre-fix. 🟡 Residual F-432 (the acceptance oracle's recall probe used an `X→cli` edge no pre-image could hold, so its two floors could never collide — folded into WP-647) · 🟡 F-433 (the judge passed `design_serves_overall_goal` asserting the scanner was "retaining detection of genuinely new edges", false for 7 of 158 files: 1 true positive, 1 false negative on the run's central semantics). |
| JD-8 | Judge findings feed healing: actionable remediation feedback (failing criteria + evidence) rides into the next attempt on EVERY judge pass, not milestone-only; a would-be HALT first yields a remediation brief | WP-519 (ADR-009 D3) | P2 | **DONE (hand-land 2026-07-08, track-B):** `buildCriterionFeedback` carries the failing-criterion justifications into the next step on EVERY judge pass (the `agent-loop.ts` milestone-only suppression removed); a would-be rule-3 HALT first yields `buildRemediationBrief` (trigger + failing criteria + rubric fails + concerns + fix contract, clamped ≤2000 chars). Live-asserted: the step after an ordinary failing PROCEED pass SEES the evidence (`remediation-live.test.ts`). 🟡 **PARTIALLY REOPENED 2026-08-12 by F-319 → WP-611 (dogfood-135):** the completion-review half of this requirement carries a FRAMING and an EVIDENCE defect for the new deterministic suite gate. `buildCompletionReviewBrief` (`src/workflow/completion-review.ts:113-126`) is hard-coded to *"do NOT change behavior, only design"* and renders each failing item as `id: justification` — for `pre_existing_suite_still_green` that is ``regression suite command `X` exited 1``, with no failing test names and no output, even though the full `CheckRun` (including its captured `output`) is already collected at `src/judge/evidence.ts:331-341`. JD-8 says findings ride into the next attempt WITH evidence; here the one bounded attempt WP-609 grants gets neither the right instruction nor the evidence. ✅ **RECLOSED 2026-08-12 — WP-611 (dogfood-136, `run-44abed04-fa4e-465b-8fd1-9c0de732c1a3`, `docs/reports/dogfood-136.md`):** the brief's framing is now chosen by the failing item's class — a failing `pre_existing_suite_still_green` produces a **REPAIR BRIEF** directing a behavior fix with the design-only line ABSENT, while an ordinary design finding keeps today's wording byte-for-byte (`src/workflow/completion-review.ts:114-127`) — and the already-collected `CheckRun.output` is carried as a bounded tail on the rubric justification (`src/judge/harness.ts:206-210`), so the repair sees the failing test NAMES. 🟡 **Residue F-323 → WP-614:** the excerpt is bounded at 1000 chars against a 2000-char TAIL-truncating whole-brief clamp (`src/workflow/completion-review.ts:19,137-139`) with the suite row rendered LAST, so at two co-occurring design findings the evidence tail and the closing instruction are both cut — the brief obeys its budget but not JD-8's *WITH evidence*. **WP-614 LANDED — dogfood-137 (`run-308cc0d4-e7ae-4676-a251-eb4c9d940426`, SUCCESS 1 step $0.1095/$20.00, 7m 0s, `docs/reports/dogfood-137.md`), with a 🔴 review hand-fix.** The repair brief no longer clamps an assembled page from the tail: it measures the fixed part (header + EVERY other failing finding + suite header + closing line) and gives the failing-test excerpt only the room actually left (`src/workflow/completion-review.ts:146-196`). At 0/1/2/3 co-occurring design findings against a 4 KB suite output the brief names the failing item, carries the output TAIL where the test names live, lists every other finding and keeps its closing instruction, inside an UNCHANGED 2000-char cap — closing F-323 (the excerpt was bounded, not reserved) and F-324 (both prior oracles asserted only that the clamp FIRED). **F-326, found and fixed in review:** the new budget used `Math.max(0, availableForLog - 2)` and `String.prototype.slice(-0)` is `slice(0)` — the WHOLE string — with no cap check on the final return, so a fixed part landing within 4 chars of the cap emitted the entire suite output: **46,095 chars against a 2000-char cap**, straight into the repair step's prompt. Fixed at `src/workflow/completion-review.ts:168-189`; +1 regression test sweeping 1001 justification lengths (`test/judge/deterministic-rubric-oracle.test.ts:363-393`); gate suite 138 → **142** green. **JD-8 caveat → WP-616:** the fix reached its budget by deleting the 1000-char bound at the settle site (`src/judge/harness.ts:205-208`), which also unbounded the verdict rationale (`src/judge/verdict.ts:44,167-169`) and `chikory trace` (`src/cli/trace.ts:501,505`) to the 64 KB capture ceiling (F-328). **REOPENED at the dogfood-144 review — 🔴 F-359 → WP-627.** WP-519 routes a failing-CRITERION justification and a would-be HALT into the next attempt, but a failing **design rubric row** raised by the pass that seals never reaches the executor at all. The bounded design-fix retry (`packages/sdk-ts/src/workflow/agent-loop.ts:1301` → `buildCompletionReviewBrief`) is guarded by `hasStanding` (`:1284`), and every judge pass pushes its failing rubric rows into `standingFindings` (`:1134`) — so at `judge.cadence: 1` on a converging step the guard is always true and the repair path is unreachable by construction. MEASURED on `run-a8fe46f3-b5e1-4f83-961c-2142220daa12`: the judge diagnosed a real ~15-line gap precisely, the run sealed FAILED with `injections 0` and 1 of `max_steps: 6` used, $0.0831 of $20 spent. `MAX_COMPLETION_REVIEWS = 2` (`packages/sdk-ts/src/workflow/completion-review.ts:16`) already bounds the dogfood-121 oscillation the guard was written against, so one attempt is safe. JD-8 is not satisfied until findings feed healing at the DESIGN altitude too. **WP-627 CLOSED at the dogfood-145 review** (`run-1f2a02e0-4615-47fa-8847-ea37c4164cfb`, `docs/reports/dogfood-145.md`, landed this review): the `hasStanding` early-seal branch is deleted and fresh/standing/sealing-pass findings flow through ONE decision (`packages/sdk-ts/src/workflow/agent-loop.ts:1284`) that grants exactly one bounded repair (`:1286-1303`) and seals FAILED (resumable) naming the item when the re-review still fails (`:1307`, `:1318`). Both `seal("SUCCESS", "design findings recorded")` arms are gone, so healing did not arrive at the cost of gating. Pinned by `packages/sdk-ts/test/runner/sealing-design-repair-live.test.ts` (6 live tests incl. `BOUNDED`, `MULTI-STEP`, `CUMULATIVE`) plus 8 re-pinned expectations; suite 189 files / 1,510 tests. **JD-8 stays in-progress, blocked one level up → 🔴 F-361 / WP-629:** a finding a LATER pass cleared still condemns the seal, because `standingFindings` is append-only (`:1134-1146`, never pruned) and the completion review runs with `criteria: []` (`:1269`) — zero acceptance checks, no `test_results` evidence, so F-344's "green checks CLEAR the concern" charter (`packages/sdk-ts/src/judge/prompt.ts:206-209`) names evidence the pass does not hold. MEASURED on this very run: `tests_pass` ✗ at pass #1, ✓ at pass #2, and pass #3 condemned the run on the pass-#1 text with both ACs green on disk. Feedback now reaches the executor at the design altitude; a *cleared* finding still cannot leave. |

## CM — Context & memory (spec §5.4)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| CM-1 | Context-rot mitigation first-class, co-designed with checkpointing | WP-203 | P2 | in-progress — **contract landed (ADR-006, 2026-06-14)**: compaction runs *at* the checkpoint boundary (`writeCheckpoint`, CM-1 co-design point); pure `planCompaction` decides what folds, the LLM digest + journal write are the non-pure wiring. The digest-wiring slice (S2) is **blocked on the WP-202 Memory Pointer store** (`CompactionResult.digestRef`); the pure **S4 compaction-trace renderer** (`formatEntryLine` `case "compaction"` rendering `tokensBefore→tokensAfter` + digest presence) is **done** (dogfood-026 `run-f9d699d4`, harvested IDENTICAL + uncommitted on `main`) — **WP-203's pure *trace* surface is complete**, the compaction JIF entry is legible in `chikory trace --watch`. The pure **S2 digest-prompt half** (`DIGEST_SYSTEM_PROMPT` + `buildDigestMessages(toDigest): Message[]` in `src/runner/compaction-prompt.ts`, the analog of `planner/prompt.ts`/`judge/prompt.ts`, over the frozen `CompactionPlan.toDigest` + `Message`, type-only `Message`, no schema/contract change) is **done** (dogfood-029 `run-74f88081`, harvested IDENTICAL + uncommitted on `main` — `docs/reports/dogfood-029.md`): 3 files, 4 new tests. **WP-203's entire pure surface is now exhausted** (S4 trace + S2 digest-prompt). **The digest WIRING is now done by hand** (TASK-PROTOCOL §4, branch `feat/wp-202-203-memory-compaction-wiring`, uncommitted): the `compactContext` activity (`runner/activities.ts`) folds the older recall-tier summaries at the checkpoint boundary via a real `review`-stage router call, stores the digest behind a Memory Pointer, journals a `compaction` `CompactionResult`, and the workflow carries `digestRef` into subsequent `ContextBundle.memoryRefs` — so a resume rehydrates the gist, not rotted context. Cost-guarded + best-effort; `test/runner/compaction-wiring.test.ts`. S3 recall-tier projection (no frozen pure contract yet) and OTel emission remain. The Python parity of the pure digest-prompt half is done via dogfood-030 `run-1a97e2ca` (WP-201; `docs/reports/dogfood-030.md`): the TS prompt regime now has Python SDK parity; no compaction runtime wiring changed. **WP-594 DONE (dogfood-147 `run-a26d41eb-…`, 2026-08-16):** the pressure-triggered fold was a structural no-op below the default `keepLastN=5` floor (F-272, MEASURED dogfood-125 — pressure fired 3/4 steps, folded 0) because `compactContext`'s `underPressure` branch reused the SAME constant for both `triggerAfterSteps` and `keepLastN`. Now builds `{ triggerAfterSteps: 1, keepLastN: 1 }` under pressure (`activities.ts:2284-2289`), so a short run folds below the floor while still keeping the latest step verbatim; the unpressured default and `planCompaction` are byte-unchanged. +6 `test/runner/` tests (388→394). |
| CM-2 | Compaction, structured note-taking, sub-agent architectures as runtime primitives | WP-203; **WP-637 (a predecessor's unearned verdict must reach the successor's note) ✅** | P2 | in-progress — `CompactionPolicy.keepLastN` keeps recent summaries verbatim, structured `ContextBundle.notes` survive compaction verbatim by construction (ADR-006); `planCompaction` pure core + tests landed. Sub-agent architectures still P2. 🟢 **CLOSED for the cross-node note — WP-637 DONE, dogfood-157 (`run-8b00e28a-3abf-4f91-bd92-4438c79087b0`, terminal SUCCESS, 2 steps, $0.1428/$20.00, AC 3/3 re-run green, harvest byte-IDENTICAL 7/7, `docs/reports/dogfood-157.md`), landed this review.** `buildStructuredCompactionNote` — the only channel carrying a predecessor's outcome into a dependent node's prompt (`packages/sdk-ts/src/chain/chain-loop.ts:255`) — now renders `inconclusive_check: <check>` (`packages/sdk-ts/src/chain/compaction-note.ts:39`–`:41`), placed ABOVE the unbounded `changed_paths` so the 1,200-char clip cannot truncate it away (pinned on a 400-path input, `packages/sdk-ts/test/chain/compaction-note.test.ts:164`). A predecessor whose checks really ran still produces the same five lines byte-for-byte (`packages/sdk-ts/test/chain/compaction-note.test.ts:145`). The compounding-error claim dogfood-156's goal scoped out is now made. |
| CM-3 | Memory Pointer Pattern: external storage + short refs in context | WP-202, **WP-606 (step summary refs — DONE, dogfood-155)** | P2 | **done for P2 — the LIVE loop recall + principled eviction landed via dogfood-087 (`run-8b8b81f7-991b-43c6-bf8f-0dbc48c6d6f7`, 2026-07-05, delivery uncommitted byte-IDENTICAL on the working tree — `docs/reports/dogfood-087.md`):** net-new pure `resolveMemoryRecallRequest(executorText, refs)` (marker `[memory recall <id>]`, full-id or 12-char-prefix match) + `decideMemoryEviction(refs, policy?)` + local `MemoryEvictionPolicy` in `src/runner/memory-pointer.ts`; the durable loop (`src/workflow/agent-loop.ts`) now resolves an executor recall request → fetches the fuller excerpt via the EXISTING `recallPointerExcerpt` (new `recallArtifactExcerpt` activity reading the artifact store by id-prefix) → carries it into the NEXT `ContextBundle.notes["memory.recall"]`, and bounds `carriedRefs` with `decideMemoryEviction` (the compaction `context_snapshot` digest refs are always protected) under the WP-271 `unattended` policy; additive `memory recalls N · evicted M` trace telemetry with counters persisted on the journal payload (no frozen-`Checkpoint` shape change). LIVE Temporal proof: an 8-chunk unattended run recalls a pointerized early output into a later step, stays bounded, reaches SUCCESS with no approval; a no-recall/no-policy run is byte-EQUIVALENT (asserted). No contract change, no new dep, 731 tests green. Residual: 🟡 F-113 — eviction activates off `spec.unattended` presence rather than a dedicated `MemoryEvictionPolicy` spec knob (track-B). _(History:)_ the local-FS content-addressed `ArtifactStore` (`src/artifacts/local.ts`: `put`/`get`/`excerpt`, sha256 ids stable across resume/branch) is landed; the pure Memory Pointer **workflow** half is **done** (dogfood-028 `run-7681a607`, harvested IDENTICAL + staged on `main`, pending commit — `docs/reports/dogfood-028.md`): `shouldPointerize(bytes, policy)` (`bytes > policy.maxInlineBytes`; exactly-at inlines) + `formatPointerReference(ref): string` (`[memory <kind> <12-char id>] <bytes>B — <summary>`) in `src/runner/memory-pointer.ts` — the analog of `buildVerdict`/`evaluateBaselinePrecheck`; local `MemoryPointerPolicy` type, type-only `ArtifactRef` import, no `types.ts` change; 3 files, 5 new tests. **The non-pure interception wiring is now done by hand** (TASK-PROTOCOL §4, branch `feat/wp-202-203-memory-compaction-wiring`, uncommitted): the agent loop gates each step's stored `transcriptRef`/`diffRef` through `shouldPointerize` (16 KB default) and surfaces the large ones into the next step's `ContextBundle.memoryRefs` (rendered via `formatPointerReference`); `test/runner/memory-pointer-interception.test.ts`. This unblocked and shipped alongside the WP-203 S2 digest wiring. The `store.excerpt` **recall path** — the READ half of the pattern — is now **done** (dogfood-062 `run-01add160-0b20-49ed-9af1-6598c6c558ae`, runtime `c6f1b32`, delivery STAGED byte-IDENTICAL on the working tree pending harvest — `docs/reports/dogfood-062.md`): a pure `parsePointerReference(line): ParsedPointerReference | null` (the exact inverse of `formatPointerReference` — regex `/^\[memory ([^\s]+) ([^\s]+)\] ([0-9]+)B — (.*)$/u`, literal U+2014 separator, `Number.parseInt(bytes,10)`, null-on-malformed, round-trips) + `recallPointerExcerpt(line, excerptFn)` (parse → injected async store-excerpt, the `deps.runCheck` DI pattern; `null` without calling the fn on malformed) + the local `ParsedPointerReference` type in `src/runner/memory-pointer.ts`, re-exported from the barrel, +5 tests — the pure-first shape `shouldPointerize`/`formatPointerReference` landed in; `codex`/`gpt-5.5` SUCCESS 1 step, judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2, additive, no contract change. Remaining optional follow-ups: a TaskSpec policy knob (a `types.ts`/contract change, §4-walled) and the non-pure agent-loop wire that calls the REAL `store.excerpt` through `recallPointerExcerpt` (§4). 🟢 **WP-616 + WP-631 DONE — dogfood-149 (`branch-run-f57c3f17-a3d7-4273-b62f-82beb8c63a03-step-5-770afe4b`, terminal FAILED, 3 steps $0.2759/$20.00, 10m 48s, AC 3/3 re-run green, harvest byte-IDENTICAL 6/6, `docs/reports/dogfood-149.md`), delivery landed this review after independent verification.** The CM-3 discipline now holds on the JUDGE'S OWN inputs, at both sites where it had failed. **Site 1 (F-328):** `computeVerdict`'s rationale is bounded at every one of its five return branches (`clampRationale`, `packages/sdk-ts/src/judge/verdict.ts:62`, ≤4096) and each rubric item is summarised to its status prefix before `:\n` and clamped to 512 (`renderRubricItemSummary`, `:48`) — closing the 44× regression WP-614 introduced, WITHOUT taking the raw suite log away from `buildCompletionReviewBrief`, which reads the untouched `JudgeForm`. **Site 2 (F-365):** the per-rubric-id accumulation is bounded where the completion-review prompt is RENDERED, not where findings are collected (`renderCompletionReviewConcerns`, `packages/sdk-ts/src/judge/prompt.ts:243`, cap 3072) — oldest and newest survive in full, middle findings are greedily re-admitted while they fit, and `- … [N findings omitted]` states the count, so bounding costs no evidence and the single `standingRubricFindings` channel is untouched. Verified at this review beyond the ACs: a property sweep over 4,000 randomised shapes found zero bound violations, including budgets smaller than the section header. 🔴 **Residue F-370 — a specification defect, not a code one:** the goal demanded an absolute character bound AND untruncated endpoints, which collide when the two endpoints alone exceed the cap; the judge upheld each half in turn and the run sealed FAILED with 3/3 ACs green. Precedence is settled in-code (`packages/sdk-ts/src/judge/prompt.ts:236-241`): **bounded wins**, and 🟠 F-371 (hand-fixed) makes the clamp announce the characters it removed (`clampFinding`, `:210-224`) so a shortened finding is never silent — the CM-3 rule is *bounded AND accountable*, never *bounded and quiet*. **WP-606 DONE (dogfood-155, `run-c1f1b066-d59e-4f8e-848f-2b1508b2ba7b`, SUCCESS 3 steps $0.2235/$20.00, `docs/reports/dogfood-155.md`) — the pattern applied to the step summary itself, which had it inverted.** A step's `record.summary` was spending 155 bytes per reference on an absolute `file:///…/.chikory/runs/run-<id>/workspace/…#L106-L147` URL into a directory deleted the moment the run ends — a long ref that does not resolve, where the pattern asks for a short one that does. `normalizeWorkspaceRefs` (`packages/sdk-ts/src/executors/step.ts:46`) rewrites only refs **inside** the run workspace into `path:line` form, once, at the shared `runCliStep` seam (`:216`) that codex, claude-code and gemini-cli all cross unedited; a path outside the workspace is left exactly as written and the raw `transcriptRef` archive is untouched. Measured on the previous run's own persisted summary: 6,660 → 5,006 B (**−24.8%**), 15 → 0 dead URLs. The string has two consumers in the durable loop — `recentSummaries` into the next step's prompt and `estimateTokensFromText` (`src/workflow/agent-loop.ts:982`), the pacing governor's projection — so the saving is taken twice. 🟡 F-392 hand-fixed at the same review: `native`, the 4th registered adapter, bypasses `runCliStep` and now normalises at `src/executors/native.ts:245`. ⚠️ F-197: the delivering run cannot exercise it (its own summaries still measure 32.7% URLs); confirm on the next run's journal. |
| CM-4 | Tiered memory (core/archival/recall), memory-poisoning safeguards | WP-204 | P2 | **DONE (dogfood-079, `chain-f4b08133-77c7-47b3-ae63-8a3803bbfc14`, runtime `d4ab544`, harvested `9304c68`; `docs/reports/dogfood-079.md`).** Pure unit-tested `src/memory/` primitives: `TieredMemory` core (put/get/list, bounded, no I/O), append-only archival overflow (`evictOldestIfNeeded`), `recall` across both tiers (best-match/most-recent), and **provenance enforced** — `validateProvenance` throws on a write with no `sourceRef`/`origin` (the memory-poisoning safeguard). Additive `src/memory/` area, no contract/StepRecord/memory-pointer change, no new dep; tsc+eslint + full suite **621 passed \| 19 skipped**. Built as the **WP-508 rung-2 chain re-host** (4 sequential judge-gated nodes + a live mid-chain kill→resume — see WP-265/WP-508). |

## CG — Cost governance (spec §5.5)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| CG-1 | Terminal states / deterministic exits break retry loops | WP-103, WP-124 (invariant #4) | P1 | done |
| CG-2 | Spend controls; transparent, predictable, checkpoint-aware budget governance | WP-124, WP-105; WP-218 (token-denominated budgets — USD gate inert on $0-metered runs, dogfood-002 F-9); WP-223 (watch can drop the SUSPENDED-at-cap transition, dogfood-004 F-15 — delivered via dogfood-007 `run-22b337a9`, commit pending review); dashboards in WP-407 | P1→P4 | in-progress (P1 USD gate done; WP-218 pricing/blind-meter-warning slice landed `2a4dd21` via dogfood-004 `run-9edbcd28`, meter proven live on dogfood-005 `run-34926e85` — $2.14/$5.00 metered, judge share 1.9%; WP-223 closes the transparency break — SUSPENDED/AWAITING_APPROVAL lines now derive from durable journal entries, never poll sampling; `budget_tokens` slice: **token gate landed end-to-end by hand 2026-06-14** — pure `estimateNextStepTokens`/`tokenBudgetBreached` in `runner/budget.ts` + the agent-loop pre-step gate (armed on `budgetTokens`; breach records a token HALT via the additive `budget_event` `cause: "tokens"`/`remainingTokens` shape, then seals a resumable FAILED — hard cap, no token top-up channel) + an integration test. Makes the gate real on $0-metered runs where the USD gate reads $0 (F-9). WP-218 complete; **WP-515 landed 2026-07-08 (F-96):** a blind-metered step (cap-killed / failed pre-usage-event, real tool calls, 0 tokens) gets its usage estimated from the run's observed per-tool-call rate inside `executeStep` before journaling (`src/runner/killed-step-usage.ts`, additive `StepPayload.usageEstimate`) — timed-out spend now hits both budget gates and the trace instead of vanishing from the ledger; dashboards P4) |

## AR — Artifact-centric state (spec §5.6)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| AR-1 | Repo snapshots, task trees, test results, browser state, PR diffs as first-class runtime objects | WP-002 (`ArtifactRef`), WP-122 (snapshots), WP-131 (diff/test evidence), WP-211 (browser state) | P1→P2 | in-progress (contracts, snapshots, diff/test evidence done; browser state P2) |

## OB — Observability (spec §5.7)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| OB-1 | Observe reasoning; trace decision trees | WP-142 | P1 | done |
| OB-2 | Inject corrections mid-run | WP-212 | P2 | **DONE (dogfood-075, `run-bb715500`, WP-212 → 🟢, 3 files STAGED uncommitted; `docs/reports/dogfood-075.md`).** `chikory inject <run-id> "<guidance>"` is wired onto the existing signal path: `cmdInject` (`cli/commands.ts:448`, mirrors `cmdCancel` — `runner.get(runId)` → `handle.inject(guidance)` → `runner.close()`) + `case "inject"` dispatch/help (`cli/main.ts:262`,`:42`) + arg validation (missing run-id/guidance → nonzero exit). A LIVE Temporal-backed test proves the injected sentinel is journaled as a kind-`injection` entry verbatim AND reaches the next step's `context.injections`; full suite green (589 passed \| 19 skipped). The durable SUBSTRATE (`injectSignal` handler `agent-loop.ts:140` → `pendingInjections` → next-step context `agent-loop.ts:365`; journaled kind `injection` `activities.ts:738`; `RunHandle.inject` `runner.ts:89`, `types.ts:422`) was already built; this closed the operator-facing CLI gap. ⚠️ The run sealed FAILED on a spec-artifact AC-1 filename-pin (the delivery is complete — see F-82 → WP-266, dogfood-075), NOT an OB-2 gap. First `spec_format=loose` headline (WP-265 rung 1). |
| OB-3 | Set success criteria upfront | WP-005 (acceptance_criteria in TaskSpec) | P0 | done |
| OB-4 | OTel-compliant traces by default | WP-105, WP-134 (invariant #3) | P1 | **done** (LLM-call/judge spans done; **durable-loop spans landed dogfood-089 `run-c3a1c54e`** — `chikory.run` root + `chikory.run.step`/`chikory.checkpoint`/`chikory.soak.reentry` emitted from activities, live-proven span tree; **F-116 CLOSED by dogfood-090 `run-45ce1499`** — `resolveRunRootContext(runId)` derives a stable W3C traceId/spanId so the tree stays run-rooted across every durable park with no process-local memory, **live-proven on a genuine second worker process**; track-B refinements 🟡 F-117 private-`_spanContext` mutation / 🟡 F-118 root-span-lifetime under WP-105) **WP-626 DONE (dogfood-151, `run-7faebdb5-7164-4931-9b9e-056b0d175091`) — a span attribute must not publish a constant as a measurement:** an executor that cannot enumerate its tool calls is now marked (`toolCallsObserved?: boolean`, `packages/sdk-ts/src/types.ts:422`, mirrored in `docs/spec/CONTRACTS.md:155`, `src/schemas.ts:440`, `docs/components/executors.md:46`), and BOTH span writers omit `tool.calls` when the mark is `false` — the executor-level `chikory.step` (`src/executors/step.ts:84`, delivered by the run) and the runner-level `chikory.run.step` (`src/otel.ts:237`, 🔴 F-376, missed by the run AND its judge, hand-fixed in review and pinned by `packages/sdk-ts/test/executors/step-span-observability.test.ts`). An observed zero and every pre-WP-626 unmarked record still publish `tool.calls` unchanged. |
| OB-5 | Trajectory-level forensics, whole path debuggable after the fact | WP-122 (journal), WP-142 (renderer), WP-231 (dogfood landing-scope audit), WP-232 (chain-launch verification, dogfood-041 F-32), WP-403 (web) | P1→P4 | in-progress (P1 journal + renderer done; **WP-231 done via dogfood-032 `run-4f1fbe0a`, landed `ded282b`**: `dogfood-verify` now has a landed-scope section and `scripts/dogfood-landed-scope.sh` compares the run workspace diff with the landed commit, flagging extra/missing/different paths; **`chikory trace <chain-id>` chain forensics branch DELIVERED** via dogfood-041 (`cmdTrace` → `ChainJournal` → `renderChainTrace`); **WP-232 queued (dogfood-041 F-32)** — `dogfood-verify` must flag a chain-intended spec that produced only a single run journal (no `ChainJournal`), so a `run`-vs-`chain` launch mismatch can't pass as a green chain run; web browser P4) |
| OB-6 | Human-observable metrics: tokens, decisions, checks, feedback frequency | WP-142, WP-209 | P1→P2 | in-progress (trace totals/footer done — tokens/decisions/checks/feedback-frequency render today; WP-209 slice 1 added the issues-found:changes-made footer line via dogfood-010, run `run-c9df353b`, commit pending review; WP-209 slice 2 added the components-over-time timeline line via dogfood-011, run `run-59e0166c`, commit pending review — both SE-3 footer halves now render; **WP-626 DONE (dogfood-151) — a metric nobody measured must not read as a measured zero:** `chikory trace` renders `unknown tool calls` for an unobservable executor (`src/cli/trace.ts:470`) while an observed zero and an unmarked historical record stay byte-identical, and the killed-step rate basis refuses an unobserved prior (`src/runner/killed-step-usage.ts:64`). Residual 🟡 F-378 → WP-633: the killed step's OWN count is still passed unmarked (`:58` ← `src/runner/activities.ts:1566`), latent behind `isBlindMeteredStep`) |

## FA — Full-application scope (spec §5.8)

> **FA-1 update (WP-237, 2026-06-20):** local single-predecessor S4 code
> handoff is implemented and proven by dogfood-042
> `chain-1cde6ae3-d05f-438e-b818-8af76419d6ae` SUCCESS 2/2: node B imported
> node A's handed-off module while retaining an isolated base/diff; cumulative
> chain harvest landed `b1b825d`. WP-239 artifact-backed fan-in is now
> implemented under ADR-007: ordered Git-bundle refs, shared-store injection,
> conflict-safe write ownership (WP-242/F-43), provenance, and topological
> cumulative harvest. **Proven live by dogfood-043
> `chain-6f1bf0ee-ce7a-42be-9416-4843b366cf0d` SUCCESS 3/3** (`docs/reports/dogfood-043.md`):
> two independent predecessors converging into one consumer, predecessors
> provably isolated, both deterministically materialized into the consumer, the
> canonical conflict/ordering runtime test judge-gated in the inner loop, and
> chain-aware harvest reconstructed the non-linear delivery (6 files). Structured
> WP-203 compaction notes remain a follow-up.
> Child approval/resume visibility (WP-241, F-42) is now DOGFOOD-PROVEN LIVE:
> `followChain` surfaces a parked in-flight child + the chain-level unblock
> command, and `chikory chain approve|resume <chain-id>` keeps the parent worker
> attached. The dogfood-044 re-run (`chain-1bfb9d13-…`, `docs/reports/dogfood-044.md`)
> exercised it end-to-end via WP-243's deterministic park-injection seam
> (`debug.parkBeforeStep`): node B parked at step 0, the chain surfaced it, and
> `chikory chain resume <chain-id> --add-budget 5` drove it to SUCCESS 2/2.
> WP-241 → done, WP-243 → done, F-42 closed live. Chain-aware verification remains
> WP-232 (F-41).

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| FA-1 | Entire production-ready apps: brownfield, multi-repo, migrations, long-horizon | ADR-003 (capability via wrapped agents); WP-219 (goal decomposition & run chaining — the long-horizon gap, dogfood-002 F-10); WP-220 (`chikory land` — the per-slice landing primitive chaining calls between runs, delivered via dogfood-005 `run-34926e85`); WP-214 (multi-repo — ✅ DONE dogfood-096 `run-f77c33db`, THE P2 exit-gate run, landed `554e08f`, `docs/reports/dogfood-096.md`: resume/chain/scope-gate/check-cwd/trace/report/OTel all multi-repo + live 2-repo Temporal proof); WP-232 (chain-launch verification, dogfood-041 F-32); WP-233 (durable/resumable chain planning layer, dogfood-041 attempt-2 F-33 — **(a) pure `classifyPlanGateFailure` classifier LANDED dogfood-072 `run-1ac16aa8-…`, `docs/reports/dogfood-072.md`; (b) part 1 — the `planAndGateChain` consumer WIRE — LANDED dogfood-073 `run-a5f8c5fe-…`, `docs/reports/dogfood-073.md`: pure `renderPlanGateFailureNotice` (`src/chain/plan-gate-notice.ts`) wired at the gate-failure return (`src/cli/chain.ts:132-135`) so an operator sees "INFRA fault, SAFE to re-run: …" vs "REJECTED … NOT safe to re-run as-is: …" (raw-rationale fallback on null) — the F-33 operator-facing fix, the (a) orphan killed; only the Plan-persistence/resume half (part 2, §4 contract-shaped) remains**); P3 brownfield benchmark proves it | P1→P3 | in-progress (landing primitive done; **ADR-005 accepted + WP-219 S1 contracts landed `d56f35a`**; S3 pure core now has `readyNodes` landed via dogfood-015 and `hasDependencyCycle` delivered via dogfood-016 `run-2418f473`; **S2 planner function contract landed by hand 2026-06-14** — `PlanInput`/`GoalPlanner.decompose` + pure `planCoverageGaps` (CONTRACTS §7a, ADR-005 §S2); **S2 prompt half delivered** (dogfood-022 `run-499218ef`) — pure `PLANNER_SYSTEM_PROMPT`/`PLAN_RESPONSE_SCHEMA`/`buildPlannerMessages` in `src/planner/prompt.ts`, mirroring `judge/prompt.ts`; **S2 assembly half delivered** (dogfood-023 `run-2d40ded5`) — pure `buildPlan(reply, input, opts): Plan` + `BuildPlanOptions` in `src/planner/assemble.ts`, mirroring `buildVerdict` (three structural checks → frozen `Plan`); **S2's pure surface complete**; **S2b plan-judge prompt half delivered** (dogfood-024 `run-28073328`) — pure `PLAN_JUDGE_SYSTEM_PROMPT`/`PLAN_VERDICT_RESPONSE_SCHEMA`/`buildPlanJudgeMessages` in `src/planner/meta-judge-prompt.ts`, mirroring `judge/prompt.ts` (schema verified field-for-field against the frozen `PlanVerdict`); **S2b verdict-assembly half delivered** (dogfood-025 `run-0d39fd12`) — pure `buildPlanVerdict` + `PlanJudgeReply` in `src/planner/meta-judge-verdict.ts`, mirroring `buildVerdict`, folding `planCoverageGaps` in as a deterministic coverage override; **WP-219's entire pure surface is now complete** (S2 planner + S2b plan meta-judge both mirror the executor judge); **the S2/S2b non-pure harnesses landed by hand 2026-06-19 on `main`** — `runPlannerPass` + `DecomposingPlanner` (`src/planner/harness.ts`) and `runPlanJudgePass` (`src/planner/meta-judge-harness.ts`, family-diversity-enforced), so **goal decomposition is now end-to-end runnable** (decompose → meta-judge-gate); **S3 wall cleared by hand 2026-06-19** — ADR-005 gained the §S3 transition rules and the `NodeOutcome` + `ChainRecord.nodeOutcomes` contract was frozen across all langs (`types.ts`/`schemas.ts`/`CONTRACTS.md §7a`/`sdk-py`/`fixtures`; TS 339 + py 74 green), splitting S3 into **S3-pure** (the chain-state reducer `advanceChain`/`deriveChainStatus`, `src/chain/advance.ts`, the `computeVerdict` analog — **DONE** via dogfood-038 `run-61e8b0a1`, harvested IDENTICAL + uncommitted on the working tree: `deriveChainStatus` four-rule precedence (ESCALATE→AWAITING_PLAN_APPROVAL / FAILED→FAILED / all-SUCCESS→SUCCESS / RUNNING) + pure immutable `advanceChain` node-fold, type-only contract imports, re-export at `index.ts:72`, 6 vitest cases, no contract change; **WP-219's S3-pure primitive set is now complete** — `readyNodes` + `hasDependencyCycle` + `advanceChain` + `deriveChainStatus`) and **S3-wiring** (the non-pure Temporal chain executor looping `readyNodes` → child run per node → `advanceChain` fold → halt-and-replan on a `FAILED` seal, hand-design, TASK-PROTOCOL §4); **S3-wiring substrate hand-landed 2026-06-20** — `ChainJournal`/`chainRecordFrom` (D4 store), the `chainLoop` workflow, and chain activities (360 TS pass, +15 chain tests; D3 replan, S4 handoff, parallel fan-out, S5, CLI glue deferred) — which **unblocked S6**: **S6-pure chain-trace renderer DONE** (dogfood-037 `run-295b2947`, harvested IDENTICAL + uncommitted on the working tree — `docs/reports/dogfood-037.md`): pure `renderChainTrace(record, entries): string` in a new `src/chain/trace.ts`, the chain analog of the per-run `renderTrace` (six-line render — header/`goal:`/60-rule/per-node rows in plan order/`totals:` footer/optional `failed: <reason>` from a `terminal` FAILED `ChainEntry`), type-only `ChainRecord`/`PlanNode`/`ChainEntry` imports, re-export at `index.ts:73`, 5 vitest cases, no contract change; **WP-219's entire dogfoodable pure surface is now exhausted** — `readyNodes` + `hasDependencyCycle` + `advanceChain` + `deriveChainStatus` + `renderChainTrace`; what remains is non-pure hand-design (the `chikory trace <chain-id>` CLI branch loading `ChainJournal` → `renderChainTrace`, the S3-wiring D3 replan, S4 handoff, S5 suspend/resume); **the `chikory chain` CLI launch path hand-landed 2026-06-20** (`cmdChain` decompose → plan-meta-judge gate → `startChain`/`chainLoop` → `followChain`, 4 unit tests, 369 TS pass); **`chikory trace <chain-id>` CLI branch DELIVERED** via dogfood-041 `run-a28655c9` (harvested IDENTICAL + staged uncommitted — `docs/reports/dogfood-041.md`): `cmdTrace` falls back from a missing run journal to `chainJournalPath` → `ChainJournal` → `chainRecordFrom` → `renderChainTrace`, run-journal authoritative on id collision; **⚠️ HOWEVER the durable multi-run chain executor has NEVER run against a real goal** — dogfood-041 was THE FIRST CHAIN DOGFOOD on paper but was launched with `chikory run` instead of `chikory chain` (single-run SUCCESS, no `.chikory/chains/`, no node runs, planner + plan meta-judge + `chainLoop` never invoked); **WP-219's durable-multi-run thesis still has ZERO end-to-end dogfood evidence — the first real chain run is owed (F-32 → WP-232)**; **attempt 2 (2026-06-20, launched with `chikory chain`) engaged the chain path — the planner decomposed a real goal into a multi-node Plan — but died at the plan meta-judge gate on a transient `transport error: fetch failed`, host-side before `startChain`, with ZERO persisted state (no `ChainJournal`, the Plan thrown away): the planning layer is non-durable and a transient infra fault is fatal + conflated with a substantive plan rejection (F-33 → WP-233); proxy not health-checked (F-34 → WP-234)**; **attempt 3 (2026-06-20, proxy now health-checked via `dogfood.sh:80-95`, WP-234 done) got the furthest yet — the planner AND the plan meta-judge LLM calls BOTH succeeded (family-diverse `gpt-5.5` + `gemini-3.1-pro-preview`) — then the gate died deterministically on its own schema contradiction: the response schema + system prompt require `uncoveredCriteria` but the `.strict()` parse schema `PlanJudgeReplySchema` rejected it (F-35 → WP-235, FIXED this session — `PlanJudgeReplySchema` now accepts `uncoveredCriteria: z.array(z.string()).default([])`, the deterministic `planCoverageGaps` floor stays authoritative, regression test added; 13 meta-judge tests pass). The first real chain run is now code-unblocked — attempt 4 is the first launch where a PROCEED verdict can flow into `startChain`/`chainLoop`**; **attempt 4 (2026-06-20) reached that point — the planner decomposed a clean 2-node plan and the meta-judge LLM PROCEEDed with a correct node→AC mapping — but the DETERMINISTIC coverage floor `planCoverageGaps` overrode PROCEED→REVISE because the planner's invented per-node criterion ids didn't equal the goal ids `AC-1/AC-2/AC-3` (F-36 → WP-236, FIXED this session: `PLANNER_SYSTEM_PROMPT` + `buildPlannerMessages` now instruct the planner to reuse each goal criterion id VERBATIM on the covering node; floor unchanged as a genuine safety net; 41 planner tests pass). The last GATING blocker is removed — a PROCEED can finally flow into `startChain`/`chainLoop` and the nodes can execute**; **🎉 attempt 5 (2026-06-20): THE CHAIN RAN END-TO-END — durable 2-node chain (`chain-d3794c24-…`, `.chikory/chains/…/chain.db` + 2 per-node run journals), plan gate PROCEED, per-node judge gating, the judge HALTing a stuck empty-diff node (true-positive budget guard), and halt-on-FAILED chain semantics ALL worked: node 1 SUCCESS (changes made 1), node 2 FAILED/HALT (changes made 0) → chain FAILED 1/2. The thesis pillar (durable multi-run execution + a judge catching a bad node) is PROVEN to run. It FAILED only on the deferred S4 context handoff (F-37 → WP-237, now the critical-path keystone): node 2 `dependsOn` node 1 but ran against a fresh HEAD clone without node 1's diff (`chain-loop.ts:88` spawns each node from `template.repos`/HEAD, no `parentRunId`), so it had nothing to verify. Secondary: the planner emitted a verification-only node with no diff — F-38 → WP-238, FIXED this session (`PLANNER_SYSTEM_PROMPT` now forbids verify-only/test-only nodes and requires every node to produce a non-empty diff; 6 prompt tests pass). Until WP-237 S4 handoff lands, only single/independent-node chains can go green (no compounding-error coverage)**. **Update (dogfood-066, `chain-15509162-b259-483c-b313-f9d7dbafbcfa`, runtime `e423b64`, `docs/reports/dogfood-066.md`): the FIRST durable chain launched against REAL product code (WP-256 spec-staleness gate, 2 nodes) surfaced a NEW chain-decomposition bug — F-62 → WP-257.** The chain DECOMPOSE + meta-judge PROCEED + durable node-A journal ran correctly (✅ the F-32 chain-path-exercised guard is satisfied), but node A HALTed because `runPlannerPass` paraphrased node A's brief into a one-line `node.goal` and DROPPED the spec's verbatim grep-pinned fixtures (`WP-255`/`WP-25`), while preserving the strict AC-1 grep — `planNodeToTaskSpec` (`src/chain/node-spec.ts:91`) hands that paraphrase to the executor as its `goal`, so the executor (which built a sane parser + 5 passing tests against REAL `plan.md` ids) was graded against an AC referencing literals it never received → structurally unwinnable → judge budget-waste HALT after 3 consecutive AC-1 fails ($3.76/$6); node B (the WP-239 dependent handoff) never dispatched, so the handoff went un-exercised this run. F-49 discipline ("ACs grep mandated literals so the executor can't skip the primitive") silently assumes a single `run` carries the brief; it is FALSE for a chain. **WP-257 (🔴): the planner must preserve verbatim/grep-pinned literals from the goal into each node's `goal` — blocks the durable-chain pillar for any grep-pinned goal.** Positive: the 3-strike budget-waste HALT bounded the bug's cost cleanly (F-63, no WP)). **Update (dogfood-067, `chain-d18a8c1b-f681-4fe3-ba35-c38fa0f54b65`, runtime `a4b8e7a`, `docs/reports/dogfood-067.md`): the durable-chain pillar now has its FIRST end-to-end SUCCESS on real open-WP product code — the WP-239 dependent handoff is finally exercised LIVE.** The dogfood-067 spec (written single-`run`) was launched as `chikory chain` and SUCCEEDED 2/2: decompose → meta-judge PROCEED → node-1 `parseWpStatus` sealed SUCCESS → **node-2 cloned node-1's handed-off tree (`baseCommit f7f494a == node-1 head`) and IMPORTED `parseWpStatus` (AC-2 grep forbade reimplementation)** → node-2 sealed SUCCESS → chain SUCCESS ($2.5073, judge 0.46%, 6m44s; AC re-run green tsc+eslint+9 vitest). **F-66 (🟢 positive, no WP): the handoff dogfood-066 left un-exercised is now proven.** Two residuals: **F-64 → WP-257 (still 🔴)** — the planner STILL paraphrased node-1's goal, this time dropping the parent goal's positional semantics ("FIRST cell is the id"/"THIRD cell is the status") rather than a grep literal; non-fatal ONLY because dogfood-067 hardened the `WP-25` literal into the AC-1 description, so the chain "passed" while the executor built a divergent (header-driven) parser — evidence a chain can silently build the WRONG function even when green. **F-65 → WP-258 (🔴)** — WP-256's gate landed ORPHANED (`assessSpecStaleness` has zero live consumers), the F-60 pattern recurring; WP-256 → 🟡 until the gate is wired into the launch precheck). **Update (dogfood-068, `chain-aa25aa5c-c3e7-4a5c-a8cf-d33723f0655b`, plan `plan-8844e604-…`, runtime `e0da13f`, `docs/reports/dogfood-068.md`): the SECOND consecutive durable chain to land real open-WP product code — WP-258 wired WP-256's gate LIVE (WP-256 + WP-258 → 🟢, F-65 orphan killed).** Single-`run`-authored spec launched as `chikory chain`, SUCCEEDED 2/2: node-1 `precheck-module` → node-2 `wire-precheck-cli` via the WP-239 handoff (`baseCommit 4631f4d == node-1 head`), $1.6988, judge 0.62%, AC re-run green (tsc+eslint+547 vitest). **F-69 (🟢): the chain pillar is repeatable, not a one-off.** **But F-64/WP-257 (still 🔴) bit a THIRD time and leaked CONTRACT DRIFT past a green chain:** the planner compressed node-1's `node.goal` and dropped the mandated interface fields + param shape, so the executor shipped `evaluateSpecStalenessPrecheck` with NO `stale` field and POSITIONAL `(specText, planText)` args (vs the mandated `{ targetWpId, stale, warning }` / `{ goal, planText }`) — green because the AC grep pins symbol NAMES, not interface SHAPES (`grep -q "stale"` matched the test's `.toContain("stale")` string), and tsc/eslint only enforce internal consistency → **F-67 → WP-259** (AC must assert type-shape, not just names). Plus **F-68 → WP-260** (the wire reads the target WP from the whole yaml, not `spec.goal`). **WP-257 is now triply-evidenced (066 HALT → 067 wrong semantics → 068 wrong contract) — the durable-chain pillar's own root bug, the strongest unblocked real-product slice.**) **Update (dogfood-069, `chain-bdaef796-667f-4e17-8ad2-4bd49951bb7f-node-implement_literal_preservation`, plan `plan-91e4a59a-…`, runtime `361c4b4`, `docs/reports/dogfood-069.md`): WP-257's PURE VERIFIER HALF LANDED → WP-257 🟡, and the run DOGFOODED ITS OWN BUG.** Landed the pure `planLiteralGaps(plan)` guardrail (`src/planner/literal-preservation.ts` — `extractGoalLiterals` first-seen/de-duped backtick literals + `planLiteralGaps` returning parent literals no node preserves, EXACT-TOKEN `WP-25`≠`WP-255` rule) + a 7-case test, mirroring `coverage.ts`'s `planCoverageGaps`, pure, type-only `Plan` import; SUCCESS 3 steps, $2.73/$5, AC re-run green (full suite 554 passed). **🔴 F-70 → WP-261:** the spec was authored single-`run` (3× "NOT a chain", because a chain re-risks F-64) but was launched as `chikory chain` (3rd consecutive launch-mode divergence) — F-64 recurred, the planner's 2-sentence `node.goal` dropped 32/35 mandated literals (the delivered `planLiteralGaps` over this run's own `(parent, node)` goal returns those 32 gaps), steps 1-2 burned ~63% of cost failing AC-1, the run survived only because the F-49 grep pins persisted into the ACs. **F-71 (🟢, no WP): the landed verifier is proven correct against the LIVE defect** → the §4 `runPlannerPass`/`buildPlan` REVISE wire (operator-landed / track-B) is the single highest-value remaining WP-257 slice and would have caught this paraphrase at plan time. WP-257 → 🟡 (pure half done; REVISE wire remains).) **Update (dogfood-070, `chain-14f72c09-debe-4f60-a6ca-1c3b3b7d0e65` nodes node-1/node-2, plan `plan-be71d6b1-…`, runtime `07f9687`, `docs/reports/dogfood-070.md`): WP-261's PURE DECISION HALF LANDED → WP-261 🟡, and the run DOGFOODED ITS OWN BUG A SECOND TIME.** Landed the pure launch-mode guard (`src/cli/launch-mode-precheck.ts` — `detectIntendedSingleRun(specText)` true on the conventional single-run markers + `assessLaunchModeMismatch({intendedSingleRun, launchedAsChain})` returning a non-null `LaunchModeMismatch` only when both true, mirroring the WP-258 `evaluateSpecStalenessPrecheck` warn-shape, pure, no contract change) + an 8-case test; SUCCESS 2 nodes, $1.98/$5, both PROCEED first-try, AC re-run green (full suite 562 passed). **🔴 F-72:** the spec was authored single-`run` ("NOT a chain" / "HONOR THE LAUNCH MODE THIS TIME") but was launched as `chikory chain` — the **4th consecutive** launch-mode divergence — and F-64 recurred (the planner compressed the ~2000-word goal into two two-sentence node goals dropping the marker regexes, the `warning` substrings, the truth table, and the verbatim test cases); it survived first-try each node ($1.98, cheaper than 069) ONLY because the F-49 grep pins held the shape and the executor converged on the un-pinned `warning` substrings by luck (silent-divergence tail — reinforces WP-257). **The delivered `assessLaunchModeMismatch({true, true})` returns non-null for THIS spec — the guard would have refused/warned its own launch.** **F-73 (🟢, no WP): the guard is proven against its own launch** → the §4 `cmdChain` wire (call `detectIntendedSingleRun` + `assessLaunchModeMismatch` in `cmdChain`, warn/refuse pending `--force`) is the single highest-value remaining WP-261 slice (operator-landed / track-B, the WP-258 wire shape). WP-261 → 🟡 (pure decision done; `cmdChain` wire remains).) **Update (dogfood-071, `chain-fd45e5a6-6805-4898-b171-e290823c84b2-node-node-1`, plan `plan-2976e9af-…`, runtime `fc90348`, `docs/reports/dogfood-071.md`): WP-261's §4 `cmdChain` WIRE LANDED → WP-261 🟢.** `cmdChain` (`src/cli/chain.ts:403-411`) now imports the guard and refuses a single-`run`-authored spec launched as `chikory chain` (emit `warning` via `ioPair.err` + `return 1`) BEFORE the planner runs — zero LLM cost; + a behavioral test invoking `cmdChain`. SUCCESS 1 node, $1.13/$5, PROCEED 2/2, AC re-run green (full suite 563 passed). Kills the F-65 orphan (the pure guard now has a real consumer). **🔴 F-74 (🔴→🟢, 5th consecutive launch-mode divergence — the run whose job is to refuse this was itself launched by making it; closes on commit, this wire IS the fix).** **🔴 F-75 → WP-262: the F-64 paraphrase caused REAL contract drift that shipped GREEN** — the planner's one-line node goal dropped the mandated guard-after-`parseTaskSpec` placement, the SECOND override-hint `ioPair.err` line, the non-empty override semantics, and the parse-valid test fixture; substring-only ACs + an under-built behavioral test (unparseable fixture + `err.toHaveLength(1)` locking the drop) passed the drift — the WP-257 silent-divergence tail realized in DELIVERY, not luck; WP-257 now quadruply-evidenced (066 HALT → 067 wrong semantics → 068 wrong contract → 071 wrong CONTRACT-shipped-green). WP-261 → 🟢 (a small spec-conformance remainder rides F-75 → WP-262, not a reopen). **Update (2026-07-02 hand-land session, committed `141adf5`): WP-257's §4 REVISE wire LANDED — `buildPlanVerdict` (`src/planner/meta-judge-verdict.ts`) now downgrades PROCEED→REVISE when the plan drops mandated goal literals (`planLiteralGaps`), catching the F-64 paraphrase at plan time → WP-257 🟢; and WP-262(a) fixed the shipped `cmdChain` guard drift (guard placement after `parseTaskSpec`, the visible override-hint line, non-empty override) → WP-262 🟢. The chain-planner silent-divergence root bug is now closed at the plan gate.** **Update (dogfood-082, `run-ef4824e2`, 2026-07-03, harvested `1d2c7e5`, `docs/reports/dogfood-082.md`): the two WP-219 chain-runtime capabilities `chain-loop.ts` named "remain deferred" — D3 HALT-AND-REPLAN + S4 STRUCTURED COMPACTION-NOTE HANDOFF — are DELIVERED, additive, LIVE-proven (WP-219 → 🟡).** Pure `decideReplan` (`src/chain/replan.ts`, D3 decision sibling of `deriveChainStatus`/`advanceChain`) wired into `chain-loop.ts`'s FAILED-node path → `replanRemaining` splice + `node_replanned` (ONE additive journal-event kind, NO frozen-contract shape change); pure size-bounded `buildStructuredCompactionNote` (`src/chain/compaction-note.ts`) threaded into the S4 handoff ALONGSIDE the git bundle; a LIVE Temporal `replan-live.test.ts` proves one node FAILS → REPLAN → `node_replanned` → chain SUCCESS, and zero-budget → same chain seals FAILED on the byte-unchanged halt. Delivered as the WP-265 rung-3 horizon proof host (6 durable checkpoints under `bounded_work_unit`). Remaining WP-219 non-pure: S5 suspend/resume + chain-level end-to-end evidence (F-32 → WP-232). |
| FA-2 | Reliable, redundant execution; fluent restart on failure | WP-123 | P1 | done |
| FA-3 | Token awareness + intelligent pacing during planning AND execution (batch size, test count, checkpoint cadence, breaks) | WP-207; inputs from WP-105 accounting | P2 | in-progress — pure context-window pacing decision done via dogfood-031 `run-c4aa79d3`: `decideContextWindowPacing(usage, policy)` returns `continue` / `compact` / `park` over projected context-window pressure with local types only and 5 focused tests; non-pure runner/journal integration — **journaling+wiring half DELIVERED via dogfood-051 (`run-663a1baa-1a78-406f-91c5-bd0afa2fdcaa`, runtime `54a2c41`, uncommitted on the working tree byte-IDENTICAL; `docs/reports/dogfood-051.md`): `decideContextWindowPacing` now wired into the live agent loop, journaling a durable replay-safe `pacing` entry each step from REAL per-step token usage (`recordPacingEvent` activity, idempotent `appendOnce` keyed on `pacingEventIndex`), surfaced as `pacing events N` in `chikory trace` + a per-entry `% window` line** (`codex`/`gpt-5.5` one-shot 5 files, judge ✓ PROCEED 1/1, $2.8487/$5, **2178k/12k tokens** series-high, full suite 454 passed incl. the real-Temporal ARMED/crash-recovery paths). **SUMMARY half DELIVERED + LIVE-OBSERVED via dogfood-052 (`run-7e13ae2a-a233-4e2d-9fdc-d564a9eee5bc`, runtime `0880806`, uncommitted byte-IDENTICAL; `docs/reports/dogfood-052.md`): a pure `summarizePacing(entries): PacingSummary` reducer (`src/runner/pacing-summary.ts`) folds the `pacing` entries into PEAK window utilization + `compact`/`park` counts, surfaced as `peak window X% (compact C · park P)` in the trace totals (additive/byte-identical no-pacing path); `codex`/`gpt-5.5` one-shot 5 files, judge ✓ PROCEED 1/1, $0.8032/$5, vitest 25 + tsc + eslint exit 0. F-53 CLOSED — first live read: this run rendered `peak window 602% (compact 0 · park 1)` over its own journaled `pacing` entry (`action park · projectedTokens 1,203,440 · remainingTokens -1,003,440 · utilization 6.0172`).** **The act half (cadence) is now DONE (2026-06-24, branch `feat/wp207-pressure-compaction`):** the live pacing decision drives compaction — under `compact`/`park` pressure the runner folds history at the checkpoint boundary (effective `{triggerAfterSteps: keepLastN}` policy) instead of waiting for the count trigger (8); each `compaction` entry is tagged `trigger:"pacing"\|"count"`, `chikory trace` renders ` (pacing)`, and a `debug.contextWindowTokens` seam makes it deterministically provable (real-Temporal test `compaction-wiring.test.ts`, full suite 460 passed). WP-203 → 🟢, WP-207 act half → 🟢. The remaining `park`→durable-suspend behavior (a single overflowing step can't be helped by folding) is split out as **WP-250**. Context-rot is now observed AND acted on (387k–793k input tokens/step; dogfood-052 projected 1.2M against a 200k window). **The act-half `trigger` field is now ACTIONABLE in the trace TOTALS (dogfood-053, `run-41f2744f-82d6-4e54-825d-9704f77b1ee7`, runtime `4abb478`, uncommitted byte-IDENTICAL; `docs/reports/dogfood-053.md`):** a pure `summarizeCompaction(entries): CompactionSummary` reducer (`src/runner/compaction-summary.ts`, the `summarizePacing` analog — digestRef-gated `folds`, `trigger:"pacing"` subset `pacingFolds`) surfaces `compactions N (pacing M)` additively (byte-identical no-compaction path); `codex`/`gpt-5.5` one-shot 5 files, judge ✓ PROCEED 1/1, $0.8086/$5, vitest 27 + tsc + eslint exit 0. **Residual F-54 → WP-251:** the build run PARKED (`peak window 604% (compact 0 · park 1)`, 0 folds), so the telemetry is unit-proven but not yet observed live — closes on a seam-forced multi-step fold (the F-53 close shape). **DENOMINATOR CALIBRATED via dogfood-057 (WP-252, `run-6b23da51-c440-432a-bbf8-51d4ee8a24af`, runtime `3a3dc8d`, uncommitted byte-IDENTICAL; `docs/reports/dogfood-057.md`): a NEW pure `src/runner/context-window.ts` — `CONTEXT_WINDOW_TABLE` + `lookupContextWindow` (the `lookupPricing` longest-prefix analog) + `resolveContextWindowForSpec` — WIRED into `agent-loop.ts:355` so the live pacing decision divides projected tokens by the routing model's REAL window (`gpt-5.5`→400k, etc.) instead of a hardcoded 200k, the `debug.contextWindowTokens` seam still winning; `codex`/`gpt-5.5` one-shot 3 files, judge ✓ PROCEED 2/2, $1.1870/$5, vitest 6 + full suite 480 passed. F-55 NOW FIXED IN CODE (recurred dogfood-052→056); the headline `peak window %` is no longer a magic-number divisor. Closure is the F-53 live-read shape — the first calibrated read is the next run (this run's own trace predates the wire, reads `peak window 904%`).** **FIRST CALIBRATED LIVE READ via dogfood-058 (`run-67d39267-…`, `docs/reports/dogfood-058.md`): `action compact · projectedTokens 716,994 · utilization 1.792485` = `716994/400000` → `peak window 179%` (vs pre-wire 904%), confirming WP-252 — but the `compact 1 · park 0` was read PREMATURELY as "park-saturation broke".** **CORRECTED via dogfood-059 (WP-215 S4, `run-bc841ce6-…`, `docs/reports/dogfood-059.md`): a TRIVIAL 3-file additive task PARKED at `peak window 370%` (`projectedTokens 1,480,248 · utilization 3.70062`, denominator still exactly 400k — calibration HELD). The NUMERATOR is the residual defect → new F-56 → WP-254: `projectedTokens = spentTokens + estimate = (734,193+5,931)×2` feeds a fresh codex subprocess's summed-across-27-internal-turns throughput as live window occupancy, and the window keys off `routing.stages.code.model` not the codex executor. Even raw 734k/400k = 1.835× overflows though the provider accepted 734k input (real window > 400k, zero genuine pressure). Park-saturation is the 7th F-54 data point; the calibrated denominator did NOT make the metric trustworthy on codex steps — WP-254 fixes the measurement.** **WP-254 PURE NUMERATOR HALF LANDED via dogfood-064 (`run-6aa5081e-bda6-406a-b40f-ddf2355e17a5`, runtime `74875b9`, STAGED byte-IDENTICAL pending harvest, `docs/reports/dogfood-064.md`): `estimateResidentContextTokens(parts: ResidentContextParts)` in `src/runner/pacing.ts` (sum the system preamble + only the RETAINED TAIL of `recentSummaries` since folding drops the oldest; clamp `retainedSummaryCount` to `[0,length]`, clamp result ≥0, empty/negative/no-mutation) + barrel re-export + 6 vitest cases — the live-occupancy value the agent-loop SHOULD feed instead of cumulative `spentTokens`. `codex`/`gpt-5.5`, judge `gemini-3.1-pro-preview` ✓ PROCEED 2/2 scope ✓, $0.0084/$5, AC re-run green (vitest 11; full suite 521 passed). The non-pure §4 feed swap (`agent-loop.ts:350`) that RETIRES F-56 remains operator-landed.** **§4 FEED SWAP NOW LANDED (operator, 2026-06-29, uncommitted pending review) — F-56 RETIRED AT SOURCE:** `agent-loop.ts:~348` feeds `decideContextWindowPacing` `currentInputTokens: estimateResidentContextTokens(buildResidentContextParts(...))` + `estimatedNextStepTokens: estimateTokensFromText(record.summary)` — BOTH numerator terms swapped off the codex subprocess throughput (new pure `CHARS_PER_TOKEN`/`estimateTokensFromText`/`buildResidentContextParts` helpers in `pacing.ts`, additive + tested); full suite 526 passed, `recordPacingEvent` payload/position unchanged (Temporal-replay-safe). **WP-254 → 🟢.** **NEW F-59 → WP-255:** dogfood-064's step was KILLED at 2.45× its `maxSeconds=600` cap (ran 24m32s), zeroing its telemetry (`0/0` tokens) — so the budget gate read $0 executor spend AND F-56 did NOT recur (`peak window 0%`) only because the numerator had no tokens to over-read (masked, not fixed). **WP-255 NOW LANDED IN FULL (operator, 2026-06-29):** (a) TREE-REAP — `process.ts` `spawn(detached:true)` + `process.kill(-pid)` group-kill bounds a killed step near `maxSeconds` (new `hang-grandchild` conformance case on both adapters); (b) KILLED-STEP TELEMETRY — `parseClaudeCodeOutput` (now curried `(model)=>(stdout)`) recovers the last `assistant`-turn usage priced via `computeCostUsd` when killed before the `result` event (codex already preserves the last `turn.completed`), and `step.ts` enriches the kill `failure.reason` with the actual `{elapsed}s ({ratio}× cap)` so the overrun is VISIBLE in the trace. Full suite 527 passed. **WP-255 → 🟢** (only an optional structured `describeStepDeadline` journal field remained, now low-value). The durable + judge-grades-artifacts layers still sealed a correct SUCCESS despite the kill. **dogfood-065 (`run-358273a3-dbbe-4103-8166-4f065606f309`, runtime `cfd4cba`, delivery STAGED, `docs/reports/dogfood-065.md`) landed that pure `describeStepDeadline` descriptor anyway — flawless (6 cases, AC-1+AC-2 green, full suite 533 passed, $0.7048/$5) but ORPHANED (🔴 F-60 → WP-256): zero runtime consumers (only the barrel + its test), because operator commit `0533a4c` had landed the same `elapsedSeconds`/`overrunRatio` arithmetic INLINE in `step.ts:150-163` 15 min before the run launched. Nothing re-checks a queued spec against HEAD at launch, so the auto-commit operator-follow-up loop closed WP-255 out from under the spec → WP-256 (launch-time staleness gate, sibling of WP-228/WP-231/WP-249).** **WP-251 LIVE-FOLD OBSERVATION ATTEMPTED + FALSE-FAILED via dogfood-091 (`run-7fca16bc-8296-4b24-a9bc-b7e7641dd533`, FAILED, NOT harvested, `docs/reports/dogfood-091.md`):** the 4-part product delivery (net-new pure `describeCompactionPressure(entries)` pressure-join reducer joining pacing decisions to `trigger:"pacing"` folds + `chikory trace` render + live Temporal fold test) was CORRECT and full-suite-green in-workspace, but the run false-FAILed on 🔴 **F-119** — AC-2 `test "$(grep -rc … src/)" -ge 2` is unsatisfiable (multiline → `integer expression expected` → the judge budget-waste HALT guard guillotined a green delivery). Separately 🟡 **F-120** — the real run never folded (`peak window 1% · compact 0`): the `CHIKORY_CONTEXT_WINDOW_TOKENS=1200000` seam was sized against codex's internal ≈400k–900k tokens, but the pacing window tracks Chikory's ASSEMBLED-CONTEXT tokens (~3k projected) → never crossed the compact band. **WP-251 STILL 🟡 un-live-proven; re-runs as dogfood-092.** **RE-RUN dogfood-092 (`run-4481c735-d3b2-4885-b755-d2ad2c73a551`, 🟢 SUCCESS · 4 steps · $2.93/$80 · 9m46s, uncommitted byte-IDENTICAL, `docs/reports/dogfood-092.md`):** the SAME 4-part delivery landed CLEAN + full-suite-green (781 passed), and CURED both 091 launch bugs — AC-2 rewritten to `grep -roh … | wc -l` (F-119) + window sized to the assembled-context scale `=2000` (F-120/F-121), confirmed by `peak window 115% · compact 4` and the first pacing entry `remaining 227 + projected 1773 = 2000`. **BUT the real run STILL folded 0×** (`pressure-steps 4 (unfolded 4)`, 0 compaction entries): a 4-step run never accumulates `> keepLastN=5` resident summaries, so `planCompaction` (`compaction.ts:22`; effective trigger = keepLastN under pressure, `activities.ts:1103`) folds nothing on EVERY compact decision — **🟡 F-122, the 3rd distinct live-fold miss (053-park / 091-undershoot / 092-too-short); horizon length, NOT window size, is the last knob. WP-251 STILL 🟡 un-live-proven; re-runs as dogfood-093 with `min_durable_steps: 6-7`** (same ~2000 window) so summaries cross 5 and a real `trigger:"pacing"` fold fires by ~step 6. 🟡 F-123 → track-B trace hint (silent green when a sized window folds nothing). **✅ WP-251 NOW 🟢 LIVE-PROVEN — dogfood-093 (`run-60a32aff-0b81-4cbd-9d39-7eb50b8b9561`, 🟢 SUCCESS · 7 steps · $4.53/$80 · 20m52s, uncommitted byte-IDENTICAL, `docs/reports/dogfood-093.md`): the F-122 fix (`min_durable_steps: 6` + 7 chunks, SAME ~2000 window) crossed `keepLastN=5` and `planCompaction` FOLDED TWICE — the FIRST dogfood RUN to journal a live `trigger:"pacing"` compaction (`compactions 2 (pacing 2) · first pacing fold step 5 · pressure-steps 7 (unfolded 5)`; compaction #1 foldedCount 1 @423→224 tok, #2 foldedCount 2 @588→295). F-123 increment landed too: pure `pressureFoldGapWarning(description)` (loud on `pressureSteps>0 && pacingFolds===0`, silent here) + additive `firstPacingFoldStep` on `describeCompactionPressure`, both rendered additively in `chikory trace`; 790 TS green, 0 rollbacks. The context-rot pillar (FA-3/SE-2) is now OBSERVED live end-to-end — the 4-attempt arc closes (053-park/091-undershoot/092-too-short/093-FOLD). 🟡 F-124 (compaction entry lacks `stepIndex` → inferred first-fold step; track-B under WP-251). **✅ F-124 CLOSED — dogfood-101 (`run-6c9873e3`, `docs/reports/dogfood-101.md`): `stepIndex?: number` on `CompactionResult` + written at the compaction write site (`activities.ts:1871`) so the `appendOnce({field:"stepIndex"})` idempotency key now matches (crash-safe de-dup) and `describeCompactionPressure` derives the EXACT first-fold step (legacy inference kept as fallback); additive, 900 TS green. ℹ️ F-140 (PART-2 unit test tautological, real regression via Temporal wiring test — track-B note).** **🔴 F-125 (window-sizing calibration gap) → ✅ MECHANISM LANDED (hand-fix track-B this sitting, uncommitted; `calibrateContextWindow` + `pacing.autoCalibrate`, 797 TS + 84 py green), re-proven by dogfood-095. Surfaced dogfood-094 (`run-b319713b`, `docs/reports/dogfood-094.md`): the `CHIKORY_CONTEXT_WINDOW_TOKENS` seam is a static per-launch guess, and there is NO calibration against the ACTUAL per-workload assembled context. It has now mis-fired FOUR distinct ways (053-overshoot→park / 091-undershoot→no-compact / 092-too-short→no-fold / 094-too-big→count-only). On dogfood-094's greenfield signals build the assembled context was ~2.4k, so window `4000` kept pacing at ≤65% (`peak window 65% · compact 0`) and the 3 folds were `trigger:"count"`, not pacing. Fix (LANDED): pure `calibrateContextWindow(observedProjectedTokens, opts?)` sizes the window so the first observed step sits at ~0.75 utilization (just under the 0.8 compact band), locked once after step 1 from the journaled resident-token count (deterministic across a Temporal replay), gated behind the opt-in `pacing.autoCalibrate` field (absent = unchanged fixed window). Un-gated the 24h P2 exit-gate run (a hand-guessed window can no longer mis-size it). Also 🟡 F-127 (`CHIKORY_KILL_AT_STEP` durable-resume drill seam) landed the same sitting — a reproducible mid-run crash for the suspend/resume axis.** **Execution-RATE pacing (the "breaks"/checkpoint-cadence clause) is now covered by WP-310 (limit-pacing governor, hand-landed 2026-07-12 — see RT-12): observed burn vs sustainable quota pace (rolling-5h + weekly windows) vs the `horizon` deadline pace drives push/steady/throttle/predict-limit each step, with durable `source:"pace"` throttle sleeps and a `pace` trace line.** |
| FA-4 | Assumption ledger: planner + executor declare assumptions as structured journal entries; run/chain reports render them | WP-516 (ADR-008, chain-autonomy rung C-3); WP-232 (rung C-1 prerequisite) | P2 | planned |
| FA-5 | Clarification gate: high-grade assumptions proactively escalated as a formulated question; park → user answer → resume | WP-517 (ADR-008, rung C-4); reuses WP-206 suspend + WP-212 inject | post-P3 | planned (sequenced behind the Stage-1 benchmark gate, ADR-008 D6) |
| FA-6 | Tech-spec-only input: spec/architecture document in → derived goal + ACs + checks, different-family derivation judge, user AC-approval | WP-518 (ADR-008, rung C-5) | post-P3 | planned (depends on FA-5 wiring) |

## SE — Orchestrator self-evaluation (spec §5.9)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| SE-1 | Checkpoint notifications at judge milestones | WP-208 | P2 | in-progress — **pure delivery layer complete**: slice 1 `notificationsFor` derivation (`JournalEntry[]` + `NotificationPolicy` → `Notification[]`, dogfood-012); slice 2 `slackPayloadFor` (`Notification` → Slack `{ text }`, dogfood-013); slice 3 pure half `desktopPayloadFor` (`Notification` → `{ title, body }`, dogfood-014, landed `3e1336f`). Remaining: the side-effectful Slack/desktop delivery + runner call-site — non-pure, rides the chain/runner work. |
| SE-2 | Window-fit reasoning for implementation and judging passes | WP-207 | P2 | in-progress — WP-207 pure helper done via dogfood-031 (`decideContextWindowPacing`, `docs/reports/dogfood-031.md`); remaining work is using that decision in planner/runner cadence and journaling — **the observability half is COMPLETE: dogfood-051 wired `decideContextWindowPacing` into the live agent loop + journaled a replay-safe `pacing` entry each step + surfaced `pacing events N`; dogfood-052 (`run-7e13ae2a-…`, `docs/reports/dogfood-052.md`) added the pure `summarizePacing` reducer surfacing `peak window X% (compact C · park P)` in the trace totals and OBSERVED it live (F-53 closed — `peak window 602% (compact 0 · park 1)` over this run's own pacing entry)**. **The cadence half is now DONE (2026-06-24): the pacing decision drives compaction — under window pressure the runner folds history at the checkpoint boundary instead of waiting for the count trigger (WP-203 S2, `trigger:"pacing"` + ` (pacing)` trace marker + `debug.contextWindowTokens` seam, real-Temporal proof).** WP-207 act half → 🟢. The `park`→durable-suspend control-flow remainder is split out as WP-250. **The act-half `trigger` field is now surfaced in the trace TOTALS via dogfood-053 (`run-41f2744f-…`, `docs/reports/dogfood-053.md`): a pure `summarizeCompaction` reducer renders `compactions N (pacing M)` additively; residual F-54 → WP-251 (the build run parked at 604%, 0 folds → telemetry not yet observed live, closes on a seam-forced multi-step fold).** **Window-fit accuracy LANDED via dogfood-057 (WP-252): the pacing denominator now sources from the routing model's real window (`src/runner/context-window.ts` `lookupContextWindow`/`resolveContextWindowForSpec` wired into `agent-loop.ts:355`, `debug.contextWindowTokens` seam still winning), so `decideContextWindowPacing` reasons against the executor's actual window instead of a hardcoded 200k — F-55 fixed in code, first calibrated live read on the next run.** **WP-250 `park`→durable-suspend DELIVERED + all-green + HARVESTED (dogfood-078, `chain-6dff03ee-ebcb-4bdb-87b2-7ed7163b1a58`, runtime `a698e51`, `docs/reports/dogfood-078.md`): pure `shouldParkForWindow(decision)` (`pacing.ts:127`) drives `agent-loop.ts:559` to park at `status="SUSPENDED"` on `await condition(…)` (zero-compute) journaling a DISTINCT `cause:"window"` halt/`top_up` (`activities.ts:906` union widened), with operator/chain surfaces (`commands.ts`/`chain.ts:281`) reporting the window-park + a `chikory resume` hint. Additive, no contract/StepRecord/pacing-decision shape change, no new dep; AC-2 re-run on the harvested tree green (tsc+eslint + full suite 594 passed \| 19 skipped). ⚠️ The chain sealed FAILED on a writeSet-gate false-fail (F-89 → WP-510) NOT a delivery defect; hand-harvested. WP-250 → 🟡 — the spec's PRIMARY artifact, a LIVE Temporal window-park→resume durable test through the frozen seam, was NOT delivered (only unit tests); the workflow `condition`-park is untested at runtime and OWES `test/runner/window-park.test.ts` (F-90 → WP-511, track-B). The rung-2 chain-host that produced this is itself blocked by F-88 → WP-509 (planner did not decompose).** **WP-251 live-fold observation (the F-54 close) is now 🟢 DONE — dogfood-093 rendered the first live `trigger:"pacing"` fold (`compactions 2 (pacing 2) · first pacing fold step 5`); see FA-3.** |
| SE-3 | Process metrics: components over time; issues found vs changes made | WP-209 | P2 | in-progress (both trace-footer halves done — issues-found:changes-made via WP-209 slice 1 / dogfood-010, run `run-c9df353b`; components-over-time via WP-209 slice 2 / dogfood-011, run `run-59e0166c`; both commit pending review on `main`. OTel metric emission deferred to a later WP-209 slice) |

## IF — Interface & architecture (spec §8)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| IF-1 | Thin TypeScript SDK | P1 lanes M1–M5 | P1 | planned |
| IF-2 | Thin Python SDK | WP-201 | P2 | in-progress (slice 1 — contracts + shared conformance suite — landed `eb5c57e` via dogfood-002; pure compaction digest-prompt parity done via dogfood-030 `run-1a97e2ca`: `DIGEST_SYSTEM_PROMPT` + `build_digest_messages(to_digest) -> list[Message]` in `packages/sdk-py/src/chikory/compaction_prompt.py`, re-exported from `chikory`, 4 focused pytest cases, no contract/runtime wiring change; **branch-target parity done via dogfood-035 `run-b0bc3865`, committed `88e496c` on `main`, landing-scope MATCH** — `docs/reports/dogfood-035.md`: a new `packages/sdk-py/src/chikory/branch_target.py` with a local frozen `BranchTarget` dataclass + `parse_branch_target` + `branch_name_for_target` mirroring the TS `src/cli/branch-target.ts` source-of-truth behavior-for-behavior, re-exported from `chikory`, 16 pytest cases, no contract change; **Memory Pointer parity done via dogfood-036 `run-51645fbb`** (harvested IDENTICAL + uncommitted on the working tree — `docs/reports/dogfood-036.md`): a new `packages/sdk-py/src/chikory/memory_pointer.py` with a local frozen `MemoryPointerPolicy` dataclass + `should_pointerize` + `format_pointer_reference` mirroring the TS `runner/memory-pointer.ts` source-of-truth byte-for-byte (12-char id truncation, em dash U+2014), `ArtifactRef` reused from `chikory/types.py:197`, re-exported from `chikory`, 5 pytest cases, no contract change; **chain-state reducer parity done via dogfood-039 `run-6bb8ac7e`** (harvested IDENTICAL + uncommitted on the working tree — `docs/reports/dogfood-039.md`): a new `packages/sdk-py/src/chikory/chain_advance.py` with `derive_chain_status` (four-rule ADR-005 §S3 precedence — ESCALATE→`AWAITING_PLAN_APPROVAL` / FAILED→`FAILED` / all-nodes-SUCCESS→`SUCCESS` / else `RUNNING`) + `advance_chain` (pure immutable node-fold then recompute status), a 1:1 port of the TS `src/chain/advance.ts` (dogfood-038), `ChainRecord`/`NodeOutcome`/`ChainStatus` reused from `chikory/types.py`, re-exported from `chikory`, 6 pytest cases (80 py green), no contract change; **context-window pacing parity queued (dogfood-040)**: `decide_context_window_pacing` + local `ContextWindowUsage`/`ContextWindowPacingPolicy`/`ContextWindowPacingDecision` dataclasses in a new `packages/sdk-py/src/chikory/pacing.py`, mirroring the TS `src/runner/pacing.ts` source-of-truth (WP-207 `continue`/`compact`/`park` over projected context-window pressure), all local types, no contract change. _(The `renderChainTrace` Python parity stays blocked behind pacing — it needs the not-yet-ported `ChainEntry` store type.)_) |
| IF-3 | Durable workflows on existing engine (Temporal) — partner, don't rebuild | ADR-001, WP-004, WP-121 | P0–P1 | done |
| IF-4 | CLI surface | WP-141, WP-142; WP-220 (`chikory land`); WP-223 (durable watch transitions); WP-224 (`land --verify` + git-stderr seam); WP-227 (final journal drain at terminal boundary — landed `26b9964`); WP-229 (surface the ESCALATE reason in `--watch` — **delivered** dogfood-018 `run-59115f35`); WP-228 (launch baseline-satisfied precheck, dogfood-017 F-25); WP-241 (`chikory chain approve\|resume <chain-id>` + parked-child surfacing in chain `--watch` — **DONE, dogfood-proven live** via the WP-243 seam in the dogfood-044 re-run `chain-1bfb9d13-…`: node B parked at step 0, chain surfaced it, `chikory chain resume --add-budget 5` → SUCCESS 2/2, F-42 closed); WP-243 (deterministic park-injection seam `debug.parkBeforeStep` — **DONE**, landed `4dfcac1`, proved WP-241 live) | P1→P2 | in-progress (core CLI, trace, land, durable transition rendering done; WP-227 terminal-boundary drain done; **WP-229 done** — `followRun` now renders `judge escalated: <reason>` before AWAITING_APPROVAL on the watch stream, F-27 closed; redundant-spec guard WP-228 **in progress** — S1 pure decision half (`evaluateBaselinePrecheck`, `src/cli/precheck.ts`) queued as dogfood-027, the non-pure check-execution + warn/`--force` wiring the hand-design follow-up; **second empirical proof 2026-06-20 — `run-3d7bca84` re-ran already-landed dogfood-038 and burned $0.62 / 465k input tokens on a 0-diff no-op SUCCESS because the wiring is still missing**; **third proof 2026-06-21 (dogfood-044 F-45) — the false-start re-run `chain-8c303011-…` cloned a HEAD that already held the deliverables → empty diff → node-a FAILED after burning ≈$0.2714; precheck must cover `chikory chain`, not just `chikory run`**); **a SECOND launcher-guard is now LIVE — the WP-256 spec-staleness gate (dogfood-068, WP-258; **F-81 FIXED 2026-07-02, committed `141adf5`: `parseWpStatus` now reads the real `Tag`/`Notes` §6 schema (completion from Notes prose, fail-open) instead of a nonexistent `status` column, so the gate FINALLY FIRES against production plan.md — verified live (WP-220/255/264 → stale; WP-212/256/263 → fresh); WP-256/258 → 🟢**): `cmdRun` calls `evaluateSpecStalenessPrecheck(spec.goal, planText)` after `parseTaskSpec` and warns `[chikory] WARNING: stale spec: …` when the goal targets an already-done WP (warn-only, best-effort `plan.md` read via injectable `CliDeps.readPlanText`). **F-68 → WP-260 FIXED 2026-07-02: it now reads the target WP from the parsed `spec.goal`, not the whole yaml preamble → 🟢**) |
| IF-5 | Cloud control plane: hosted judges, checkpointers, trace browser | WP-401–408 (full design: components/control-plane.md) | P4 | planned |

## ST — Strategy, moat, business (spec §6, §9, §10)

| ID | Requirement | WP(s) | Phase | Status |
|---|---|---|---|---|
| ST-1 | DevAI-extended benchmark: 60–100 multi-hour tasks, greenfield + brownfield, open leaderboard | WP-301–304; WP-541 | P3 | **in-progress — WP-301 harness DONE; WP-302 authoring guide and 3 pinned/runnable brownfield tasks DONE; WP-541 verified-base publication gate DONE in dogfood-119 (`run-310d1b40-3b6d-4def-8cf4-38dfe218e78a`, landed `c78179c`, 2026-07-28): summaries retain every task, identify unverified bases, and compute I-SR/D-SR only over verified tasks.** Corpus is **5/5 pinned and base-verified** (117 / 1128 / 3680 / 1208 / 22 tests green after WP-586/WP-587). **✅ P3-rung-4 CLIMBED and PUBLISHED — dogfood-123 (`run-3e2a6791-cab1-46c7-bc5b-b3d0730b92c5`, 2026-08-06, `docs/reports/dogfood-123.md`):** `benchmarks/publications/p3-rung-4/` publishes the first like-for-like head-to-head with 95% Wilson ranges — **Chikory 19/19 I-SR [83.2%, 100.0%] vs raw Claude Code 18/19 I-SR [75.4%, 99.1%] / 16/19 D-SR [62.4%, 94.5%]** over `brownfield-001…005`, identical ordered task set, both arms 5/5 verified. **Intervals overlap — 19 requirements cannot separate two arms at 95%.** ST-1's own target (60–100 tasks) is what closes that, and it is now the binding constraint on the rung-5 exit gate, not the harness. **✅ WP-303 DATA HALF LANDED — dogfood-124 (`run-80fe6b8f-5ceb-4089-b13b-195390bf682b`, 2026-08-07, `docs/reports/dogfood-124.md`): `chikory-bench leaderboard` + `benchmarks/publications/leaderboard/`, ranked by the interval FLOOR, reporting the two arms as NOT separated.** WP-303's static site + methodology prose remain open; the OpenHands arm has never run; Stage-1 published-numbers deadline is ~2026-09-08. **✅ WP-593 DONE — dogfood-125 (`run-f3d47cf8-6d56-4c7b-85d1-fcfe185badef`, 2026-08-07, `docs/reports/dogfood-125.md`; the run sealed FAILED on a false out-of-rubric escalation, F-271, but the delivery is correct and landed):** `chikory-bench probe --task <file> [--out <dir>]` proves a task DISCRIMINATES before it may be scored — base ref and the new validated `repo.fix_ref` in SEPARATE workspaces, `base_verification_command` at BOTH refs, a red base yielding `inconclusive` (never `discriminating`), and per-requirement `discriminating` / `non-discriminating` / `unsatisfiable` from the exit-code pair. **Corpus growth is now UNBLOCKED but not yet started:** no corpus task carries a `fix_ref` and nothing has been probed, so the 19-requirement ceiling that keeps the intervals overlapping is unchanged. **✅ WP-595 DONE — dogfood-126 (`run-14291f34-7144-43b3-9a82-2ef776b4d008`, 2026-08-08, SUCCESS 1 step $0.0469, `docs/reports/dogfood-126.md`):** the probe's verdict is now BINDING. `probe --record <file>` persists a durable discrimination ledger, `summarize(…, ledger?)` counts a task's requirements only when it holds a `discriminating` verdict taken at the ref it was SCORED at, every excluded task is retained and named, and with no ledger every published artifact re-summarizes unchanged. Review hand-fixes gave it an operator path (`run --discrimination-ledger <file>`, F-274) and made a damaged ledger a refusal rather than a silent reset (F-275). **✅ WP-596 DONE — dogfood-127 (`run-41fb5957-933b-467e-a16d-36df443f6f41`, 2026-08-08, SUCCESS 1 step $0.0472, `docs/reports/dogfood-127.md`):** `probe --tasks <dir> --record <file>` sweeps a whole task directory durably — each verdict persisted the moment it lands (atomically, after F-282), a task skipped only when its ledger entry matches BOTH refs the task declares today, a moved ref forcing a re-probe, and one unprobeable task named rather than ending the sweep. The probe → gate → sweep pipeline is now COMPLETE. **✅ WP-597 DONE — dogfood-128 (`run-951a565d-73a5-4cc8-a230-9b63d872fba9`, 2026-08-08, SUCCESS 1 step $0.0425, `docs/reports/dogfood-128.md`): the pipeline finally has INPUT.** `brownfield-002/003/004/005` carry a real `repo.fix_ref` (each verified by hand as the direct child of its pinned base, subject matching the PR the task already named in prose); `AUTHORING.md` carries the gold-patch rule in the frozen Format v1 block and the pin checklist; `validate --require-probeable` names every runnable task that can never be probed and the field it lacks; `probe --tasks` records `unprobeable` as its own outcome rather than a probe failure. **🟠 STILL BLOCKED FOR THE EXIT GATE (F-284 → WP-598): 4 of 5, and the missing one carries the whole published separation.** `brownfield-001` is a self-performed zod v3→v4 migration with no upstream fix commit, and `runProbe` materializes the fix from the task's OWN `repo.url` (`probe.ts:121-122`) — so a self-authored gold patch is not expressible in the schema. Measured post-landing: `validate ../tasks --require-probeable` → `UNPROBEABLE brownfield-001`, exit 1; `probe --tasks benchmarks/tasks` can never exit 0; arming the WP-595 gate today DROPS the only task separating the arms. **✅ WP-598 DONE — dogfood-129 (`run-6ac4329e-c172-4f85-b1fc-c0ef9fb60851`, 2026-08-09, SUCCESS 6 steps $0.3634, `docs/reports/dogfood-129.md`): the last SCHEMA blocker is gone.** A pinned brownfield task may declare `repo.fix_patch` — a repo-relative patch file in this repository — instead of `repo.fix_ref`; `probe` materializes the fix by checking out `repo.ref` and applying that patch, so a migration upstream never performed is now provable. A patch that is missing, fails to apply, or applies to nothing is a loud error naming the file with nothing recorded; the ledger `fixRef` for a patch-backed task is the patch's sha256, so editing the patch invalidates the proof through WP-596's existing skip test; declaring both sources is a validation error and declaring neither stays unprobeable (no exemption). **🟠 THE EXIT GATE IS NOW BLOCKED ON AUTHORING, NOT ON PRODUCT.** Measured 2026-08-09: `validate ../tasks --require-probeable` still exits 1 on `brownfield-001`, because writing its actual zod v3→v4 migration patch is a 3–6h OPERATOR research lift that WP-598 deliberately scoped out. Nothing in the harness now stands in the way. **🟠 F-292 (dogfood-130 arming, 2026-08-09): the WP-595 gate cannot verify ANY suite that has already run** — not one stored per-task result carries `repoRef` (`suite.ts` began recording it only with WP-595 on 2026-08-07, and every stored suite predates that), so `isTaskDiscriminationVerified` (`results.ts:173`) excludes 100% of tasks in `benchmarks/results/p3-rung-4/`. The gate is armable only for suites run from now on; what the publication of record can claim about discrimination today is **nothing**, and WP-600 (dogfood-130) measures and publishes exactly that. ST-1's own 60–100-task target remains the binding constraint on the rung-5 exit gate; the 19-requirement ceiling that keeps the intervals overlapping is unchanged. **✅ WP-651 DONE — dogfood-166 (`run-89bbcd8d-8ddd-4600-a450-ad1720174bbc`, 2026-08-22, SUCCESS 4 steps $0.3227, `docs/reports/dogfood-166.md`): the denominator is no longer ZERO.** A requirement declares `kind: guard | discriminator | scored` (default `discriminator`, so no existing corpus meaning changed silently, `benchmarks/harness/src/task.ts:23`); a guard is REQUIRED but not scored, and a failed guard zeroes its task's scored satisfaction; the declared label must agree with `benchmarks/results/discrimination.json` or `chikory-bench validate` refuses it by name (`benchmarks/harness/src/main.ts:252`); a task with nothing left to score is refused at authoring time (`benchmarks/harness/src/task.ts:229`) AND excluded by name in the scorer (`benchmarks/harness/src/results.ts:200`). **Re-measured by hand at the review, on committed evidence:** `resummarize` over the stored `p3-rung-4/chikory` arm (ref-stamped from the ledger) + the committed ledger now reports **4/5 verified tasks, 5/5 verified requirements satisfied, I-SR 100.0% [56.6%, 100.0%]** — it reported **0/5 tasks, 0/0, I-SR 0.0%** before. The corpus is labelled 11 guard / 5 discriminator with all 19 requirement `id`/`description`/`check` fields byte-unchanged. **🔴 THE PUBLISHED BUNDLE IS UNCHANGED, AND P3-RUNG-5 IS STILL NOT CLIMBED.** `benchmarks/publications/p3-rung-4-corrected/chikory-summary.json` still reads `tasksVerified 0, iSr 0`: the stored arms predate `repoRef`, so WP-600 trap G refuses them for a second, independent reason. The non-zero denominator exists only for a ref-carrying arm, and producing one is **WP-304** — operator work, since an LLM executor may not supervise a multi-hour quota-bound suite (dogfood-122's lesson). **🔴 WP-652 QUEUED AND BLOCKING WP-304 (F-441/F-442/F-443, dogfood-166 review — each MEASURED by driving the real CLI over corpus copies):** one unprobed `kind: discriminator` requirement appended to `brownfield-004` makes `validate` exit 0 (`5 valid, 0 invalid`) while `resummarize` drops **4/5 tasks, denominator 5 → 3/5 tasks, denominator 4** — growing the corpus, which is ST-1's own 60–100-task target, today SHRINKS the published number; a ledger-`discriminating` requirement declared `guard` also passes validate clean and drops the interval floor **56.6% → 51.0%**; and `resummarize` guesses its tasks dir from the working directory and records nothing, returning **I-SR 0.0%** on identical evidence when the guess is wrong. **The operator arm must not run until this lands** — it would publish a number nobody can reproduce. |
| ST-2 | Feedback-loop dataset: traces, eval outcomes, recovery paths, cost/routing patterns | WP-306 | P3 | **capture pipeline DONE (hand-landed 2026-07-11): `chikory dataset export` — opt-in, local-first, secret-gated; dataset accumulates from every run onward** |
| ST-3 | MIT open source; publish own DevAI-extended numbers within 90 days | WP-304, WP-305 | P3 | **in-progress — the first own numbers are published locally** (`benchmarks/publications/p3-rung-4/`, dogfood-123, 2026-08-06): 5-task slice, both arms, 95% Wilson ranges, each arm linked to its raw suite dir by a repo-relative path (WP-588). **A local leaderboard now exists — `benchmarks/publications/leaderboard/` (WP-303 data half, dogfood-124, 2026-08-07)** — ranked by `iSrRange.low` and declaring **no winner** because the two arms' intervals overlap. **✅ THE LEADERBOARD IS NOW A READABLE PAGE — `benchmarks/publications/leaderboard/index.html` (WP-303 site half, dogfood-139, 2026-08-14), which COMPLETES WP-303:** self-contained, interval-led, methodology on the page, every evidence link resolving, regenerable from the bundle it publishes. Not yet HOSTED on a public URL, and still not DevAI-extended scale — the P3-rung-5 exit gate now rests entirely on **WP-304** (the OpenHands arm plus a corpus wide enough to separate 19 requirements). |
| ST-4 | Unit/usage pricing, generous free tier, $29–199 mid-tiers, enterprise SSO/audit | WP-405–407 | P4 | planned |
| ST-5 | Stage 3 vertical with measurable SLOs | WP-501–507 (design: components/vertical-agent.md; vertical choice = ADR-004, data-gated on Stage 2) | P5 | planned |

## Non-functional constraints (spec §4, §11, CLAUDE.md)

| ID | Constraint | Enforced by |
|---|---|---|
| NF-1 | Not a framework / vibe-coder / LangChain wrapper; minimal abstraction, maximal observability | Plan §1.2; ADR-003; review checklist in TASK-PROTOCOL.md |
| NF-2 | No magic: transparency and control lead | Trajectory journal is append-only ground truth; every automated decision (verdict, retry, compaction) is journaled with rationale |
| NF-3 | No provider lock-in anywhere in core path | Invariant #1; WP-104 swap test |
| NF-4 | Governance/compliance designed in for regulated buyers (ZDR caveat) | docs/SECURITY.md (threat model + compliance trajectory); WP-406; local-first default (RT-9) |
| NF-5 | Communicate market/benchmark figures in ranges | WP-303 methodology, WP-304; **WP-588** (a published range must link to durable evidence — `publishableRawResultsDir` refuses a path inside an ephemeral `.chikory/runs/…` workspace and emits repo-relative). **First honored in practice 2026-08-06** (`benchmarks/publications/p3-rung-4/report.md`, dogfood-123): the 95% Wilson interval is the headline claim and the point estimate is labelled secondary. **✅ NF-5 is now MECHANICAL, not editorial — dogfood-124 (WP-303 data half):** `chikory-bench leaderboard` orders arms by `iSrRange.low` and can only mark a pair `separated` when the intervals are DISJOINT, so an overlap CANNOT be rendered as a win — `benchmarks/publications/leaderboard/leaderboard.md` states "the arms are not separated (no winner)" over Chikory [83.2%, 100.0%] vs raw Claude Code [75.4%, 99.1%]. **WP-591** extends WP-588's durability rule to every published pointer (`publishableRepoPath`), after the leaderboard's first bundle pointer resolved from nowhere (F-267). **✅ NF-5 IS NOW HUMAN-READABLE TOO — dogfood-139 (WP-303 site half, `run-ab72b901-4f3f-4b98-b6e1-ca00324ff0a6`, 2026-08-14):** `benchmarks/publications/leaderboard/index.html` is a self-contained page that leads with the 95% interval, states *"Overlap at 95% confidence; the arms are not separated"*, names no winner, and carries a methodology (corpus size, requirement count, adapters, I-SR/D-SR, Wilson) read from the bundle rather than typed in. The claim follows the DATA in both directions — a disjoint pair reports separation and names the leading arm. Three review hand-fixes hardened the honesty guarantee against its own inputs: the page no longer fabricates a corpus when it has no entries (F-336), bundle-derived strings can no longer inject markup or a network asset (F-337), and its evidence links no longer depend on the directory the generator ran from (F-338). |
| NF-6 | No LLM-layer mocks in integration tests; test code held to the same type contract as `src/` | CI policy (WP-003), CLAUDE.md. **WP-230 done** (dogfood-020 `run-3575ba23`): `typecheck` now runs a second `tsc -p tsconfig.test.json` pass over `src/**`+`test/**`; verified it trips on a bad fixture (`TS2353`). dogfood-019 F-29 (contract-violating test fixtures shipping green) closed. |
| NF-7 | No dead-end failures: every non-infra failure gets ≥1 bounded, journaled automated heal attempt before terminal seal; human escalation is the last resort (run + chain + **the plan gate above both**) | ADR-009 (self-heal doctrine); WP-519/WP-520/WP-521; **WP-542** (tier-0 gate repair — the plan meta-judge no longer dead-ends a launch, F-207); **WP-543** (no chain incarnation ends without a terminal seal — an ANSWERED node escalation resolves to a resumable FAILED, F-208); **WP-544** (a bound may only count what the agent controls — an infra cap-kill and a reverted verdict spend no strike; heal budgets are per node lineage, not chain-wide; and the chain RE-ENTERS a resumable child before rewriting the node, F-209…F-214); **WP-553** (a provider quota wall parks until reset instead of spending strikes — the limit signal now leaves the adapter at all, F-228/F-234); **WP-554** (a CONVERGED run may seal itself: an out-of-rubric concern over an empty diff with every criterion and rubric item passing is not a failure, F-229); **WP-602** (a REJECTED escalation is a heal trigger, not an epitaph — the operator's reason becomes the remediation brief verbatim under a `maxRejectStrikes` budget, and only a reason-less reject or an exhausted budget still dead-ends, F-296; dogfood-131, `d499128`); **WP-605** (a heal undoes only what the verdict condemned — the rollback is decided from the verdict, not the trigger, so work an operator never objected to survives the correction while a HALT still rolls back, F-302; dogfood-132); heal attempts journaled per NF-2 |
