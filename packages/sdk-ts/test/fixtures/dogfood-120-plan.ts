/**
 * Real artifacts from dogfood-120's chain `chain-0723ac0b-4eba-413a-933f-2d1646a4f643`,
 * lifted verbatim from its journal (`chain_entries` kind `plan` / `plan_verdict`).
 *
 * These are fixtures for the F-223 budget proof: a synthetic 3-node outline is
 * small enough to fit any cap, which is exactly why the defect survived. The real
 * plan's six node goals total 3798 characters.
 */

/** `planOutline(plan)` over the real 6-node plan the gate rejected. */
export const DOGFOOD_120_PLAN_OUTLINE: readonly string[] = [
  "N-1: Implement the brownfield baseline/comparison capability within `benchmarks/`: make the raw command adapter materialize `repo.url` at exactly `repo.ref` into its workspace, fail closed on clone/checkout errors, avoid the separate base-verification checkout, and preserve the Chikory adapter's empty-workspace contract. Add the `compare` outcome for two real `summary.json` inputs, requiring identical five-task sets, `tasksVerified === tasks === 5`, and no `unverifiedTasks`; emit arm metadata, raw-directory references, I-SR/D-SR point estimates, and headline 95% Wilson intervals. Add at least seven focused tests while keeping strict ESM, named exports, no `any`, no dependency/provider/frozen-contract changes, no committed target `node_modules`, and sequential pinned-tool commands without nesting `devbox run`.",
  "N-2 (after N-1): Research and author brownfield-004 as one independently reviewable public-OSS benchmark at a full 40-hex pre-fix commit, with `status: pinned`, an outcome-shaped multi-hour bug/refactor/upgrade goal, independently checkable requirements, a plausible wrong turn, and a lockfile-respecting `base_verification_command` using the repository's own package manager. Record reproducible evidence that every new check is RED on the untouched pin for the intended reason and GREEN on a known-good fix, plus upstream provenance and review; do not use an invented fixture repository or trivial leaf utility.",
  "N-3 (after N-1, N-2): Independently research and author brownfield-005 as a different real public-OSS benchmark at a full 40-hex pre-fix commit, with `status: pinned`, an outcome-shaped multi-hour bug/refactor/upgrade goal, independently checkable requirements, a plausible wrong turn, and a lockfile-respecting `base_verification_command` using the target's own package manager. Record upstream provenance, review, and reproducible RED-on-untouched-pin and GREEN-on-known-fix evidence, then confirm the loader exposes exactly brownfield-001/002/003/004/005 without silent exclusions; do not use an invented fixture repository or trivial leaf utility.",
  "N-4 (after N-1, N-3): Launch the identical five pinned tasks through the repository's one-stop Chikory benchmark path, retaining the directive-family preflight so Gemini executes and Codex judges. Preserve the resulting suite summary and per-task machine-readable raw artifacts under `benchmarks/results/`, with 5/5 verified bases, exact task ids, family metadata, wall-clock totals, and requirement counts; do not retain provider credentials, full transcripts, cloned target repositories, or target dependency trees.",
  "N-5 (after N-4): After the Chikory arm succeeds, run the same ordered brownfield-001/002/003/004/005 set sequentially through raw Claude Code using the raw command adapter, with Claude Code editing each exactly materialized pin from the task goal and no in-loop Chikory judge. Preserve its suite summary and per-task machine-readable artifacts under `benchmarks/results/`, including 5/5 base verification, family metadata, wall-clock totals, and requirement counts; never substitute a synthetic fixture, upstream known fix, second Chikory configuration, credentials, full transcripts, cloned repositories, or target dependency trees.",
  "N-6 (after N-5): Create the auditable landing bundle at `benchmarks/publications/p3-rung-4/` by copying only the two small real suite summaries and generating the comparison with the harness `compare` outcome. Make Wilson score ranges—not point claims—the headline, and record exact task ids, raw `benchmarks/results/` directory references, adapter/executor/judge families, wall-clock totals, requirement counts, I-SR/D-SR point estimates, and finite 95% intervals; add a concise report without credentials, transcripts, clones, or target `node_modules`.",
];

/** The longest rationale the plan meta-judge actually returned for that plan. */
export const DOGFOOD_120_GATE_RATIONALE = "The plan covers all five goal acceptance criteria and preserves the required decomposition: baseline/comparison capability, two separately researched and evidenced task-authoring slices, distinct Chikory and raw-Claude-Code campaign arms, and a final publication bundle. Dependencies are coherent and ensure both new tasks and tooling exist before execution, the raw Claude Code arm runs sequentially after the Chikory arm, and the comparison bundle is produced only after both real arms complete. Node scopes include exact-pin materialization, fail-closed behavior, Wilson-interval comparison validation, loader-owned five-task validation, real campaign artifacts, publication provenance, and type/lint/test-floor preservation.";

/** The REVISE rationale that rejected it on attempt 1 — what a real brief carries. */
export const DOGFOOD_120_REVISE_RATIONALE = "The six nodes provide the requested decomposition: baseline/comparison tooling, two separately researched and validated task-authoring slices, sequential Chikory and raw-Claude-Code runs, and the final publication bundle. AC-1, AC-2, and AC-5 are assigned to N-1; AC-3 to N-3; and AC-4 to N-6, with supporting execution responsibilities in N-4 and N-5. Dependencies are coherent, especially the required N-4-to-N-5 ordering, and each node’s scope states the relevant contracts, evidence, artifacts, and exclusions. [literal override: plan drops mandated goal literals: devbox run, benchmarks/ - cannot PROCEED]";

/** The machine-checked gaps attempt 1 was rejected for — both are literal gaps. */
export const DOGFOOD_120_REVISE_GAPS: readonly string[] = [
  "mandated goal literal `devbox run` appears in no node goal — copy it verbatim into the goal of the node that owns it",
  "mandated goal literal `benchmarks/` appears in no node goal — copy it verbatim into the goal of the node that owns it",
];
